
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let const_drawCurTitle:[UInt8] = [0x6c,0x7d,0x7d,0x61,0x68,0x72,0x64,0x6c,0x7d,0x72,0x7d,0x6c,0x74,0x72,0x7e,0x78,0x6e,0x6e,0x68,0x68,0x69,0x72,0x63,0x62,0x79,0x64,0x6b,0x64,0x6e,0x6c,0x79,0x64,0x62,0x63]

private func hostAverage(state num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let user_aliveProfessionalString:[UInt8] = [0x16,0x7,0x7,0x1b,0x12,0x8,0x1e,0x16,0x7,0x8,0x4,0x2,0x15,0x4,0x14,0x5,0x1e,0x15,0x12,0x8,0x4,0x2,0x14,0x14,0x12,0x12,0x13,0x8,0x19,0x18,0x3,0x1e,0x11,0x1e,0x14,0x16,0x3,0x1e,0x18,0x19]

/*: "mf/recharge/createApplePay" :*/
fileprivate let showModelUrl:String = "nearby hi spark spotmf/re"
fileprivate let data_rebuildId:[Character] = ["/","c","r"]
fileprivate let k_femaleData:String = "eacenter"

/*: "productId" :*/
fileprivate let main_usMsg:[Character] = ["p","r","o","d","u","c","t"]
fileprivate let dataAddUrl:String = "texture sub someone thin evenId"

/*: "source" :*/
fileprivate let appUsedPath:[Character] = ["s","o","u","r","c","e"]

/*: "IAP充值创建订单失败: :*/
fileprivate let mainManagerSearcherString:String = "IAP\u{5145}值创"
fileprivate let kRecordUrl:String = "建订单\u{5931}败:"

/*: "orderNum" :*/
fileprivate let main_formalMsg:[Character] = ["o","r"]
fileprivate let data_stepProgressFormat:String = "derNumspine location one"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let data_boundMessage:String = "mf/reowner feature process"
fileprivate let show_obtainId:String = "con compute independent agoge/a"
fileprivate let noti_whatName:String = "break honey help phone signatureyNotify"

/*: "IAP充值校验失败: :*/
fileprivate let showContrastPath:String = "observe"
fileprivate let const_statusReserveFormat:[Character] = ["A","P","充","\u{503c}","\u{6821}","\u{9a8c}","失","\u{8d25}",":"]

/*: "transactionId" :*/
fileprivate let dataFailureMsg:String = "tranbeata"
fileprivate let const_decideKey:String = "length minuteId"

/*: "reportMoney" :*/
fileprivate let main_variableData:String = "substantialeposubstantialt"
fileprivate let const_buttonStr:String = "curve interestedMoney"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let const_totalShakeName:String = "eager disappearmf/Au"
fileprivate let constTextAmInfrastructureMsg:String = "/Applefor slow right thread"
fileprivate let appMemorySpellMsg:String = "tail"

/*: "IAP订阅创建订单失败: :*/
fileprivate let show_whoKey:String = "outside root as implementIAP订阅"
fileprivate let noti_profilePath:[Character] = ["创","建","订","单","失","败",":"]

/*: "orderId" :*/
fileprivate let show_mountKey:[UInt8] = [0x9a,0x9d,0x8f,0x90,0x9d,0x74,0x8f]

fileprivate func grayWhat(sufficient num: UInt8) -> UInt8 {
    let value = Int(num) - 43
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let constSharedReachMessage:String = "pin tarmf/Aut"
fileprivate let showServiceGapId:String = "Appledate arrow show running regular"
fileprivate let k_funeralYetId:String = "shake"

/*: "IAP订阅校验失败: :*/
fileprivate let const_suspendFormat:String = "heavy its infrastructure launchIAP订阅校验"
fileprivate let show_cancelToName:String = "file:"

/*: "App" :*/
fileprivate let kStateZzName:String = "comply factory yourApp"

/*: "OrderTransactionInfo_ :*/
fileprivate let showTimeMinePath:[Character] = ["O","r","d","e","r","T","r","a","n","s","a","c","t","i","o","n","I","n"]
fileprivate let mainOddValue:String = "fo_begin capable"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let app_phaseStr:[UInt8] = [0x5e,0x81,0x73,0x74,0x81,0x63,0x81,0x70,0x7d,0x82,0x70,0x72,0x83,0x78,0x7e,0x7d,0x58,0x7d,0x75,0x7e,0x6e,0x62,0x84,0x71,0x82,0x72,0x81,0x78,0x71,0x74,0x6e]

fileprivate func rootKind(link num: UInt8) -> UInt8 {
    let value = Int(num) - 15
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "verifyData" :*/
fileprivate let showFindGreetData:[UInt8] = [0x4f,0x3e,0x4b,0x42,0x3f,0x52,0x1d,0x3a,0x4d,0x3a]

fileprivate func userTunDirection(mid num: UInt8) -> UInt8 {
    let value = Int(num) + 39
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let kFailLocationStr:[Character] = ["I","A","P","查","询","内","购","\u{5546}","品"]
fileprivate let dataSecondId:String = "recommendation, "
fileprivate let data_whenString:String = "trade source wherectId:"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let kTailResumeNeedName:String = "IAP查询appropriate gender mirror deadline"
fileprivate let showAboveMessage:String = ", produobserve to woman"
fileprivate let mainForwardPath:String = "required operatects."

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let k_bubbleButtValue:[Character] = ["I","A","P","查"]
fileprivate let notiSignalFormat:[Character] = ["\u{8be2}","\u{5185}","购","\u{5546}","品","失","败",","," ","E","r","r","o","r",":"]

/*: "IAP-purchased: :*/
fileprivate let noti_lessMsg:String = "IAP-ptoward string"
fileprivate let show_effectFirstPath:String = "manage container concrete scaleased:"

/*: , 订单号: :*/
fileprivate let k_propertyData:String = ", 订\u{5355}号:"

/*: , 商品Id: :*/
fileprivate let k_blueName:[Character] = [","," ","商","品","I","d",":"]

/*: , 苹果transId: :*/
fileprivate let noti_youUrl:String = ", 苹\u{679c}tr"

/*: "IAP-failed: :*/
fileprivate let const_measureRevealKey:String = "Imigration"
fileprivate let app_independentTitle:String = "P-faiacross satisfy reward long"

/*: "IAP-deferred: :*/
fileprivate let k_productId:String = "list green postIAP-defer"
fileprivate let constEasyEquipmentStr:String = "red:nature observer note my return"

/*: "IAP未知交易类型: :*/
fileprivate let data_resumeValue:String = "pure inmate arrow reach instanceIAP未知"
fileprivate let k_distinctionFormat:String = "交易类型:"

/*: " 未知的交易类型" :*/
fileprivate let noti_anotherKey:String = " 未role的交易类型"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let appSiblingThroughTitle:[Character] = ["I","A","P","获","取","本","地","收"]
fileprivate let noti_perUrl:String = "check"
fileprivate let appSignificantBoyData:[Character] = ["数","据","失","败",","," ","t","r","a","n","s","a","c","t","i","o","n","I","d",":"]

/*: , orderId: :*/
fileprivate let notiEndStr:String = ", orderyield versus admin consume"
fileprivate let const_bodValue:String = "outside latId:"

/*: , payType: :*/
fileprivate let const_noData:String = "s injury, payT"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ReductionInclude.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let app_markerFormat = NSNotification.Name(rawValue: String(bytes: const_drawCurTitle.map{hostAverage(state: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let k_exceptDuringTitle = NSNotification.Name(rawValue: String(bytes: user_aliveProfessionalString.map{$0^87}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let kDifficultEnhanceStr = 9

/// 支付类型
//: enum ApplePayType {
enum TrainExit {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum ByConnect: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (ByConnect, Double, TrainExit) -> Void

//: class AppleIAPManager: NSObject {
class ReductionInclude: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: TrainExit = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = ReductionInclude()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(tellIn),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func tellIn() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension ReductionInclude {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func minWithStripMedal(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(showModelUrl.suffix(5)) + "charge" + String(data_rebuildId) + k_femaleData.replacingOccurrences(of: "center", with: "te") + "ApplePay")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(main_usMsg) + String(dataAddUrl.suffix(2)))] = productId
        //: dict["source"] = source
        dict[(String(appUsedPath))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                MusicThread.layerLanguage(msg: (mainManagerSearcherString + kRecordUrl) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(main_formalMsg) + String(data_stepProgressFormat.prefix(6)))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func correctApp(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(data_boundMessage.prefix(5)) + "char" + String(show_obtainId.suffix(4)) + "pplePa" + String(noti_whatName.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    MusicThread.layerLanguage(msg: (showContrastPath.replacingOccurrences(of: "observe", with: "I") + String(const_statusReserveFormat)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.systemFee(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.locationForm()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: app_markerFormat, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(main_variableData.replacingOccurrences(of: "substantial", with: "r") + String(const_buttonStr.suffix(5)))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension ReductionInclude {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func flagResume(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(const_totalShakeName.suffix(5)) + "toSub" + String(constTextAmInfrastructureMsg.prefix(6)) + "CreateOrd" + appMemorySpellMsg.replacingOccurrences(of: "tail", with: "er"))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(main_usMsg) + String(dataAddUrl.suffix(2)))] = productId
        //: dict["source"] = source
        dict[(String(appUsedPath))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                MusicThread.layerLanguage(msg: (String(show_whoKey.suffix(5)) + String(noti_profilePath)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: show_mountKey.map{grayWhat(sufficient: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func appropriateWant(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(constSharedReachMessage.suffix(6)) + "oSub/" + String(showServiceGapId.prefix(5)) + "PaySucces" + k_funeralYetId.replacingOccurrences(of: "shake", with: "s"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    MusicThread.layerLanguage(msg: (String(const_suspendFormat.suffix(7)) + show_cancelToName.replacingOccurrences(of: "file", with: "失败")) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.systemFee(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.place()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: k_exceptDuringTitle, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(main_variableData.replacingOccurrences(of: "substantial", with: "r") + String(const_buttonStr.suffix(5)))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension ReductionInclude {
    /// 初始化数据
    //: private func iap_initData() {
    private func underPress() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = mustAnalyze(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = mustAnalyze(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func mustAnalyze(payType: TrainExit) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = locationForm()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = place()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func locationForm() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(kStateZzName.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = data_toStr.string(forKey: noti_modelString) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(showTimeMinePath) + String(mainOddValue.prefix(3))) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func place() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(kStateZzName.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = data_toStr.string(forKey: noti_modelString) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: app_phaseStr.map{rootKind(link: $0)}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func canvasCrush(_ transactionId: String, _ payType: TrainExit) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: showFindGreetData.map{userTunDirection(mid: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: showFindGreetData.map{userTunDirection(mid: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension ReductionInclude {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func easyLab() {
        //: iap_initData()
        underPress()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            perEvolution(dict[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            perEvolution(dict[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func perEvolution(_ transactionId: String?, _ payType: TrainExit) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        systemFee(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension ReductionInclude {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func transferAsk(productId: String, payType: TrainExit, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        underPress()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            minWithStripMedal(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.delayForRaw(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            flagResume(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.delayForRaw(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func delayForRaw(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            MusicThread.layerLanguage(msg: (String(kFailLocationStr) + dataSecondId.replacingOccurrences(of: "recommendation", with: "失败") + "produ" + String(data_whenString.suffix(5))) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.speaker()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func speaker() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension ReductionInclude: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            MusicThread.layerLanguage(msg: (String(kTailResumeNeedName.prefix(5)) + "内购商品\u{5931}败" + String(showAboveMessage.prefix(7)) + String(mainForwardPath.suffix(4)) + "count为0."))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_ request: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        MusicThread.layerLanguage(msg: (String(k_bubbleButtValue) + String(notiSignalFormat)) + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_ request: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension ReductionInclude: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    MusicThread.layerLanguage(msg: (String(noti_lessMsg.prefix(5)) + "urch" + String(show_effectFirstPath.suffix(5))) + "\(currentPayType)" + (k_propertyData) + "\(String(describing: createOrderId))" + (String(k_blueName)) + "\(transaction.payment.productIdentifier)" + (noti_youUrl + "ansId:") + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    systemFee(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MusicThread.layerLanguage(msg: (const_measureRevealKey.replacingOccurrences(of: "migration", with: "A") + String(app_independentTitle.prefix(5)) + "led:") + "\(currentPayType)" + (k_propertyData) + "\(String(describing: createOrderId))" + (String(k_blueName)) + "\(transaction.payment.productIdentifier)" + (noti_youUrl + "ansId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: let vc = TalkingContactServiceView.init()
                let vc = BrownView()
                //: vc.show()
                vc.mobileSimilar()
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MusicThread.layerLanguage(msg: (String(k_productId.suffix(9)) + String(constEasyEquipmentStr.prefix(4))) + "\(currentPayType)" + (k_propertyData) + "\(String(describing: createOrderId))" + (String(k_blueName)) + "\(transaction.payment.productIdentifier)" + (noti_youUrl + "ansId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MusicThread.layerLanguage(msg: (String(data_resumeValue.suffix(5)) + k_distinctionFormat.capitalized) + "\(currentPayType)" + (k_propertyData) + "\(String(describing: createOrderId))" + (String(k_blueName)) + "\(transaction.payment.productIdentifier)" + (noti_youUrl + "ansId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((noti_anotherKey.replacingOccurrences(of: "role", with: "知")))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func systemFee(_ transactionId: String, _ payType: TrainExit) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = canvasCrush(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            MusicThread.layerLanguage(msg: (String(appSiblingThroughTitle) + noti_perUrl.replacingOccurrences(of: "check", with: "据") + String(appSignificantBoyData)) + "\(transactionId)" + (String(notiEndStr.prefix(7)) + String(const_bodValue.suffix(3))) + "\(String(describing: createOrderId))" + (String(const_noData.suffix(6)) + "ype:") + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))] == transactionId || $0[String(bytes: show_mountKey.map{grayWhat(sufficient: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: show_mountKey.map{grayWhat(sufficient: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: showFindGreetData.map{userTunDirection(mid: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.locationForm()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))] == transactionId || $0[String(bytes: show_mountKey.map{grayWhat(sufficient: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: show_mountKey.map{grayWhat(sufficient: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: showFindGreetData.map{userTunDirection(mid: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.place()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > kDifficultEnhanceStr {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            correctApp(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(dataFailureMsg.replacingOccurrences(of: "beat", with: "s") + "ction" + String(const_decideKey.suffix(2)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            appropriateWant(transactionId, params: paramsArr.first!)
        }
    }
}
