
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_thirdKey:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let userCollectionString:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let showExactlyEmbraceName:String = "window likeheadPi"
fileprivate let showPageMissingKey:[Character] = ["c"]

/*: "nickname" :*/
fileprivate let constReadString:String = "nicjob"

/*: "birthday" :*/
fileprivate let main_randomStr:[Character] = ["b"]
fileprivate let const_taTitle:String = "iryellowhday"

/*: "videoUrl" :*/
fileprivate let constBusySwitcherPath:[Character] = ["v","i","d","e","o","U","r"]
fileprivate let notiBoardResolveData:[Character] = ["l"]

/*: "cover" :*/
fileprivate let constGraphicKey:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let data_latUndertakeUrl:String = "lwhycat"
fileprivate let noti_lowerPath:[Character] = ["i","o","n"]

/*: "cmd" :*/
fileprivate let app_writingShadeData:[UInt8] = [0x2c,0x22,0x2b]

private func weekEdit(disabled num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "refuseCall" :*/
fileprivate let k_sendPath:[Character] = ["r"]
fileprivate let k_sureMsg:[Character] = ["e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let const_intervalStr:[UInt8] = [0x65,0x60,0x75,0x60]

private func onBrown(mount num: UInt8) -> UInt8 {
    return num ^ 1
}

/*: "Call interruption" :*/
fileprivate let show_innerUrl:[Character] = ["C","a","l","l"," ","i","n","t","e"]
fileprivate let kPoolKeyValue:[Character] = ["r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let show_elementPath:String = "density inside announcement weak intostopC"
fileprivate let const_problemName:[Character] = ["a","l","l"]

/*: "logId" :*/
fileprivate let k_locationValue:[UInt8] = [0xe,0x11,0x9,0xeb,0x6]

fileprivate func foreEmpty(comply num: UInt8) -> UInt8 {
    let value = Int(num) + 94
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let mainExampleThereFormat:String = "cancevertical"
fileprivate let dataPlayerMovementUrl:String = "template recommend paint nearCall"

/*: "No answer" :*/
fileprivate let const_programStr:String = "permanent rocket triggerNo answ"
fileprivate let user_careData:String = "labelr"

/*: "duration" :*/
fileprivate let mainEarnZzContactStr:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let main_orientationStr:String = "She hdelay bod lack secret job"
fileprivate let mainAutomaticUrl:[Character] = ["n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let const_correctName:String = "cancelSdecrease strip"
fileprivate let main_spellMessage:String = "reward"

/*: "He has hung up" :*/
fileprivate let mainCoreMsg:String = "sale block currently insert previousHe has h"
fileprivate let noti_scaleValue:String = "ung upinfrastructure fun"

/*: "endType" :*/
fileprivate let show_fadeFormat:String = "endTypeintegration global feed something panel"

/*: "stopSex" :*/
fileprivate let show_storageStateValue:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let data_despiteStr:[UInt8] = [0xd2,0xf5,0xe8,0xee,0xfd,0xfd,0xf2,0xf8,0xf2,0xfe,0xf5,0xef,0xbb,0xfc,0xf4,0xf7,0xff,0xbb,0xf8,0xf4,0xf2,0xf5,0xe8,0xb7,0xbb,0xf8,0xfa,0xf7,0xf7,0xbb,0xf3,0xfa,0xe8,0xbb,0xfe,0xf5,0xff,0xfe,0xff]

private func retMode(construct num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "The other hung up the phone" :*/
fileprivate let data_staffAdjustmentPath:String = "direct it old framework ofThe ot"
fileprivate let user_invitationFactoryString:String = "broad largehung "
fileprivate let constFinishPopMessage:String = "package audience context aspect aspecthe pho"
fileprivate let show_conversationData:String = "nentity"

/*: "type" :*/
fileprivate let userTimingStr:String = "absype"

/*: "jumpUrl" :*/
fileprivate let showParticleUrl:[Character] = ["j","u","m","p","U","r"]
fileprivate let data_labAlongString:[Character] = ["l"]

/*: "Cancel" :*/
fileprivate let userAgoPath:[Character] = ["C","a","n","c","e"]
fileprivate let const_proTitle:[Character] = ["l"]

/*: "Get gold coins" :*/
fileprivate let user_chainMaintainPath:String = "resign certain pack style offerGet go"
fileprivate let app_personalFormat:String = "advice in unlessld c"

/*: "refuseSex" :*/
fileprivate let notiMixString:String = "refucurve"
fileprivate let dataProPurePath:[Character] = ["S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let const_mobileTitle:String = "onAccpast oval empty"
fileprivate let kFixedData:[Character] = ["l"]

/*: "acceptCall" :*/
fileprivate let app_outsideTitle:String = "ACCEPT"
fileprivate let notiPermanentMessage:String = "Callanother curve transition"

/*: "Calls are cancelled" :*/
fileprivate let notiRecoverName:String = "Callstip light advice trigger undertake"
fileprivate let show_pointCalculationData:String = "cancafterllafterd"

/*: "Continue matching with other users？" :*/
fileprivate let user_ontoListenMsg:[UInt8] = [0x6f,0x43,0x42,0x58,0x45,0x42,0x59,0x49,0xc,0x41,0x4d,0x58,0x4f,0x44,0x45,0x42,0x4b,0xc,0x5b,0x45,0x58,0x44,0xc,0x43,0x58,0x44,0x49,0x5e,0xc,0x59,0x5f,0x49,0x5e,0x5f,0xc3,0x90,0xb3]

/*: "Yes" :*/
fileprivate let user_effectPartnerMsg:String = "Yesperiod detect pose architecture"

/*: "The call has ended" :*/
fileprivate let userFastKey:String = "skip leaveThe call"
fileprivate let noti_chainTitle:String = " has esmart function compare"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ButtonViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let userBeginScalePath = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class ButtonViewController: PetViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: FactorModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PerformExtraMaster? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: PageSecondCeiling? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: DeliveryView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        PanCurrentStatus.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        PanCurrentStatus.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PanCurrentStatus.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_thirdKey.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! BuildLast
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.belowMoveAgain()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            tempArray()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            appropriateIn(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.usageVC = nil
            PanCurrentStatus.shared.usageVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.secondFactor()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        theDisplayProjection()
        //: setupSubViewsConstraint()
        scaleEnvironment()
        //: initNotificationObserver()
        segmentDown()
        //: req_callGetUserInfo()
        topicHandle()
        //: timeoutRules()
        styleDate()
        //: setUpBGMPlayer()
        profile()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.sumensity()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MountCustom.share.appConfigMode.disableShootScreen {
            if MountCustom.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = MustView(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.usage = false
        PanCurrentStatus.shared.usage = false
        //: TalkingSocketManager.shared.isCalling = false
        PanCurrentStatus.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.gift()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: RoundingEnd = {
        //: let v = TalkingBGVideoView()
        let v = RoundingEnd()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.panelFor(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: AssociateView = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = AssociateView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: TimeThrough = {
        //: let v = TalkingVideoWindowView()
        let v = TimeThrough()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = mainCellMsg - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if ThanTrain.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: data_visitorPath + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.imageThumb()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ProvideOvalBehavior = {
        //: let m = TalkingVideoChatManager()
        let m = ProvideOvalBehavior()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension ButtonViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func topicHandle() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ProvideOvalBehavior.responseGem(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: userCollectionString.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(showExactlyEmbraceName.suffix(6)) + String(showPageMissingKey))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(constReadString.replacingOccurrences(of: "job", with: "k") + "name")].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(main_randomStr) + const_taTitle.replacingOccurrences(of: "yellow", with: "t"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(constBusySwitcherPath) + String(notiBoardResolveData))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(constGraphicKey))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(data_latUndertakeUrl.replacingOccurrences(of: "why", with: "o") + String(noti_lowerPath))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.combineHolder()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.underPartner()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func underPartner() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = FunctionalHidden.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = FunctionalHidden.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.conversationTell(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.anIndicator(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension ButtonViewController {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func tempArray() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ChainForce.behindCustomerRow { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.signWhen(showMsg: show_beginTakeName)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (String(k_sendPath) + String(k_sureMsg)), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: userCollectionString.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.pathShade()
                //: return
                return
            }

            //: self.startPreview()
            self.sumensity()
            //: self.stopBGM()
            self.freshBy()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PerformExtraMaster.magnitudeerest(type: .Response)
            //: self.startBGM()
            self.belowMoveAgain()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func sumensity() {
        //: self.videoManager.startPreview()
        self.videoManager.resolve()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func gift() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.freshBy()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        ClearButton.consumptionRobot()
        //: TalkingSocketManager.shared.callerDelegate = nil
        PanCurrentStatus.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        PanCurrentStatus.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        PanCurrentStatus.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func pathShade(animated: Bool = true) {
        //: self.stopBGM()
        self.freshBy()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func imageThumb() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.notice()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.underPartner()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func it() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.directionNow()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.underPartner()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.behindWrap()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func behindWrap() {
        //: guard MountCustom.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = DeliveryView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.wait()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension ButtonViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func segmentDown() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(betweenFamily(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(duringPlayerFinish(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: user_accessibleString,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(objectSpace(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: mainSubtleEnvironmentMsg,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        userBeginScalePath.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func betweenFamily(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            pathShade()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func objectSpace(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard PanCurrentStatus.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        PanCurrentStatus.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        PanCurrentStatus.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PanCurrentStatus.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.locationSize()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func duringPlayerFinish(notification: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.underPartner()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension ButtonViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.signWhen(showMsg: (String(show_innerUrl) + String(kPoolKeyValue)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (String(show_elementPath.suffix(5)) + String(const_problemName)), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (mainExampleThereFormat.replacingOccurrences(of: "vertical", with: "l") + String(dataPlayerMovementUrl.suffix(4))), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (String(k_sendPath) + String(k_sureMsg)), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: userCollectionString.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.third()
                //: TalkingSocketManager.shared.usageVC = nil
                PanCurrentStatus.shared.usageVC = nil
            }
            //: popCurrentViewController()
            pathShade()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension ButtonViewController {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func profile() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DescriptionBind.Call : DescriptionBind.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PerformExtraMaster.magnitudeerest(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func belowMoveAgain() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.impressionForce()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func freshBy() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.curveEstimated()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension ButtonViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func styleDate() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? noti_checkData : self.chatModel?.theAgainst()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            anyOrigin()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.anyOrigin()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func anyOrigin() {
        //: guard !TalkingSocketManager.shared.usage else { return }
        guard !PanCurrentStatus.shared.usage else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.signWhen(showMsg: (String(const_programStr.suffix(7)) + user_careData.replacingOccurrences(of: "label", with: "e")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (mainExampleThereFormat.replacingOccurrences(of: "vertical", with: "l") + String(dataPlayerMovementUrl.suffix(4))), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (String(k_sendPath) + String(k_sureMsg)), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: userCollectionString.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        PanCurrentStatus.shared.banAcross(info: socketDict)
        //: popCurrentViewController()
        pathShade()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension ButtonViewController {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func logEnableMe(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(mainEarnZzContactStr))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.system(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.transitionFeaturePopEmbrace()
            //: createTalkRoom()
            it()
            //: self.stopBGM()
            self.freshBy()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func appropriateIn(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.notice()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.panelFor(show: false)
        //: self.stopBGM()
        self.freshBy()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.transitionFeaturePopEmbrace()
        //: self.createTalkRoom()
        self.it()
    }
}

// MARK: - FromReduce【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension ButtonViewController: FromReduce {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func isMissReload(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(main_orientationStr.prefix(5)) + "as hu" + String(mainAutomaticUrl)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(const_correctName.prefix(7)) + main_spellMessage.replacingOccurrences(of: "reward", with: "ex"))].stringValue == BasicIndependent.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(mainCoreMsg.suffix(8)) + String(noti_scaleValue.prefix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.signWhen(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.third()
            //: TalkingSocketManager.shared.usageVC = nil
            PanCurrentStatus.shared.usageVC = nil
        }

        //: popCurrentViewController()
        pathShade()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func someTop(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        appropriateIn(logId: json[String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func more(data: [String: Any]) {
        //: TalkingAlertShow.hideAlert()
        MainTranslate.scheduleWhat()
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(show_fadeFormat.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(main_orientationStr.prefix(5)) + "as hu" + String(mainAutomaticUrl)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(show_storageStateValue))].stringValue == BasicIndependent.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(mainCoreMsg.suffix(8)) + String(noti_scaleValue.prefix(6))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if MountCustom.share.appStatus == AppSkinStatus.normal.rawValue {
            if MountCustom.share.appStatus == SensorRow.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: data_despiteStr.map{retMode(construct: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(data_staffAdjustmentPath.suffix(6)) + "her " + String(user_invitationFactoryString.suffix(5)) + "up t" + String(constFinishPopMessage.suffix(6)) + show_conversationData.replacingOccurrences(of: "entity", with: "e")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.signWhen(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.third()
            //: TalkingSocketManager.shared.usageVC = nil
            PanCurrentStatus.shared.usageVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.maleFlag()
        //: popCurrentViewController()
        pathShade()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func dealRate(msg: String, data: [String: Any]) {
        //: guard MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MountCustom.share.appStatus == SensorRow.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(userTimingStr.replacingOccurrences(of: "abs", with: "t"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(showParticleUrl) + String(data_labAlongString))] as? String else { return }
            //: let httpsUrl = MountCustom.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = MountCustom.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = EquipmentImplementation()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: RetainProcess.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            RetainProcess.share.mpCurrency(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            MainTranslate.packageCos(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(userAgoPath) + String(const_proTitle)).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(user_chainMaintainPath.suffix(6)) + String(app_personalFormat.suffix(4)) + "oins").localized)
            {
                //: TalkingAlertShow.hideAlert()
                MainTranslate.scheduleWhat()
                //: } rightBlock: {
            } rightBlock: {
                //: RetainProcess.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                RetainProcess.share.bod(clickEvent: mainBorderTitle, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func priceLocal(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.thePressure(giftArr: giftArr)
    }
}

// MARK: - SymbolConnect【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ButtonViewController: SymbolConnect {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func count(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(main_orientationStr.prefix(5)) + "as hu" + String(mainAutomaticUrl)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(notiMixString.replacingOccurrences(of: "curve", with: "se") + String(dataProPurePath))].stringValue == BasicIndependent.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(mainCoreMsg.suffix(8)) + String(noti_scaleValue.prefix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.signWhen(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.third()
            //: TalkingSocketManager.shared.usageVC = nil
            PanCurrentStatus.shared.usageVC = nil
        }

        //: popCurrentViewController()
        pathShade()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func no(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        appropriateIn(logId: json[String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - KitWrap【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension ButtonViewController: KitWrap {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func documentSingle(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(k_sendPath) + String(k_sureMsg)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.signWhen(showMsg: (String(const_programStr.suffix(7)) + user_careData.replacingOccurrences(of: "label", with: "e")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.third()
                //: TalkingSocketManager.shared.usageVC = nil
                PanCurrentStatus.shared.usageVC = nil
            }
            //: popCurrentViewController()
            pathShade()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(const_mobileTitle.prefix(5)) + "eptCal" + String(kFixedData)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MountCustom.share.appStatus == SensorRow.normal.rawValue else { return }
            //: RetainProcess.share.func__jumpToWebRecharge(sufficient: false)
            RetainProcess.share.bod(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(const_mobileTitle.prefix(5)) + "eptCal" + String(kFixedData)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.signWhen(showMsg: msg)
            //: popCurrentViewController()
            pathShade()
        }
    }
}

// MARK: - IndependentMove【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension ButtonViewController: IndependentMove {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func quickByTemporary() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (app_outsideTitle.lowercased() + String(notiPermanentMessage.prefix(4))), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: userCollectionString.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func choice() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.signWhen(showMsg: (String(notiRecoverName.prefix(5)) + " are " + show_pointCalculationData.replacingOccurrences(of: "after", with: "e")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (mainExampleThereFormat.replacingOccurrences(of: "vertical", with: "l") + String(dataPlayerMovementUrl.suffix(4))), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.signWhen(showMsg: (String(notiRecoverName.prefix(5)) + " are " + show_pointCalculationData.replacingOccurrences(of: "after", with: "e")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (String(k_sendPath) + String(k_sureMsg)), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: userCollectionString.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: if chatModel?.isRandomVCPush ?? false {
            if chatModel?.isRandomVCPush ?? false {
                //: TalkingAlertShow.alert(title: nil, message: "Continue matching with other users？".localized, leftBtnTitle: "No".localized, rightBtnTitle: "Yes".localized, leftBlock: {
                MainTranslate.packageCos(title: nil, message: String(bytes: user_ontoListenMsg.map{$0^44}, encoding: .utf8)!.localized, leftBtnTitle: "No".localized, rightBtnTitle: (String(user_effectPartnerMsg.prefix(3))).localized, leftBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: false)
                    self.frameworkTo(isNewRandow: false)
                    //: }, rightBlock: {
                }, rightBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: true)
                    self.frameworkTo(isNewRandow: true)
                    //: })
                })
                //: return
                return
                    //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
                self.signWhen(showMsg: (String(userFastKey.suffix(8)) + String(noti_chainTitle.prefix(6)) + "nded").localized)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (String(show_elementPath.suffix(5)) + String(const_problemName)), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            }
        }

        //: popCurrentViewController()
        pathShade()
    }

    /// 随机匹配通话挂断时
    //: func randowCallIsEndTool(isNewRandow: Bool) {
    func frameworkTo(isNewRandow: Bool) {
        //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
        self.signWhen(showMsg: (String(userFastKey.suffix(8)) + String(noti_chainTitle.prefix(6)) + "nded").localized)
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
        PanCurrentStatus.shared.banAcross(info: [String(bytes: app_writingShadeData.map{weekEdit(disabled: $0)}, encoding: .utf8)!: (String(show_elementPath.suffix(5)) + String(const_problemName)), String(bytes: const_intervalStr.map{onBrown(mount: $0)}, encoding: .utf8)!: [String(bytes: k_locationValue.map{foreEmpty(comply: $0)}, encoding: .utf8)!: self.chatModel?.logId]])

        //: if !isNewRandow{
        if !isNewRandow {
            //: if let targetVC = navigationController?.viewControllers.first(where: { $0 is RandomMatchVC }) {
            if let targetVC = navigationController?.viewControllers.first(where: { $0 is SpecifyConsume }) {
                //: navigationController?.viewControllers = [targetVC]
                navigationController?.viewControllers = [targetVC]
                //: navigationController?.popToViewController(targetVC, animated: false)
                navigationController?.popToViewController(targetVC, animated: false)
            }
            //: }else {
        } else {
            //: popCurrentViewController()
            pathShade()
            //: NotificationCenter.default.post(name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kImplementationMultiMsg, object: nil)
        }
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func opend(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.since(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func spanHome(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.panelFor(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.airInstall(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.underPartner()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func densePermanent() {
        //: TalkingSocketManager.shared.usageVC = self
        PanCurrentStatus.shared.usageVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = PageSecondCeiling.editorOf()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.priorEnable()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.data()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.alongLarge()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        pathShade()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func outLimited() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension ButtonViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func theDisplayProjection() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.sumRecord(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func scaleEnvironment() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
