
//: Declare String Begin

/*: "fee" :*/
fileprivate let user_numberMysteryMessage:String = "fpersonalpersonal"

/*: "VIPFee" :*/
fileprivate let const_ignorePath:[Character] = ["V"]
fileprivate let mainPortPath:String = "IPFeecharacter schedule spread"

/*: "chattingNum" :*/
fileprivate let mainAllowKey:[Character] = ["c","h","a","t","t","i"]
fileprivate let user_teamKitString:String = "ngNumtun port tick coordinate"

/*: "You're already in her live room" :*/
fileprivate let showButtValue:[UInt8] = [0x6d,0x6f,0x6f,0x72,0x20,0x65,0x76,0x69,0x6c,0x20,0x72,0x65,0x68,0x20,0x6e,0x69,0x20,0x79,0x64,0x61,0x65,0x72,0x6c,0x61,0x20,0x65,0x72,0x27,0x75,0x6f,0x59]

/*: "toUid" :*/
fileprivate let mainQuantityerestPath:[UInt8] = [0x78,0x63,0x59,0x65,0x68]

private func talkTrade(future num: UInt8) -> UInt8 {
    return num ^ 12
}

/*: "momentId" :*/
fileprivate let mainNetOperationStr:[UInt8] = [0x64,0x49,0x74,0x6e,0x65,0x6d,0x6f,0x6d]

/*: "extra" :*/
fileprivate let constFilterMessage:[UInt8] = [0x61,0x72,0x74,0x78,0x65]

/*: "uid" :*/
fileprivate let main_normProjectValue:[UInt8] = [0xd8,0xcc,0xc7]

fileprivate func goldCount(carrier num: UInt8) -> UInt8 {
    let value = Int(num) - 99
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let appShotRemoveName:[UInt8] = [0x3c,0x3b,0x39,0x36,0x3b,0x32,0x20,0x41,0x2e,0x41,0x42,0x40]

fileprivate func warningDense(decision num: UInt8) -> UInt8 {
    let value = Int(num) - 205
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isNewUser" :*/
fileprivate let dataPolicyName:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "userStatus" :*/
fileprivate let notiWhoMessage:[UInt8] = [0x1,0x7,0x11,0x6,0x27,0x0,0x15,0x0,0x1,0x7]

/*: "Account is restricted！" :*/
fileprivate let app_answerAdministrativeMessage:[Character] = ["A","c","c","o","u"]
fileprivate let dataBroadcastComplyStr:[Character] = ["n","t"," ","i","s"," ","r","e","s","t","r"]
fileprivate let show_bottomFormat:String = "icted！scenario event every permanent time"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let mainUnableAlongsideId:[UInt8] = [0x11,0x2c,0x34,0x37,0x30,0x2f,0xeb,0x3f,0x3a,0xeb,0x30,0x39,0x3f,0x30,0x3d,0xeb,0x3f,0x33,0x30,0xeb,0x2e,0x33,0x2c,0x3f,0xeb,0x3d,0x3a,0x3a,0x38,0xf9,0xeb,0x1b,0x37,0x30,0x2c,0x3e,0x30,0xeb,0x3f,0x3d,0x44,0xeb,0x2c,0x32,0x2c,0x34,0x39,0xeb,0x37,0x2c,0x3f,0x30,0x3d]

fileprivate func pullAverageParty(error num: UInt8) -> UInt8 {
    let value = Int(num) - 203
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "url" :*/
fileprivate let notiBigCalculationMsg:String = "loadrl"

/*: "userDetail" :*/
fileprivate let userContactKey:[Character] = ["u","s","e","r","D","e","t"]
fileprivate let showBoyUrl:String = "aiposition"

/*: "&type=6" :*/
fileprivate let mainJudgeUrl:[Character] = ["&","t","y","p","e","="]
fileprivate let show_helloFormat:String = "design"

/*: "&type=7" :*/
fileprivate let show_whyUrl:String = "light pass continuing&t"
fileprivate let main_commentAgoData:String = "deadline source cycleype=7"

/*: "amount" :*/
fileprivate let const_kingFormat:[UInt8] = [0x47,0x4b,0x49,0x53,0x48,0x52]

/*: "https://" :*/
fileprivate let main_flagInfrastructureKey:[Character] = ["h","t","t","p","s",":","/"]
fileprivate let const_againstKey:[Character] = ["/"]

/*: "http://" :*/
fileprivate let appRoundingTunFormat:[Character] = ["h","t","t","p",":","/","/"]

/*: "transparency" :*/
fileprivate let noti_rateWindowHungValue:String = "bod"
fileprivate let constOutsideBanData:[Character] = ["r","a","n","s","p","a","r","e","n","c","y"]

/*: "version= :*/
fileprivate let userItsId:[Character] = ["v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let constInstallPrimaryId:String = "team bot purchase chapter&packa"

/*: &bundleId= :*/
fileprivate let userSomeoneValue:String = "&bundlking parent"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RetainProcess.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class RetainProcess: NSObject {
public class RetainProcess: NSObject {
    //: @objc static public let share = RetainProcess()
    @objc public static let share = RetainProcess()
    //: private override init() {}
    override private init() {}
}

//: extension RetainProcess {
extension RetainProcess {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func qualityTotaleractionFormSequenceDangerousMedalAgainst(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        notiConsumptionMsg.curveLocate(eventID: mainWhiteMessage)
        //: guard TalkingSocketManager.shared.usage == false else {
        guard PanCurrentStatus.shared.usage == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.signWhen(showMsg: noti_makePermanentValue)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ChainForce.behindCustomerRow { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            AccelerateDelay.contrastSecond { succeed, result, errorModel in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = CrucialOrange()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(user_numberMysteryMessage.replacingOccurrences(of: "personal", with: "e"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(const_ignorePath) + String(mainPortPath.prefix(5)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(String(mainAllowKey) + String(user_teamKitString.prefix(5)))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.alongLarge()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func videoCustom(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.alongLarge() else { return }
        //: if let profileVC = currentVC as? TalkingUserDetailViewController {
        if let profileVC = currentVC as? DrawImplementation {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.deal()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = DrawImplementation(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        foreExpected()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func isSuspend(uid: String, enterType: LightPostExercise) {
        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(WhenUniform.show().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.signWhen(showMsg: String(bytes: showButtValue.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        MapLine().oneDocument(streamerUid: uid) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = LabelStrategy.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.romanValue()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: ThinChallenge.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! ThinChallenge).maintainObtain()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            WhenUniform.show().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = ThinChallenge()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                notiConsumptionMsg.curveLocate(eventID: appRankValue, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                notiConsumptionMsg.curveLocate(eventID: kConstantValue, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func blindArt(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = TitleViewController(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = romanValue()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func roundBeside(chatID: String, isFrom: PermanentStop = .Normal, completion: ((_ vc: SinceTime) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != StopBrush.getXiaoMiID() {
        if chatID != StopBrush.pinBy() { // 系统消息
            //: ProgressHUD.show()
            LevelBoxIndex.naturalBy()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: mainQuantityerestPath.map{talkTrade(future: $0)}, encoding: .utf8)!: chatID, String(bytes: mainNetOperationStr.reversed(), encoding: .utf8)!: "0", String(bytes: constFilterMessage.reversed(), encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            VariableCombine.scanFor(param: param) { [weak self] succeed, result, errorModel in
                //: ProgressHUD.dismiss()
                LevelBoxIndex.gestureOff()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = BoundMeasureTrack.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = BoundMeasureTrack.prompt(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                StageImplement.consumptionDisappear(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: main_normProjectValue.map{goldCount(carrier: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: appShotRemoveName.map{warningDense(decision: $0)}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: dataPolicyName.reversed(), encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: notiWhoMessage.map{$0^116}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: data_appearanceData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.signWhen(showMsg: (String(app_answerAdministrativeMessage) + String(dataBroadcastComplyStr) + String(show_bottomFormat.prefix(6))).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.optionProcessing(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: BoundMeasureTrack())
                let chatVC = self.optionProcessing(chatID: chatID, isFrom: isFrom, userWrap: BoundMeasureTrack())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: BoundMeasureTrack ) -> TalkingPrivateChatController {
    func optionProcessing(chatID: String, isFrom: PermanentStop = .Normal, userWrap: BoundMeasureTrack) -> SinceTime {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = romanValue()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: SinceTime.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! SinceTime
                    //: if vc.msgView.targetID == chatID {
                    if vc.msgView.targetID == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = SinceTime(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func meAm(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        LevelBoxIndex.naturalBy()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        VariableCombine.usOfMove(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            LevelBoxIndex.gestureOff()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.signWhen(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if StopBrush.isGroupChat(groupId) {
            if StopBrush.serverCommand(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.everyDangerous(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = SampleColumn(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.romanValue()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, desc in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.everyDangerous(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = SampleColumn(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.romanValue()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.signWhen(showMsg: String(bytes: mainUnableAlongsideId.map{pullAverageParty(error: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func everyDangerous(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.romanValue()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: SampleColumn.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! SampleColumn
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func overPair(uid: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func examine() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = romanValue()
        //: let vc = TalkingFeedbackVC.init()
        let vc = TableSumeraction()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func signContain(jumpModel: RedManagerModel) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (notiBigCalculationMsg.replacingOccurrences(of: "load", with: "u")) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            mpCurrency(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(userContactKey) + showBoyUrl.replacingOccurrences(of: "position", with: "l")) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func userQuestion() {
        //: RetainProcess.share.func__pushToWebVC(webViewType: .SubscribePage)
        RetainProcess.share.versusResManageLocalUniqueColorExit(webViewType: .SubscribePage)
    }
}

// MARK: - 跳转H5

//: extension RetainProcess {
extension RetainProcess {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func versusResManageLocalUniqueColorExit(webViewType: FractionMoveButt) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        adminDown(webViewType: webViewType, webConfig: EquipmentImplementation())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func platformMark(popupCallIndex: Int, appendParams: String = (String(mainJudgeUrl) + show_helloFormat.replacingOccurrences(of: "design", with: "6"))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = RouteView()
        //: view.show()
        view.betweenAccessVolume()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.adjustExample(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                balanceGreat(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func bod(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(show_whyUrl.suffix(2)) + String(main_commentAgoData.suffix(5)))) {
        //: let payWinType = MountCustom.share.appUserConfigMode.payWinType
        let payWinType = MountCustom.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = RouteView()
        //: view.show()
        view.betweenAccessVolume()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (MountCustom.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (MountCustom.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.adjustExample(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                balanceGreat(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func balanceGreat(appendParams: String = (String(show_whyUrl.suffix(2)) + String(main_commentAgoData.suffix(5)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = AverageGraph.strengthButtonEdit(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: RetainProcess.share.func__pushToWebVC(urlStr: urlStr)
        RetainProcess.share.mpCurrency(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        notiConsumptionMsg.paintDown(eventID: user_buttonDirectionMethodName, parameterStr: [String(bytes: const_kingFormat.map{$0^38}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func adjustExample(webViewType: FractionMoveButt, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = EquipmentImplementation()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        adminDown(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            constConstantDelayValue = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                notiConsumptionMsg.paintDown(eventID: clickEvent, parameterStr: [String(bytes: const_kingFormat.map{$0^38}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func tableMore() {
        //: guard !MountCustom.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !MountCustom.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = MountCustom.share.appConfigMode.urlH5Domain + MountCustom.share.appUserConfigMode.discountUrl
        let httpsUrl = MountCustom.share.appConfigMode.urlH5Domain + MountCustom.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (String(main_flagInfrastructureKey) + String(const_againstKey)), with: (String(appRoundingTunFormat)))
        //: RetainProcess.share.func__pushToWebVC(urlStr: httpUrl)
        RetainProcess.share.mpCurrency(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func adminDown(webViewType: FractionMoveButt, webConfig: EquipmentImplementation) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = AverageGraph.strengthButtonEdit(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        pasFormat(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func mpCurrency(urlStr: String?, webConfig: EquipmentImplementation? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? EquipmentImplementation() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        pasFormat(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func pasFormat(urlStr: String?, webViewType: FractionMoveButt?, webConfig: EquipmentImplementation) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(noti_rateWindowHungValue.replacingOccurrences(of: "bod", with: "t") + String(constOutsideBanData))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(userItsId)) + "\(kJoinKey)" + (String(constInstallPrimaryId.suffix(6)) + "geId=") + "\(data_scientificComposeFormat)" + (String(userSomeoneValue.prefix(6)) + "eId=") + "\(show_embraceValue)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = GrainRequest(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = romanValue() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: GrainRequest.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! GrainRequest).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.highlightLike(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: mainCellMsg, height: mainCellMsg / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension RetainProcess {
public extension RetainProcess {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func romanValue() -> UIViewController? {
        //: return currentViewController()
        return alongLarge()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct RedManagerModel: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
