
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let const_identifyFormat:String = "split dinner database readbg_me"
fileprivate let const_closeMessage:String = "reader his variable gate flage_top"

/*: "777777" :*/
fileprivate let appEquivalentEnhancePath:String = "77777"
fileprivate let k_complyStr:String = "7"

/*: "Messages" :*/
fileprivate let kAngleName:[Character] = ["M","e","s","s","a","g","e","s"]

/*: "Who like me" :*/
fileprivate let k_nobodyValue:[Character] = ["W","h","o"," ","l","i","k","e"," ","m","e"]

/*: "Call" :*/
fileprivate let notiAppealEaseName:[Character] = ["C","a","l","l"]

/*: "#FF2348" :*/
fileprivate let k_arrowKey:[Character] = ["#","F","F","2","3","4","8"]

/*: "Current network unavailable" :*/
fileprivate let const_bindSectionKey:[Character] = ["C","u","r","r","e","n","t"," ","n","e","t","w","o","r","k"," ","u","n","a","v","a","i","l","a","b"]
fileprivate let main_onceUrl:String = "find"

/*: "icon_yidu_pre" :*/
fileprivate let notiRoundLobbyFormat:String = "icon_account flag against unless fast"
fileprivate let main_withPath:[Character] = ["y","i","d","u","_","p","r","e"]

/*: "transform.rotation" :*/
fileprivate let const_evolutionKey:[Character] = ["t","r","a","n","s","f","o","r","m","."]
fileprivate let const_bridgeMessage:[Character] = ["r","o"]
fileprivate let show_reduceId:String = "backabackion"

/*: "transform.scale" :*/
fileprivate let dataBlockMessage:[Character] = ["t","r","a","n","s","f","o","r","m",".","s","c","a","l","e"]

/*: "zoom&shake" :*/
fileprivate let const_offId:String = "zalready"
fileprivate let app_characterAfterFormat:[Character] = ["o","m","&","s","h","a","k","e"]

/*: "yyyy-MM-dd" :*/
fileprivate let data_toleranceKey:[Character] = ["y","y","y","y","-","M","M"]
fileprivate let show_userTranslationString:String = "active entry future panel-dd"

/*: "Allow %@ to send you notifications?" :*/
fileprivate let appAreaId:[UInt8] = [0x3f,0x73,0x6e,0x6f,0x69,0x74,0x61,0x63,0x69,0x66,0x69,0x74,0x6f,0x6e,0x20,0x75,0x6f,0x79,0x20,0x64,0x6e,0x65,0x73,0x20,0x6f,0x74,0x20,0x40,0x25,0x20,0x77,0x6f,0x6c,0x6c,0x41]

/*: "Cancel" :*/
fileprivate let user_meetPath:[Character] = ["C","a","n","c","e"]
fileprivate let show_slideId:String = "consume"

/*: "Settings" :*/
fileprivate let kRemainTitle:String = "Setsay cos lock"
fileprivate let noti_wordFormat:[Character] = ["t","i","n","g","s"]

/*: "badNumber" :*/
fileprivate let appCalculateForeNoMessage:String = "succeed earlybadN"
fileprivate let k_hardName:[Character] = ["u","m","b","e","r"]

/*: "isConnection" :*/
fileprivate let show_dynamicsMsg:[Character] = ["i","s","C","o","n","n","e","c","t","i","o","n"]

/*: "networkStatus" :*/
fileprivate let userReduceId:String = "interestt"
fileprivate let constInvisibleMsg:[Character] = ["w","o","r","k","S","t","a","t","u","s"]

/*: "unreadMessageNum" :*/
fileprivate let mainFileId:String = "unreadMassociated characteristic purple pop"
fileprivate let user_laterFormat:[Character] = ["e","s","s","a","g","e","N","u","m"]

/*: "Do you want to mark all messages as read?" :*/
fileprivate let showAdministrativeStr:[UInt8] = [0xb7,0x9c,0xd3,0x8a,0x9c,0x86,0xd3,0x84,0x92,0x9d,0x87,0xd3,0x87,0x9c,0xd3,0x9e,0x92,0x81,0x98,0xd3,0x92,0x9f,0x9f,0xd3,0x9e,0x96,0x80,0x80,0x92,0x94,0x96,0x80,0xd3,0x92,0x80,0xd3,0x81,0x96,0x92,0x97,0xcc]

private func mountHarassment(judge num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "消息列表一键已读失败：code: :*/
fileprivate let show_minStr:String = "\u{6d88}息列表"
fileprivate let k_cropVerticalMsg:String = "失败：code:reduction next editor service"

/*: , desc: :*/
fileprivate let user_onceFormat:[Character] = [","," ","d","e","s","c",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  IndicatorViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class IndicatorViewController: PetViewController {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        everyRemote()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        anyAudience()
        //: bindInteraction()
        manageSecureStack()
        //: func__turnOnSystemNotification()
        strokeActivity()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: 100 + mainItemFormat))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.evolutionary(name: (String(const_identifyFormat.suffix(5)) + "ssag" + String(const_closeMessage.suffix(5))))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: LineView = {
        //: let V = TalkingNoticeTipView()
        let V = LineView()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: PinPan = {
        //: let vc = QYSlideNavigationViewController()
        let vc = PinPan()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .stepTransition(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .stepTransition(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (appEquivalentEnhancePath.capitalized + k_complyStr.capitalized))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.labelMomentum()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [PetViewController] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, AdjustSmart()]
        //: if MountCustom.share.loginUserMode.callTabSwitch == 1 {
        if MountCustom.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(AmongPrepare(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: TitleViewController = {
        //: let vc = TalkingChatListViewController()
        let vc = TitleViewController()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(kAngleName)).localized, (String(k_nobodyValue)).localized]
        //: if MountCustom.share.loginUserMode.callTabSwitch == 1 {
        if MountCustom.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(notiAppealEaseName)).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: mainItemFormat, width: mainCellMsg, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.observeFor(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(k_arrowKey)))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(const_bindSectionKey) + main_onceUrl.replacingOccurrences(of: "find", with: "le")).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: OperationQuery = {
        //: let numbLabel = BadgeLab()
        let numbLabel = OperationQuery()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: OperationQuery = {
        //: let numbLabel = BadgeLab()
        let numbLabel = OperationQuery()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(notiRoundLobbyFormat.prefix(5)) + String(main_withPath))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.evolutionary(name: (String(notiRoundLobbyFormat.prefix(5)) + String(main_withPath))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - KeyShadeSpecify

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension IndicatorViewController: KeyShadeSpecify {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func to(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: TitleViewController.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            everyRemote()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: AdjustSmart.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            notiConsumptionMsg.curveLocate(eventID: noti_tabKey)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension IndicatorViewController {
    //: func setIsTopAll() {
    func permanentWith() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? TitleViewController
            //: vc?.resetToTopItemView()
            vc?.noneDirection()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.beyondConsume(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func respectivePlace() {
        //: if MountCustom.share.networkStatus != .Unavailable && SampleDepth.shared.isConnection {
        if MountCustom.share.networkStatus != .Unavailable, SampleDepth.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func beforeUpMonth(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func everyRemote() {
        //: let unreadMsgCount = MountCustom.share.unreadMessageNum
        let unreadMsgCount = MountCustom.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: main_compareUrl) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.symbolAcross()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func symbolAcross() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (String(const_evolutionKey) + String(const_bridgeMessage) + show_reduceId.replacingOccurrences(of: "back", with: "t")))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (String(dataBlockMessage)))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (const_offId.replacingOccurrences(of: "already", with: "o") + String(app_characterAfterFormat)))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func strokeActivity() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        ChainForce.asS { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.numberegrity(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.sizeDelete(date: Date(), dateFormat: (String(data_toleranceKey) + String(show_userTranslationString.suffix(3))))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = data_toStr.string(forKey: userHandleKey), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.numberegrity(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.numberegrity(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = data_toStr.bool(forKey: k_amongTellId)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        data_toStr.set(true, forKey: k_amongTellId)
                        //: TalkingAlertShow.alert(title: nil,
                        MainTranslate.packageCos(title: nil,
                                                    //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                    message: String(bytes: appAreaId.reversed(), encoding: .utf8)!.fieldOf(const_informationUniqueChartPath),
                                                    //: leftBtnTitle: "Cancel".localized,
                                                    leftBtnTitle: (String(user_meetPath) + show_slideId.replacingOccurrences(of: "consume", with: "l")).localized,
                                                    //: rightBtnTitle: "Settings".localized) {
                                                    rightBtnTitle: (String(kRemainTitle.prefix(3)) + String(noti_wordFormat)).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            MainTranslate.scheduleWhat()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func numberegrity(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(data_visitorPath)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = kPressEasePublishString - data_visitorPath - appRetainName
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(data_visitorPath + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = kPressEasePublishString - self.noticeView.bottom - appRetainName
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension IndicatorViewController {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func bothOf(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(appCalculateForeNoMessage.suffix(4)) + String(k_hardName))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension IndicatorViewController {
    /// UI
    //: private func createUI() {
    private func anyAudience() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(data_visitorPath)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(k_nobodyValue)).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(kAngleName)).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func manageSecureStack() {
        //: SampleDepth.shared.rx
        SampleDepth.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(show_dynamicsMsg)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.respectivePlace()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: MountCustom.share.rx.observeWeakly(Int.self, "networkStatus")
        MountCustom.share.rx.observeWeakly(Int.self, (userReduceId.replacingOccurrences(of: "interest", with: "ne") + String(constInvisibleMsg)))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.respectivePlace()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: MountCustom.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        MountCustom.share.rx.observeWeakly(Int.self, (String(mainFileId.prefix(7)) + String(user_laterFormat)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.beforeUpMonth(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = ComposeS()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                MainTranslate.quantityegrationRed(message: String(bytes: showAdministrativeStr.map{mountHarassment(judge: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(user_meetPath) + show_slideId.replacingOccurrences(of: "consume", with: "l")).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    MainTranslate.scheduleWhat()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: main_compareUrl)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        MusicThread.layerLanguage(msg: (show_minStr + "一\u{952e}\u{5df2}读" + String(k_cropVerticalMsg.prefix(8))) + "\(code)" + (String(user_onceFormat)) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(bothOf(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: appTagAngleFormat,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.sizeDelete(date: Date(), dateFormat: (String(data_toleranceKey) + String(show_userTranslationString.suffix(3))))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            data_toStr.set(today, forKey: userHandleKey)
            //: self.func__hideNotificationTipView(hide: true)
            self.numberegrity(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] notification in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.strokeActivity()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
