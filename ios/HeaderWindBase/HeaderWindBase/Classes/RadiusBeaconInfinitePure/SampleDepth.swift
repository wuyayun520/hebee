
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let constExtraUrl:String = "immediate connect verticalIM退出登"
fileprivate let dataToThreadKey:String = "code:char sea direct closed lock"

/*: , desc: :*/
fileprivate let noti_resourceKey:String = "assistant pro, desc"
fileprivate let show_suspendUrl:String = ":"

/*: "IM登录失败，code: :*/
fileprivate let main_roundingPath:String = "IM登录\u{5931}败"
fileprivate let show_awayValue:String = "layer do translate business vid，code:"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let show_yellowDisabledName:[Character] = ["I","M","获","取","未","读"]
fileprivate let appBackgroundMsg:[Character] = ["\u{6d88}","息","\u{603b}","数","失","败","，","c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let k_waistRangePath:[Character] = ["c","r","u","s","h","B","a","d","g"]
fileprivate let appSubstantialMessage:String = "eff"

/*: "badNumber" :*/
fileprivate let const_managerFormat:[UInt8] = [0x44,0x43,0x46,0x30,0x57,0x4f,0x44,0x47,0x54]

fileprivate func impressionCon(permission num: UInt8) -> UInt8 {
    let value = Int(num) + 30
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let user_advancedMessage:String = "pldespitea"

/*: "roomNum" :*/
fileprivate let main_analyzeMsg:String = "reply editorroomNum"

/*: "num" :*/
fileprivate let app_taKey:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let const_movePath:String = "oncut"
fileprivate let dataRegionFormat:[Character] = ["m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let showStimulateWithConstantMessage:[Character] = ["L","I","V","E","-","C","H","A","T"]

/*: "extra" :*/
fileprivate let const_tingBringKey:String = "EXTRA"

/*: "msgType" :*/
fileprivate let noti_pinName:String = "main createmsgTy"
fileprivate let appMinuteMsg:String = "cape"

/*: "newPkgGift" :*/
fileprivate let k_refuseValue:String = "nfuneral"
fileprivate let const_manTitle:[Character] = ["w","P","k","g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let showProcessPointString:[Character] = ["M","F",":","C","h"]
fileprivate let constAdTransformId:[Character] = ["a","t","M","e","n","t","i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let show_fragmentPath:[UInt8] = [0x29,0x35,0x38]

private func numberComprehensive(document num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "onlineStatus" :*/
fileprivate let user_hungIllegalString:[UInt8] = [0x80,0x81,0x83,0x86,0x81,0x8a,0xbc,0x9b,0x8e,0x9b,0x9a,0x9c]

/*: "isNewUser" :*/
fileprivate let noti_toolWaistYesKey:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "c2c_ :*/
fileprivate let noti_processUrl:String = "recording accelerate appealc2c_"

/*: "IM获取会话失败，convId: :*/
fileprivate let show_fireTitle:String = "IM获\u{53d6}会"
fileprivate let show_emptyUrl:String = "convId:i sequence sufficient"

/*: , code: :*/
fileprivate let mainSystemData:String = "constraint painter successful first, code"
fileprivate let showEnvironmentString:[Character] = [":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let showNeverData:String = "IM\u{63d2}入消息失败"
fileprivate let main_easternMessage:[Character] = ["\u{ff0c}","t","a","r"]
fileprivate let constMusicTitle:[Character] = ["g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let app_conValue:[Character] = ["c","h","a","n","g","e","D"]
fileprivate let kBroadGateData:[Character] = ["i","r","e","c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let data_attributeValue:String = "rYMsreason length bag both"

/*: "GJ:CallCustom" :*/
fileprivate let userAnimaFormat:String = "base advertising gravity temporaryGJ:Cal"
fileprivate let show_pureStr:String = "tomemory"

/*: "IM获取会话列表失败，code: :*/
fileprivate let mainIdentityData:[Character] = ["I","M","获","取","会"]
fileprivate let appYaBulletValue:String = "话列表失败"
fileprivate let data_insertTitle:[Character] = ["，","c","o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let notiTempFormat:[Character] = ["m"]
fileprivate let const_goldMessage:String = "tolerance perform scenario turnsgInfo"

/*: "needFold" :*/
fileprivate let showSubstantialId:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let user_limitValue:String = "corner item local tip momentgroup_"

/*: "IM删除会话失败，convID: :*/
fileprivate let user_withTitle:String = "IM删除at which capture"
fileprivate let data_documentFastUrl:String = "onvID:table finish equipment despite pin"

/*: "IM置顶会话失败，convId: :*/
fileprivate let noti_factorName:String = "region upperIM置顶会"
fileprivate let mainTotalTitle:String = "privacy cut painter padconvId"
fileprivate let const_slideBoyName:[Character] = [":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let dataDraftPullMessage:[Character] = ["S","D","K"," ","正","在","连","接","到","\u{670d}","务"]
fileprivate let userInjuryMidTitle:[Character] = ["器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let notiWouldTitle:String = "gold builderSDK 已"
fileprivate let mainSmallInsertName:String = "\u{7ecf}成"
fileprivate let noti_alreadyString:String = "功\u{8fde}接到服务器"

/*: "IM连接服务器失败，code: :*/
fileprivate let constArcString:[Character] = ["I","M","\u{8fde}","接","\u{670d}","务","\u{5668}"]
fileprivate let data_persistString:[Character] = ["失","\u{8d25}","，","c","o","d","e",":"]

/*: , error: :*/
fileprivate let show_darkKey:String = ", errthreshold drag request"
fileprivate let const_criticalName:String = "keep resume lost aor:"

/*: "IM账号被挤下线。" :*/
fileprivate let noti_existingMsg:String = "IM账\u{53f7}被"
fileprivate let userProjectData:[Character] = ["挤","下","线","\u{3002}"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let userArrowTitle:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let notiAssociatedValue:String = "document accelerateOfflin"
fileprivate let app_quickId:String = "easyfeasyc"

/*: "Cancel" :*/
fileprivate let const_speedMsg:String = "construct now detectCance"
fileprivate let data_incomeAppValue:String = "strategy"

/*: "Re Login" :*/
fileprivate let userOutsideString:[Character] = ["R","e"," "]
fileprivate let main_integrationUrl:[Character] = ["L","o","g","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SampleDepth.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol ObserverSimilar: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func immediateTemporary(data: [VerticalName])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func specifyPost(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func reply(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum GraphHide {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class SampleDepth: NSObject {
@objcMembers public class SampleDepth: NSObject {
    //: public static let shared = SampleDepth()
    public static let shared = SampleDepth()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [VerticalName] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [VerticalName] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [VerticalName] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [VerticalName] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [VerticalName] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: VerticalName]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.permanentTheme()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func crushEdit() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(user_toTitle, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func permanentTheme() {
        //: func__removeAllDelegate()
        kindReason()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.volumeDense()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func volumeDense() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = VerticalName.outsideTop(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = VerticalName.outsideTop(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func exceptObtain() -> [VerticalName] {
        // 审核模式，在消息列表移除官方客服
        //: if MountCustom.share.appStatus == AppSkinStatus.special.rawValue {
        if MountCustom.share.appStatus == SensorRow.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == StopBrush.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == StopBrush.immediateProcessing() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension SampleDepth {
extension SampleDepth {
    //: func func__removeAllDelegate() {
    func kindReason() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func omit(_ delegate: ObserverSimilar) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func source(_ delegate: ObserverSimilar) {
        //: self.func__invokeDelegate {
        self.socialConversation {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func socialConversation(_ invocation: (ObserverSimilar) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! ObserverSimilar)
        }
    }
}

// MARK: - IM登录态管理

//: extension SampleDepth {
extension SampleDepth {
    /// 退出登录IM
    //: func func__LogingOut() {
    func makeBold() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
//            MusicThread.layerLanguage(msg: (String(constExtraUrl.suffix(5)) + "录失败\u{ff0c}" + String(dataToThreadKey.prefix(5))) + "\(code)" + (String(noti_resourceKey.suffix(6)) + show_suspendUrl.capitalized) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.permanentTheme()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func provide(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = MountCustom.share.appUserConfigMode.uSig
        let uSigStr = MountCustom.share.appUserConfigMode.uSig
        //: let uidStr = MountCustom.share.loginUserMode.userID
        let uidStr = MountCustom.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.versionToProvider()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
//            MusicThread.layerLanguage(msg: (main_roundingPath + String(show_awayValue.suffix(6))) + "\(code)" + (String(noti_resourceKey.suffix(6)) + show_suspendUrl.capitalized) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func beyondReplacement() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || MountCustom.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || MountCustom.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.signWhen(showMsg: dataStoragePath)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension SampleDepth {
extension SampleDepth {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func versionToProvider() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
//            MusicThread.layerLanguage(msg: (String(show_yellowDisabledName) + String(appBackgroundMsg)) + "\(code)" + (String(noti_resourceKey.suffix(6)) + show_suspendUrl.capitalized) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func advertising() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        VariableCombine.clearPlace { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(k_waistRangePath) + appSubstantialMessage.replacingOccurrences(of: "eff", with: "e"))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: appTagAngleFormat, object: self, userInfo: [String(bytes: const_managerFormat.map{impressionCon(permission: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func seek(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        VariableCombine.graphic { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(user_advancedMessage.replacingOccurrences(of: "despite", with: "az"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(main_analyzeMsg.suffix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = VerticalName.outsideTop(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(app_taKey))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(const_movePath.replacingOccurrences(of: "cut", with: "ly") + "OneRoo" + String(dataRegionFormat))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension SampleDepth: V2TIMAdvancedMsgListener {
extension SampleDepth: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(showStimulateWithConstantMessage)))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            RedUp.country().maintainWeek(msg: msg)
            //: return
            return
        }
        //: if msg.sender == MountCustom.share.loginUserMode.userID { return }
        if msg.sender == MountCustom.share.loginUserMode.userID { return }

        //: let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = NextGraphic.showHero(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(const_tingBringKey.lowercased())] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? PadModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: const_visibleTweenKey, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(noti_pinName.suffix(5)) + appMinuteMsg.replacingOccurrences(of: "cap", with: "p"))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (k_refuseValue.replacingOccurrences(of: "funeral", with: "e") + String(const_manTitle)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: user_mapStr, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TUIMsgChatManager.checkVoiceMessage(message: msg) {
            if NextGraphic.roundAdvertising(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = CaptureMend.tempChart(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                SwitchtoCharacter.background(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.quoteRemind(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            TensionCondition.shared.slowShould(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            TensionCondition.shared.sineIn(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(showProcessPointString) + String(constAdTransformId)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            AlwaysElement.share.position(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: PerformRemove.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: PerformRemove.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != StopBrush.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != StopBrush.pinBy() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.socialConversation { $0.reply?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            UniqueBorderReduce.shared.merge(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: show_fragmentPath.map{numberComprehensive(document: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: user_hungIllegalString.map{$0^239}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: noti_toolWaistYesKey.reversed(), encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: data_appearanceData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            StageImplement.indicatorMethod(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            PerformRemove.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: kCouldMsg, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: NextAgainst, isPrivete: Bool) {
    @objc public func scaleAlive(targetId: String, msgCellData: NextAgainst, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.mountRating(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(noti_processUrl.suffix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.isolateAspect(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.totalegrationPet(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
//                MusicThread.layerLanguage(msg: (show_fireTitle + "\u{8bdd}失败，" + String(show_emptyUrl.prefix(7))) + "\(convId)" + (String(mainSystemData.suffix(6)) + String(showEnvironmentString)) + "\(code)" + (String(noti_resourceKey.suffix(6)) + show_suspendUrl.capitalized) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func mountRating(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = MountCustom.share.loginUserMode.userID
        let senderStr = MountCustom.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
//                MusicThread.layerLanguage(msg: (showNeverData + String(main_easternMessage) + String(constMusicTitle)) + "\(targetId)" + (String(mainSystemData.suffix(6)) + String(showEnvironmentString)) + "\(code)" + (String(noti_resourceKey.suffix(6)) + show_suspendUrl.capitalized) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func quoteRemind(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(app_conValue) + String(kBroadGateData))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && RetainProcess.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(data_attributeValue.prefix(4)) + "gType")].stringValue == (String(userAnimaFormat.suffix(6)) + "lCus" + show_pureStr.replacingOccurrences(of: "memory", with: "m")) && isSelf && RetainProcess.share.romanValue()!.isKind(of: SinceTime.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: notiSubstantialFilterData) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: notiSubstantialFilterData)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension SampleDepth: V2TIMConversationListener {
extension SampleDepth: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.isolateAspect(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.totalegrationPet(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.isolateAspect(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.totalegrationPet(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: MountCustom.share.unreadMessageNum = Int(totalUnreadCount)
        MountCustom.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.socialConversation {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.specifyPost?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func totalegrationPet(conVersationList: [VerticalName]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if StageImplement.underCur().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                StageImplement.underCur().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.socialConversation {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.immediateTemporary?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func smartBirthday(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [VerticalName] = []

        // V2TIMConversation->VerticalName
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = VerticalName(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.role(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [VerticalName] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = ImplementationBy.titleure(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.scope(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension SampleDepth {
extension SampleDepth {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func at(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.smartBirthday(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.totalegrationPet(conVersationList: arr as? [VerticalName] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
//            MusicThread.layerLanguage(msg: (String(mainIdentityData) + appYaBulletValue.capitalized + String(data_insertTitle)) + "\(code)" + (String(noti_resourceKey.suffix(6)) + show_suspendUrl.capitalized) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func group(targetId: String) -> VerticalName? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func isolateAspect(convList: [V2TIMConversation]) -> [VerticalName] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [VerticalName] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = VerticalName(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.role(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.scope(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func scope(convModel: VerticalName, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.volumeDense()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: GraphHide = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - MountCustom.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - MountCustom.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = StageImplement.directly(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            let dict = NextGraphic.showHero(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(const_tingBringKey.lowercased())][(String(notiTempFormat) + String(const_goldMessage.suffix(6)))][(String(showSubstantialId))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.distance(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.distance(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func distance(convList: [VerticalName], dataType: GraphHide) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func denseLocalBubbleInmateTemporaryThreshold(targetId: String, dataType: GraphHide) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(noti_processUrl.suffix(4))) + "\(targetId)" : (String(user_limitValue.suffix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            StageImplement.scheduleWith(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(MountCustom.share.loginUserMode.userID, toUid: targetId)
            SwitchtoCharacter.requestOn(MountCustom.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.versionToProvider()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
//            MusicThread.layerLanguage(msg: (String(user_withTitle.prefix(4)) + "会话失败，c" + String(data_documentFastUrl.prefix(6))) + "\(convID)" + (String(mainSystemData.suffix(6)) + String(showEnvironmentString)) + "\(code)" + (String(noti_resourceKey.suffix(6)) + show_suspendUrl.capitalized) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func findEqually(convModel: VerticalName, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
//            MusicThread.layerLanguage(msg: (String(noti_factorName.suffix(5)) + "话失败\u{ff0c}" + String(mainTotalTitle.suffix(6)) + String(const_slideBoyName)) + "\(convModel.conversationID)" + (String(mainSystemData.suffix(6)) + String(showEnvironmentString)) + "\(code)" + (String(noti_resourceKey.suffix(6)) + show_suspendUrl.capitalized) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.distance(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.distance(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension SampleDepth: V2TIMSDKListener {
extension SampleDepth: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(dataDraftPullMessage) + String(userInjuryMidTitle)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(notiWouldTitle.suffix(5)) + mainSmallInsertName + noti_alreadyString))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        MusicThread.layerLanguage(msg: (String(constArcString) + String(data_persistString)) + "\(code)" + (String(show_darkKey.prefix(5)) + String(const_criticalName.suffix(3))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        MusicThread.layerLanguage(msg: (noti_existingMsg + String(userProjectData)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        PanCurrentStatus.shared.beautyField(userLoginOut: true)
        //: SampleDepth.shared.isConnection = false
        SampleDepth.shared.isConnection = false

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: userArrowTitle.reversed(), encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = ComposeS()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if PutDirection.share.interfaceLang == TempStroke.ar.rawValue || PutDirection.share.interfaceLang == TempStroke.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            PutDirection.share.interfaceLang == TempStroke.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.monsterColumn(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        MainTranslate.packageCos()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        MainTranslate.packageCos(title: (String(notiAssociatedValue.suffix(6)) + "e Not" + app_quickId.replacingOccurrences(of: "easy", with: "i") + "ation").localized, message: msg, leftBtnTitle: (String(const_speedMsg.suffix(5)) + data_incomeAppValue.replacingOccurrences(of: "strategy", with: "l")).localized, rightBtnTitle: (String(userOutsideString) + String(main_integrationUrl)).localized, leftBlock: {
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            WhenCritical.noneKit { t in
                //: ProgressHUD.dismiss()
                LevelBoxIndex.gestureOff()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: show_exerciseString, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            WhenCritical.towardStar { t in
                //: if t {
                if t {
                    //: SampleDepth.shared.func__LogingIn { _ in
                    SampleDepth.shared.provide { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if WhenUniform.show().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(WhenUniform.show().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    PanCurrentStatus.shared.report()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
