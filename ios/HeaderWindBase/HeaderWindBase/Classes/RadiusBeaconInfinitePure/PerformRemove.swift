
//: Declare String Begin

/*: "CacheIntimatePhotoUnlockMsgIdArrayKey" :*/
fileprivate let kEvolveData:[UInt8] = [0x68,0x4a,0x48,0x43,0x4e,0x62,0x45,0x5f,0x42,0x46,0x4a,0x5f,0x4e,0x7b,0x43,0x44,0x5f,0x44,0x7e,0x45,0x47,0x44,0x48,0x40,0x66,0x58,0x4c,0x62,0x4f,0x6a,0x59,0x59,0x4a,0x52,0x60,0x4e,0x52]

private func itPara(construct num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "CacheQuestionReplayMsgIdIdArrayKey" :*/
fileprivate let user_careString:[UInt8] = [0xc1,0xdf,0xe1,0xe6,0xe3,0xcf,0xf3,0xe3,0xf1,0xf2,0xe7,0xed,0xec,0xd0,0xe3,0xee,0xea,0xdf,0xf7,0xcb,0xf1,0xe5,0xc7,0xe2,0xc7,0xe2,0xbf,0xf0,0xf0,0xdf,0xf7,0xc9,0xe3,0xf7]

fileprivate func triggerThen(norm num: UInt8) -> UInt8 {
    let value = Int(num) + 130
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "CachePrivateChatVideoTipsIsShow_ :*/
fileprivate let app_purchaseId:[UInt8] = [0x88,0xaa,0xa8,0xa3,0xae,0x9b,0xb9,0xa2,0xbd,0xaa,0xbf,0xae,0x88,0xa3,0xaa,0xbf,0x9d,0xa2,0xaf,0xae,0xa4,0x9f,0xa2,0xbb,0xb8,0x82,0xb8,0x98,0xa3,0xa4,0xbc,0x94]

/*: "Quick greeting can only be sent to him once" :*/
fileprivate let userEnableMKey:[UInt8] = [0x54,0x70,0x6c,0x66,0x6e,0x25,0x62,0x77,0x60,0x60,0x71,0x6c,0x6b,0x62,0x25,0x66,0x64,0x6b,0x25,0x6a,0x6b,0x69,0x7c,0x25,0x67,0x60,0x25,0x76,0x60,0x6b,0x71,0x25,0x71,0x6a,0x25,0x6d,0x6c,0x68,0x25,0x6a,0x6b,0x66,0x60]

private func priceIgnore(accessible num: UInt8) -> UInt8 {
    return num ^ 5
}

/*: "txt" :*/
fileprivate let constWeAdvertisingTitle:[Character] = ["t","x","t"]

/*: "audio" :*/
fileprivate let constCurrentReceiverMessage:String = "auinserto"

/*: "Please add greeting text" :*/
fileprivate let userDueId:String = "picture service healthy independent sequencePleas"
fileprivate let appCoordinateProductStr:String = " grepremium white pas"
fileprivate let mainInsteadKey:String = "xsystem"

/*: "You have not set up a photo greeting, please set it up before sending!" :*/
fileprivate let mainDenseMediumStr:[UInt8] = [0xee,0xd8,0xc2,0x97,0xdf,0xd6,0xc1,0xd2,0x97,0xd9,0xd8,0xc3,0x97,0xc4,0xd2,0xc3,0x97,0xc2,0xc7,0x97,0xd6,0x97,0xc7,0xdf,0xd8,0xc3,0xd8,0x97,0xd0,0xc5,0xd2,0xd2,0xc3,0xde,0xd9,0xd0,0x9b,0x97,0xc7,0xdb,0xd2,0xd6,0xc4,0xd2,0x97,0xc4,0xd2,0xc3,0x97,0xde,0xc3,0x97,0xc2,0xc7,0x97,0xd5,0xd2,0xd1,0xd8,0xc5,0xd2,0x97,0xc4,0xd2,0xd9,0xd3,0xde,0xd9,0xd0,0x96]

/*: "Cancel" :*/
fileprivate let app_literalOriginStr:[Character] = ["C","a","n","c","e"]
fileprivate let noti_popValue:String = "connect"

/*: "Go to set" :*/
fileprivate let data_manTitle:[Character] = ["G","o"," ","t","o"," ","s","e","t"]

/*: "#startTime#" :*/
fileprivate let dataYaData:[Character] = ["#","s","t","a","r"]
fileprivate let dataAppearDetailKey:String = "me storage among startTime#"

/*: "#endTime#" :*/
fileprivate let user_availableKey:[Character] = ["#","e","n","d","T","i","m","e","#"]

/*: "replayId" :*/
fileprivate let data_respectiveYetName:[Character] = ["r","e","p","l","a","y","I"]
fileprivate let appTweenYearUrl:String = "information"

/*: "text" :*/
fileprivate let constShadeUrl:[Character] = ["t","e","x","t"]

/*: "img" :*/
fileprivate let const_pasName:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let notiWeeklyValue:String = "veheo"

/*: "gift" :*/
fileprivate let appGirlClipUrl:[Character] = ["g","i","f","t"]

/*: "PrivateChat 点击引用消息 :*/
fileprivate let const_stripSaleUrl:[Character] = ["P","r","i","v","a","t","e","C","h","a"]
fileprivate let mainStyleFormat:[Character] = ["t"," ","点","击","引","用","\u{6d88}","息"]

/*: "Sent " :*/
fileprivate let show_inmatePath:String = "show year miss level firstSent "

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PerformRemove.swift
//  AbroadTalking
//
//  Created by young on 2023/1/5.
//

//: import UIKit
import UIKit

/// 一键打招呼Block
//: typealias QuickGreetingBlock = (_ errorCode: Int?, _ errorMsg: String?, _ textDict: [String: Any]?, _ audioDict: [String: Any]?) -> Void
typealias QuickGreetingBlock = (_ errorCode: Int?, _ errorMsg: String?, _ textDict: [String: Any]?, _ audioDict: [String: Any]?) -> Void

/// 图片打招呼Block
//: typealias BeautyPhotosBlock = (_ isPushSet: Bool, _ PhotoDict: [String: Any]?) -> Void
typealias BeautyPhotosBlock = (_ isPushSet: Bool, _ PhotoDict: [String: Any]?) -> Void

//: public class TalkingPrivateChatManager: NSObject {
public class PerformRemove: NSObject {
    /// 记录私信对端已读回执时间 [key：userID，value：对端最后已读回执时间]
    //: var readReceiptDict = [String: Int]()
    var readReceiptDict = [String: Int]()

    //: @objc static let share = TalkingPrivateChatManager()
    @objc static let share = PerformRemove()
    //: private override init() {}
    override private init() {}
    // 私密照片已解锁礼物消息数组的key
    //: @objc let UnlockMsgIdArrayKey = "CacheIntimatePhotoUnlockMsgIdArrayKey"
    @objc let UnlockMsgIdArrayKey = String(bytes: kEvolveData.map{itPara(construct: $0)}, encoding: .utf8)!
    // 问题回答消息已回复消息数组的key
    //: let QuestionReplayMsgIdIdArrayKey = "CacheQuestionReplayMsgIdIdArrayKey"
    let QuestionReplayMsgIdIdArrayKey = String(bytes: user_careString.map{triggerThen(norm: $0)}, encoding: .utf8)!
}

// MARK: - 插入音视频通话提示消息

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    /// 判断是否需要插入音视频通话提示消息
    /// - Parameters:
    ///   - toUid: 对方Id
    ///   - msgView: TUI视图
    ///   - completionHandler: 回调
    //: static func chat_privateChatVideo_insertTipMsg(toUid: String, msgView: TUIMessageController, needInsertHandler: (() -> Void)?) {
    static func save(toUid: String, msgView: SearchionViewController, needInsertHandler: (() -> Void)?) {
        // 非审核模式
        //: guard MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MountCustom.share.appStatus == SensorRow.normal.rawValue else { return }
        // 男性
        //: guard MountCustom.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue else { return }

        // 是否展示过提示文案
        //: let TalkingPrivateChatVideoTipsIsShow = "CachePrivateChatVideoTipsIsShow_\(String(MountCustom.share.loginUserMode.userID))_\(toUid)"
        let TalkingPrivateChatVideoTipsIsShow = String(bytes: app_purchaseId.map{$0^203}, encoding: .utf8)! + "\(String(MountCustom.share.loginUserMode.userID))_\(toUid)"
        //: let isShow = Defaults.bool(forKey: TalkingPrivateChatVideoTipsIsShow)
        let isShow = data_toStr.bool(forKey: TalkingPrivateChatVideoTipsIsShow)
        //: guard isShow == false else { return }
        guard isShow == false else { return }

        // 消息达到5条
        //: guard msgView.uiMsgs.count >= 5 else { return }
        guard msgView.uiMsgs.count >= 5 else { return }

        //: var hasVoiceVideo = false
        var hasVoiceVideo = false // 是否有音视频通话消息
        //: var outgoingMsgCount = 0
        var outgoingMsgCount = 0 // 消息发送方已发送消息数量
        //: for indexModel in msgView.uiMsgs {
        for indexModel in msgView.uiMsgs {
            //: let indexModel = indexModel as? NextAgainst
            let indexModel = indexModel as? NextAgainst
            //: if indexModel != nil {
            if indexModel != nil {
                //: if indexModel!.isKind(of: PropertyReadAngle.self) {
                if indexModel!.isKind(of: PropertyReadAngle.self) { // 是否包含音视频文本
                    //: let textMsgModel = indexModel as! PropertyReadAngle
                    let textMsgModel = indexModel as! PropertyReadAngle
                    //: if textMsgModel.isVoiceVideo == true {
                    if textMsgModel.isVoiceVideo == true {
                        //: hasVoiceVideo = true
                        hasVoiceVideo = true
                        //: break
                        break
                    }
                }

                // 语音消息 || 文本消息 || 图片消息 || 视频消息
                //: if indexModel!.isKind(of: EnhanceTier.self) ||
                if indexModel!.isKind(of: EnhanceTier.self) ||
                    //: indexModel!.isKind(of: PropertyReadAngle.self) ||
                    indexModel!.isKind(of: PropertyReadAngle.self) ||
                    //: indexModel!.isKind(of: BehaviorIsolate.self) ||
                    indexModel!.isKind(of: BehaviorIsolate.self) ||
                    //: indexModel!.isKind(of: LitePage.self) {
                    indexModel!.isKind(of: LitePage.self)
                {
                    //: if indexModel?.direction == .MsgDirectionOutgoing {
                    if indexModel?.direction == .MsgDirectionOutgoing { // 消息发送方
                        //: outgoingMsgCount += 1
                        outgoingMsgCount += 1
                    }
                }
            }
        }

        //: guard hasVoiceVideo == false else {
        guard hasVoiceVideo == false else {
            //: Defaults.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            data_toStr.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            //: return
            return
        }
        //: guard outgoingMsgCount >= 5 else { return }
        guard outgoingMsgCount >= 5 else { return }

        // 回调外部插入音视频提示消息
        //: if needInsertHandler != nil {
        if needInsertHandler != nil {
            //: needInsertHandler!()
            needInsertHandler!()
            //: Defaults.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            data_toStr.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
        }
    }
}

// MARK: - 一键打招呼

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    /// 判断是否可以一键打招呼，回调结果
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - handler: errorCode：1 = 已发送过；2=未添加打招呼数据;
    //: static func chat_sendQuickGreeting(toUserId: String, completionHandler: @escaping QuickGreetingBlock) {
    static func clubFilter(toUserId: String, completionHandler: @escaping QuickGreetingBlock) {
        //: let key = "\(MountCustom.share.loginUserMode.userID)_\(TalkingSendAutoGreetIsToKey)_\(toUserId)"
        let key = "\(MountCustom.share.loginUserMode.userID)_\(main_persistStr)_\(toUserId)"
        //: let ishave = UserDefaults.standard.bool(forKey: key)
        let ishave = UserDefaults.standard.bool(forKey: key)
        //: guard !ishave else {
        guard !ishave else {
            //: completionHandler(1, "Quick greeting can only be sent to him once".localized, nil, nil)
            completionHandler(1, String(bytes: userEnableMKey.map{priceIgnore(accessible: $0)}, encoding: .utf8)!.localized, nil, nil)
            //: return
            return
        }

        //: ProgressHUD.show()
        LevelBoxIndex.naturalBy()
        //: TalkingChatRequestTool.req_getAutoGreetContent { succeed, result, errorModel in
        VariableCombine.post { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            LevelBoxIndex.gestureOff()
            //: guard succeed == true else {
            guard succeed == true else {
                //: completionHandler(2, errorModel?.errorMsg ?? "", nil, nil)
                completionHandler(2, errorModel?.errorMsg ?? "", nil, nil)
                //: return
                return
            }
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: completionHandler(2, "", nil, nil)
                completionHandler(2, "", nil, nil)
                //: return
                return
            }
            //: let textDict = dict["txt"] as? [String: Any]
            let textDict = dict[(String(constWeAdvertisingTitle))] as? [String: Any]
            //: let audioDict = dict["audio"] as? [String: Any]
            let audioDict = dict[(constCurrentReceiverMessage.replacingOccurrences(of: "insert", with: "di"))] as? [String: Any]
            //: if textDict?.count == 0 && audioDict?.count == 0 {
            if textDict?.count == 0, audioDict?.count == 0 {
                //: completionHandler(2, "Please add greeting text".localized, nil, nil)
                completionHandler(2, (String(userDueId.suffix(5)) + "e add" + String(appCoordinateProductStr.prefix(4)) + "eting te" + mainInsteadKey.replacingOccurrences(of: "system", with: "t")).localized, nil, nil)
                //: return
                return
            }

            // 一键打招呼本地存储
            //: chat_changeSaveGreetData(toUserId: toUserId, isHave: true)
            belowTa(toUserId: toUserId, isHave: true)
            //: completionHandler(nil, nil, textDict, audioDict)
            completionHandler(nil, nil, textDict, audioDict)
        }
    }

    /// 一键打招呼状态本地存储
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - isHave: 是否已发送
    //: static func chat_changeSaveGreetData(toUserId: String, isHave: Bool) {
    static func belowTa(toUserId: String, isHave: Bool) {
        // 一键打招呼本地cunc
        //: let key = "\(MountCustom.share.loginUserMode.userID)_\(TalkingSendAutoGreetIsToKey)_\(toUserId)"
        let key = "\(MountCustom.share.loginUserMode.userID)_\(main_persistStr)_\(toUserId)"
        //: UserDefaults.standard.set(isHave, forKey: key)
        UserDefaults.standard.set(isHave, forKey: key)
    }

    /// 图片打招呼
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - handler: errorCode：1 = 已发送过；2=未添加打招呼数据；
    //: static func chat_sendBeautyPhotos(toUserId: String, completionHandler: @escaping BeautyPhotosBlock) {
    static func anBy(toUserId: String, completionHandler: @escaping BeautyPhotosBlock) {
        //: ProgressHUD.show()
        LevelBoxIndex.naturalBy()
        //: TalkingChatRequestTool.req_getIntimatePhoto(toUid: toUserId) { succeed, result, errorModel in
        VariableCombine.thirdWith(toUid: toUserId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            LevelBoxIndex.gestureOff()
            /// 未添加打招呼图片
            //: if errorModel?.errorCode == 111 {
            if errorModel?.errorCode == 111 {
                //: let config = ShowAlertConfig()
                let config = ComposeS()
                //: config.alignment = .center
                config.alignment = .center
                //: if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
                if PutDirection.share.interfaceLang == TempStroke.pt.rawValue {
                    //: config.width = 330
                    config.width = 330
                }
                //: TalkingAlertShow.customAlert(message: "You have not set up a photo greeting, please set it up before sending!".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Go to set".localized, leftBlock: {
                MainTranslate.quantityegrationRed(message: String(bytes: mainDenseMediumStr.map{$0^183}, encoding: .utf8)!.localized, leftBtnTitle: (String(app_literalOriginStr) + noti_popValue.replacingOccurrences(of: "connect", with: "l")).localized, rightBtnTitle: (String(data_manTitle)).localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    MainTranslate.scheduleWhat()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: TalkingAlertShow.hideAlert()
                    MainTranslate.scheduleWhat()
                    //: completionHandler(true, nil)
                    completionHandler(true, nil)
                    //: }, config: config)
                }, config: config)
                //: return
                return
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? [String: Any] else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: completionHandler(false, dict)
                completionHandler(false, dict)
            }
        }
    }
}

// MARK: - 插入与官方客服聊天提示消息

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    ///  判断是否需要插入与官方客服聊天提示消息
    /// - Parameter needInsertHandler: 回调
    //: static func chat_customerServiceCenter_insertTipMsg(needInsertHandler: ((_ tipsStr: String?) -> Void)?) {
    static func visitorShow(needInsertHandler: ((_ tipsStr: String?) -> Void)?) {
        //: guard MountCustom.share.appConfigMode.CSConfig.systemTips.count > 0 else {
        guard MountCustom.share.appConfigMode.CSConfig.systemTips.count > 0 else { // 接口没下发，无需提示
            //: if needInsertHandler != nil {
            if needInsertHandler != nil {
                //: needInsertHandler!(nil)
                needInsertHandler!(nil)
            }
            //: return
            return
        }

        // 根据设备所在时区转换时间
        //: let startTime = NSDate.getCurrentZoneDateString(shDateStr: MountCustom.share.appConfigMode.CSConfig.startTime)
        let startTime = NSDate.remainAd(shDateStr: MountCustom.share.appConfigMode.CSConfig.startTime)
        //: let endTime = NSDate.getCurrentZoneDateString(shDateStr: MountCustom.share.appConfigMode.CSConfig.endTime)
        let endTime = NSDate.remainAd(shDateStr: MountCustom.share.appConfigMode.CSConfig.endTime)
        //: let isIncluded = NSDate.compareCurrentTimeIsIncluded(startTime: startTime, endTime: endTime)
        let isIncluded = NSDate.roundingGold(startTime: startTime, endTime: endTime)

        //: guard isIncluded == false else {
        guard isIncluded == false else {
            //: if needInsertHandler != nil {
            if needInsertHandler != nil {
                //: needInsertHandler!(nil)
                needInsertHandler!(nil)
            }
            //: return
            return
        }

        // 非工作时间段内，添加提示
        //: var tips = MountCustom.share.appConfigMode.CSConfig.systemTips
        var tips = MountCustom.share.appConfigMode.CSConfig.systemTips
        //: tips = tips.replacingOccurrences(of: "#startTime#", with: "\(startTime)")
        tips = tips.replacingOccurrences(of: (String(dataYaData) + String(dataAppearDetailKey.suffix(6))), with: "\(startTime)")
        //: tips = tips.replacingOccurrences(of: "#endTime#", with: "\(endTime)")
        tips = tips.replacingOccurrences(of: (String(user_availableKey)), with: "\(endTime)")
        //: if needInsertHandler != nil {
        if needInsertHandler != nil {
            //: needInsertHandler!(tips)
            needInsertHandler!(tips)
        }
    }
}

// MARK: - 刷新私密消息（照片、视频）展示状态

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    /// 接收到解锁私密消息（照片、视频）的礼物，刷新本地消息状态
    /// - Parameters:
    ///   - cellData: 数据
    ///   - msgView: TUI视图
    //: static func chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: NextAgainst, msgView: TUIMessageController) {
    static func infrastructureDisappear(cellData: NextAgainst, msgView: SearchionViewController) {
        //: guard msgView.uiMsgs.count > 0 else {
        guard msgView.uiMsgs.count > 0 else {
            // --------- 【外部页面】收到了解锁礼物消息 ---------
            // 首次进入到当前页面，刷新消息状态
            //: var array = Defaults.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String]
            var array = data_toStr.object(forKey: PerformRemove.share.UnlockMsgIdArrayKey) as? [String]

            // 本地有私密消息解锁礼物缓存，且是图片消息 或 视频消息
            //: guard array?.count ?? 0 > 0 &&
            guard array?.count ?? 0 > 0,
                  //: (cellData.isKind(of: BehaviorIsolate.self) ||
                  cellData.isKind(of: BehaviorIsolate.self) ||
                  //: cellData.isKind(of: LitePage.self)) else { return }
                  cellData.isKind(of: LitePage.self) else { return }

            // 找到本人发送的私密消息，更新状态为已解锁
            //: if array!.contains(cellData.msgModel.msgInfo.msgId) {
            if array!.contains(cellData.msgModel.msgInfo.msgId) {
                //: inner_changeIntimateMsg(cellData, msgView)
                net(cellData, msgView)

                // 更新本地缓存
                //: array!.removeAll(where: { $0 == cellData.msgModel.msgInfo.msgId })
                array!.removeAll(where: { $0 == cellData.msgModel.msgInfo.msgId })
                //: Defaults.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
                data_toStr.set(array, forKey: PerformRemove.share.UnlockMsgIdArrayKey)
            }

            //: return
            return
        }

        //  --------- 在【当前页面】收到解锁礼物消息 ---------
        // 只有收到礼物解锁消息才刷新
        //: guard cellData.isKind(of: VisitKind.self) && cellData.msgModel.gift.unlockMsgId.count > 0 else { return }
        guard cellData.isKind(of: VisitKind.self), cellData.msgModel.gift.unlockMsgId.count > 0 else { return }

        // 找到本人发送的私密消息，更新状态为已解锁
        //: let unlockMsgId = cellData.msgModel.gift.unlockMsgId
        let unlockMsgId = cellData.msgModel.gift.unlockMsgId
        //: for msgCellData in msgView.uiMsgs {
        for msgCellData in msgView.uiMsgs {
            //: let data = msgCellData as! NextAgainst
            let data = msgCellData as! NextAgainst
            //: if data.msgModel.msgInfo.msgId == unlockMsgId {
            if data.msgModel.msgInfo.msgId == unlockMsgId {
                //: inner_changeIntimateMsg(data, msgView)
                net(data, msgView)
                //: break
                break
            }
        }
    }

    /// 刷新本地消息状态
    /// - Parameters:
    ///   - cellData: 数据
    ///   - msgView: TUI视图
    //: static private func inner_changeIntimateMsg(_ cellData: NextAgainst, _ msgView: TUIMessageController) {
    private static func net(_ cellData: NextAgainst, _ msgView: SearchionViewController) {
        //: if cellData.isKind(of: BehaviorIsolate.self) {
        if cellData.isKind(of: BehaviorIsolate.self) { // 私密照片
            //: let model = cellData.msgModel.msgInfo.intimatePhoto
            let model = cellData.msgModel.msgInfo.intimatePhoto
            //: if model.lockStatus != 0 {
            if model.lockStatus != 0 {
                //: model.lockStatus = 0
                model.lockStatus = 0
                //: msgView.changeIntimatePhotoMsg(cellData, photoModel: model)
                msgView.restriction(cellData, pro: model)
            }

            //: } else if cellData.isKind(of: LitePage.self) {
        } else if cellData.isKind(of: LitePage.self) { // 私密视频
            //: let model = cellData.msgModel.msgInfo.video
            let model = cellData.msgModel.msgInfo.video
            //: if model.lockStatus != 0 {
            if model.lockStatus != 0 {
                //: model.lockStatus = 0
                model.lockStatus = 0
                //: msgView.changeIntimateVideoMsg(cellData, videoModel: model)
                msgView.couldSampleConstructEquivalentSign(cellData, barEase: model)
            }
        }
    }
}

// MARK: - 问题消息快捷回复

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    /// 接收到问题回答消息，判断是否需要展示消息快捷回复弹窗
    /// - Parameters:
    ///   - isRecharge: 是否充值
    ///   - msgView: TUI视图
    ///   - handler: 回调
    //: static func chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: Bool,
    static func margin(isRecharge: Bool,
                       //: msgView: TUIMessageController,
                       msgView: SearchionViewController,
                       //: handler: ((AbTalkingChatMsgInfoModel?) -> Void)?) {
                       handler: ((SignRegulateModel?) -> Void)?)
    {
        //: guard MountCustom.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue else { return }
        //: guard isRecharge == false else { return }
        guard isRecharge == false else { return }
        // 获取已处理消息ID数组
        //: let cacheMsgIds = Defaults.object(forKey: TalkingPrivateChatManager.share.QuestionReplayMsgIdIdArrayKey) as? [String] ?? []
        let cacheMsgIds = data_toStr.object(forKey: PerformRemove.share.QuestionReplayMsgIdIdArrayKey) as? [String] ?? []
        //: for case let msgModel as NextAgainst in msgView.uiMsgs.reversed() {
        for case let msgModel as NextAgainst in msgView.uiMsgs.reversed() {
            //: let msgInfo = msgModel.msgModel.msgInfo
            let msgInfo = msgModel.msgModel.msgInfo
            //: if msgInfo.isEnd && msgInfo.replay.isEmpty {
            if msgInfo.isEnd, msgInfo.replay.isEmpty { // 问答已结束
                //: handler?(nil)
                handler?(nil)
                //: return
                return
            }
            //: if msgInfo.isEnd && !msgInfo.replay.isEmpty && !cacheMsgIds.contains(msgInfo.msgId) {
            if msgInfo.isEnd, !msgInfo.replay.isEmpty, !cacheMsgIds.contains(msgInfo.msgId) { // 找到符合条件问答消息
                //: handler?(msgInfo)
                handler?(msgInfo)
                //: return
                return
            }
        }
        //: handler?(nil)
        handler?(nil)
    }
}

// MARK: - 未回复Reply to get points~逻辑

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    /// 是否是回复获取积分消息
    /// - Parameters:
    ///   - direction: 消息方向
    ///   - msgType: 消息类型
    ///   - msgTime: 消息时间
    /// - Returns: 结果
    //: static func isReplyToGetPointsMsg(_ direction: TMsgDirection, _ msgType: Int, msgTime: Date?) -> Bool {
    static func insertDown(_ direction: PhaseStep, _ msgType: Int, msgTime: Date?) -> Bool {
        // 非审核模式, messageType == 5, 时间<24小时
        //: guard MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else { return false }
        guard MountCustom.share.appStatus == SensorRow.normal.rawValue else { return false }
        //: guard direction == .MsgDirectionIncoming else { return false }
        guard direction == .MsgDirectionIncoming else { return false }
        //: guard msgType == 5 else { return false }
        guard msgType == 5 else { return false }
        //: guard let msgTime = msgTime else { return false }
        guard let msgTime = msgTime else { return false }
        //: guard Date().timeIntervalSince(msgTime) < 24*60*60 else { return false }
        guard Date().timeIntervalSince(msgTime) < 24 * 60 * 60 else { return false }
        //: return true
        return true
    }
}

// MARK: - VIP展示消息已读逻辑

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    /// 是否需要插入已读回执引导充值VIP提醒消息
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func needInsertVIPReadReceiptMsg(cellData: NextAgainst) -> Bool {
    static func builder(cellData: NextAgainst) -> Bool {
        //: if MountCustom.share.loginUserMode.loungePlus == false,
        if MountCustom.share.loginUserMode.loungePlus == false,
           //: cellData.innerMessage.groupID == nil,
           cellData.innerMessage.groupID == nil,
           //: cellData.status == .Msg_Status_Succ,
           cellData.status == .Msg_Status_Succ,
           //: cellData.direction == .MsgDirectionOutgoing,
           cellData.direction == .MsgDirectionOutgoing,
           //: cellData.msgModel.msgInfo.callCmd.count <= 0 {
           cellData.msgModel.msgInfo.callCmd.count <= 0
        {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 是否展示消息 已读/未读 标识
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func isShowReadMsg(cellData: NextAgainst) -> Bool {
    static func duringInside(cellData: NextAgainst) -> Bool {
        // vip && 非群聊 && 发送成功 && 本人发送 && 非音视频通话文本消息，展示消息是否已读标识
        //: if MountCustom.share.loginUserMode.loungePlus == true,
        if MountCustom.share.loginUserMode.loungePlus == true,
           //: cellData.innerMessage.groupID == nil,
           cellData.innerMessage.groupID == nil,
           //: cellData.status == .Msg_Status_Succ,
           cellData.status == .Msg_Status_Succ,
           //: cellData.direction == .MsgDirectionOutgoing,
           cellData.direction == .MsgDirectionOutgoing,
           //: cellData.msgModel.msgInfo.callCmd.count <= 0 {
           cellData.msgModel.msgInfo.callCmd.count <= 0
        {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 消息对方是否已读
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func msgIsRead(cellData: NextAgainst) -> Bool {
    static func missButt(cellData: NextAgainst) -> Bool {
        //: if cellData.msgModel.params["replayId"] != nil { // 问题回答消息默认已读
        if cellData.msgModel.params[(String(data_respectiveYetName) + appTweenYearUrl.replacingOccurrences(of: "information", with: "d"))] != nil { // 问题回答消息默认已读
            //: return true
            return true
        }
        //: guard let selfMsgTime = cellData.innerMessage.timestamp else { return false }
        guard let selfMsgTime = cellData.innerMessage.timestamp else { return false }
        //: if cellData.innerMessage.isPeerRead == true {
        if cellData.innerMessage.isPeerRead == true {
            //: return true
            return true
        }
        //: let readTimeInterval = TalkingPrivateChatManager.share.readReceiptDict[cellData.innerMessage.userID] ?? 0
        let readTimeInterval = PerformRemove.share.readReceiptDict[cellData.innerMessage.userID] ?? 0
        //: let readTime = Date(timeIntervalSince1970: TimeInterval(readTimeInterval))
        let readTime = Date(timeIntervalSince1970: TimeInterval(readTimeInterval))
        //: if selfMsgTime <= readTime {
        if selfMsgTime <= readTime {
            //: return true
            return true
        }
        //: return false
        return false
    }
}

// MARK: - Reply消息逻辑

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    /// 回复消息
    /// - Parameters:
    ///   - cellData: 数据
    ///   - targetId: 对方uid
    /// - Returns: 引用消息模型
    //: static func handleReplyMsg(cellData: NextAgainst, targetId: String) -> AbTalkingChatMsgQuoteModel {
    static func someStructure(cellData: NextAgainst, targetId: String) -> RadioModel {
        //: let quoteModel = AbTalkingChatMsgQuoteModel()
        let quoteModel = RadioModel()
        //: quoteModel.msgId = cellData.msgModel.msgInfo.msgId
        quoteModel.msgId = cellData.msgModel.msgInfo.msgId
        //: if cellData.direction == .MsgDirectionIncoming {
        if cellData.direction == .MsgDirectionIncoming {
            //: quoteModel.uid = targetId
            quoteModel.uid = targetId
            //: } else {
        } else {
            //: quoteModel.uid = MountCustom.share.loginUserMode.userID
            quoteModel.uid = MountCustom.share.loginUserMode.userID
        }
        //: let renderData = AbTalkingChatMsgQuoteDataModel()
        let renderData = PinSmallModel()
        //: if cellData.isKind(of: PropertyReadAngle.self) {
        if cellData.isKind(of: PropertyReadAngle.self) {
            //: quoteModel.renderType = "text"
            quoteModel.renderType = (String(constShadeUrl))
            //: renderData.content = cellData.msgModel.msgInfo.content
            renderData.content = cellData.msgModel.msgInfo.content

            //: } else if cellData.isKind(of: BehaviorIsolate.self) {
        } else if cellData.isKind(of: BehaviorIsolate.self) {
            //: quoteModel.renderType = "img"
            quoteModel.renderType = (String(const_pasName))
            //: renderData.previewUrl = cellData.msgModel.msgInfo.imageUri
            renderData.previewUrl = cellData.msgModel.msgInfo.imageUri

            //: } else if cellData.isKind(of: LitePage.self) {
        } else if cellData.isKind(of: LitePage.self) {
            //: quoteModel.renderType = "video"
            quoteModel.renderType = (notiWeeklyValue.replacingOccurrences(of: "eh", with: "id"))
            //: renderData.videoUrl = cellData.msgModel.msgInfo.video.videoUrl
            renderData.videoUrl = cellData.msgModel.msgInfo.video.videoUrl
            //: renderData.coverUrl = cellData.msgModel.msgInfo.video.coverImg
            renderData.coverUrl = cellData.msgModel.msgInfo.video.coverImg

            //: } else if cellData.isKind(of: EnhanceTier.self) {
        } else if cellData.isKind(of: EnhanceTier.self) {
            //: let audioCelldata = cellData as! EnhanceTier
            let audioCelldata = cellData as! EnhanceTier
            //: quoteModel.renderType = "audio"
            quoteModel.renderType = (constCurrentReceiverMessage.replacingOccurrences(of: "insert", with: "di"))
            //: let voiceCache = DBUserInfoManager.cache_getAudioDB(message: audioCelldata.innerMessage)
            let voiceCache = StageImplement.mergeMight(message: audioCelldata.innerMessage)
            //: renderData.duration = Int(voiceCache.db_audioLength) ?? 0
            renderData.duration = Int(voiceCache.db_audioLength) ?? 0

            //: } else if cellData.isKind(of: VisitKind.self) {
        } else if cellData.isKind(of: VisitKind.self) {
            //: quoteModel.renderType = "gift"
            quoteModel.renderType = (String(appGirlClipUrl))
            //: let giftCellData = cellData as! VisitKind
            let giftCellData = cellData as! VisitKind
            //: renderData.giftName = giftCellData.msgModel.gift.name
            renderData.giftName = giftCellData.msgModel.gift.name
            //: renderData.giftNameI18n = giftCellData.msgModel.gift.nameI18n
            renderData.giftNameI18n = giftCellData.msgModel.gift.nameI18n
            //: renderData.giftIcon = giftCellData.msgModel.gift.imgPreview
            renderData.giftIcon = giftCellData.msgModel.gift.imgPreview
            //: renderData.giftPrice = giftCellData.msgModel.gift.price
            renderData.giftPrice = giftCellData.msgModel.gift.price
            //: renderData.giftNum = Int(giftCellData.msgModel.gift.num) ?? 0
            renderData.giftNum = Int(giftCellData.msgModel.gift.num) ?? 0
        }

        //: quoteModel.renderData = renderData
        quoteModel.renderData = renderData
        //: return quoteModel
        return quoteModel
    }

    /// 点击“引用”消息
    /// - Parameters:
    ///   - cellData: 数据
    ///   - targetId: 对方uid
    //: static func handleClickReplyMsg(cellData: NextAgainst, targetId: String) {
    static func equally(cellData: NextAgainst, targetId: String) {
        //: let renderData = cellData.msgModel.quoteMsgInfo
        let renderData = cellData.msgModel.quoteMsgInfo
        //: UploadLogTool.writeLog(msg: "PrivateChat 点击引用消息\(renderData.renderType).")
        MusicThread.layerLanguage(msg: (String(const_stripSaleUrl) + String(mainStyleFormat)) + "\(renderData.renderType).")
        //: if renderData.renderType == "text" {
        if renderData.renderType == (String(constShadeUrl)) {
            //: let popView = QuoteDetailPopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let popView = DrawingComposeSustainable(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
            //: popView.msgDetailText = renderData.renderData.content
            popView.msgDetailText = renderData.renderData.content
            //: popView.show()
            popView.turn()

            //: } else if renderData.renderType == "img" {
        } else if renderData.renderType == (String(const_pasName)) {
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(k_playUrl)
            //: filePath = (filePath as NSString).appendingPathComponent((renderData.renderData.previewUrl as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((renderData.renderData.previewUrl as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = CurrentScenario()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: model.imgUrl =  renderData.renderData.previewUrl
                model.imgUrl = renderData.renderData.previewUrl
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = FrequencyModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [CurrentScenario] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = TellView(momentModel: currenModel, index: 0, type: .normal)
            //: TalkingPrivateChatManager.share.getNavigationController()?.pushViewController(vc, animated: true)
            PerformRemove.share.foreExpected()?.pushViewController(vc, animated: true)

            //: } else if renderData.renderType == "video" {
        } else if renderData.renderType == (notiWeeklyValue.replacingOccurrences(of: "eh", with: "id")) {
            //: let videoPath = renderData.renderData.videoUrl
            let videoPath = renderData.renderData.videoUrl
            //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
            let vc = GraphEmbraceViewController(videoPath: videoPath)
            //: TalkingPrivateChatManager.share.getNavigationController()?.pushViewController(vc, animated: true)
            PerformRemove.share.foreExpected()?.pushViewController(vc, animated: true)

            //: } else if renderData.renderType == "audio" {
        } else if renderData.renderType == (constCurrentReceiverMessage.replacingOccurrences(of: "insert", with: "di")) {
            //: let cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: renderData.msgId)
            let cacheWrap = SwitchtoCharacter.limit(with: renderData.msgId)
            //: let playModel = TalkingVoiceMsgPlayModel()
            let playModel = TransformBeyond()
            //: if cacheWrap != nil {
            if cacheWrap != nil {
                //: playModel.db_voiceCacheWrap = cacheWrap!
                playModel.db_voiceCacheWrap = cacheWrap!

                //: } else {
            } else {
                //: let model = WCDBVoiceMsgTable()
                let model = SwitchtoCharacter()
                //: model.msgId = renderData.msgId
                model.msgId = renderData.msgId
                //: model.db_voiceUri = renderData.renderData.url
                model.db_voiceUri = renderData.renderData.url
                //: model.db_senduid = renderData.uid
                model.db_senduid = renderData.uid
                //: if renderData.uid == MountCustom.share.loginUserMode.userID {
                if renderData.uid == MountCustom.share.loginUserMode.userID {
                    //: model.db_touid = targetId
                    model.db_touid = targetId
                    //: } else {
                } else {
                    //: model.db_touid = MountCustom.share.loginUserMode.userID
                    model.db_touid = MountCustom.share.loginUserMode.userID
                }
                //: model.db_audioLength = "\(renderData.renderData.duration)"
                model.db_audioLength = "\(renderData.renderData.duration)"
                //: WCDBVoiceMsgTable.db_insertVoiceMsg(model)
                SwitchtoCharacter.tillAverage(model)
                //: playModel.db_voiceCacheWrap = model
                playModel.db_voiceCacheWrap = model
            }

            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            DuringRegulate.shared.environment()
            //: TalkingVoiceMsgPlayManager.shared.starPlayReplyMsg(playModel: playModel)
            DuringRegulate.shared.indicatorLimit(playModel: playModel)

            //: } else if renderData.renderType == "gift" {
        } else if renderData.renderType == (String(appGirlClipUrl)) {
            //: let popView = QuoteDetailPopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let popView = DrawingComposeSustainable(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
            //: popView.msgDetailText = "Sent ".localized + renderData.renderData.giftNameLocal() + " x\(renderData.renderData.giftNum)"
            popView.msgDetailText = (String(show_inmatePath.suffix(5))).localized + renderData.renderData.themePop() + " x\(renderData.renderData.giftNum)"
            //: popView.show()
            popView.turn()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatManager {
extension PerformRemove {
    /// 检测用户短视频版本是否可用【> = 1.1.6 可用】
    /// - Returns: 是否可用
    //: static func checkShortVideo(userVersion: String) -> Bool {
    static func pastRun(userVersion: String) -> Bool {
        //: guard let version = Int(userVersion.replacingOccurrences(of: ".", with: "")) else {
        guard let version = Int(userVersion.replacingOccurrences(of: ".", with: "")) else {
            //: return false
            return false
        }
        //: if version*100 < 116*100 {
        if version * 100 < 116 * 100 {
            //: return false
            return false
        }

        //: return true
        return true
    }
}
