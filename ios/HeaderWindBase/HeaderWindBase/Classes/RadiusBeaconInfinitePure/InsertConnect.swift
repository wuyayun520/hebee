
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_guideUrl:[UInt8] = [0xa4,0xa3,0xa4,0xb9,0xe5,0xae,0xa2,0xa9,0xa8,0xbf,0xf7,0xe4,0xed,0xa5,0xac,0xbe,0xed,0xa3,0xa2,0xb9,0xed,0xaf,0xa8,0xa8,0xa3,0xed,0xa4,0xa0,0xbd,0xa1,0xa8,0xa0,0xa8,0xa3,0xb9,0xa8,0xa9]

/*: "#322870" :*/
fileprivate let show_replyId:String = "#322870"

/*: "icon_home_position" :*/
fileprivate let const_placeKey:[Character] = ["i","c","o","n","_","h"]
fileprivate let user_anotherBassPath:[Character] = ["o","m"]
fileprivate let dataPointRespectiveUrl:String = "e_poappeal type manager"

/*: "btn_video_minimize" :*/
fileprivate let app_workHappyUrl:String = "btn_vipaint entity challenge anima"
fileprivate let userConTitle:[Character] = ["d","e","o","_","m","i","n","i","m","i","z","e"]

/*: "btn_video_drop_nor" :*/
fileprivate let appCutData:[Character] = ["b","t","n","_","v","i","d","e","o","_","d","r","o"]
fileprivate let k_fragmentName:String = "random lost join assistantp_nor"

/*: "btn_video_call_nor" :*/
fileprivate let app_migrationUrl:[Character] = ["b","t","n","_","v","i","d","e","o","_","c","a","l","l","_","n","o","r"]

/*: "transform.scale" :*/
fileprivate let showConnectUrl:String = "tname"
fileprivate let show_versionMidString:String = "ANSF"
fileprivate let notiSizeEaseExceptionMessage:String = "catween"

/*: "animation" :*/
fileprivate let const_logUrl:String = "animadragio"
fileprivate let mainUsedMsg:[Character] = ["n"]

/*: "btn_voice_Micr_nor" :*/
fileprivate let showBlindKey:[Character] = ["b","t","n","_","v","o","i","c","e","_","M","i"]
fileprivate let k_tillIncludePath:String = "cr_norhung go out"

/*: "btn_voice_Micr_pre" :*/
fileprivate let app_instructionStr:String = "btn_dynamic pending pass symbol"
fileprivate let show_customKey:[Character] = ["v","o","i","c","e","_","M","i","c","r","_","p","r","e"]

/*: "btn_voice_hands_nor" :*/
fileprivate let showContextFormat:[Character] = ["b","t","n","_","v","o","i","c","e"]
fileprivate let noti_controlComputeString:String = "accept every_hand"

/*: "btn_voice_hands_pre" :*/
fileprivate let const_executeComplyUrl:String = "phase scenebtn_voic"
fileprivate let appMyStr:[Character] = ["e","_","h","a","n","d","s","_","p","r","e"]

/*: "icon_coin_nor" :*/
fileprivate let data_socialTitle:String = "appointed due mighticon_c"
fileprivate let data_complaintTitle:String = "extrar"

/*: "btn_voice_gift_nor" :*/
fileprivate let app_topicEnoughMessage:String = "strength mp continuebtn_vo"
fileprivate let mainResourceFormat:[Character] = ["g","i","f","t","_","n","o","r"]

/*: "Waiting to answer the phone..." :*/
fileprivate let user_fragmentTitle:String = "decrease exception pet discover crushWait"
fileprivate let data_scaleAlterValue:String = "o ansshare face layoff step"
fileprivate let kThumbName:String = "the pintro occur"

/*: "00:00" :*/
fileprivate let appLastTitle:[Character] = ["0","0",":","0","0"]

/*: "Waiting for %@ to answer the phone..." :*/
fileprivate let showDistinctionUrl:[UInt8] = [0x2e,0x2e,0x2e,0x65,0x6e,0x6f,0x68,0x70,0x20,0x65,0x68,0x74,0x20,0x72,0x65,0x77,0x73,0x6e,0x61,0x20,0x6f,0x74,0x20,0x40,0x25,0x20,0x72,0x6f,0x66,0x20,0x67,0x6e,0x69,0x74,0x69,0x61,0x57]

/*: "CIGaussianBlur" :*/
fileprivate let k_scanUrl:[Character] = ["C","I","G","a","u","s","s","i","a"]
fileprivate let userVariableFormat:[Character] = ["n","B","l","u","r"]

/*: "toUid" :*/
fileprivate let mainThinPath:String = "live playertoUid"

/*: "giftId" :*/
fileprivate let notiWeekMessage:[Character] = ["g"]
fileprivate let const_dealUrl:[Character] = ["i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let app_hiFrameworkString:String = "spine sharedgiftNum"

/*: "from" :*/
fileprivate let kDecideUrl:String = "FROM"

/*: "audioCall" :*/
fileprivate let mainShakeName:[Character] = ["a","u","d","i","o","C","a","l","l"]

/*: "pkgItemsetId" :*/
fileprivate let appFreshId:[Character] = ["p","k","g","I"]
fileprivate let mainFlexMessage:[Character] = ["t","e","m","s","e","t","I","d"]

/*: "msgInfo" :*/
fileprivate let constForwardData:String = "composition speed tempmsgInfo"

/*: "msgType" :*/
fileprivate let dataVariableStr:String = "msgTyphi border sound perform mask"
fileprivate let dataStaffUrl:String = "scale"

/*: "gift" :*/
fileprivate let showCurrentMsg:[Character] = ["g","i","f","t"]

/*: "totalMfCoin" :*/
fileprivate let main_heMinimizePath:String = "totalMfClocal implementation ready"
fileprivate let notiMeetingYouValue:[Character] = ["o","i","n"]

/*: "cellData" :*/
fileprivate let k_secretStr:[UInt8] = [0x8c,0x8a,0x83,0x83,0xab,0x8e,0x9b,0x8e]

private func itShot(original num: UInt8) -> UInt8 {
    return num ^ 239
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InsertConnect.swift
//  AbroadTalking
//
//  Created by young on 2022/12/15.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VoiceInteractionViewDelegate: NSObjectProtocol {
protocol FactorReadFuture: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func arcComponent()

    /// 接听音频通话
    //: func interactionView_shouldAcceptCall()
    func notLite()

    /// 是否静音
    /// - Parameter isMute: 是否静音（默认否）
    //: func interactionView_shouldChangeMute(isMute: Bool)
    func languageIn(isMute: Bool)

    /// 是否免提
    /// - Parameter isHandsfree: 是否免提（默认否）
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool)
    func displayOf(isHandsfree: Bool)

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func pastEqual()

    /// 更新小窗口通话时间
    /// - Parameter time: 通话时间
    //: func interactionView_shouldUpdateTime(time: Double)
    func publisher(time: Double)
}

//: class Talking1v1VoiceInteractionView: UIView {
class InsertConnect: UIView {
    //: weak var delegate: Talking1v1VoiceInteractionViewDelegate?
    weak var delegate: FactorReadFuture?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = FactorModel() // 引用类型，整个音频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: FactorModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        thirdCenter()
        //: setupSubViewsConstraint()
        whenTransition()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        efficiencyTotal()
        //: initCachEffectData()
        depthLoop()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_guideUrl.map{$0^205}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
//        self.player.stopPlay()
    }

    // MARK: - Lazy Load

    // 渐变背景视图
    //: lazy var bgView: UIImageView = {
    lazy var bgView: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        // 渐变色
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        // 宽高+1是为了适配在某些设备上出现渐变填充不满的情况
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: mainCellMsg + 1, height: kPressEasePublishString + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (show_replyId.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: v.layer.addSublayer(layer)
        v.layer.addSublayer(layer)

        //: return v
        return v
        //: }()
    }()

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(changeInteractionShowState), for: .touchUpInside)
        btn.addTarget(self, action: #selector(recentBy), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 100)/2
        v.layer.cornerRadius = actualWidth(w: 100) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 20)
        lab.font = UIFont.monsterColumn(fontSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: MethodButton = {
        //: let btn = TalkingButton()
        let btn = MethodButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(const_placeKey) + String(user_anotherBassPath) + String(dataPointRespectiveUrl.prefix(4)) + "sition")), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.observeFor(fontSize: 12)
        //: btn.titleLabel?.lineBreakMode = .byTruncatingTail
        btn.titleLabel?.lineBreakMode = .byTruncatingTail
        //: return btn
        return btn
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = UIColor(white: 1, alpha: 1)
        lab.textColor = UIColor(white: 1, alpha: 1)
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.observeFor(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(app_workHappyUrl.prefix(6)) + String(userConTitle))), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(mendForEven), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(appCutData) + String(k_fragmentName.suffix(5)))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(alongButton), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(app_migrationUrl))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(specify), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (showConnectUrl.replacingOccurrences(of: "name", with: "r") + show_versionMidString.lowercased() + "orm.s" + notiSizeEaseExceptionMessage.replacingOccurrences(of: "tween", with: "le"))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (const_logUrl.replacingOccurrences(of: "drag", with: "t") + String(mainUsedMsg)))

        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭麦克风按钮
    //: private lazy var micBtn: UIButton = {
    private lazy var micBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_Micr_nor"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(showBlindKey) + String(k_tillIncludePath.prefix(6)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_Micr_pre"), for: .selected)
        btn.setImage(UIImage.evolutionary(name: (String(app_instructionStr.prefix(4)) + String(show_customKey))), for: .selected)
        //: btn.addTarget(self, action: #selector(microphoneButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(structureBrown), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭免提按钮
    //: lazy var handsfreeBtn: UIButton = {
    lazy var handsfreeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_hands_nor"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(showContextFormat) + String(noti_controlComputeString.suffix(5)) + "s_nor")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_hands_pre"), for: .selected)
        btn.setImage(UIImage.evolutionary(name: (String(const_executeComplyUrl.suffix(8)) + String(appMyStr))), for: .selected)
        //: btn.addTarget(self, action: #selector(handsfreeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(amSwitcher), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_coin_nor"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(data_socialTitle.suffix(6)) + "oin_n" + data_complaintTitle.replacingOccurrences(of: "extra", with: "o"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_coin_nor"), for: .selected)
        btn.setImage(UIImage.evolutionary(name: (String(data_socialTitle.suffix(6)) + "oin_n" + data_complaintTitle.replacingOccurrences(of: "extra", with: "o"))), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(belowPanel), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_gift_nor"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(app_topicEnoughMessage.suffix(6)) + "ice_" + String(mainResourceFormat))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_gift_nor"), for: .selected)
        btn.setImage(UIImage.evolutionary(name: (String(app_topicEnoughMessage.suffix(6)) + "ice_" + String(mainResourceFormat))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(exceptPro), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.monsterColumn(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: ZoneOf = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = ZoneOf(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: KeepView = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = KeepView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: SortFinish = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = SortFinish()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VoiceInteractionView {
extension InsertConnect {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func efficiencyTotal() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "Waiting to answer the phone...".localized
            desLab.text = (String(user_fragmentTitle.suffix(4)) + "ing t" + String(data_scaleAlterValue.prefix(5)) + "wer " + String(kThumbName.prefix(5)) + "hone...").localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Recieved:
        case .Recieved:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = false
            acceptBtn.isHidden = false
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: micBtn.isHidden = false
            micBtn.isHidden = false
            //: handsfreeBtn.isHidden = false
            handsfreeBtn.isHidden = false
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: locationBtn.isHidden = true
            locationBtn.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                sensorPermissionDetail()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (String(appLastTitle))
            }
            //: startTalkCount()
            pop()
            //: touchHiddenTimer()
            betweenEntityEvolution()
        }

        //: updateLayout()
        readyEstimated()
        //: afterGetInfo()
        warning()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func warning() {
        //: if chatModel.videoStage == .Waiting {
        if chatModel.videoStage == .Waiting {
            //: desLab.text = "Waiting for %@ to answer the phone...".localizedArguments(chatModel.pairNickName)
            desLab.text = String(bytes: showDistinctionUrl.reversed(), encoding: .utf8)!.fieldOf(chatModel.pairNickName)
        }
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.containRetain(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        // 设置背景头像
        //: if !chatModel.pairHeadPic.isEmptyString {
        if !chatModel.pairHeadPic.isEmptyString {
            //: bgView.setUrlImage(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
            bgView.containRetain(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if image != nil {
                if image != nil {
                    //: self.bgView.image = self.blurImage(image!)
                    self.bgView.image = self.thirdInstead(image!)
                }
            }
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func mendForEven() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.pastEqual()
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func alongButton() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.arcComponent()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func specify() {
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.notLite()
    }

    /// 麦克风按钮事件
    //: @objc private func microphoneButtonClick() {
    @objc private func structureBrown() {
        //: micBtn.isSelected = !micBtn.isSelected
        micBtn.isSelected = !micBtn.isSelected
        //: delegate?.interactionView_shouldChangeMute(isMute: micBtn.isSelected)
        delegate?.languageIn(isMute: micBtn.isSelected)
    }

    /// 免提按钮事件
    //: @objc private func handsfreeButtonClick() {
    @objc private func amSwitcher() {
        //: delegate?.interactionView_shouldChangeSpeaker(isHandsfree: !handsfreeBtn.isSelected)
        delegate?.displayOf(isHandsfree: !handsfreeBtn.isSelected)
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func exceptPro() {
        //: func__sendGift()
        halfInOdd()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func belowPanel() {
        //: RetainProcess.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        RetainProcess.share.adjustExample(webViewType: .RechargeHalfPage)
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func recentBy() {
        //: let isHidden = !hangupBtn.isHidden
        let isHidden = !hangupBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: micBtn.isHidden = isHidden
        micBtn.isHidden = isHidden
        //: handsfreeBtn.isHidden = isHidden
        handsfreeBtn.isHidden = isHidden
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            fail()
            //: } else {
        } else {
            //: touchHiddenTimer()
            betweenEntityEvolution()
        }
    }

    //: func blurImage(_ image: UIImage) -> UIImage {
    func thirdInstead(_ image: UIImage) -> UIImage {
        //: let context = CIContext(options: nil)
        let context = CIContext(options: nil)
        //: let inputImage = CIImage(image: image)
        let inputImage = CIImage(image: image)
        // 使用高斯模糊滤镜
        //: let filter = CIFilter(name: "CIGaussianBlur")!
        let filter = CIFilter(name: (String(k_scanUrl) + String(userVariableFormat)))!
        //: filter.setValue(inputImage, forKey: kCIInputImageKey)
        filter.setValue(inputImage, forKey: kCIInputImageKey)
        // 设置模糊半径值（越大越模糊）
        //: filter.setValue(5, forKey: kCIInputRadiusKey)
        filter.setValue(5, forKey: kCIInputRadiusKey)
        //: let outputCIImage = filter.outputImage!
        let outputCIImage = filter.outputImage!
        //: let rect = CGRect(origin: CGPoint.zero, size: image.size)
        let rect = CGRect(origin: CGPoint.zero, size: image.size)
        //: let cgImage = context.createCGImage(outputCIImage, from: rect)
        let cgImage = context.createCGImage(outputCIImage, from: rect)
        // 显示生成的模糊图片
        //: let newImage = UIImage(cgImage: cgImage!)
        let newImage = UIImage(cgImage: cgImage!)
        //: return newImage
        return newImage
    }
}

// MARK: - 送礼

//: extension Talking1v1VoiceInteractionView {
extension InsertConnect {
    //: func func__sendGift() {
    func halfInOdd() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        EdgeCell.share.sumeraction {
            //: self.func__showGiftChoiceView()
            self.mention()
        }
    }

    //: func func__showGiftChoiceView() {
    func mention() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: MountCustom.share.loginUserMode.mf_coin)
        giftView.conversionProjection(needReload: true, mf_coin: MountCustom.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.ceiling()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: PublishZone, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftModel: giftModel, giftNum: num)
            self.brushGray(giftModel: giftModel, giftNum: num)
        }
    }

    //: func func_giftViewDismiss() {
    func duringOf() {
        //: giftView.dismissView()
        giftView.vidDepth()
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func brushGray(giftModel: PublishZone, giftNum: String) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            signWhen(showMsg: dataStoragePath)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == MountCustom.share.loginUserMode.userID {
        if chatModel.iUid == MountCustom.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(mainThinPath.suffix(5)))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(mainThinPath.suffix(5)))] = chatModel.iUid
        }
        //: params["giftId"] = giftModel.gid
        params[(String(notiWeekMessage) + String(const_dealUrl))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(app_hiFrameworkString.suffix(7)))] = giftNum
        //: params["from"] = "audioCall"
        params[(kDecideUrl.lowercased())] = (String(mainShakeName))
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(appFreshId) + String(mainFlexMessage))] = giftModel.pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        VariableCombine.tillNow(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.voiceToAsset(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.club(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.pathFuture(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func pathFuture(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(constForwardData.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(dataVariableStr.prefix(6)) + dataStaffUrl.replacingOccurrences(of: "scale", with: "e"))] = (String(showCurrentMsg))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(constForwardData.suffix(7)))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        constantTotalegration(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(main_heMinimizePath.prefix(8)) + String(notiMeetingYouValue))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(main_heMinimizePath.prefix(8)) + String(notiMeetingYouValue))] as! NSNumber
            //: MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: MountCustom.share.loginUserMode.mf_coin)
        giftView.conversionProjection(needReload: false, mf_coin: MountCustom.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == MountCustom.share.loginUserMode.userID {
        if chatModel.iUid == MountCustom.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo as [String: Any], toUid: toUid, isSender: true, isTip: false)
        let cellData = CaptureMend.beforeExtend(withExtral: extralInfo as [String: Any], toUid: toUid, isSender: true, isTip: false)
        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: k_denseKey, object: self, userInfo: [String(bytes: k_secretStr.map{itShot(original: $0)}, encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func voiceToAsset(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard MountCustom.share.loginUserMode.status != 1 else {
            guard MountCustom.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    signWhen(showMsg: errorStr)
                }
                //: return
                return
            }
            //: RetainProcess.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            RetainProcess.share.bod(clickEvent: kNumberMessage, sufficient: false)
            //: giftView.dismissView()
            giftView.vidDepth()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ReducePosition(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
            //: view.show()
            view.outWrap()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                signWhen(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func depthLoop() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == MountCustom.share.loginUserMode.userID {
        if chatModel.iUid == MountCustom.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.allow(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func constantTotalegration(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.collection(dict: giftDic) ?? PauseDoing()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in EdgeCell.share.someRequest() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [AreaModel]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.sophisticated(model: giftModel)
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.event(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func remind(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in EdgeCell.share.someRequest() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [AreaModel]
        }
        //: for i in 0 ..< giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.sophisticated(model: giftModel as! PauseDoing)
        }
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.modifyMagnitudeegration(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VoiceInteractionView {
extension InsertConnect {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func pop() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.sensorPermissionDetail()
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func sensorPermissionDetail() {
        //: let duration = getCurrentTalkingTime()
        let duration = againstTarget()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        // 更新小窗口通话时间
        //: delegate?.interactionView_shouldUpdateTime(time: duration)
        delegate?.publisher(time: duration)
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func everyStage(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        sensorPermissionDetail()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func againstTarget() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VoiceInteractionView {
extension InsertConnect {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func betweenEntityEvolution() {
        //: invalidateIdleTimer()
        fail()
        //: perform(#selector(hideInteractionView), with: nil, afterDelay: 10)
        perform(#selector(totalFindScope), with: nil, afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func fail() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(totalFindScope), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func totalFindScope() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.recentBy()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VoiceInteractionView {
extension InsertConnect {
    /// 添加视图
    //: private func setupSubviews() {
    private func thirdCenter() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairPicV)
        addSubview(pairPicV)
        //: addSubview(pairNameLab)
        addSubview(pairNameLab)
        //: addSubview(locationBtn)
        addSubview(locationBtn)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(micBtn)
        addSubview(micBtn)
        //: addSubview(handsfreeBtn)
        addSubview(handsfreeBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(timeLab)
        addSubview(timeLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func whenTransition() {
        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.top.equalTo(pairPicV.snp.bottom).offset(14)
            make.top.equalTo(pairPicV.snp.bottom).offset(14)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(actualWidth(w: 133))
            make.width.equalTo(actualWidth(w: 133))
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(90)
            make.top.equalTo(pairNameLab.snp.bottom).offset(90)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(data_visitorPath + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }
        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-noti_stageFormat - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: let letftPlace = (ScreenWidth-actualWidth(w: 45)*4-actualWidth(w: 25)*3)/2
        let letftPlace = (mainCellMsg - actualWidth(w: 45) * 4 - actualWidth(w: 25) * 3) / 2
        //: micBtn.snp.makeConstraints { make in
        micBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(letftPlace)
            make.leading.equalTo(letftPlace)
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-16)
            make.bottom.equalTo(-noti_stageFormat - 16)
            //: make.size.equalTo(actualWidth(w: 45))
            make.size.equalTo(actualWidth(w: 45))
        }
        //: handsfreeBtn.snp.makeConstraints { make in
        handsfreeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalTo(locationBtn.snp.bottom).offset(10)
            make.top.equalTo(locationBtn.snp.bottom).offset(10)
            //: make.leading.equalTo(54)
            make.leading.equalTo(54)
            //: make.trailing.equalTo(-54)
            make.trailing.equalTo(-54)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(data_visitorPath + 16)
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func readyEstimated() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2-42-70)
                make.leading.equalTo(mainCellMsg / 2 - 42 - 70)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-noti_stageFormat - 161)
                //: make.size.equalTo(70)
                make.size.equalTo(70)
            }
            //: acceptBtn.snp.remakeConstraints { make in
            acceptBtn.snp.remakeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2+42)
                make.leading.equalTo(mainCellMsg / 2 + 42)
                //: make.bottom.equalTo(hangupBtn)
                make.bottom.equalTo(hangupBtn)
                //: make.size.equalTo(hangupBtn)
                make.size.equalTo(hangupBtn)
            }
            //: } else {
        } else {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-noti_stageFormat - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
        }
    }
}
