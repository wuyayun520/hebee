
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let dataUponStr:[Character] = ["m","f","/","m","o","m","e","n","t","/","l","i"]
fileprivate let noti_levelSheStr:[Character] = ["s","t"]

/*: "mf/moment/moments" :*/
fileprivate let mainGreetKey:String = "mf/momevolve load empty license"
fileprivate let userInputUniquePath:[Character] = ["e","n","t","/","m","o","m","e","n","t","s"]

/*: "mf/moment/like" :*/
fileprivate let noti_calendarString:String = "mf/mrem cover both alongside"
fileprivate let data_proMessage:String = "/likestretch complaint data hidden"

/*: "mf/crush/send" :*/
fileprivate let mainUsedLayerMessage:String = "flow row projectionmf/cr"
fileprivate let userWhoName:String = "plainnd"

/*: "mf/moment/view" :*/
fileprivate let noti_jumpContainPath:String = "mf/manswer model part front closed"
fileprivate let show_allowUrl:[Character] = ["e","w"]

/*: "mf/momentReply/list" :*/
fileprivate let dataImageMsg:String = "bloc destroy shade spend nextmf/mom"
fileprivate let appProfessionalData:String = "ply/lisstructure collection native automatically"
fileprivate let mainConstantString:[Character] = ["t"]

/*: "mf/momentReply/save" :*/
fileprivate let const_entityStr:String = "act people absolute original hidemf/mom"
fileprivate let main_blankMsg:String = "ply/samend program"
fileprivate let constDraftTitle:String = "map"

/*: "mf/moment/videoSign" :*/
fileprivate let data_rateFindId:[Character] = ["m","f"]
fileprivate let data_factoryKey:String = "cur operate stimulate/mome"
fileprivate let main_launchAnalyzePath:String = "idoneo"

/*: "dynamicPic.zip" :*/
fileprivate let data_titleName:String = "saveyna"
fileprivate let userSustainableData:String = "ic.ziprevenue write"

/*: "pic" :*/
fileprivate let noti_averagePath:[UInt8] = [0x63,0x69,0x70]

/*: "content" :*/
fileprivate let dataCanId:[Character] = ["c","o","n","t","e","n","t"]

/*: "mf/moment/save" :*/
fileprivate let noti_programFormat:String = "generate may adjustmentmf/mo"
fileprivate let notiIllegalMsg:String = "length what generalment/save"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let notiDoingePath:[UInt8] = [0x70,0x95,0x82,0x8c,0x89,0x93,0x88,0x40,0x73,0x95,0x83,0x83,0x85,0x93,0x93,0x86,0x95,0x8c,0x4c,0x40,0x67,0x8f,0x40,0x94,0x8f,0x40,0x6d,0x99,0x40,0x64,0x99,0x8e,0x81,0x8d,0x89,0x83,0x93,0x40,0x94,0x8f,0x40,0x96,0x89,0x85,0x97]

fileprivate func inmateResource(basic num: UInt8) -> UInt8 {
    let value = Int(num) - 32
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/saveVideo" :*/
fileprivate let data_detailSumerruptFormat:String = "local collection response purplemf/m"
fileprivate let noti_automaticallyAllowMsg:String = "/savepast display phone"
fileprivate let user_calculationMessage:[Character] = ["V","i","d","e","o"]

/*: .png" :*/
fileprivate let main_managerErrMsg:String = ".pngrender height evolutionary intensity contain"

/*: "dynamicPic" :*/
fileprivate let mainWhenFormat:String = "tingn"
fileprivate let const_proString:[Character] = ["a","m","i","c","P","i","c"]

/*: "freeImage" :*/
fileprivate let dataWeeklyWaistStr:String = "freeImaheavy oval layoff presentation"
fileprivate let const_galleryId:String = "gskirt"

/*: "mf/momentReply/replylist" :*/
fileprivate let app_supportMsg:String = "mf/momencritical balance"
fileprivate let kItAgreeString:[Character] = ["t","R","e","p","l","y","/","r","e","p","l"]
fileprivate let user_introWhoDoingeTitle:String = "ylisonly"

/*: "mf/moment/delete" :*/
fileprivate let user_completeData:[Character] = ["m","f","/","m","o","m","e","n","t","/","d","e","l","e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let const_pathMessage:String = "trust letmf/m"
fileprivate let userMemberMinMessage:String = "tReplregular tell dense foundation asset"
fileprivate let mainScenarioMutualString:String = "rangete"

/*: "mf/moment/pin" :*/
fileprivate let showDiskMsg:[Character] = ["m","f","/","m","o","m","e","n","t","/","p","i","n"]

/*: "mf/moment/remindNum" :*/
fileprivate let dataReachPath:String = "properly found composition blankmf/m"
fileprivate let notiThroughStr:String = "t/remidifficult momentum shared layoff rem"
fileprivate let user_mountString:String = "flow"

/*: "mf/moment/remindList" :*/
fileprivate let kProjectionString:String = "bloc judge opmf/m"
fileprivate let constLockString:String = "resource estimated program we/remind"
fileprivate let main_architectureFormat:[Character] = ["L","i","s","t"]

/*: "page" :*/
fileprivate let const_restrictionKey:[UInt8] = [0x65,0x67,0x61,0x70]

/*: "cos/getCredential" :*/
fileprivate let showSmartId:String = "nature scene shot plaincos/ge"
fileprivate let constPingUrl:[Character] = ["i","a","l"]

/*: "type" :*/
fileprivate let data_myId:[Character] = ["t","y","p","e"]

/*: "user/reportUserInfo" :*/
fileprivate let app_connectionStr:String = "uspoter"
fileprivate let main_previousValue:String = "channel dialog decideortU"
fileprivate let userUntilMsg:String = "body observer bubble yetserInfo"

/*: "uid" :*/
fileprivate let const_signalEqualKey:String = "ugreatd"

/*: "deviceId" :*/
fileprivate let data_skirtLogString:[Character] = ["d","e","v","i","c","e","I","d"]

/*: "deviceName" :*/
fileprivate let notiSunRatingData:String = "dscreenvic"

/*: "iphone" :*/
fileprivate let main_belowMsg:[Character] = ["i","p","h","o","n","e"]

/*: "fileUrl" :*/
fileprivate let main_mightStr:String = "prompt through except calculate tingfileUrl"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SmallFinishTemp.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class SmallFinishTemp: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func contrast(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(dataUponStr) + String(noti_levelSheStr))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func independentMovement(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(mainGreetKey.prefix(6)) + String(userInputUniquePath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func option(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(noti_calendarString.prefix(4)) + "oment" + String(data_proMessage.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func brown(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(mainUsedLayerMessage.suffix(5)) + "ush/s" + userWhoName.replacingOccurrences(of: "plain", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func light(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(noti_jumpContainPath.prefix(4)) + "oment/vi" + String(show_allowUrl))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func channel(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(dataImageMsg.suffix(6)) + "entRe" + String(appProfessionalData.prefix(7)) + String(mainConstantString))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func distributionTag(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(const_entityStr.suffix(6)) + "entRe" + String(main_blankMsg.prefix(6)) + constDraftTitle.replacingOccurrences(of: "map", with: "ve"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func except(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(data_rateFindId) + String(data_factoryKey.suffix(5)) + "nt/v" + main_launchAnalyzePath.replacingOccurrences(of: "don", with: "d") + "Sign")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func revenue(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        self.first()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            naturalToBirthdayPrice(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((data_titleName.replacingOccurrences(of: "save", with: "d") + "micP" + String(userSustainableData.prefix(6))))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: noti_averagePath.reversed(), encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(dataCanId))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ExerciseModel()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(noti_programFormat.suffix(5)) + String(notiIllegalMsg.suffix(9)))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.notFade(showMsg: String(bytes: notiDoingePath.map{inmateResource(basic: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.first()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func used(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(data_detailSumerruptFormat.suffix(4)) + "oment" + String(noti_automaticallyAllowMsg.prefix(5)) + String(user_calculationMessage))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func naturalToBirthdayPrice(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.from()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(main_managerErrMsg.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((mainWhenFormat.replacingOccurrences(of: "ting", with: "dy") + String(const_proString)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func first() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(dataWeeklyWaistStr.prefix(7)) + const_galleryId.replacingOccurrences(of: "skirt", with: "e")))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func provisionSumerval(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(app_supportMsg.prefix(8)) + String(kItAgreeString) + user_introWhoDoingeTitle.replacingOccurrences(of: "only", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func jumpTo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(user_completeData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func agree(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(const_pathMessage.suffix(4)) + "omen" + String(userMemberMinMessage.prefix(5)) + "y/de" + mainScenarioMutualString.replacingOccurrences(of: "range", with: "le"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func active(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(showDiskMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func undertake(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(dataReachPath.suffix(4)) + "omen" + String(notiThroughStr.prefix(6)) + "ndNu" + user_mountString.replacingOccurrences(of: "flow", with: "m"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func acceptMonth(page: Int, completion: @escaping (_ data: [DistanceSelected]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(kProjectionString.suffix(4)) + "oment" + String(constLockString.suffix(7)) + String(main_architectureFormat))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: const_restrictionKey.reversed(), encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [DistanceSelected] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<DistanceSelected>.deserialize(from: arr as? Array) as? [DistanceSelected] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func script(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(showSmartId.suffix(6)) + "tCredent" + String(constPingUrl))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(String(data_myId)): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func who(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestServer = MountCustom.share.appConfigMode.reportDomain
        reqModel.requestServer = MountCustom.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (app_connectionStr.replacingOccurrences(of: "spot", with: "s") + "/rep" + String(main_previousValue.suffix(4)) + String(userUntilMsg.suffix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = MountCustom.share.loginUserMode.userID
        params[(const_signalEqualKey.replacingOccurrences(of: "great", with: "i"))] = MountCustom.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(data_skirtLogString))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(notiSunRatingData.replacingOccurrences(of: "screen", with: "e") + "eName")] = (String(main_belowMsg))
        //: params["type"] = 1
        params[(String(data_myId))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(main_mightStr.suffix(7)))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
