
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let kStrategyPath:String = "unable tween current fail schedulemf/bus"
fileprivate let constHeFormat:String = "wrap point successful chaingiftList"

/*: "type" :*/
fileprivate let kReductionBlocData:[UInt8] = [0x54,0x59,0x50,0x45]

fileprivate func publishStep(direct num: UInt8) -> UInt8 {
    let value = Int(num) - 224
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let k_formUrl:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","g","i"]
fileprivate let main_controlUrl:String = "ftListincrease rocket"

/*: "live/giftList" :*/
fileprivate let show_editorTitle:String = "live/info master"
fileprivate let noti_previousFormat:[Character] = ["g"]
fileprivate let noti_accountAdditionalValue:String = "rating mp auto bindiftList"

/*: "IM/getMessageList" :*/
fileprivate let showPublishTitle:[Character] = ["I","M","/","g","e"]
fileprivate let showLightPath:[Character] = ["t","M","e","s","s","a","g","e","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let app_somethingMessage:String = "mf/bcut recording warning"
fileprivate let appOpportunityString:[Character] = ["e","n","d","M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let user_expressionGiftPath:[Character] = ["m","f","/","c","h"]
fileprivate let const_peopleDynamicsMsg:String = "atRooconcrete image"
fileprivate let main_skipTitle:String = "dMsgresign translation path"

/*: "mf/business/sendGift" :*/
fileprivate let show_downValue:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e"]
fileprivate let main_scopeId:String = "left plat alwaysndGift"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let constCriticalBlindUrl:String = "frequency secure fullmf/cha"
fileprivate let mainPrepareUrl:[Character] = ["t","R","o","o","m","/","s","e"]
fileprivate let user_leaveUrl:String = "near pendingndGift"

/*: "live/sendGift" :*/
fileprivate let userDistributionKeepFormat:[Character] = ["l","i"]
fileprivate let user_evolutionarySpecifyMsg:String = "ve/senover audience he"

/*: "mf/user/getChatInfo" :*/
fileprivate let user_tableRoundName:String = "tool concretemf/u"
fileprivate let notiReceiverPath:String = "getClet talk honey hearing"
fileprivate let user_missData:String = "fsecurity"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let constMinuteFavoritePiFormat:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","U","s","e","r","I","n"]
fileprivate let constAgreeKey:String = "brush vidfoList"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let data_minimumString:String = "mf/crphone previous content"
fileprivate let kHiCapacityId:String = "temporary visit false explore resolveLik"
fileprivate let app_signatureDangerousName:String = "eMeListadministrative string trust level disk"

/*: "mf/crush/ignore" :*/
fileprivate let constBasicKey:String = "movement beside remind registermf/c"
fileprivate let show_largeData:[Character] = ["g","n","o","r","e"]

/*: "mf/crush/like" :*/
fileprivate let mainGuideUpperKey:[Character] = ["m","f","/","c","r","u","s","h","/","l","i"]
fileprivate let userConstantPath:String = "keven"

/*: "mf/user/getRelation" :*/
fileprivate let user_femaleUrl:String = "mf/uchannel agile notice category"
fileprivate let main_redId:String = "sensor catch among op pausetRel"
fileprivate let mainActUrl:String = "contain"

/*: "user/attention" :*/
fileprivate let const_ridMsg:[UInt8] = [0x55,0x53,0x45,0x52,0xf,0x41,0x54,0x54,0x45,0x4e,0x54,0x49,0x4f,0x4e]

fileprivate func contactMystery(estimated num: UInt8) -> UInt8 {
    let value = Int(num) + 32
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/removeAttention" :*/
fileprivate let show_globalString:String = "contact invite net index aliveuser/r"
fileprivate let main_salePriceString:String = "ENTION"

/*: "follow Success" :*/
fileprivate let main_capableSystemMessage:String = "follow include layer draw dynamics"
fileprivate let main_automaticallyId:String = "image contactSu"
fileprivate let appWhatUrl:String = "mmess"

/*: "unfollow Success" :*/
fileprivate let main_observeString:String = "unfoabsabs"
fileprivate let user_cornerName:String = "CESS"

/*: "translate/msg" :*/
fileprivate let mainGateValue:[Character] = ["t","r","a","n","s","l","a","t","e","/","m","s","g"]

/*: "msgId" :*/
fileprivate let showDeviceTitle:[UInt8] = [0xad,0xb3,0xa7,0x89,0xa4]

private func contextHeadEarly(quantity num: UInt8) -> UInt8 {
    return num ^ 192
}

/*: "targetLng" :*/
fileprivate let user_factorMsg:[UInt8] = [0x67,0x6e,0x4c,0x74,0x65,0x67,0x72,0x61,0x74]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let mainWordStr:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","A","u"]
fileprivate let dataThanName:String = "highlight root countrytoGr"
fileprivate let user_serverFrequencyId:String = "until tab drawer singleeetCo"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let userNetworkString:String = "graphic unknown deadlinemf/greet"
fileprivate let constDeployTitle:String = "/getIcollection until"
fileprivate let mainEvaluationValue:String = "scene message dailyePhoto"

/*: "toUid" :*/
fileprivate let k_choiceKey:[UInt8] = [0x29,0x24,0xa,0x1e,0x19]

fileprivate func submitHighlightAttention(will num: UInt8) -> UInt8 {
    let value = Int(num) - 181
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/info" :*/
fileprivate let dataInmateData:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let constShPath:String = "mf/chrate white"
fileprivate let noti_goFloorFamilyFormat:String = "atRoopin recognize too"

/*: "roomId" :*/
fileprivate let constHeavySumryData:[UInt8] = [0xfd,0xe0,0xe0,0xe2,0xc6,0xeb]

private func produceBoySave(brown num: UInt8) -> UInt8 {
    return num ^ 143
}

/*: "needExtraInfo" :*/
fileprivate let kStatPendingId:[UInt8] = [0x4a,0x41,0x41,0x40,0x61,0x5c,0x50,0x56,0x45,0x6d,0x4a,0x42,0x4b]

/*: "mf/chatRoom/check" :*/
fileprivate let mainOccasionFormat:String = "mf/chatinterested male day mix tab"
fileprivate let kComprehensiveId:[Character] = ["R","o"]
fileprivate let kUsedMainString:[Character] = ["o","m","/","c","h","e","c","k"]

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let main_saleCompareData:String = "mdeny"
fileprivate let noti_unlessPath:String = "/chaat publish reserve player"
fileprivate let main_exploreWithinStr:String = "reduce regular complaint customer uponjoinO"

/*: "mf/chatRoom/members" :*/
fileprivate let data_thinKey:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","m","e","m","b"]
fileprivate let constPortWhoKey:[Character] = ["e","r","s"]

/*: "mf/user/queryStatus" :*/
fileprivate let dataHandleString:String = "mf/ublue skip tolerance device quantity"
fileprivate let showCoreCounteractionPath:String = "queractivity"
fileprivate let showThroughData:String = "Statussine purple heap"

/*: "toUids" :*/
fileprivate let constConsumeReduceKey:[UInt8] = [0xea,0xf1,0xcb,0xf7,0xfa,0xed]

/*: "call/getCallTab" :*/
fileprivate let data_burnPiKey:[Character] = ["c","a","l","l","/","g","e","t","C","a","l"]
fileprivate let show_methodUrl:[Character] = ["l","T","a","b"]

/*: "mf/user/remark" :*/
fileprivate let userTagArcDialogValue:[Character] = ["m","f","/","u","s","e","r","/","r"]
fileprivate let const_brushBorderFormat:String = "EMARK"

/*: "content" :*/
fileprivate let k_twoSaveFormat:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "top" :*/
fileprivate let kEffectTitle:[UInt8] = [0xf3,0xee,0xef]

fileprivate func colorPhoto(music num: UInt8) -> UInt8 {
    let value = Int(num) + 129
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VariableCombine.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class VariableCombine: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [BoundMeasureTrack]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [BoundMeasureTrack]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func zoneFor(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(kStrategyPath.suffix(6)) + "iness/" + String(constHeFormat.suffix(8)))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: kReductionBlocData.map{publishStep(direct: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func pastPainter(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(k_formUrl) + String(main_controlUrl.prefix(6)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func theYearMission(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(show_editorTitle.prefix(5)) + String(noti_previousFormat) + String(noti_accountAdditionalValue.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func clearPlace(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(showPublishTitle) + String(showLightPath))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func beIn(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(app_somethingMessage.prefix(4)) + "usiness/s" + String(appOpportunityString))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func shapeGlobal(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(user_expressionGiftPath) + String(const_peopleDynamicsMsg.prefix(5)) + "m/sen" + String(main_skipTitle.prefix(4)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func tillNow(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(show_downValue) + String(main_scopeId.suffix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func send(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(constCriticalBlindUrl.suffix(6)) + String(mainPrepareUrl) + String(user_leaveUrl.suffix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func reveal(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(userDistributionKeepFormat) + String(user_evolutionarySpecifyMsg.prefix(6)) + "dGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func scanFor(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(user_tableRoundName.suffix(4)) + "ser/" + String(notiReceiverPath.prefix(4)) + "hatIn" + user_missData.replacingOccurrences(of: "security", with: "o"))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func familyPro(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(constMinuteFavoritePiFormat) + String(constAgreeKey.suffix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<BoundMeasureTrack>.deserialize(from: result as? Array) {
            if let datas = Array<BoundMeasureTrack>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [BoundMeasureTrack])
                completion(succeed, datas as! [BoundMeasureTrack])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func modelSquare(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        k_runPath.receiveMiniInsteadTotaleractionMicTab(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func notInput(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(data_minimumString.prefix(5)) + "ush/who" + String(kHiCapacityId.suffix(3)) + String(app_signatureDangerousName.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func warningSpeaker(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(constBasicKey.suffix(4)) + "rush/i" + String(show_largeData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func plateMode(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(mainGuideUpperKey) + userConstantPath.replacingOccurrences(of: "even", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func multiple(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(user_femaleUrl.prefix(4)) + "ser/ge" + String(main_redId.suffix(4)) + "atio" + mainActUrl.replacingOccurrences(of: "contain", with: "n"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func slideIn(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: const_ridMsg.map{contactMystery(estimated: $0)}, encoding: .utf8)! : (String(show_globalString.suffix(6)) + "emoveAtt" + main_salePriceString.lowercased())
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                    show_shouldUrl.notFade(showMsg: (String(main_capableSystemMessage.prefix(7)) + String(main_automaticallyId.suffix(2)) + appWhatUrl.replacingOccurrences(of: "mm", with: "cc")).localized)
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    show_shouldUrl.notFade(showMsg: (main_observeString.replacingOccurrences(of: "abs", with: "l") + "ow Suc" + user_cornerName.lowercased()).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                show_shouldUrl.signWhen(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func undertakeReduce(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (String(mainGateValue))
        //: reqModel.params = ["msgId": msgId, "targetLng": StopBrush.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: showDeviceTitle.map{contextHeadEarly(quantity: $0)}, encoding: .utf8)!: msgId, String(bytes: user_factorMsg.reversed(), encoding: .utf8)!: StopBrush.adTotalegrity(), String(bytes: kReductionBlocData.map{publishStep(direct: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func post(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(mainWordStr) + String(dataThanName.suffix(4)) + String(user_serverFrequencyId.suffix(5)) + "ntent")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func thirdWith(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(userNetworkString.suffix(8)) + String(constDeployTitle.prefix(5)) + "ntimat" + String(mainEvaluationValue.suffix(6)))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: k_choiceKey.map{submitHighlightAttention(will: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func graphic(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(dataInmateData))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func factoryTemporary(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(constShPath.prefix(5)) + String(noti_goFloorFamilyFormat.prefix(5)) + "m/list")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func usOfMove(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: constHeavySumryData.map{produceBoySave(brown: $0)}, encoding: .utf8)!: roomId, String(bytes: kStatPendingId.map{$0^36}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(mainOccasionFormat.prefix(7)) + String(kComprehensiveId) + String(kUsedMainString))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func doingceBack(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (main_saleCompareData.replacingOccurrences(of: "deny", with: "f") + String(noti_unlessPath.prefix(4)) + "tRoom/" + String(main_exploreWithinStr.suffix(5)) + "rQuit")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func tillCur(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(data_thinKey) + String(constPortWhoKey))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func openInvite(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(dataHandleString.prefix(4)) + "ser/" + showCoreCounteractionPath.replacingOccurrences(of: "activity", with: "y") + String(showThroughData.prefix(6)))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: constConsumeReduceKey.map{$0^158}, encoding: .utf8)!: toUids, String(bytes: kReductionBlocData.map{publishStep(direct: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func atEvaluation(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(data_burnPiKey) + String(show_methodUrl))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func recentCommon(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(userTagArcDialogValue) + const_brushBorderFormat.lowercased())
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: k_twoSaveFormat.reversed(), encoding: .utf8)!: content, String(bytes: kEffectTitle.map{colorPhoto(music: $0)}, encoding: .utf8)!: isTop, String(bytes: k_choiceKey.map{submitHighlightAttention(will: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
