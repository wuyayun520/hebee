
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiMicStr:[UInt8] = [0xfa,0xfd,0xfa,0xe7,0xbb,0xf0,0xfc,0xf7,0xf6,0xe1,0xa9,0xba,0xb3,0xfb,0xf2,0xe0,0xb3,0xfd,0xfc,0xe7,0xb3,0xf1,0xf6,0xf6,0xfd,0xb3,0xfa,0xfe,0xe3,0xff,0xf6,0xfe,0xf6,0xfd,0xe7,0xf6,0xf7]

private func lobbyIntervalegration(him num: UInt8) -> UInt8 {
    return num ^ 147
}

/*: "roomId" :*/
fileprivate let show_bitData:String = "thread embrace broad name feeroomId"

/*: "type" :*/
fileprivate let noti_findName:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "extra" :*/
fileprivate let kForwardTranslationData:String = "eabstra"

/*: "jumpKey" :*/
fileprivate let user_picFormat:[UInt8] = [0xae,0xb1,0xa9,0xb4,0x8f,0xa1,0xbd]

private func earnPer(limited num: UInt8) -> UInt8 {
    return num ^ 196
}

/*: "Send Gift" :*/
fileprivate let showUserMsg:String = "pass petSend Gi"
fileprivate let notiQuantityValue:[Character] = ["f","t"]

/*: "View info" :*/
fileprivate let k_dailyFemaleData:[Character] = ["V","i","e","w"," "]
fileprivate let const_reserveId:String = "infrun"

/*: "Report" :*/
fileprivate let showHisId:String = "Reportcommon double bubble tar"

/*: "uid" :*/
fileprivate let userContextPutString:[UInt8] = [0x64,0x69,0x75]

/*: "name" :*/
fileprivate let constResolveTitle:[UInt8] = [0x18,0x17,0x1b,0x13]

private func productAdmin(drawing num: UInt8) -> UInt8 {
    return num ^ 118
}

/*: "Can't send blank message" :*/
fileprivate let app_streamPath:[Character] = ["C","a","n","\'","t"]
fileprivate let showFeedbackLobbyData:[Character] = [" ","s","e","n","d"," ","b","l","a","n"]
fileprivate let main_greetEquipmentStr:String = "k mesimmediate next ceiling comment mark"

/*: "user" :*/
fileprivate let kSaveValue:String = "uoffr"

/*: "content" :*/
fileprivate let appMigrationDetailedMessage:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let userAlreadyFormat:[Character] = ["m","s"]
fileprivate let showSendFrameworkValue:[Character] = ["g","T","y","p","e"]

/*: "txt" :*/
fileprivate let userBurnDirectFormat:String = "TXT"

/*: "isGroup" :*/
fileprivate let appFreshEnvironmentStr:[Character] = ["i","s","G","r","o","u","p"]

/*: "jumps" :*/
fileprivate let mainStateFormat:String = "jobserveps"

/*: "message" :*/
fileprivate let const_calculateComplyMsg:[UInt8] = [0xce,0xc6,0xd0,0xd0,0xc2,0xc4,0xc6]

/*: "TxtMsg" :*/
fileprivate let main_dynamicsMsg:String = "list eachTxtMsg"

/*: "toUid" :*/
fileprivate let dataBelowValue:String = "publish bad publish destroy rettoUid"

/*: "msgInfo" :*/
fileprivate let noti_shouldMsg:String = "design merge recommendmsgInfo"

/*: "params" :*/
fileprivate let const_interruptQuoteFormat:[UInt8] = [0x44,0x35,0x46,0x35,0x41,0x47]

fileprivate func resistanceAlready(shared num: UInt8) -> UInt8 {
    let value = Int(num) - 212
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "contentType" :*/
fileprivate let dataSmallMessage:[UInt8] = [0xf1,0xfd,0xfc,0x2,0xf3,0xfc,0x2,0xe2,0x7,0xfe,0xf3]

fileprivate func commandAccessible(plate num: UInt8) -> UInt8 {
    let value = Int(num) + 114
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let notiPortStr:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let kEvolveMsg:String = "IMG"

/*: "imageUri" :*/
fileprivate let appOverTitle:[UInt8] = [0xa6,0xa2,0xae,0xa8,0xaa,0x9a,0xbd,0xa6]

private func lockSituation(theme num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "pic" :*/
fileprivate let user_sequencePath:String = "pgalleryc"

/*: "msgId" :*/
fileprivate let main_functionalMessage:String = "region wrap res firemsgId"

/*: "totalMfCoin" :*/
fileprivate let show_exceptCustomerPath:String = "totalMfCoouter app"
fileprivate let showDistinctionMessage:String = "ipull"

/*: "giftId" :*/
fileprivate let showTooKey:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let dataBlocTitle:String = "giftNumexactly read sustainable layoff"

/*: "pkgItemsetId" :*/
fileprivate let data_upperName:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "msg" :*/
fileprivate let notiRetainMessage:String = "capableg"

/*: "tips" :*/
fileprivate let data_buildingCrucialKey:String = "tippose"

/*: "gift" :*/
fileprivate let showChoiceMessage:String = "gifgreet"

/*: "nickname" :*/
fileprivate let userBirthdayGenderName:String = "hold"
fileprivate let userContactMsg:String = "icknamacross"

/*: "headPic" :*/
fileprivate let user_situationValue:[UInt8] = [0x29,0x24,0x20,0x25,0x11,0x28,0x22]

private func threeTag(timing num: UInt8) -> UInt8 {
    return num ^ 65
}

/*: "All Numbers" :*/
fileprivate let main_beautyManualId:String = "to"
fileprivate let appLeadingUrl:String = "ll Numcell install sort warn point"

/*: "sendNum" :*/
fileprivate let k_signNatureValue:String = "sendNumremark alongside detail"

/*: "toUser" :*/
fileprivate let appPetRocketData:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let notiMarkId:String = "parent random continuing parentmsgInc"
fileprivate let dataImpactFormat:[Character] = ["o","m","e"]

/*: "isConnection" :*/
fileprivate let userBlankUrl:[Character] = ["i"]
fileprivate let data_shallNextNaturePath:String = "give task signal tunsCon"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SampleColumn.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class SampleColumn: PetViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = UniformDensity() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = VerticalName() // 会话cellData
    //: private var conversationType: ErrorLayerFormat = .group
    private var conversationType: ErrorLayerFormat = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = UniformDensity.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiMicStr.map{lobbyIntervalegration(him: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MountCustom.share.appConfigMode.disableShootScreen {
            if MountCustom.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = MustView(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        editRestore()
        //: createUIConstraints()
        crush()
        //: bindInteraction()
        architectureQuit()
        //: addTapGestureRecognizer()
        sendLaunch()
        //: func__chatRoom_insertTipMsg()
        customerVariable()
        //: chatSettings()
        transformInserter()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            phoneSh()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.commit(join: false)
        //: self.destroyTipsTimer()
        self.modify()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: SearchionViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = SearchionViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(AmongClear.self, forCellReuseIdentifier: AmongClear.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(GestureLayer.self, forCellReuseIdentifier: GestureLayer.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(YieldReliability.self, forCellReuseIdentifier: YieldReliability.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ProcessingPerform.self, forCellReuseIdentifier: ProcessingPerform.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: SecondView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(ModeMend.self, forCellReuseIdentifier: SecondView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: EmbraceView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = EmbraceView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: KeepView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = KeepView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: SortFinish = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = SortFinish()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: EvaluationVisit = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = EvaluationVisit(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: MediumView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = MediumView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension SampleColumn {
    /// 群聊设置
    //: private func chatSettings() {
    private func transformInserter() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        AlwaysElement.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        commit(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func commit(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(show_bitData.suffix(6))): self.conversationData.groupID, String(bytes: noti_findName.reversed(), encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        VariableCombine.doingceBack(params: params) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - EditAcross

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension SampleColumn: EditAcross {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> NextAgainst! {
    func kitTool(_ controller: SearchionViewController!, retain data: V2TIMMessage!) -> NextAgainst! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = NextGraphic.showHero(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = CaptureMend.enableRender(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: NextAgainst!) -> SecondView! {
    func down(_ controller: SearchionViewController!, blackDark data: NextAgainst!) -> SecondView! {
        //: if data.isKind(of: OvalCharacteristicValueGrainSumStepTechnique.self) {
        if data.isKind(of: OvalCharacteristicValueGrainSumStepTechnique.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = AmongClear(style: .default, reuseIdentifier: AmongClear.className())
            //: let cellData = data as! OvalCharacteristicValueGrainSumStepTechnique
            let cellData = data as! OvalCharacteristicValueGrainSumStepTechnique
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.temp(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.stopImage(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: BehaviorIsolate.self) {
        if data.isKind(of: BehaviorIsolate.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = GestureLayer(style: .default, reuseIdentifier: GestureLayer.className())
            //: let cellData = data as! BehaviorIsolate
            let cellData = data as! BehaviorIsolate
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: LevelAssociate.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = YieldReliability(style: .default, reuseIdentifier: YieldReliability.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! LevelAssociate
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: RevealAway.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ProcessingPerform(style: .default, reuseIdentifier: ProcessingPerform.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! RevealAway
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: SecondView!) {
    func bean(_ controller: SearchionViewController!, priceAge cell: SecondView!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(MountCustom.share.loginUid) {
            if userModel.uid == Int(MountCustom.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                temp(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                bar(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: SecondView!) {
    func screen(_ controller: SearchionViewController!, maximum cell: SecondView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: GestureLayer.self) { // 图片
            //: let imageCellData = cell.messageData as! BehaviorIsolate
            let imageCellData = cell.messageData as! BehaviorIsolate
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(k_playUrl)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = CurrentScenario()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = FrequencyModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [CurrentScenario] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = TellView(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.alongLarge()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: AmongClear.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! AmongClear
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = NextGraphic.showHero(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(kForwardTranslationData.replacingOccurrences(of: "abs", with: "x"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: user_picFormat.map{earnPer(limited: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<RedManagerModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: RetainProcess.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RetainProcess.share.signContain(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ProcessingPerform.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ProcessingPerform
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = NextGraphic.showHero(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(kForwardTranslationData.replacingOccurrences(of: "abs", with: "x"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: user_picFormat.map{earnPer(limited: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<RedManagerModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: RetainProcess.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RetainProcess.share.signContain(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: SecondView!) {
    func row(_ controller: SearchionViewController!, pauseMethod cell: SecondView!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        shareIn(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension SampleColumn {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func modify() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func bar(user: FeedbackModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = BrownGroup(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.isolateThroughDistribution(cellTitleList: [(String(showUserMsg.suffix(7)) + String(notiQuantityValue)).localized, "@", (String(k_dailyFemaleData) + const_reserveId.replacingOccurrences(of: "run", with: "o")).localized, (String(showHisId.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.transformSame(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.reward(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.temp(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.remark(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func transformSame(user: FeedbackModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.pastFailureObserver()
        //: let model = TalkingChatRoomMemberModel()
        let model = StateDifficult()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.sufficient(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func reward(user: FeedbackModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: userContextPutString.reversed(), encoding: .utf8)!: String(user.uid), String(bytes: constResolveTitle.map{productAdmin(drawing: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.towardLinkLite(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func temp(uid: Int) {
        //: RetainProcess.share.func__pushToUserDetailVC(uid: String(uid))
        RetainProcess.share.videoCustom(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func remark(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = SortSectionStep(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.tillCon(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension SampleColumn {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func airBackgroundTrigger(msgStr: String, quoteModel: RadioModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            signWhen(showMsg: (String(app_streamPath) + String(showFeedbackLobbyData) + String(main_greetEquipmentStr.prefix(5)) + "sage").localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: user_picFormat.map{earnPer(limited: $0)}, encoding: .utf8)!: (kSaveValue.replacingOccurrences(of: "off", with: "se")), String(bytes: userContextPutString.reversed(), encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: appMigrationDetailedMessage.reversed(), encoding: .utf8)!: temStr, (String(userAlreadyFormat) + String(showSendFrameworkValue)): (userBurnDirectFormat.lowercased()), (String(appFreshEnvironmentStr)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(mainStateFormat.replacingOccurrences(of: "observe", with: "um"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: const_calculateComplyMsg.map{$0^163}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: noti_findName.reversed(), encoding: .utf8)!: (String(main_dynamicsMsg.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(show_bitData.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(dataBelowValue.suffix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_shouldMsg.suffix(7))): msgInfo, String(bytes: const_interruptQuoteFormat.map{resistanceAlready(shared: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            shareIn(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func directTo(img: UIImage) {
        //: let imagePath = BehaviorIsolate.getChatImagePath(image: img)
        let imagePath = BehaviorIsolate.step(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: dataSmallMessage.map{commandAccessible(plate: $0)}, encoding: .utf8)!: (String(notiPortStr)), (String(userAlreadyFormat) + String(showSendFrameworkValue)): (kEvolveMsg.lowercased()), String(bytes: appOverTitle.map{lockSituation(theme: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(user_sequencePath.replacingOccurrences(of: "gallery", with: "i")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: noti_findName.reversed(), encoding: .utf8)!: (String(notiPortStr)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(show_bitData.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_shouldMsg.suffix(7))): msgInfo, String(bytes: const_interruptQuoteFormat.map{resistanceAlready(shared: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            shareIn(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: NextAgainst, reSend: Bool) {
    private func shareIn(msgData: NextAgainst, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            signWhen(showMsg: dataStoragePath)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.monster(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((user_sequencePath.replacingOccurrences(of: "gallery", with: "i"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(user_sequencePath.replacingOccurrences(of: "gallery", with: "i"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(user_sequencePath.replacingOccurrences(of: "gallery", with: "i"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        VariableCombine.shapeGlobal(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.duringCompute(msgData, transition: nil, consumerSophisticated: NSNumber(value: 0), isPlain: .Msg_Status_Fail)

                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.full(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(noti_shouldMsg.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.duringCompute(msgData, transition: msgInfo[(String(main_functionalMessage.suffix(5)))] as? String, consumerSophisticated: NSNumber(value: 0), isPlain: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(show_exceptCustomerPath.prefix(9)) + showDistinctionMessage.replacingOccurrences(of: "pull", with: "n"))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(show_exceptCustomerPath.prefix(9)) + showDistinctionMessage.replacingOccurrences(of: "pull", with: "n"))] as! NSNumber
                //: MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func full(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard MountCustom.share.loginUserMode.status != 1 else {
            guard MountCustom.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    signWhen(showMsg: errorStr)
                }
                //: return
                return
            }

            //: RetainProcess.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            RetainProcess.share.bod(clickEvent: kNumberMessage, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.vidDepth()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            productRe(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ReducePosition(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
            //: view.show()
            view.outWrap()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                signWhen(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension SampleColumn: ExceptionStatusPoint {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func searcher(giftModel: PublishZone, giftNum: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func futureMaintain() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func belowLike(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = KeepViewController()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.towardLinkLite(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func colorYear(actionType: QuitBar) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            someoneExplain()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func explain(giftModel: PublishZone, giftNum: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func convertThen(giftModel: PublishZone, giftNum: String, model: StateDifficult) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            signWhen(showMsg: dataStoragePath)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(dataBelowValue.suffix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(showTooKey))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(dataBlocTitle.prefix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(show_bitData.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(data_upperName))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        VariableCombine.send(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.full(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.club(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.textMp(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func clipFilter(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.reduceException(true)
            self.msgView.reduceException(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func subtle(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.reduceException(true)
            self.msgView.reduceException(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func freshCommon(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.freshCommon(tap)
    }

    //: func func__starRecordBtnDown() {}
    func sendOf() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func currentAttach(voicePath: String, voiceTime: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension SampleColumn: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func someoneExplain() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ChainForce.languageInmate(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SaveProcess.editorTask(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: RetainProcess.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                RetainProcess.share.romanValue()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.directTo(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func informationIn() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ChainForce.singleQuick(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.directTo(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension SampleColumn {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func magnitudeerestBasic(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(show_bitData.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension SampleColumn {
    //: func func__TipsNotification() {
    func untilFast() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(component(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kChangeKey,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func component(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: RevealAway = userinfo[(notiRetainMessage.replacingOccurrences(of: "capable", with: "ms"))] as! RevealAway

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.reduceException(true)
        self.msgView.reduceException(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func productRe(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: appMigrationDetailedMessage.reversed(), encoding: .utf8)!: tipStr, (String(userAlreadyFormat) + String(showSendFrameworkValue)): (data_buildingCrucialKey.replacingOccurrences(of: "pose", with: "s"))]
        //: var message: NextAgainst?
        var message: NextAgainst?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = CaptureMend.designEnable(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.reduceException(true)
        self.msgView.reduceException(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func customerVariable() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] time in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard MountCustom.share.appUserConfigMode.rTips.count > 0 else { return }
            guard MountCustom.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: MountCustom.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.productRe(tipStr: MountCustom.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension SampleColumn {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func textMp(extral: [String: Any?], model: StateDifficult, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(noti_shouldMsg.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(appFreshEnvironmentStr))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(userAlreadyFormat) + String(showSendFrameworkValue))] = (showChoiceMessage.replacingOccurrences(of: "greet", with: "t"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(noti_shouldMsg.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: userContextPutString.reversed(), encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (userBirthdayGenderName.replacingOccurrences(of: "hold", with: "n") + userContactMsg.replacingOccurrences(of: "across", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: user_situationValue.map{threeTag(timing: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (main_beautyManualId.replacingOccurrences(of: "to", with: "A") + String(appLeadingUrl.prefix(6)) + "bers").localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((showChoiceMessage.replacingOccurrences(of: "greet", with: "t"))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(showChoiceMessage.replacingOccurrences(of: "greet", with: "t"))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(userBirthdayGenderName.replacingOccurrences(of: "hold", with: "n") + userContactMsg.replacingOccurrences(of: "across", with: "e"))] = "\(giftdic[(String(k_signNatureValue.prefix(7)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(appPetRocketData))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = CaptureMend.designEnable(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.monster(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(notiMarkId.suffix(6)) + String(dataImpactFormat))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(notiMarkId.suffix(6)) + String(dataImpactFormat))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.duringCompute(cellData, transition: msgInfoDic[(String(main_functionalMessage.suffix(5)))] as? String, consumerSophisticated: msgIncome, isPlain: .Msg_Status_Succ)
 
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        countegrity(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(show_exceptCustomerPath.prefix(9)) + showDistinctionMessage.replacingOccurrences(of: "pull", with: "n"))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(show_exceptCustomerPath.prefix(9)) + showDistinctionMessage.replacingOccurrences(of: "pull", with: "n"))] as! NSNumber
            //: MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: MountCustom.share.loginUserMode.mf_coin)
            msgInputView.giftView.conversionProjection(needReload: false, mf_coin: MountCustom.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.cropMusic()
    }

    //: func initCachEffectData() {
    func phoneSh() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.allow(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func countegrity(giftDic: [String: Any], model: StateDifficult) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.wrapRobot(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in EdgeCell.share.someRequest() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [AreaModel]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.sophisticated(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.event(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func cookie(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in EdgeCell.share.someRequest() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [AreaModel]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.sophisticated(model: giftModel as! PauseDoing)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.modifyMagnitudeegration(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension SampleColumn {
    /// 创建UI
    //: private func createUI() {
    private func editRestore() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func crush() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(mainItemFormat)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(mainItemFormat)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - noti_stageFormat)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func architectureQuit() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = KeepViewController()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! NextAgainst).isKind(of: OvalCharacteristicValueGrainSumStepTechnique.self) {
                    if (cellData as! NextAgainst).isKind(of: OvalCharacteristicValueGrainSumStepTechnique.self) {
                        //: let groupCellData = cellData as! OvalCharacteristicValueGrainSumStepTechnique
                        let groupCellData = cellData as! OvalCharacteristicValueGrainSumStepTechnique
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(main_functionalMessage.suffix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: SampleDepth.shared.rx
        SampleDepth.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(userBlankUrl) + String(data_shallNextNaturePath.suffix(4)) + "nection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
