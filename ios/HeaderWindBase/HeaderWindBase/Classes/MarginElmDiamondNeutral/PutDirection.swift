
//: Declare String Begin

/*: "http://" :*/
fileprivate let userFragmentString:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let constLostKey:String = "ownert"
fileprivate let const_explainKey:[Character] = ["t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let k_viewStr:[Character] = ["h","t","t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let const_yieldData:String = "SERV"
fileprivate let showFrontPath:String = "stream writing constant skirt termslctIn"
fileprivate let userSimultaneouslyMessage:[Character] = ["d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let notiPossibleMessage:String = "seequalve"
fileprivate let constImmediateTitle:[Character] = [" ","="," "]

/*: "app. :*/
fileprivate let userFastDonName:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let const_meStr:String = ".commember observer double"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let app_termsSomeoneMessage:String = "mf/chyour support thread"
fileprivate let dataChannelMsg:String = "number"
fileprivate let k_afterLostData:String = "/senguard lower finish obtain previous"

/*: "recharge/applePayNotify" :*/
fileprivate let noti_panelUrl:String = "misscha"
fileprivate let constQuitValue:String = "pplePaysubsequent choose too"
fileprivate let noti_statusFormat:String = "Notifycur your"

/*: "user/appleLogin" :*/
fileprivate let dataScientificValueMsg:[Character] = ["u","s","e","r","/","a","p","p","l","e","L","o","g","i"]
fileprivate let noti_intoString:String = "would"

/*: "IM/getMyBlackList" :*/
fileprivate let app_implementPath:String = "at implement secondIM/g"
fileprivate let main_lackFormat:[Character] = ["a","c","k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let main_publishPath:String = "satisfy originmf/bu"
fileprivate let constModelFormat:String = "/sendMsbalance become"
fileprivate let app_deviceId:String = "functional"

/*: "mf/business/sendGift" :*/
fileprivate let noti_uniqueFormat:String = "texture"
fileprivate let userExceptionValue:String = "f/busiby server match additional architecture"
fileprivate let user_maxWrittenTitle:String = "relation dinner within export/sendGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let constBotBridgeMessage:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","M","s","g"]

/*: "GJ21FB" :*/
fileprivate let show_trackData:String = "GJ21paintB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let app_interestPath:String = "\u{5b8c}成了检测"

/*: "接口请求失败" :*/
fileprivate let app_reductionComplaintValue:String = "接口\u{8bf7}求\u{5931}"
fileprivate let notiNearbyString:String = "败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let appDimensionId:String = "thin成了检测,"
fileprivate let main_indexName:String = "需要\u{66f4}换s"
fileprivate let notiTotaleractionFormat:String = "pervper"

/*: "HTTPProxy" :*/
fileprivate let constUndertakeKey:String = "Hborder"
fileprivate let dataMmResData:[Character] = ["T","P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let notiHereStr:String = "__SCOPEion comprehensive confirm"
fileprivate let data_edgeCurrencyKey:[Character] = ["D","_","_"]

/*: "tap" :*/
fileprivate let main_integrationId:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let appSoundPath:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let constStimulateNetworkStr:String = "iownerec"

/*: "ppp" :*/
fileprivate let userSortId:String = "densityp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let showLeaveStr:String = "func_exactly mutual"
fileprivate let show_assetId:String = "teTayes examine its significant retain"
fileprivate let userWeightSubstantialSunTitle:String = "weekly changeCooki"

/*: "https:// :*/
fileprivate let mainLocateTakeStr:String = "https://scientific policy provide"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let user_pathKey:[UInt8] = [0xd,0x1c,0x15,0xa,0x6,0x6,0xb,0xc,0x13,0xc,0x1b,0xc,0xfa,0x8,0x14,0xc,0xea,0x16,0x16,0x12,0x10,0xc,0x1a,0xfe,0x10,0x1b,0xf,0xfc,0x19,0x13,0xc7,0xe4,0xc7]

fileprivate func compareZzParty(talk num: UInt8) -> UInt8 {
    let value = Int(num) - 167
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let noti_onlyStr:String = "Uwith"

/*: "PHPSESSID" :*/
fileprivate let k_pairString:String = "jumpHjumpS"

/*: "func__saveAllCookies =  :*/
fileprivate let const_plateStartFormat:String = "func__written till launch expression"
fileprivate let main_resourceName:String = "mic odd lobby iconlC"
fileprivate let const_womanValue:String = "inviteki"

/*: "func__saveAllCookies  error" :*/
fileprivate let notiScanMsg:String = "func_progress dynamic topic"
fileprivate let main_estimatedSustainableName:String = "self plus clip stretchllCoo"
fileprivate let notiReaderWomanMessage:String = "  ertrue edit satisfy"
fileprivate let mainConnectionMessage:String = "ROR"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PutDirection.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum TempStroke: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class PutDirection: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = PutDirection()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        clubAnswer()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(userFragmentString))) || newValue.hasPrefix((constLostKey.replacingOccurrences(of: "owner", with: "h") + String(const_explainKey))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if user_locateTitle && !mainCharacteristicPath {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(k_viewStr)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(k_viewStr)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    segmentNearby(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func clubAnswer() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = data_toStr.integer(forKey: user_outputPath)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (const_yieldData.lowercased() + "erSe" + String(showFrontPath.suffix(5)) + String(userSimultaneouslyMessage)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = resistanceOfTemp()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userFragmentString))
        //: if Environment_Formal && !Encryption_Request {
        if user_locateTitle && !mainCharacteristicPath {
            //: serverPrefix = "http://"
            serverPrefix = (String(userFragmentString))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (notiPossibleMessage.replacingOccurrences(of: "equal", with: "r") + "rUrlStr" + String(constImmediateTitle)) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func resistanceOfTemp() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func providerFor(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(userFastDonName)) + "\(dataServiceName)" + (String(const_meStr.prefix(4))), (String(userFastDonName)) + "\(dataServiceName)" + (String(const_meStr.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(app_termsSomeoneMessage.prefix(5)) + "atRo" + dataChannelMsg.replacingOccurrences(of: "number", with: "om") + String(k_afterLostData.prefix(4)) + "dGift"),
         //: "recharge/applePayNotify",
         (noti_panelUrl.replacingOccurrences(of: "miss", with: "re") + "rge/a" + String(constQuitValue.prefix(7)) + String(noti_statusFormat.prefix(6))),
         //: "user/appleLogin"] + slowPathArr1
         (String(dataScientificValueMsg) + noti_intoString.replacingOccurrences(of: "would", with: "n"))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(app_implementPath.suffix(4)) + "etMyBl" + String(main_lackFormat)),
         //: "mf/business/sendMsg",
         (String(main_publishPath.suffix(5)) + "siness" + String(constModelFormat.prefix(7)) + app_deviceId.replacingOccurrences(of: "functional", with: "g")),
         //: "mf/business/sendGift",
         (noti_uniqueFormat.replacingOccurrences(of: "texture", with: "m") + String(userExceptionValue.prefix(6)) + "ness" + String(user_maxWrittenTitle.suffix(9))),
         //: "mf/chatRoom/sendMsg"]
         (String(constBotBridgeMessage))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = StopBrush.getSystemLangCode()
        let lang = StopBrush.adTotalegrity()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .basicInWrap(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (show_trackData.replacingOccurrences(of: "paint", with: "F")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension PutDirection {
    //: func checkServerUrl() {
    func midClose() {
        //: if MountCustom.share.networkStatus != .Unavailable {
        if MountCustom.share.networkStatus != .Unavailable {
            //: func_requestHost()
            noDirection()
            //: } else {
        } else {
            //: func_starCodeTime()
            eventValue()
        }
    }

    //: func func_starCodeTime() {
    func eventValue() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] num in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.noDirection()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func without() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func noDirection() {
        //: if MountCustom.share.networkStatus == .Unavailable {
        if MountCustom.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        without()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        FrequencyFind.receiver { [weak self] succeed, result, errorModel in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (app_interestPath + ",请求状态\u{6b63}常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (app_reductionComplaintValue + notiNearbyString.capitalized))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.eventValue()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (appDimensionId.replacingOccurrences(of: "thin", with: "完") + main_indexName + notiTotaleractionFormat.replacingOccurrences(of: "per", with: "er") + " url"))
                    //: self.func__changeHostUrl()
                    self.maxRecognize()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func maxRecognize() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        without()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        data_toStr.set(serverSelctIndex, forKey: user_outputPath)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userFragmentString))
        //: if Environment_Formal && !Encryption_Request {
        if user_locateTitle, !mainCharacteristicPath {
            //: serverPrefix = "http://"
            serverPrefix = (String(userFragmentString))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension PutDirection {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func historyBeside() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(constUndertakeKey.replacingOccurrences(of: "border", with: "T") + String(dataMmResData))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func weekParticle() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(notiHereStr.prefix(7)) + String(data_edgeCurrencyKey))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(main_integrationId))) || keyStr.contains((String(appSoundPath))) || keyStr.contains((constStimulateNetworkStr.replacingOccurrences(of: "owner", with: "ps"))) || keyStr.contains((userSortId.replacingOccurrences(of: "density", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension PutDirection {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func segmentNearby(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(showLeaveStr.prefix(5)) + "_upda" + String(show_assetId.prefix(4)) + "lking" + String(userWeightSubstantialSunTitle.suffix(5)) + "es = ") + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = titleOdd()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
        var urlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(userFragmentString)), with: (constLostKey.replacingOccurrences(of: "owner", with: "h") + String(const_explainKey)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((constLostKey.replacingOccurrences(of: "owner", with: "h") + String(const_explainKey))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(mainLocateTakeStr.prefix(8))) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            cornerNo(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        multipleAverageComprehensiveOwnerTing()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func cornerNo(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: user_pathKey.map{compareZzParty(talk: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func titleOdd() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userFragmentString))
        //: if Environment_Formal && !Encryption_Request {
        if user_locateTitle, !mainCharacteristicPath {
            //: serverPrefix = "https://"
            serverPrefix = (constLostKey.replacingOccurrences(of: "owner", with: "h") + String(const_explainKey))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(userFastDonName)) + "\(dataServiceName)" + (String(const_meStr.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func plate(note: Notification) {
        //: if MountCustom.share.loginUid.isEmptyString {
        if MountCustom.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            componentShadow()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            methodNumberegration()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func componentShadow() {
        //: var htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
        var htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (noti_onlyStr.replacingOccurrences(of: "with", with: "ID")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (noti_onlyStr.replacingOccurrences(of: "with", with: "ID")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID") {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))), h5Cookie.keys.contains((noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")), h5Cookie.keys.contains((k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func methodNumberegration() {
        //: let htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
        let htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == MountCustom.share.loginUserMode.userID {
            if cookie.name == (noti_onlyStr.replacingOccurrences(of: "with", with: "ID")), cookie.value == MountCustom.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID") {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (noti_onlyStr.replacingOccurrences(of: "with", with: "ID")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(noti_onlyStr.replacingOccurrences(of: "with", with: "ID"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(k_pairString.replacingOccurrences(of: "jump", with: "P") + "ESSID")] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func multipleAverageComprehensiveOwnerTing() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(const_plateStartFormat.prefix(6)) + "saveAl" + String(main_resourceName.suffix(2)) + const_womanValue.replacingOccurrences(of: "invite", with: "oo") + "es = ") + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            data_toStr.set(cookieData, forKey: kInputKey)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(notiScanMsg.prefix(5)) + "_saveA" + String(main_estimatedSustainableName.suffix(5)) + "kies" + String(notiReaderWomanMessage.prefix(4)) + mainConnectionMessage.lowercased()))
        }
    }

    //: func func__setAppCookies() {
    func permanentCommon() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = data_toStr.object(forKey: kInputKey)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func valueWith() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        data_toStr.removeObject(forKey: kInputKey)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
