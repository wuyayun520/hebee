
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataMasterSpeedUrl:[UInt8] = [0xec,0xeb,0xec,0xf1,0xad,0xe6,0xea,0xe1,0xe0,0xf7,0xbf,0xac,0xa5,0xed,0xe4,0xf6,0xa5,0xeb,0xea,0xf1,0xa5,0xe7,0xe0,0xe0,0xeb,0xa5,0xec,0xe8,0xf5,0xe9,0xe0,0xe8,0xe0,0xeb,0xf1,0xe0,0xe1]

private func radioSomeone(party num: UInt8) -> UInt8 {
    return num ^ 133
}

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let main_oddKey:[UInt8] = [0xfb,0xc4,0xd1,0xda,0x94,0xc0,0xdc,0xd1,0xda,0x94,0x91,0xf4,0x94,0xc3,0xdd,0xd8,0xd8,0x94,0xc6,0xd1,0xd7,0xdb,0xd9,0xd9,0xd1,0xda,0xd0,0x94,0xc1,0xc7,0xd1,0xc6,0xc7,0x94,0xc0,0xdb,0x94,0xcd,0xdb,0xc1,0x9a,0x94,0xf5,0x94,0xda,0xd1,0xc3,0x94,0xd7,0xdc,0xd5,0xc0,0x94,0xc3,0xdd,0xd8,0xd8,0x94,0xd6,0xd1,0x94,0xd9,0xd5,0xd0,0xd1,0x94,0xd5,0xd2,0xc0,0xd1,0xc6,0x94,0xd5,0x94,0xc7,0xc1,0xd7,0xd7,0xd1,0xc7,0xc7,0xd2,0xc1,0xd8,0x94,0xc6,0xd1,0xd7,0xdb,0xd9,0xd9,0xd1,0xda,0xd0,0xd5,0xc0,0xdd,0xdb,0xda,0x9a]

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let user_skirtSumStr:[UInt8] = [0x2e,0x67,0x6e,0x69,0x6b,0x6e,0x61,0x72,0x20,0x65,0x68,0x74,0x20,0x6e,0x69,0x20,0x64,0x65,0x79,0x61,0x6c,0x70,0x73,0x69,0x64,0x20,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x6c,0x6c,0x69,0x77,0x20,0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e,0x20,0x64,0x6e,0x61,0x20,0x72,0x61,0x74,0x61,0x76,0x61,0x20,0x72,0x75,0x6f,0x59]

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let mainCloudName:[UInt8] = [0x27,0x18,0xd,0x6,0x48,0x1c,0x0,0xd,0x6,0x48,0x29,0x18,0x18,0x4,0x11,0x6,0x9,0x5,0xd,0x48,0x1f,0x1,0x4,0x4,0x48,0x1a,0xd,0xb,0x7,0x5,0x5,0xd,0x6,0xc,0x48,0x1d,0x1b,0xd,0x1a,0x1b,0x48,0x1c,0x7,0x48,0x11,0x7,0x1d,0x46,0x48,0x3d,0x1b,0xd,0x48,0x11,0x7,0x1d,0x1a,0x48,0x1,0xc,0xd,0x6,0x1c,0x1,0x1c,0x11,0x48,0x1c,0x7,0x48,0xf,0x1a,0xd,0xd,0x1c,0x48,0xd,0x9,0xb,0x0,0x48,0x7,0x1c,0x0,0xd,0x1a,0x46]

private func rateWhat(error num: UInt8) -> UInt8 {
    return num ^ 104
}

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let main_aroundRewardString:[UInt8] = [0xcd,0xf2,0xe7,0xec,0xa2,0xf6,0xea,0xe7,0xec,0xa2,0xa7,0xc2,0xa2,0xf5,0xeb,0xee,0xee,0xa2,0xf0,0xe7,0xe1,0xed,0xef,0xef,0xe7,0xec,0xe6,0xa2,0xf7,0xf1,0xe7,0xf0,0xf1,0xa2,0xf6,0xed,0xa2,0xfb,0xed,0xf7,0xac,0xa2,0xd7,0xf1,0xe7,0xa2,0xfb,0xed,0xf7,0xf0,0xa2,0xeb,0xe6,0xe7,0xec,0xf6,0xeb,0xf6,0xfb,0xa2,0xf6,0xed,0xa2,0xe5,0xf0,0xe7,0xe7,0xf6,0xa2,0xe7,0xe3,0xe1,0xea,0xa2,0xed,0xf6,0xea,0xe7,0xf0,0xac]

private func yesterdayBean(portrait num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "#FF2348" :*/
fileprivate let appBodExitId:[Character] = ["#","F","F","2","3","4","8"]

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let data_skinDatabaseTitle:[UInt8] = [0x7a,0x4c,0x56,0x4,0x51,0x46,0x3,0x44,0x4c,0x4a,0x4d,0x44,0x3,0x57,0x4c,0x3,0x47,0x46,0x4f,0x46,0x57,0x46,0x3,0x5a,0x4c,0x56,0x51,0x3,0x53,0x51,0x4c,0x45,0x4a,0x4f,0x46,0xf,0x4e,0x46,0x50,0x50,0x42,0x44,0x46,0x50,0xf,0x3,0x53,0x4b,0x4c,0x57,0x4c,0x50,0x3,0x42,0x4d,0x47,0x3,0x4e,0x42,0x57,0x40,0x4b,0x46,0x47,0x3,0x45,0x51,0x4a,0x46,0x4d,0x47,0x50,0x3,0x73,0x66,0x71,0x6e,0x62,0x6d,0x66,0x6d,0x77,0x6f,0x7a,0x3,0x42,0x4d,0x47,0x3,0x5a,0x4c,0x56,0x51,0x3,0x42,0x40,0x40,0x4c,0x56,0x4d,0x57,0x3,0x54,0x4a,0x4f,0x4f,0x3,0x41,0x46,0x3,0x76,0x6d,0x71,0x66,0x60,0x6c,0x75,0x66,0x71,0x62,0x61,0x6f,0x66,0xd,0x60,0x4c,0x4d,0x57,0x4a,0x4d,0x56,0x46,0x3,0x57,0x4c,0x3,0x47,0x46,0x4f,0x46,0x57,0x46,0x3,0x5a,0x4c,0x56,0x51,0x3,0x42,0x40,0x40,0x4c,0x56,0x4d,0x57,0x1c,0x3]

private func scheduleInstance(mix num: UInt8) -> UInt8 {
    return num ^ 35
}

/*: "UNRECOVERABLE" :*/
fileprivate let mainHiConsumeKey:String = "errorNREC"
fileprivate let show_observeCreateSectionMsg:String = "OVERAkindLE"

/*: "Delete Account" :*/
fileprivate let user_evaluateMsg:[Character] = ["D","e","l","e"]
fileprivate let data_circleBucketStr:[Character] = ["t","e"," ","A","c","c","o","u","n","t"]

/*: "Cancel" :*/
fileprivate let k_theKey:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let data_pathPressEfficiencyId:String = "camera"
fileprivate let main_sectionBasicName:String = "eoriginete"

/*: "Logout succeeded!" :*/
fileprivate let main_premiumPath:[Character] = ["L","o","g","o","u","t"," ","s","u","c","c","e","e","d","e","d"]
fileprivate let dataPictureDateString:String = "!"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TagHard.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum FlushWillSort: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum DifficultPromiseProgress: String {
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class TagHard: PetViewController {
    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = TopRunBrown()

    //: var  type = SettingsUniversal.Contact_information
    var type = FlushWillSort.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataMasterSpeedUrl.map{radioSomeone(party: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.boardUnexpected()
        //: designView()
        oddForMain()
        //: getSettingData()
        alongsideRemove()
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [DifficultPromiseProgress] = {
        //: var array = [SettingsUniversalType]()
        var array = [DifficultPromiseProgress]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString - appRetainName), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Your avatar and nickname will not be displayed in the ranking.".localized, "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized]
        var array = ["", String(bytes: main_oddKey.map{$0^180}, encoding: .utf8)!.fieldOf(const_informationUniqueChartPath), String(bytes: user_skirtSumStr.reversed(), encoding: .utf8)!.localized, String(bytes: mainCloudName.map{rateWhat(error: $0)}, encoding: .utf8)!.localized]
        //: if MountCustom.share.loginUserMode.sex == "2" {
        if MountCustom.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: main_aroundRewardString.map{yesterdayBean(portrait: $0)}, encoding: .utf8)!.fieldOf(const_informationUniqueChartPath)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension TagHard {
    //: func getSettingData() {
    func alongsideRemove() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        SignificantMount.jobByMount(params: dict) { succeed, result, errorModel in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<TopRunBrown>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func translateAge(type: FlushWillSort) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension TagHard: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in tableView: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return actualHeight(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if MountCustom.share.loginUserMode.sex == "2" {
                if MountCustom.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return actualHeight(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return actualHeight(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return actualHeight(h: 92)
            }
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingCell.className(), for: indexPath) as! TalkingSettingCell
            let cell: IsolateMeasure = tableView.dequeueReusableCell(withIdentifier: IsolateMeasure.className(), for: indexPath) as! IsolateMeasure

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.drop(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: cell.titleBLB.textColor = UIColor.appTitleColor()
            cell.titleBLB.textColor = UIColor.labelMomentum()

            //: if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            if DataSource[indexPath.row] == DifficultPromiseProgress.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(appBodExitId)))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingNotifCell.className(), for: indexPath) as! TalkingSettingNotifCell
            let cell: MinimumView = tableView.dequeueReusableCell(withIdentifier: MinimumView.className(), for: indexPath) as! MinimumView

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.resign(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.theEager(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.promptAbout(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.promptAbout(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.promptAbout(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.promptAbout(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.promptAbout(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            if DataSource[indexPath.row].rawValue == DifficultPromiseProgress.DeleteA.rawValue {
                //: writeOff()
                blindFull()
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension TagHard {
    /// 注销
    //: private func writeOff() {
    private func blindFull() {
        //: guard TalkingSocketManager.shared.usage == false else {
        guard PanCurrentStatus.shared.usage == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.signWhen(showMsg: noti_makePermanentValue)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: data_skinDatabaseTitle.map{scheduleInstance(mix: $0)}, encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.movement((mainHiConsumeKey.replacingOccurrences(of: "error", with: "U") + show_observeCreateSectionMsg.replacingOccurrences(of: "kind", with: "B")))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.similar(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        MainTranslate.galleryWith(title: (String(user_evaluateMsg) + String(data_circleBucketStr)).localized, attributedMessage: string, leftBtnTitle: (String(k_theKey)).localized, rightBtnTitle: (data_pathPressEfficiencyId.replacingOccurrences(of: "camera", with: "D") + main_sectionBasicName.replacingOccurrences(of: "origin", with: "l")).localized) {
            //: TalkingAlertShow.hideAlert()
            MainTranslate.scheduleWhat()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            MainTranslate.scheduleWhat()

            //: self.LockAccount()
            self.nameure()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                MainTranslate.sensorAll(title: (data_pathPressEfficiencyId.replacingOccurrences(of: "camera", with: "D") + main_sectionBasicName.replacingOccurrences(of: "origin", with: "l")).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                MainTranslate.sensorAll(title: (data_pathPressEfficiencyId.replacingOccurrences(of: "camera", with: "D") + main_sectionBasicName.replacingOccurrences(of: "origin", with: "l")).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func nameure() {
        //: ProgressHUD.show()
        LevelBoxIndex.naturalBy()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        SignificantMount.outsideAdvanced(params: [:]) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            LevelBoxIndex.gestureOff()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.measureOnBorder()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.notFade(showMsg: (String(main_premiumPath) + dataPictureDateString.capitalized).localized)
            }
        }
    }

    //: private func logoutTool() {
    private func measureOnBorder() {
        //: guard TalkingSocketManager.shared.usage == false else {
        guard PanCurrentStatus.shared.usage == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.signWhen(showMsg: noti_makePermanentValue)
            //: return
            return
        }

        //: ProgressHUD.show()
        LevelBoxIndex.naturalBy()
        //: TalkingLoginRequestTool.req_loginOut { t in
        WhenCritical.noneKit { t in
            //: ProgressHUD.dismiss()
            LevelBoxIndex.gestureOff()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: show_exerciseString, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension TagHard {
    //: private func designView() {
    private func oddForMain() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: TalkingSettingCell.className())
        MainTable.register(IsolateMeasure.self, forCellReuseIdentifier: IsolateMeasure.className())
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: TalkingSettingNotifCell.className())
        MainTable.register(MinimumView.self, forCellReuseIdentifier: MinimumView.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
