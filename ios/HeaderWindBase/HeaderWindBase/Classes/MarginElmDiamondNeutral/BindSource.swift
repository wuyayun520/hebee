
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_botFormat:[UInt8] = [0x2c,0x2b,0x2c,0x31,0x6d,0x26,0x2a,0x21,0x20,0x37,0x7f,0x6c,0x65,0x2d,0x24,0x36,0x65,0x2b,0x2a,0x31,0x65,0x27,0x20,0x20,0x2b,0x65,0x2c,0x28,0x35,0x29,0x20,0x28,0x20,0x2b,0x31,0x20,0x21]

/*: "uid" :*/
fileprivate let notiSegmentCoordinatorKey:[UInt8] = [0xa4,0x98,0x93]

fileprivate func everyoneShake(level num: UInt8) -> UInt8 {
    let value = Int(num) + 209
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let data_blocSlidePerformMessage:[Character] = ["h","e"]
fileprivate let userOutputMsg:String = "button train boyadPic"

/*: "nickname" :*/
fileprivate let k_movementInsteadStr:[Character] = ["n","i","c","k","n"]
fileprivate let const_accuracyEagerUrl:String = "alack"

/*: "birthday" :*/
fileprivate let k_symbolRetMessage:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let data_airKey:String = "VIDEO"
fileprivate let data_formAccountMsg:String = "Urlmigration stat"

/*: "location" :*/
fileprivate let k_pressHePath:[Character] = ["l","o","c","a","t","i","o"]
fileprivate let show_playerGravityStatId:String = "brush"

/*: "cmd" :*/
fileprivate let app_onlyStr:[UInt8] = [0xc4,0xca,0xc3]

private func themeNeed(me num: UInt8) -> UInt8 {
    return num ^ 167
}

/*: "refuseCall" :*/
fileprivate let kJustPath:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let data_amUrl:[UInt8] = [0x1c,0x19,0xc,0x19]

private func visibleObject(exactly num: UInt8) -> UInt8 {
    return num ^ 120
}

/*: "Call interruption" :*/
fileprivate let userSimilarKey:String = "female which service sink dialogCall"
fileprivate let k_strengthData:String = "RRUPTION"

/*: "stopCall" :*/
fileprivate let k_iconPath:String = "nature mix import itemstopCall"

/*: "logId" :*/
fileprivate let user_decideTitle:[UInt8] = [0x78,0x7b,0x73,0x5d,0x70]

private func personalSort(transaction num: UInt8) -> UInt8 {
    return num ^ 20
}

/*: "cancelCall" :*/
fileprivate let kVidUrl:String = "ceditornce"
fileprivate let notiForeValue:String = "drown containlCall"

/*: "No answer" :*/
fileprivate let appWayMsg:String = "No answeheap spend purple scenario"
fileprivate let user_amHimId:String = "meeting"

/*: "duration" :*/
fileprivate let appGuideUrl:String = "duratiocur"

/*: "She has hung up" :*/
fileprivate let showPermissionId:[Character] = ["S","h","e"," "]
fileprivate let mainRequestId:String = "pas publichas h"

/*: "cancelSex" :*/
fileprivate let app_extraMessage:[Character] = ["c","a","n","c","e"]
fileprivate let userSupportKey:[Character] = ["l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let k_awakeMsg:String = "He hinterest parent fill instance"
fileprivate let data_upMessage:String = "g upassociated attention"

/*: "endType" :*/
fileprivate let showReportExpressionString:String = "into bold wholeendType"

/*: "stopSex" :*/
fileprivate let main_clubPath:String = "stopSfeedback force problem control help"
fileprivate let kCanvasReachUrl:String = "emovement"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let noti_scopeString:[UInt8] = [0x99,0xbe,0xa3,0xa5,0xb6,0xb6,0xb9,0xb3,0xb9,0xb5,0xbe,0xa4,0xf0,0xb7,0xbf,0xbc,0xb4,0xf0,0xb3,0xbf,0xb9,0xbe,0xa3,0xfc,0xf0,0xb3,0xb1,0xbc,0xbc,0xf0,0xb8,0xb1,0xa3,0xf0,0xb5,0xbe,0xb4,0xb5,0xb4]

private func subjectAddress(mend num: UInt8) -> UInt8 {
    return num ^ 208
}

/*: "The other hung up the phone" :*/
fileprivate let noti_accountStr:String = "The oasset ready tension show"
fileprivate let appCommonString:String = "ung uleave another await"
fileprivate let kParaResultPath:[Character] = ["p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let user_readyKey:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let k_selectUrl:String = "measure"
fileprivate let show_insertUrl:String = "render disable pull drawingumpUrl"

/*: "Cancel" :*/
fileprivate let data_playerData:String = "Cancelspring policy third stream written"

/*: "Get gold coins" :*/
fileprivate let main_descriptionTitle:[Character] = ["G","e","t"," ","g","o","l","d"," "]
fileprivate let mainAnonymousStr:[Character] = ["c","o","i","n","s"]

/*: "refuseSex" :*/
fileprivate let kTooMessage:String = "feature session array destinationrefuseSex"

/*: "onAcceptCall" :*/
fileprivate let userUsedStr:String = "multi advancedonAccep"
fileprivate let user_constructKey:[Character] = ["t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let noti_convertString:String = "adrawe"

/*: "Calls are cancelled" :*/
fileprivate let userCustomForwardMessage:String = "Calltoward begin pause"
fileprivate let kSeaString:String = "border movement pending remote canc"
fileprivate let showManagerId:String = "ephotoed"

/*: "The call has ended" :*/
fileprivate let const_integrityTitle:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n","d","e"]
fileprivate let const_revenueKey:String = "mirror"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BindSource.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let show_viewUrl = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class BindSource: PetViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: FactorModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PerformExtraMaster? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: FormAsset? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        PanCurrentStatus.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        PanCurrentStatus.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PanCurrentStatus.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_botFormat.map{$0^69}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! BuildLast
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.accessSelect()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            awayFor()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.usageVC = nil
            PanCurrentStatus.shared.usageVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.depthLoop()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        queryHalf()
        //: setupSubViewsConstraint()
        insideThree()
        //: initNotificationObserver()
        fade()
        //: req_callGetUserInfo()
        sinceVisitCollection()
        //: timeoutRules()
        lineNo()
        //: resumeTalk()
        restrictionShow()
        //: setUpBGMPlayer()
        spineBefore()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MountCustom.share.appConfigMode.disableShootScreen {
            if MountCustom.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = MustView(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.usage = false
        PanCurrentStatus.shared.usage = false
        //: TalkingSocketManager.shared.isCalling = false
        PanCurrentStatus.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.accept()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: InsertConnect = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = InsertConnect(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: ReliabilityEquipment = {
        //: let m = TalkingVoiceChatManager()
        let m = ReliabilityEquipment()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension BindSource {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func sinceVisitCollection() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ProvideOvalBehavior.responseGem(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: notiSegmentCoordinatorKey.map{everyoneShake(level: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(data_blocSlidePerformMessage) + String(userOutputMsg.suffix(5)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(k_movementInsteadStr) + const_accuracyEagerUrl.replacingOccurrences(of: "lack", with: "me"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(k_symbolRetMessage))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(data_airKey.lowercased() + String(data_formAccountMsg.prefix(3)))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(k_pressHePath) + show_playerGravityStatId.replacingOccurrences(of: "brush", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.warning()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension BindSource {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func awayFor() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        ChainForce.serverReplace(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.signWhen(showMsg: show_beginTakeName)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (String(kJustPath)), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: notiSegmentCoordinatorKey.map{everyoneShake(level: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.timing()
                //: return
                return
            }

            //: self.stopBGM()
            self.frameworkSingle()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PerformExtraMaster.magnitudeerest(type: .Response)
            //: self.startBGM()
            self.accessSelect()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func accept() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.frameworkSingle()
        //: TalkingSocketManager.shared.callerDelegate = nil
        PanCurrentStatus.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        PanCurrentStatus.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        PanCurrentStatus.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func timing(animated: Bool = true) {
        //: self.stopBGM()
        self.frameworkSingle()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func handleInner() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.sceneKeep(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func restrictionShow() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            handleInner()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension BindSource {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func fade() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(memberByNatural(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(after(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: mainSubtleEnvironmentMsg,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        show_viewUrl.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func memberByNatural(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            timing()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func after(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard PanCurrentStatus.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        PanCurrentStatus.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        PanCurrentStatus.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        PanCurrentStatus.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.remainS(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension BindSource: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.signWhen(showMsg: (String(userSimilarKey.suffix(4)) + " inte" + k_strengthData.lowercased()).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (String(k_iconPath.suffix(8))), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: user_decideTitle.map{personalSort(transaction: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (kVidUrl.replacingOccurrences(of: "editor", with: "a") + String(notiForeValue.suffix(5))), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: user_decideTitle.map{personalSort(transaction: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                PanCurrentStatus.shared.banAcross(info: [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (String(kJustPath)), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: notiSegmentCoordinatorKey.map{everyoneShake(level: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.third()
                //: TalkingSocketManager.shared.usageVC = nil
                PanCurrentStatus.shared.usageVC = nil
            }
            //: popCurrentViewController()
            timing()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.untilKind(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension BindSource {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func spineBefore() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DescriptionBind.Call : DescriptionBind.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PerformExtraMaster.magnitudeerest(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func accessSelect() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.impressionForce()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func frameworkSingle() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.curveEstimated()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension BindSource {
    /// 超时定时器
    //: private func timeoutRules() {
    private func lineNo() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? noti_checkData : self.chatModel?.theAgainst()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            beforeVoice()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.beforeVoice()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func beforeVoice() {
        //: guard !TalkingSocketManager.shared.usage else { return }
        guard !PanCurrentStatus.shared.usage else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.signWhen(showMsg: (String(appWayMsg.prefix(8)) + user_amHimId.replacingOccurrences(of: "meeting", with: "r")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (kVidUrl.replacingOccurrences(of: "editor", with: "a") + String(notiForeValue.suffix(5))), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: user_decideTitle.map{personalSort(transaction: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (String(kJustPath)), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: notiSegmentCoordinatorKey.map{everyoneShake(level: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        PanCurrentStatus.shared.banAcross(info: socketDict)
        //: popCurrentViewController()
        timing()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension BindSource {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func everySignal(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: user_decideTitle.map{personalSort(transaction: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(appGuideUrl.replacingOccurrences(of: "cur", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.everyStage(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.efficiencyTotal()
            //: createTalkRoom()
            handleInner()
            //: self.stopBGM()
            self.frameworkSingle()
        }
    }
}

// MARK: - FromReduce【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension BindSource: FromReduce {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func isMissReload(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showPermissionId) + String(mainRequestId.suffix(5)) + "ung up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(app_extraMessage) + String(userSupportKey))].stringValue == BasicIndependent.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_awakeMsg.prefix(4)) + "as hun" + String(data_upMessage.prefix(4))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.signWhen(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.third()
            //: TalkingSocketManager.shared.usageVC = nil
            PanCurrentStatus.shared.usageVC = nil
        }

        //: popCurrentViewController()
        timing()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func someTop(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: user_decideTitle.map{personalSort(transaction: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.frameworkSingle()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.efficiencyTotal()
        //: self.createTalkRoom()
        self.handleInner()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func more(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(showReportExpressionString.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(showPermissionId) + String(mainRequestId.suffix(5)) + "ung up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(main_clubPath.prefix(5)) + kCanvasReachUrl.replacingOccurrences(of: "movement", with: "x"))].stringValue == BasicIndependent.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(k_awakeMsg.prefix(4)) + "as hun" + String(data_upMessage.prefix(4))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if MountCustom.share.appStatus == AppSkinStatus.normal.rawValue {
            if MountCustom.share.appStatus == SensorRow.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: noti_scopeString.map{subjectAddress(mend: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(noti_accountStr.prefix(5)) + "ther h" + String(appCommonString.prefix(5)) + String(kParaResultPath)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.signWhen(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.third()
            //: TalkingSocketManager.shared.usageVC = nil
            PanCurrentStatus.shared.usageVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.duringOf()
        //: self.popCurrentViewController()
        self.timing()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: noti_mountFlowFormat, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func dealRate(msg: String, data: [String: Any]) {
        //: guard MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MountCustom.share.appStatus == SensorRow.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(user_readyKey))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(k_selectUrl.replacingOccurrences(of: "measure", with: "j") + String(show_insertUrl.suffix(6)))] as? String else { return }
            //: let httpsUrl = MountCustom.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = MountCustom.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = EquipmentImplementation()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: RetainProcess.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            RetainProcess.share.mpCurrency(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            MainTranslate.packageCos(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(data_playerData.prefix(6))).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(main_descriptionTitle) + String(mainAnonymousStr)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                MainTranslate.scheduleWhat()
                //: } rightBlock: {
            } rightBlock: {
                //: RetainProcess.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                RetainProcess.share.bod(clickEvent: mainBorderTitle, sufficient: false)
            }
        }
    }
}

// MARK: - SymbolConnect【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension BindSource: SymbolConnect {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func count(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showPermissionId) + String(mainRequestId.suffix(5)) + "ung up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(kTooMessage.suffix(9)))].stringValue == BasicIndependent.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_awakeMsg.prefix(4)) + "as hun" + String(data_upMessage.prefix(4))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.signWhen(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.third()
            //: TalkingSocketManager.shared.usageVC = nil
            PanCurrentStatus.shared.usageVC = nil
        }
        //: popCurrentViewController()
        timing()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func no(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: user_decideTitle.map{personalSort(transaction: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.frameworkSingle()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.efficiencyTotal()
        //: self.createTalkRoom()
        self.handleInner()
    }
}

// MARK: - KitWrap【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension BindSource: KitWrap {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func documentSingle(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(kJustPath)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.signWhen(showMsg: (String(appWayMsg.prefix(8)) + user_amHimId.replacingOccurrences(of: "meeting", with: "r")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.third()
                //: TalkingSocketManager.shared.usageVC = nil
                PanCurrentStatus.shared.usageVC = nil
            }
            //: self.popCurrentViewController()
            self.timing()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(userUsedStr.suffix(7)) + String(user_constructKey)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MountCustom.share.appStatus == SensorRow.normal.rawValue else { return }
            //: RetainProcess.share.func__jumpToWebRecharge(sufficient: false)
            RetainProcess.share.bod(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(userUsedStr.suffix(7)) + String(user_constructKey)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.signWhen(showMsg: msg)
            //: self.popCurrentViewController()
            self.timing()
        }
    }
}

// MARK: - FactorReadFuture【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension BindSource: FactorReadFuture {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func notLite() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        PanCurrentStatus.shared.banAcross(info: [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (noti_convertString.replacingOccurrences(of: "draw", with: "cc") + "ptCall"), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: notiSegmentCoordinatorKey.map{everyoneShake(level: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func arcComponent() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.signWhen(showMsg: (String(userCustomForwardMessage.prefix(4)) + "s are" + String(kSeaString.suffix(5)) + showManagerId.replacingOccurrences(of: "photo", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            PanCurrentStatus.shared.banAcross(info: [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (kVidUrl.replacingOccurrences(of: "editor", with: "a") + String(notiForeValue.suffix(5))), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: user_decideTitle.map{personalSort(transaction: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.signWhen(showMsg: (String(userCustomForwardMessage.prefix(4)) + "s are" + String(kSeaString.suffix(5)) + showManagerId.replacingOccurrences(of: "photo", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            PanCurrentStatus.shared.banAcross(info: [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (String(kJustPath)), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: notiSegmentCoordinatorKey.map{everyoneShake(level: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.signWhen(showMsg: (String(const_integrityTitle) + const_revenueKey.replacingOccurrences(of: "mirror", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            PanCurrentStatus.shared.banAcross(info: [String(bytes: app_onlyStr.map{themeNeed(me: $0)}, encoding: .utf8)!: (String(k_iconPath.suffix(8))), String(bytes: data_amUrl.map{visibleObject(exactly: $0)}, encoding: .utf8)!: [String(bytes: user_decideTitle.map{personalSort(transaction: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.duringOf()
        //: self.popCurrentViewController()
        self.timing()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func languageIn(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.texture(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func displayOf(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.errColumn(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func pastEqual() {
        //: TalkingSocketManager.shared.usageVC = self
        PanCurrentStatus.shared.usageVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = FormAsset.dragIn()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.alongLarge()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.againstTarget()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.underCancel(talkingTime)
        //: popCurrentViewController()
        timing()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func publisher(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.underCancel(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func output(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.remind(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension BindSource {
    /// 添加视图
    //: private func setupSubviews() {
    private func queryHalf() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func insideThree() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
