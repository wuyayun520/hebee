
//: Declare String Begin

/*: "msgType" :*/
fileprivate let user_signalName:String = "must upper delivery absolute boardmsgTy"
fileprivate let main_medalUrl:String = "plength"

/*: "tips" :*/
fileprivate let showSharedId:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let const_constraintUrl:String = "extquantity"

/*: "msgInfo" :*/
fileprivate let appContinuingValue:String = "structure interestmsgInfo"

/*: "user" :*/
fileprivate let const_hereSpaceKey:String = "USER"

/*: "uid" :*/
fileprivate let notiNormExtraString:String = "combinedid"

/*: "nickname" :*/
fileprivate let showMobileComputeFormat:[UInt8] = [0x41,0x46,0x4c,0x44,0x41,0x4e,0x42,0x4a]

private func streamPerson(creation num: UInt8) -> UInt8 {
    return num ^ 47
}

/*: "headPic" :*/
fileprivate let userOriginTitle:[UInt8] = [0xd4,0xd1,0xcd,0xd0,0xbc,0xd5,0xcf]

fileprivate func musicConstruct(symbol num: UInt8) -> UInt8 {
    let value = Int(num) + 148
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "age" :*/
fileprivate let noti_significantTitle:[UInt8] = [0xf2,0xf4,0xf6]

/*: "gift" :*/
fileprivate let user_pastUrl:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let showWedFormat:String = "void conversion requireisGro"
fileprivate let mainMomentPath:String = "explorep"

/*: "WantGift" :*/
fileprivate let user_circleMessage:[Character] = ["W","a","n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let show_buttString:String = "generatexgenerate"

/*: "img" :*/
fileprivate let data_pinFormat:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let user_resAtStr:String = "videformat"

/*: "audio" :*/
fileprivate let constSlowGreenUrl:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let k_coordinateMessage:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let app_builderStr:String = "CONTE"
fileprivate let constSubjectBlueBurnName:[Character] = ["n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let main_minimizePath:String = "method reward blockImgMsg"

/*: "imageUri" :*/
fileprivate let show_largePaintInflationName:String = "imageUras slow put instead injury"
fileprivate let appReplaceValue:String = "particle"

/*: "type" :*/
fileprivate let kSpanMsg:String = "tyvid"

/*: "pic" :*/
fileprivate let constChannelValidTitle:String = "mobilec"

/*: "isBurn" :*/
fileprivate let app_choiceExpectedKey:String = "isBurfield hold pull"
fileprivate let notiEmbraceCountensityUrl:[Character] = ["n"]

/*: "giftId" :*/
fileprivate let userExtraBuildValue:[Character] = ["g","i","f","t","I"]
fileprivate let constCustomerName:[Character] = ["d"]

/*: "params" :*/
fileprivate let main_roleCarrierMsg:String = "paramicon"

/*: "audioLength" :*/
fileprivate let constProSecurePath:[Character] = ["a","u","d","i","o","L","e","n"]
fileprivate let data_startQuickManStr:String = "musich"

/*: "len" :*/
fileprivate let notiAdjustLabMsg:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let app_colorAccelerateData:[Character] = ["a","u"]
fileprivate let show_assertUrl:String = "here delaydioSan"

/*: "senderId" :*/
fileprivate let noti_compositionEvolutionMsg:String = "running"
fileprivate let dataConUrl:[Character] = ["e","n","d","e","r","I","d"]

/*: "AudioMsg" :*/
fileprivate let dataReloadPermissionId:String = "scope except off where successfulAudi"

/*: "content" :*/
fileprivate let k_bounceSkinString:[Character] = ["c","o","n","t","e","n","t"]

/*: "TxtMsg" :*/
fileprivate let main_localKey:String = "threshold"
fileprivate let showWithoutStr:String = "xtMsgcount expression"

/*: "message" :*/
fileprivate let show_dangerousKey:String = "messsurege"

/*: "quoteMsgId" :*/
fileprivate let notiWedKey:String = "application"
fileprivate let showUnableMsg:String = "stat never invitation point transferuoteM"

/*: "quoteMsgInfo" :*/
fileprivate let user_editStr:String = "quoteMsocial license thin"
fileprivate let dataProblemFormat:[Character] = ["s","g","I","n","f","o"]

/*: "replayId" :*/
fileprivate let notiEasternString:String = "situation it shotreplayI"
fileprivate let k_heapTitle:String = "miss"

/*: "rYMsgType" :*/
fileprivate let mainMatchPath:String = "icon statusrYMsg"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let constFloorString:String = "GJ:Sborder gem sun"
fileprivate let user_hisStr:String = "toGrtemporary agent"
fileprivate let dataBecomeUrl:[Character] = ["T","x"]
fileprivate let showTillMsg:String = "tCustomam state"

/*: "changeDirection" :*/
fileprivate let mainMovieWayValue:String = "chchoosege"
fileprivate let notiStarValue:[Character] = ["t","i","o","n"]

/*: "system" :*/
fileprivate let app_listData:[Character] = ["s","y","s","t","e"]
fileprivate let appDecidePermanentFormat:[Character] = ["m"]

/*: "title" :*/
fileprivate let dataRemoteMessage:[Character] = ["t","i","t","l","e"]

/*: "icon" :*/
fileprivate let showSourceStr:String = "icobserver"

/*: "all" :*/
fileprivate let main_observeName:String = "ownll"

/*: "icon_chats_pcr" :*/
fileprivate let showEvolutionStr:[Character] = ["i","c","o","n","_","c","h","a","t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let k_birthdayTitle:String = "statpass"

/*: "msgIncome" :*/
fileprivate let noti_blockId:String = "mscloud"
fileprivate let noti_downId:String = "Incometrack need limited exactly"

/*: "msgId" :*/
fileprivate let user_heapId:[Character] = ["m","s","g","I","d"]

/*: "NextIndex" :*/
fileprivate let mainTemporaryUrl:String = "production processTMsgTr"
fileprivate let kBringLogName:String = "better"
fileprivate let main_analyzeCountroBlockName:String = "symbol yesterday lackStatus"

/*: "TMsgTranslatedContent" :*/
fileprivate let const_multiAmongString:String = "two outer interaction audienceTMsgT"
fileprivate let userDismissExercisePath:String = "brushansla"
fileprivate let user_exceptionPath:String = "teinvitation"

/*: "formatType" :*/
fileprivate let constPlatValue:[Character] = ["f","o","r","m","a","t","T"]
fileprivate let notiBagMessage:String = "YPE"

/*: "plain" :*/
fileprivate let k_variableDailyStr:String = "plairoman"

/*: "imageData" :*/
fileprivate let app_ownPlusFormat:[Character] = ["i","m","a","g","e","D"]
fileprivate let constTotalerestUrl:[Character] = ["a","t","a"]

/*: "VideoMsg" :*/
fileprivate let app_exitTitle:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "match" :*/
fileprivate let user_someoneKey:String = "matspendh"

/*: "userIntro" :*/
fileprivate let const_concreteId:String = "heavy state attributeuserIntro"

/*: "targetId" :*/
fileprivate let notiOddReName:String = "targetIdlight cur clock publisher"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let noti_maintainId:String = "teamteamteamteam"
fileprivate let mainFlagCoverMsg:String = "-MM-dtop this weak"
fileprivate let user_latTitle:String = "filter fee sea portraitm:ss"

/*: "sendTime" :*/
fileprivate let dataTakePath:[Character] = ["s","e","n","d","T","i","m"]
fileprivate let const_protectionKey:String = "vid"

/*: "isRead" :*/
fileprivate let notiEmptyThenValue:[Character] = ["i"]
fileprivate let noti_monthActionString:String = "sReadtypical picture"

/*: "audioData" :*/
fileprivate let const_sumSingleStr:[Character] = ["a","u","d","i","o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let const_cutViewFailUrl:[Character] = ["a","u","d","i","o","U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CaptureMend.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum UndertakeOver: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - CaptureMend

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class CaptureMend: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func arc() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> NextAgainst? {
    public class func beforeExtend(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> NextAgainst? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = designEnable(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: SampleDepth.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            SampleDepth.shared.scaleAlive(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> NextAgainst? {
    public class func designEnable(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> NextAgainst? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] = (String(showSharedId))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(const_constraintUrl.replacingOccurrences(of: "quantity", with: "ra")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = orange(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = RevealAway(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = PadModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? IndependentModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(appContinuingValue.suffix(7)))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(const_hereSpaceKey.lowercased())] = [
            //: "uid": MountCustom.share.loginUserMode.userID,
            (notiNormExtraString.replacingOccurrences(of: "combined", with: "u")): MountCustom.share.loginUserMode.userID,
            //: "nickname": MountCustom.share.loginUserMode.nickname,
            String(bytes: showMobileComputeFormat.map{streamPerson(creation: $0)}, encoding: .utf8)!: MountCustom.share.loginUserMode.nickname,
            //: "headPic": MountCustom.share.loginUserMode.headPic,
            String(bytes: userOriginTitle.map{musicConstruct(symbol: $0)}, encoding: .utf8)!: MountCustom.share.loginUserMode.headPic,
            //: "age": MountCustom.share.loginUserMode.age,
            String(bytes: noti_significantTitle.map{$0^147}, encoding: .utf8)!: MountCustom.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(const_constraintUrl.replacingOccurrences(of: "quantity", with: "ra")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = orange(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (String(user_pastUrl)) {
            //: var data: NextAgainst
            var data: NextAgainst
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(showWedFormat.suffix(5)) + mainMomentPath.replacingOccurrences(of: "explore", with: "u"))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = LevelAssociate(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = VisitKind(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = VisitKind(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? PadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (String(user_circleMessage)) {
            //: let data = EagerShould(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = EagerShould(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? PadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (show_buttString.replacingOccurrences(of: "generate", with: "t")) {
            //: var data: NextAgainst
            var data: NextAgainst
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(showWedFormat.suffix(5)) + mainMomentPath.replacingOccurrences(of: "explore", with: "u"))] as? String == "1" { // 群聊
                //: data = OvalCharacteristicValueGrainSumStepTechnique(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = OvalCharacteristicValueGrainSumStepTechnique(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = PropertyReadAngle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = PropertyReadAngle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? PadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (String(data_pinFormat)) {
            //: let data = BehaviorIsolate(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = BehaviorIsolate(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? PadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (user_resAtStr.replacingOccurrences(of: "format", with: "o")) {
            //: let data = LitePage(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = LitePage(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? PadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (String(constSlowGreenUrl)) {
            //: let data = EnhanceTier(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = EnhanceTier(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? PadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(user_pastUrl))] != nil && (msgInfo[(String(k_coordinateMessage))] as? Int) == 4 {
            //: let data = PropertyReadAngle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = PropertyReadAngle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? PadModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: NextAgainst) -> NextAgainst? {
    public class func displayReplace(withData cellData: NextAgainst) -> NextAgainst? {
        // 图片
        //: if let reCellData = cellData as? BehaviorIsolate {
        if let reCellData = cellData as? BehaviorIsolate {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(StopBrush.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(StopBrush.pureVisitor())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(app_builderStr.lowercased() + String(constSubjectBlueBurnName))] = (String(main_minimizePath.suffix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] = (String(data_pinFormat))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(show_largePaintInflationName.prefix(7)) + appReplaceValue.replacingOccurrences(of: "particle", with: "i"))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(appContinuingValue.suffix(7)))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(kSpanMsg.replacingOccurrences(of: "vid", with: "pe"))] = (String(main_minimizePath.suffix(6)))
            //: params["pic"] = filePath
            params[(constChannelValidTitle.replacingOccurrences(of: "mobile", with: "pi"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(app_choiceExpectedKey.prefix(5)) + String(notiEmbraceCountensityUrl))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(app_choiceExpectedKey.prefix(5)) + String(notiEmbraceCountensityUrl))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(userExtraBuildValue) + String(constCustomerName))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(userExtraBuildValue) + String(constCustomerName))] = "0"
            }

            //: dic["params"] = params
            dic[(main_roleCarrierMsg.replacingOccurrences(of: "icon", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = designEnable(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? LitePage {
        if let reCellData = cellData as? LitePage {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? EnhanceTier {
        if let reCellData = cellData as? EnhanceTier {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = StageImplement.mergeMight(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(constProSecurePath) + data_startQuickManStr.replacingOccurrences(of: "music", with: "gt"))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(notiAdjustLabMsg))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(app_colorAccelerateData) + String(show_assertUrl.suffix(6)) + "dbox")] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(app_colorAccelerateData) + String(show_assertUrl.suffix(6)) + "dbox")] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] = (String(constSlowGreenUrl))
            //: msgInfo["senderId"] = MountCustom.share.loginUserMode.userID
            msgInfo[(noti_compositionEvolutionMsg.replacingOccurrences(of: "running", with: "s") + String(dataConUrl))] = MountCustom.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(appContinuingValue.suffix(7)))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(kSpanMsg.replacingOccurrences(of: "vid", with: "pe"))] = (String(dataReloadPermissionId.suffix(4)) + "oMsg")
            //: dic["params"] = params
            dic[(main_roleCarrierMsg.replacingOccurrences(of: "icon", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = designEnable(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? PropertyReadAngle {
        if let reCellData = cellData as? PropertyReadAngle {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(k_bounceSkinString))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] = (show_buttString.replacingOccurrences(of: "generate", with: "t"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(appContinuingValue.suffix(7)))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(kSpanMsg.replacingOccurrences(of: "vid", with: "pe"))] = (main_localKey.replacingOccurrences(of: "threshold", with: "T") + String(showWithoutStr.prefix(5)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(show_dangerousKey.replacingOccurrences(of: "sure", with: "a"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.gem() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(notiWedKey.replacingOccurrences(of: "application", with: "q") + String(showUnableMsg.suffix(5)) + "sgId")] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(user_editStr.prefix(6)) + String(dataProblemFormat))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: if let replayId = reCellData.msgModel.params["replayId"] { // 问题回答消息重发
            if let replayId = reCellData.msgModel.params[(String(notiEasternString.suffix(7)) + k_heapTitle.replacingOccurrences(of: "miss", with: "d"))] { // 问题回答消息重发
                //: params["replayId"] = replayId
                params[(String(notiEasternString.suffix(7)) + k_heapTitle.replacingOccurrences(of: "miss", with: "d"))] = replayId
            }

            //: dic["params"] = params
            dic[(main_roleCarrierMsg.replacingOccurrences(of: "icon", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = designEnable(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> NextAgainst? {
    public class func enableRender(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> NextAgainst? {
        //: var cellData: NextAgainst?
        var cellData: NextAgainst?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(const_constraintUrl.replacingOccurrences(of: "quantity", with: "ra"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(appContinuingValue.suffix(7)))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(const_hereSpaceKey.lowercased())] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? PadModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == MountCustom.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == MountCustom.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == MountCustom.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == MountCustom.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(mainMatchPath.suffix(5)) + "Type")] as? String == (String(constFloorString.prefix(4)) + "ess:Au" + String(user_hisStr.prefix(4)) + "eet:" + String(dataBecomeUrl) + String(showTillMsg.prefix(7))) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(mainMovieWayValue.replacingOccurrences(of: "choose", with: "an") + "Direc" + String(notiStarValue))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = draft(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (String(app_listData) + String(appDecidePermanentFormat)) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = PadModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? IndependentModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = RevealAway(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(k_bounceSkinString))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = BehaviorIsolate(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BehaviorIsolate(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = LitePage(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = LitePage(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = LevelAssociate(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = VisitKind(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = VisitKind(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = purchaseAdmin(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(const_constraintUrl.replacingOccurrences(of: "quantity", with: "ra"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(appContinuingValue.suffix(7)))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? SignRegulateModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = EagerShould(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = EagerShould(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = EnhanceTier(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = EnhanceTier(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? EnhanceTier {
            if let audioCellData = cellData as? EnhanceTier {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = deviceMechanism(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = SwitchtoCharacter.limit(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = SwitchtoCharacter.background(tempChart(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BounceShadow(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(String(dataRemoteMessage))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(String(k_bounceSkinString))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(data_pinFormat))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = OwnerTransformStrategy(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = OvalCharacteristicValueGrainSumStepTechnique(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = OvalCharacteristicValueGrainSumStepTechnique(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = PropertyReadAngle(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PropertyReadAngle(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? FeedbackModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(showSourceStr.replacingOccurrences(of: "observer", with: "on"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: MountCustom.share.loginUserMode.headPic ?? ""
                        MountCustom.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = SignRegulateModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(k_bounceSkinString))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(const_constraintUrl.replacingOccurrences(of: "quantity", with: "ra"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = PushModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (main_observeName.replacingOccurrences(of: "own", with: "a"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(showEvolutionStr))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.exerciseDoing(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(k_birthdayTitle.replacingOccurrences(of: "pass", with: "us"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(noti_blockId.replacingOccurrences(of: "cloud", with: "g") + String(noti_downId.prefix(6)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(k_coordinateMessage))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(user_heapId))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(mainTemporaryUrl.suffix(6)) + "anslat" + kBringLogName.replacingOccurrences(of: "better", with: "e") + String(main_analyzeCountroBlockName.suffix(6)))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = NextIndex(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(const_multiAmongString.suffix(5)) + userDismissExercisePath.replacingOccurrences(of: "brush", with: "r") + "tedCon" + user_exceptionPath.replacingOccurrences(of: "invitation", with: "nt"))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = EmbraceSceneModel.behavior()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = EmbraceSceneModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = ResolveGreenModel.spell()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = ResolveGreenModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = ObserverModel.sort()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func draft(_ extraDic: [String: Any]) -> UndertakeOver {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(const_constraintUrl.replacingOccurrences(of: "quantity", with: "ra"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(appContinuingValue.suffix(7)))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? PadModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(constPlatValue) + notiBagMessage.lowercased())] as? String == (k_variableDailyStr.replacingOccurrences(of: "roman", with: "n")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(show_largePaintInflationName.prefix(7)) + appReplaceValue.replacingOccurrences(of: "particle", with: "i"))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(app_ownPlusFormat) + String(constTotalerestUrl))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(app_exitTitle)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = spacePartner(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(user_someoneKey.replacingOccurrences(of: "spend", with: "c"))] as? Int) == 1 || (msgInfo?[(user_someoneKey.replacingOccurrences(of: "spend", with: "c"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (String(showSharedId)) || (dic?[(String(k_coordinateMessage))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(constPlatValue) + notiBagMessage.lowercased())] as? String == "mf") && ((dic?[(String(k_coordinateMessage))] as? Int) == 15 || (dic?[(String(k_coordinateMessage))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (String(const_concreteId.suffix(9))) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String == (String(user_circleMessage)) || (msgInfo?[(String(k_coordinateMessage))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func tempChart(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.exerciseDoing(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(const_constraintUrl.replacingOccurrences(of: "quantity", with: "ra"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(appContinuingValue.suffix(7)))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(const_hereSpaceKey.lowercased())] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(noti_compositionEvolutionMsg.replacingOccurrences(of: "running", with: "s") + String(dataConUrl))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == MountCustom.share.loginUserMode.userID {
        if senderId == MountCustom.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(noti_compositionEvolutionMsg.replacingOccurrences(of: "running", with: "s") + String(dataConUrl))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(notiOddReName.prefix(8)))] = message.userID ?? (user?[(String(notiOddReName.prefix(8)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(noti_compositionEvolutionMsg.replacingOccurrences(of: "running", with: "s") + String(dataConUrl))] = "\(msgInfo?[(notiNormExtraString.replacingOccurrences(of: "combined", with: "u"))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(notiOddReName.prefix(8)))] = "\(msgInfo?[(notiNormExtraString.replacingOccurrences(of: "combined", with: "u"))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(user_heapId))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(user_heapId))] = "\(msgInfo?[(String(user_heapId))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.exerciseDoing(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(user_heapId))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(user_heapId))] = "\(customDic[(String(user_heapId))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(user_heapId))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(app_colorAccelerateData) + String(show_assertUrl.suffix(6)) + "dbox")) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(app_colorAccelerateData) + String(show_assertUrl.suffix(6)) + "dbox")] = "\(msgInfo?[(String(app_colorAccelerateData) + String(show_assertUrl.suffix(6)) + "dbox")] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(constProSecurePath) + data_startQuickManStr.replacingOccurrences(of: "music", with: "gt"))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(constProSecurePath) + data_startQuickManStr.replacingOccurrences(of: "music", with: "gt"))] = "\(msgInfo?[(String(constProSecurePath) + data_startQuickManStr.replacingOccurrences(of: "music", with: "gt"))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (noti_maintainId.replacingOccurrences(of: "team", with: "y") + String(mainFlagCoverMsg.prefix(5)) + "d HH:m" + String(user_latTitle.suffix(4)))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(dataTakePath) + const_protectionKey.replacingOccurrences(of: "vid", with: "e"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == MountCustom.share.loginUserMode.userID ? "1" : "0"
        info?[(String(notiEmptyThenValue) + String(noti_monthActionString.prefix(5)))] = senderId == MountCustom.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func deviceMechanism(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = tempChart(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(user_heapId))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func purchaseAdmin(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func orange(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func spacePartner(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(user_signalName.suffix(5)) + main_medalUrl.replacingOccurrences(of: "length", with: "e"))] as? String, msgType == (String(constSlowGreenUrl)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(app_builderStr.lowercased() + String(constSubjectBlueBurnName))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(dataReloadPermissionId.suffix(4)) + "oMsg")
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(const_sumSingleStr))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(const_cutViewFailUrl))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
