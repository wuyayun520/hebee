
//: Declare String Begin

/*: "cmd" :*/
fileprivate let showCouldParaPath:[UInt8] = [0x8d,0x83,0x8a]

private func announcementGreen(additional num: UInt8) -> UInt8 {
    return num ^ 238
}

/*: "cancelCall" :*/
fileprivate let userHungMsg:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "data" :*/
fileprivate let kRegularDealValue:[UInt8] = [0xfa,0xf7,0xa,0xf7]

fileprivate func refDiscover(decide num: UInt8) -> UInt8 {
    let value = Int(num) + 106
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "logId" :*/
fileprivate let data_uniqueId:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let notiBoxPath:String = "#method28"
fileprivate let constDynamicsUrl:String = "bold"

/*: "Tap to start video chatting" :*/
fileprivate let noti_againFoundMsg:[Character] = ["T","a","p"," ","t","o"," ","s","t"]
fileprivate let show_injuryFormat:[Character] = ["a","r","t"," ","v","i","d","e","o"," ","c","h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let app_remMicValue:String = "protocol license anonymous ificon_c"
fileprivate let showSocialValue:String = "aliver"

/*: "icon_views_number" :*/
fileprivate let k_spineValue:String = "icon_vmodel formal"
fileprivate let mainNetworkFormat:String = "can broadcast way_number"

/*: "%@ chatting" :*/
fileprivate let mainHeroTowardPath:String = "%@ cincome across"
fileprivate let main_rejectString:[Character] = ["h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let app_effUnableFormat:String = "plainon"
fileprivate let constMediumColorStr:String = "gmulti"

/*: "icon_rank_coin" :*/
fileprivate let const_portUrl:[Character] = ["i","c","o","n","_","r","a","n","k","_","c","o","i"]
fileprivate let main_cityPath:[Character] = ["n"]

/*: "vip_btn_top" :*/
fileprivate let dataMineName:[Character] = ["v","i","p","_","b"]
fileprivate let k_additionTitle:String = "lose hardtn_top"

/*: "btn_nearby_settings_nor" :*/
fileprivate let k_womanTitle:String = "building public here designbtn_ne"
fileprivate let main_errorUrl:String = "commentting"

/*: "%@ Coins/min" :*/
fileprivate let show_helpMessage:[Character] = ["%","@"," ","C","o","i","n","s"]
fileprivate let dataLineValue:String = "/minflexible tween"

/*: "img_match_connecting" :*/
fileprivate let appConsumerAssetStr:[Character] = ["i","m","g","_","m","a","t"]
fileprivate let noti_structureId:String = "associate medal question marker robotch_con"
fileprivate let dataBounceStr:String = "necplaceng"

/*: "#9F80FF" :*/
fileprivate let notiPublishWayMsg:[Character] = ["#","9","F","8","0","F"]
fileprivate let appEquipmentUrl:String = "resign"

/*: "get json error" :*/
fileprivate let const_contactValue:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let user_toPlusData:String = "intro mirror pasmatchId"

/*: "errno" :*/
fileprivate let show_scientificKey:[UInt8] = [0x3,0x14,0x14,0x8,0x9]

private func agoPad(slow num: UInt8) -> UInt8 {
    return num ^ 102
}

/*: "VIPFee" :*/
fileprivate let notiIncomeId:String = "info"
fileprivate let show_distinctionDisplayValue:[Character] = ["I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let constThresholdKey:String = "clip thisMat"
fileprivate let mainAlterUrl:[Character] = ["c","h","i","n","g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let dataReduceTradeAttachValue:[Character] = ["u","s","e","r","T","o"]
fileprivate let app_multiStr:String = "equipmentch"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let mainBasicGirlString:String = "guard literalmf/fre"
fileprivate let dataFemalePath:String = "break license plate/getV"
fileprivate let mainMutualMsg:String = "multi cover recordInfo"

/*: "fromType" :*/
fileprivate let noti_generalTransferId:[UInt8] = [0xe3,0xef,0xec,0xea,0xd1,0xf6,0xed,0xe2]

fileprivate func nativeRegion(hung num: UInt8) -> UInt8 {
    let value = Int(num) - 125
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "You've been barred from receiving calls" :*/
fileprivate let userPressMsg:[UInt8] = [0x26,0x3c,0x42,0xf4,0x43,0x32,0xed,0x2f,0x32,0x32,0x3b,0xed,0x2f,0x2e,0x3f,0x3f,0x32,0x31,0xed,0x33,0x3f,0x3c,0x3a,0xed,0x3f,0x32,0x30,0x32,0x36,0x43,0x36,0x3b,0x34,0xed,0x30,0x2e,0x39,0x39,0x40]

fileprivate func objectVery(priority num: UInt8) -> UInt8 {
    let value = Int(num) - 205
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "source" :*/
fileprivate let app_captureValue:[UInt8] = [0x68,0x64,0x6a,0x67,0x58,0x5a]

fileprivate func hisActionSomething(shape num: UInt8) -> UInt8 {
    let value = Int(num) - 245
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let k_tipTitle:[UInt8] = [0xc,0x1,0x8,0x1d]

private func tarOdd(root num: UInt8) -> UInt8 {
    return num ^ 120
}

/*: "uid" :*/
fileprivate let userPackExactlyTitle:[UInt8] = [0x11,0xd,0x0]

private func unableWhoSeekCertain(that num: UInt8) -> UInt8 {
    return num ^ 100
}

/*: "fromFreeCall" :*/
fileprivate let kCalledAgainPath:String = "fbeanom"
fileprivate let showSlowOverKey:[Character] = ["C","a","l","l"]

/*: "requestCall" :*/
fileprivate let noti_whichKey:String = "racceleratequaccelerates"

/*: "onVideoMatch" :*/
fileprivate let user_tillMessage:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let constGlobalPointUsedKey:[UInt8] = [0xc8,0xae,0xe2,0xe1,0xdc,0xda,0xce,0xe1,0xd6,0xd0,0x8d,0xe3,0xd6,0xd1,0xd2,0xdc,0xca,0x8d,0xbf,0xd2,0xd0,0xd2,0xd6,0xe3,0xd2,0x8d,0xe3,0xd6,0xd1,0xd2,0xdc,0x8d,0xd0,0xce,0xd9,0xd9,0x8d,0xd6,0xdb,0xd0,0xdc,0xda,0xd6,0xdb,0xd4]

fileprivate func cornerTap(crop num: UInt8) -> UInt8 {
    let value = Int(num) - 109
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isRandomVCPush" :*/
fileprivate let constAgainstVolumeId:[Character] = ["i","s","R","a","n","d","o","m","V","C","P","u"]
fileprivate let data_explainId:[Character] = ["s","h"]

/*: "refuseCall" :*/
fileprivate let const_factoryId:String = "forwardefus"
fileprivate let show_mustMsg:[Character] = ["e","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let constBrownFoundSpecStr:[Character] = ["o","n","C","a","n","c","e","l","C"]
fileprivate let noti_situationLowerName:String = "apoppop"

/*: "retryAfter" :*/
fileprivate let mainGreatFormat:String = "specialetspecial"
fileprivate let k_makeFormat:String = "epan"

/*: "onRequestCall" :*/
fileprivate let constDialogBeId:[Character] = ["o","n","R","e","q","u","e"]
fileprivate let dataEhName:[Character] = ["s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let dataGoId:String = "hundred hung message blue failfailT"
fileprivate let userMountHeroStr:String = "wee"

/*: "callWaitingTime" :*/
fileprivate let mainYaReceiverData:String = "cpopl"
fileprivate let userDynamicsMessage:String = "ngTimebit minute mechanism game component"

/*: "toUid" :*/
fileprivate let main_placeData:String = "center outtoUid"

/*: "Free 1 min" :*/
fileprivate let userAnotherFormat:[Character] = ["F","r","e","e"," ","1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let main_taskValue:String = "least protocol mark functional accuracy%@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CrucialOrange.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class CrucialOrange: PetViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.isResult()
            }
        }
        //: self.resetCamera()
        self.behaviorDestination()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        modeSkirt()
        //: setupSubViewsConstraint()
        behindStrengthProcessor()
        //: bindInteraction()
        tarServer()
        //: videoManager.startPreview(isMatch: true)
        videoManager.resolve(isMatch: true)
        //: setPlayer(isRandon: false)
        memoryDraw(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            manualBy()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            PanCurrentStatus.shared.banAcross(info: [String(bytes: showCouldParaPath.map{announcementGreen(additional: $0)}, encoding: .utf8)!: (String(userHungMsg)), String(bytes: kRegularDealValue.map{refDiscover(decide: $0)}, encoding: .utf8)!: [(String(data_uniqueId)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        nearbyEvaluation()
        //: if TalkingSocketManager.shared.usage == false {
        if PanCurrentStatus.shared.usage == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            PanCurrentStatus.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            PanCurrentStatus.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            PanCurrentStatus.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            PanCurrentStatus.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ProvideOvalBehavior = {
        //: let m = TalkingVideoChatManager()
        let m = ProvideOvalBehavior()
        //: let info = TalkingVideoChatModel()
        let info = FactorModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .labelMomentum()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(manualBy), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: mainCellMsg + 1, height: kPressEasePublishString + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (notiBoxPath.replacingOccurrences(of: "method", with: "32") + constDynamicsUrl.replacingOccurrences(of: "bold", with: "70")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(noti_againFoundMsg) + String(show_injuryFormat)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(app_remMicValue.suffix(6)) + "lose_n" + showSocialValue.replacingOccurrences(of: "alive", with: "o"))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(strategy), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: MethodButton = {
        //: let btn = TalkingButton()
        let btn = MethodButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.evolutionary(name: (String(k_spineValue.prefix(6)) + "iews" + String(mainNetworkFormat.suffix(7)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.evolutionary(name: (String(k_spineValue.prefix(6)) + "iews" + String(mainNetworkFormat.suffix(7)))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.stepTransition(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(mainHeroTowardPath.prefix(4)) + String(main_rejectString)).fieldOf(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.evolutionary(name: (app_effUnableFormat.replacingOccurrences(of: "plain", with: "ic") + "_loun" + constMediumColorStr.replacingOccurrences(of: "multi", with: "e")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.evolutionary(name: (String(const_portUrl) + String(main_cityPath)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.stepTransition(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.evolutionary(name: (String(dataMineName) + String(k_additionTitle.suffix(6))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: MethodButton = {
        //: let btn = TalkingButton()
        let btn = MethodButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(spreadBurn), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.evolutionary(name: (String(k_womanTitle.suffix(6)) + "arby_s" + main_errorUrl.replacingOccurrences(of: "comment", with: "et") + "s_nor")), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.evolutionary(name: (String(const_portUrl) + String(main_cityPath)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .fireReplace()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(show_helpMessage) + String(dataLineValue.prefix(4))).fieldOf(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.stepTransition(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: TailView = //: return TailView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()

    //: private lazy var connectingImg: UIImageView = {
    private lazy var connectingImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "img_match_connecting")
        imgV.image = UIImage.evolutionary(name: (String(appConsumerAssetStr) + String(noti_structureId.suffix(6)) + dataBounceStr.replacingOccurrences(of: "place", with: "ti")))
        //: let view = UIActivityIndicatorView.init()
        let view = UIActivityIndicatorView()
        //: view.color = UIColor(hex: "#9F80FF")
        view.color = UIColor(hex: (String(notiPublishWayMsg) + appEquipmentUrl.replacingOccurrences(of: "resign", with: "F")))
        //: imgV.addSubview(view)
        imgV.addSubview(view)
        //: view.snp.makeConstraints { make in
        view.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(20)
            make.size.equalTo(20)
        }
        //: view.startAnimating()
        view.startAnimating()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: return imgV
        return imgV
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension CrucialOrange {
    //: private func setPlayer(isRandon: Bool) {
    private func memoryDraw(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = PhoneAccelerate.default.memberStructure(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(const_contactValue)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func sortBecome(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(user_toPlusData.suffix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: showCouldParaPath.map{announcementGreen(additional: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: show_scientificKey.map{agoPad(slow: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        AccelerateDelay.instructionSubstantial(params: params) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.nearDetailData()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(user_toPlusData.suffix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func regulate() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        AccelerateDelay.contrastSecond { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(notiIncomeId.replacingOccurrences(of: "info", with: "V") + String(show_distinctionDisplayValue))].intValue
            //: self.refreshVideoCallPrice()
            self.isResult()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension CrucialOrange {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func spreadBurn() {
        //: RetainProcess.share.func__pushToSubscribeAlert()
        RetainProcess.share.balanceGreat()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func strategy() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        projection()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        notiConsumptionMsg.curveLocate(eventID: showSpecifyUrl)

        //: if MountCustom.share.appConfigMode.displayMatchV4PopUp, MountCustom.share.loginUserMode.freeCallTimes > 0,
        if MountCustom.share.appConfigMode.displayMatchV4PopUp, MountCustom.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.processor()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        alongLarge()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func manualBy() {
        //: guard TalkingSocketManager.shared.usage == false else {
        guard PanCurrentStatus.shared.usage == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.signWhen(showMsg: noti_makePermanentValue)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        invisibleFrame()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(constThresholdKey.suffix(3)) + String(mainAlterUrl)).localized
        //: setPlayer(isRandon: true)
        memoryDraw(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if MountCustom.share.loginUserMode.randomVideoType == 2 && MountCustom.share.loginUserMode.robotVideoNum > 0 {
        if MountCustom.share.loginUserMode.randomVideoType == 2, MountCustom.share.loginUserMode.robotVideoNum > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            notiConsumptionMsg.curveLocate(eventID: mainBrownString)
            //: req_getVideoInfo()
            workBlindMatch()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            notiConsumptionMsg.curveLocate(eventID: app_priorTopicTitle)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            PanCurrentStatus.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            PanCurrentStatus.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            PanCurrentStatus.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            PanCurrentStatus.shared.errorDelegate = self
            //: restart_randomVideo()
            readWithFind()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func readWithFind(cmd: String = (String(dataReduceTradeAttachValue) + app_multiStr.replacingOccurrences(of: "equipment", with: "u")), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        nearbyEvaluation()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        sortBecome(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func nearDetailData() {
        // 停止定时器
        //: destroyWaitingTimer()
        nearbyEvaluation()
        //: showCloseBtn()
        projection()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(noti_againFoundMsg) + String(show_injuryFormat)).localized
        //: setPlayer(isRandon: false)
        memoryDraw(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.usage == false {
        if PanCurrentStatus.shared.usage == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            PanCurrentStatus.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            PanCurrentStatus.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            PanCurrentStatus.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            PanCurrentStatus.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        isResult()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func nearbyEvaluation() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func workBlindMatch() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExerciseModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(mainBasicGirlString.suffix(6)) + "eCall" + String(dataFemalePath.suffix(5)) + "ideo" + String(mainMutualMsg.suffix(4)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: noti_generalTransferId.map{nativeRegion(hung: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        k_runPath.enhanceLikeScene(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.nearDetailData()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: MountCustom.share.loginUserMode.randomVideoType = 1
                    MountCustom.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.manualBy()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.signWhen(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: self.connectingImg.isHidden = false
            self.connectingImg.isHidden = false
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = SegmentOf.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = FeatureShared()
                //: player.setMute(bEnable: true)
                player.frame(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.callVideoUrl, view: UIView.init())
                player.property(url: model.callVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                DelayImplementation.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                ChainForce.behindCustomerRow { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.replaceMonster()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func replaceMonster() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        notiConsumptionMsg.curveLocate(eventID: show_fractionRapidMsg)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard DelayImplementation.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.signWhen(showMsg: String(bytes: userPressMsg.map{objectVery(priority: $0)}, encoding: .utf8)!.localized)
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            DelayImplementation.shared.buildTotal(type: 1) { succeed, result, errorModel in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.nearDetailData()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = ChartTypicalProject()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.alongLarge()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.nearDetailData()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func invisibleFrame() {
        //: if MountCustom.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if MountCustom.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(MountCustom.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(MountCustom.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func projection() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func behaviorDestination() {
        //: guard TalkingSocketManager.shared.usage == false else {
        guard PanCurrentStatus.shared.usage == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.resolve()
        }
    }

    /// 匹配通话结束时
    //: @objc private func closeTool() {
    @objc private func secondDisappear() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
            //: self.start_randomVideo()
            self.manualBy()
        }
    }
}

// MARK: - MarkBetween

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension CrucialOrange: MarkBetween {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func occurFilter(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = TrackLite.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(user_toPlusData.suffix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: app_captureValue.map{hisActionSomething(shape: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: k_tipTitle.map{tarOdd(root: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: userPackExactlyTitle.map{unableWhoSeekCertain(that: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = NaturalEdit.randow
        //: if MountCustom.share.loginUserMode.freeCallTimes > 0 && MountCustom.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if MountCustom.share.loginUserMode.freeCallTimes > 0 && MountCustom.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (kCalledAgainPath.replacingOccurrences(of: "bean", with: "r") + "Free" + String(showSlowOverKey)))
            //: TalkingSocketManager.shared.isFreeCall = true
            PanCurrentStatus.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: showCouldParaPath.map{announcementGreen(additional: $0)}, encoding: .utf8)!: (noti_whichKey.replacingOccurrences(of: "accelerate", with: "e") + "tCall"), String(bytes: kRegularDealValue.map{refDiscover(decide: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        PanCurrentStatus.shared.banAcross(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            PanCurrentStatus.shared.banAcross(info: [String(bytes: showCouldParaPath.map{announcementGreen(additional: $0)}, encoding: .utf8)!: (String(userHungMsg)), String(bytes: kRegularDealValue.map{refDiscover(decide: $0)}, encoding: .utf8)!: [(String(data_uniqueId)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.readWithFind(cmd: (String(user_tillMessage)))
            //: }, repeats: false)
        }, repeats: false)
        //: connectingImg.isHidden = false
        connectingImg.isHidden = false
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func eachEager(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(data_uniqueId))].intValue
    }
}

// MARK: - FromReduce【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension CrucialOrange: FromReduce {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func isMissReload(data: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func more(data: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func dealRate(msg: String, data: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func someTop(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            AssociateMaterial.spaceRegulate(body: String(bytes: constGlobalPointUsedKey.map{cornerTap(crop: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = TrackLite.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            petConnect(toUid: String(uid))
        }
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: stop_randomVideo()
        nearDetailData()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.threeMiss()
        //: var newData = data
        var newData = data
        //: newData["isRandomVCPush"] = true
        newData[(String(constAgainstVolumeId) + String(data_explainId))] = true
        //: MountCustom.share.start1v1TalkCall(info: newData, situation: .Calling)
        MountCustom.share.alter(info: newData, situation: .Calling)
    }
}

// MARK: - SymbolConnect【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension CrucialOrange: SymbolConnect {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func count(data: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        readWithFind(cmd: (const_factoryId.replacingOccurrences(of: "forward", with: "r") + String(show_mustMsg)))
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func no(data: [String: Any]) {}
}

// MARK: - KitWrap

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension CrucialOrange: KitWrap {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func documentSingle(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(constBrownFoundSpecStr) + noti_situationLowerName.replacingOccurrences(of: "pop", with: "l")) { // 取消通话异常
            //: stop_randomVideo()
            nearDetailData()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(user_tillMessage)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            nearDetailData()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(user_toPlusData.suffix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.signWhen(showMsg: msg)
            //: uploadFailTime(type: 2)
            beforeSame(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(user_tillMessage)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(mainGreatFormat.replacingOccurrences(of: "special", with: "r") + "yAft" + k_makeFormat.replacingOccurrences(of: "pan", with: "r"))].intValue), block: { [weak self] timer in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(user_toPlusData.suffix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.readWithFind(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(constDialogBeId) + String(dataEhName)), errorNo == ButtonMultiple.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            nearDetailData()
            //: guard MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MountCustom.share.appStatus == SensorRow.normal.rawValue else { return }
            //: RetainProcess.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            RetainProcess.share.bod(clickEvent: app_gemFormat, sufficient: false)
            //: uploadFailTime(type: 1)
            beforeSame(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        readWithFind(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension CrucialOrange {
    //: func uploadFailTime(type: Int) {
    func beforeSame(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(dataGoId.suffix(5)) + userMountHeroStr.replacingOccurrences(of: "we", with: "yp"))] = type
        //: parm["callWaitingTime"] = time
        parm[(mainYaReceiverData.replacingOccurrences(of: "pop", with: "al") + "Waiti" + String(userDynamicsMessage.prefix(6)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        notiConsumptionMsg.paintDown(eventID: showFailureStructureStr, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func petConnect(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(mainYaReceiverData.replacingOccurrences(of: "pop", with: "al") + "Waiti" + String(userDynamicsMessage.prefix(6)))] = time
        //: parm["toUid"] = toUid
        parm[(String(main_placeData.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        notiConsumptionMsg.paintDown(eventID: show_okString, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension CrucialOrange {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func isResult() {
        //: guard MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard MountCustom.share.appStatus == SensorRow.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !MountCustom.share.loginUserMode.loungePlus
        vipIcon.isHidden = !MountCustom.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = NaturalEdit.randow
        //: if MountCustom.share.loginUserMode.freeCallTimes > 0 && MountCustom.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if MountCustom.share.loginUserMode.freeCallTimes > 0, MountCustom.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if MountCustom.share.appUserConfigMode.callBtnStyle == 1 {
            if MountCustom.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(userAnotherFormat)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = MountCustom.share.loginUserMode.loungePlus ? vipFee:fee
            let price = MountCustom.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(main_taskValue.suffix(6))).fieldOf(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(main_taskValue.suffix(6))).fieldOf(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.monsterColumn(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = MountCustom.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = MountCustom.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = MountCustom.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = MountCustom.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !MountCustom.share.loginUserMode.loungePlus
            priceStackView.isHidden = !MountCustom.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func modeSkirt() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
        //: view.addSubview(connectingImg)
        view.addSubview(connectingImg)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func behindStrengthProcessor() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(data_visitorPath + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }

        //: connectingImg.snp.makeConstraints { make in
        connectingImg.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
            make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func tarServer() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(isResult),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: showSearchedMsg,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(regulate),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: constAssociatePath,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeTool), name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(secondDisappear), name: kImplementationMultiMsg, object: nil)
    }
}
