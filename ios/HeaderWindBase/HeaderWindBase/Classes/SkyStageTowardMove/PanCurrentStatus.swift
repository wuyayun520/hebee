
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let kReachMsg:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let main_flagValue:[UInt8] = [0x12,0xe,0x2,0xa,0x4,0x15,0x1e,0x17,0x8,0x5,0x4,0xe,0x2,0x9,0x0,0x15,0x1e,0x13,0x4,0x10,0x14,0x4,0x12,0x15,0x2,0x0,0xd,0xd,0x1e,0xf,0xe,0x15,0x8,0x7,0x8,0x2,0x0,0x15,0x8,0xe,0xf]

private func minimumRespective(here num: UInt8) -> UInt8 {
    return num ^ 65
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let constLostData:[UInt8] = [0x29,0x3e,0x38,0x32,0x2d,0x3e,0x24,0x3d,0x37,0x34,0x3a,0x2f,0x28,0x38,0x29,0x3e,0x3e,0x35,0x24,0x35,0x34,0x2f,0x32,0x3d,0x32,0x38,0x3a,0x2f,0x32,0x34,0x35]

private func versionUniform(item num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "SOCKET 连接成功." :*/
fileprivate let kSocialStrengthTitle:String = "SOCKcommit"
fileprivate let noti_recommendationKey:String = "T 连\u{63a5}成功."

/*: "SOCKET 连接失败, error: :*/
fileprivate let main_frameArtId:String = "socke"
fileprivate let constHighlightNetworkUrl:String = "失\u{8d25}, error"
fileprivate let main_hidePrivacyStr:[Character] = [":"]

/*: "SOCKET 失败 :*/
fileprivate let kRegionData:String = "cellOCK"
fileprivate let show_versionStr:String = "maximum"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let constSomeoneStr:String = "\u{6b21},"
fileprivate let noti_findViewName:String = "loop comment mic appropriate 切换过So"
fileprivate let noti_sceneMsg:String = "域\u{540d}\u{91cd}新尝试"

/*: "SOCKET 已关闭, code: :*/
fileprivate let app_decideString:[Character] = ["S","O","C","K","E","T"," ","已","关","闭",","," ","c","o","d"]
fileprivate let mainCrucialDetectData:[Character] = ["e",":"]

/*: , reason: :*/
fileprivate let showArrowMsg:String = ", rcomponent first great remain seek"
fileprivate let app_shouldTitle:[Character] = ["e","a","s","o","n",":"]

/*: /?sid= :*/
fileprivate let appSignPath:String = "translation"
fileprivate let constInstallTitle:[Character] = ["?","s","i","d","="]

/*: &uid= :*/
fileprivate let kFollowThresholdTagId:String = "&uid=record train privacy that handle"

/*: &platform=iphone&version= :*/
fileprivate let userSituationPath:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e","r"]
fileprivate let dataThePromiseId:String = "sion=date people tip consumer wrap"

/*: &packageId= :*/
fileprivate let appDelicateValue:String = "&packedge adjustment waist undertake"
fileprivate let dataBroadcastId:[Character] = ["a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let user_receiverOriginRomanString:String = "1v1schallenge until team primary"

/*: .com:7766" :*/
fileprivate let dataBlueOverId:String = "cap selected.com:"
fileprivate let dataLevelCustomValue:[Character] = ["7","7","6","6"]

/*: "43.129.185.194:7767" :*/
fileprivate let notiEhStr:String = "43.exception"
fileprivate let dataExtraKey:String = "5.194"
fileprivate let app_flowRelationPath:[Character] = ["7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let k_itsPutMessage:[UInt8] = [0x75,0x69,0x65,0x6d,0x63,0x72,0x6,0xc1,0x9b,0xb7,0xc1,0x9d,0xba,0x54,0x43,0x47,0x45,0x4e,0x47,0x44,0x4f,0x4a,0x4f,0x52,0x5f,0x65,0x4e,0x47,0x48,0x41,0x43,0x42,0xc1,0xbd,0xb7,0xc3,0xb6,0x8a,0xc9,0x9a,0xaa,0xc0,0xb0,0x8b,0xc1,0x9b,0xb7,0xc3,0xa3,0x95,0xcf,0xb1,0x8b,0x8]

private func poLarge(timing num: UInt8) -> UInt8 {
    return num ^ 38
}

/*: "cmd" :*/
fileprivate let notiBorderBusinessString:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let notiNothingPath:String = "pininner"

/*: "callHeartbeat" :*/
fileprivate let main_adStr:[Character] = ["c","a","l","l","H","e"]
fileprivate let main_levelId:[Character] = ["a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let showPlainPath:[Character] = ["r","e","c","i","e","v","e","d"]
fileprivate let showThinLiveUrl:[Character] = ["D","a","t","e"]

/*: "liveHeartBeat" :*/
fileprivate let show_lastTitle:String = "liveHbot comprehensive pro"
fileprivate let k_skipFormat:String = "eagraphic"

/*: "🔗 关闭 socket" :*/
fileprivate let show_sinceId:String = "🔗 path闭 "
fileprivate let noti_frequencyAppealDiscountString:[Character] = ["s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let showModelSystemTitle:String = "class executeThe us"
fileprivate let notiRefreshlyContinuingName:[Character] = ["s"," ","o","u","t"]

/*: "SOCKET  :*/
fileprivate let dataShadowName:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let show_zoneData:String = "重连. 重试次"
fileprivate let main_versionMessage:[Character] = ["数","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let userInfoExtendId:[Character] = ["S","O","C","K","E","T"," ","连","接","正","常"," ","不","需","要","重","新","\u{8fde}","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let showEasyValue:String = "SOCuniform"
fileprivate let constDefineTitle:String = "在连接 \u{4e0d}需\u{8981}"
fileprivate let notiSingleUrl:String = "重check接."

/*: "&resume=1" :*/
fileprivate let notiExploreName:[Character] = ["&","r","e","s","u","m","e","="]
fileprivate let main_exclusiveValue:String = "plus"

/*: "&lang= :*/
fileprivate let k_manStr:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let app_finishPath:String = "SOCKET 开始found family"
fileprivate let dataLiveStr:String = "连\u{63a5}, 地\u{5740}："

/*: "onNetDisabled" :*/
fileprivate let noti_twoKey:String = "common present max sampleonNetD"
fileprivate let noti_independentSubtleFormat:String = "eskip"

/*: "errno" :*/
fileprivate let kCombinedTitle:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let mainThemeKey:String = "dcelltcell"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let main_carePastMsg:[Character] = ["S","O","C","K","E","T"," ","连","接","保","活"]
fileprivate let mainProvideUrl:[Character] = ["c","m","d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let main_behaviorPath:[UInt8] = [0x15,0x11,0x5,0xd,0x7,0x16,0xe2,0x36,0x34,0x36,0x25,0xa6,0x81,0x5f,0xa8,0x76,0x7d,0x25,0x2f,0x26,0xfc,0xe2,0x25,0x23,0x2e,0x2e,0xa,0x27,0x23,0x34,0x36,0x24,0x27,0x23,0x36,0xf0]

fileprivate func failureArrow(keep num: UInt8) -> UInt8 {
    let value = Int(num) + 62
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let kReplacementValue:[UInt8] = [0x1b,0x7,0xb,0x3,0xd,0x1c,0x68,0x24,0x21,0x3e,0x2d,0xac,0xf7,0xd5,0xae,0xfc,0xf3,0x2b,0x25,0x2c,0x72,0x68,0x24,0x21,0x3e,0x2d,0x0,0x2d,0x29,0x3a,0x3c,0xa,0x2d,0x29,0x3c,0x66]

private func spellDay(lose num: UInt8) -> UInt8 {
    return num ^ 72
}

/*: "onP" :*/
fileprivate let constManageWindowPath:String = "onPclock signal pause data"

/*: "msg" :*/
fileprivate let mainYellowValue:String = "mmang"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let dataCrushString:String = "\u{0001f517} 处理消"
fileprivate let appPlainPath:String = "息："
fileprivate let main_sustainableBoyStr:String = "obtain gapcmdN"

/*: , errNo: :*/
fileprivate let dataCalculateId:String = ", errNsale easy depth transition"
fileprivate let main_addStr:String = "finish bold host beyond particleo:"

/*: , msg: :*/
fileprivate let appMoreFormat:String = "equally"
fileprivate let kPoseId:String = " msg:fixed news custom"

/*: , data: :*/
fileprivate let notiGalleryPath:String = ", datadouble lite thin remark cut"
fileprivate let appSeaName:String = "implement"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let data_destroyMessage:[Character] = ["S","O","C","K","E","T"," ","接","收","异","常","\u{6d88}","息","：","c"]
fileprivate let data_addressName:String = "mdName:zz strength then m revenue"

/*: "onReceivedGift" :*/
fileprivate let constDeadlineStageCountryName:String = "onRecschedule hide input"
fileprivate let noti_clickData:String = "iharassmentt"

/*: "onVideoMatch" :*/
fileprivate let mainAngleValue:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let app_feedThanMessage:String = "onReseek bod quit mp fee"
fileprivate let noti_oldSwitchdData:String = "tCallblind context construct uniform"

/*: "onCancelCall" :*/
fileprivate let main_transactionCouldPath:String = "onCanignore round contain license put"
fileprivate let notiWeightStr:[Character] = ["c","e","l","C","a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let user_chapterRawName:[Character] = ["o","n","R","e","f","u","s","e","C","a","l"]
fileprivate let app_methodId:String = "train"

/*: "onAcceptCall" :*/
fileprivate let mainLightUrl:String = "network due plat amongonAcce"

/*: "onStopCall" :*/
fileprivate let user_processorKey:String = "onStopwith speed explain ret"

/*: "freeCallTimes" :*/
fileprivate let constYesterdayCancelKey:String = "shake pic mobile cookie infofreeC"
fileprivate let user_replacementTriggerId:[Character] = ["a","l","l","T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let notiUsedShowTitle:String = "mfCoinbound cos"

/*: "onMissCoin" :*/
fileprivate let const_everyUrl:[Character] = ["o","n","M","i","s"]
fileprivate let app_countryWindowUrl:String = "line sine efficiency mechanismsCoin"

/*: "acceptCall" :*/
fileprivate let notiConversionKey:[Character] = ["a","c","c","e","p"]
fileprivate let appSpellKey:String = "spine anima observe policytCall"

/*: "stopCall" :*/
fileprivate let main_wholeKey:String = "robotop"

/*: "requestCall" :*/
fileprivate let main_photoString:String = "REQU"

/*: "[Invite you to a video call...]" :*/
fileprivate let data_historyNoteTitle:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let const_publisherString:String = "typvisit"

/*: "[Invite you to a voice call...]" :*/
fileprivate let main_signPath:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "uid" :*/
fileprivate let show_meBlankString:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let data_rapidPath:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let kNothingBeautyName:[Character] = ["h","e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let showLoadNoticePath:String = "document gameonCall"
fileprivate let userRecognizeMessage:String = "Resumereader reflect exactly illegal success"

/*: "cancelCall" :*/
fileprivate let kSizeValue:String = "C"
fileprivate let userTapBasicTitle:String = "ANCE"

/*: "refuseCall" :*/
fileprivate let noti_absUrl:String = "R"
fileprivate let user_portFormat:String = "certainfuscertain"

/*: "onSendProgramGift" :*/
fileprivate let app_integrationString:String = "due least textonSendPr"
fileprivate let main_zoneString:[Character] = ["o","g","r","a"]
fileprivate let showAspectConfirmValue:[Character] = ["m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let dataLabData:[Character] = ["o","n","G","a","m"]
fileprivate let kLabelPath:[Character] = ["e","F","s"]

/*: "broadStatus" :*/
fileprivate let app_detailString:String = "broadSno infrastructure"

/*: "onCallChatMsg" :*/
fileprivate let showThreadRankFormat:String = "infrastructure harassment raw album procedureonCall"

/*: "onUserBlock" :*/
fileprivate let const_quitTitle:[Character] = ["o","n","U","s","e","r","B"]
fileprivate let noti_loadTingId:String = "loyellow"

/*: "isRecharge" :*/
fileprivate let k_shareValue:String = "first sound meetisRec"

/*: "source" :*/
fileprivate let const_remoteKey:String = "SOURCE"

/*: "invitePop" :*/
fileprivate let const_disappearId:String = "illegal spend yield boundinvitePop"

/*: "videoPop" :*/
fileprivate let showMarginGraphVidUrl:String = "videoPotap every very contain"
fileprivate let user_applicationFormat:[Character] = ["p"]

/*: "coin" :*/
fileprivate let noti_shShakeFlowTitle:String = "cotexturen"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PanCurrentStatus.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let mainLikeMethodThresholdKey = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let const_frameId = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let k_compareOriginData = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let showPartyString = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kIsolateValue = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let main_channelName = (String(kReachMsg))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let appFutureScaleMessage = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let showAroundString = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let appReducePath = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let noti_warnMsg = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let notiFractionTotalervalStr = NSNotification.Name(rawValue: String(bytes: main_flagValue.map{minimumRespective(here: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let userMissionStatusRenderTitle = NSNotification.Name(rawValue: String(bytes: constLostData.map{versionUniform(item: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol KitWrap: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func documentSingle(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol MarkBetween: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func occurFilter(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func eachEager(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol FromReduce: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func someTop(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func more(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func isMissReload(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func dealRate(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol SymbolConnect: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func count(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func no(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class PanCurrentStatus: NSObject {
    //: enum SocketRetryType: String {
    enum EquivalentLarge: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var usageVC: AnyObject?
    var usageVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ThanTrain.shared.direction == .rightToLeft ? 0 : mainCellMsg,
        //: y: StatusBarHeight+190
        y: data_visitorPath + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ThanTrain.shared.direction == .rightToLeft ? 0 : mainCellMsg,
        //: y: StatusBarHeight+190
        y: data_visitorPath + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: KitWrap? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: MarkBetween? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: FromReduce? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: SymbolConnect? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    
    @objc dynamic var usage = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = PanCurrentStatus()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(segment(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(pad),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension PanCurrentStatus: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        MusicThread.layerLanguage(msg: (kSocialStrengthTitle.replacingOccurrences(of: "commit", with: "E") + noti_recommendationKey))
        //: sendPing()
        behindAccept()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: mainSubtleEnvironmentMsg,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: main_channelName)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        storageVisitSum()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        MusicThread.layerLanguage(msg: (main_frameArtId.uppercased() + "T 连\u{63a5}" + constHighlightNetworkUrl + String(main_hidePrivacyStr)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = MountCustom.share.reachability?.connection, status != .unavailable else {
        guard let status = MountCustom.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == showPartyString {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = darkAcrossCalculate(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            MusicThread.layerLanguage(msg: (kRegionData.replacingOccurrences(of: "cell", with: "S") + "ET 失" + show_versionStr.replacingOccurrences(of: "maximum", with: "败")) + "\(showPartyString)" + (constSomeoneStr + String(noti_findViewName.suffix(6)) + "cket" + noti_sceneMsg + "，地址：") + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.creationChapter(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.broadSophisticated(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        MusicThread.layerLanguage(msg: (String(app_decideString) + String(mainCrucialDetectData)) + "\(code)" + (String(showArrowMsg.prefix(3)) + String(app_shouldTitle)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.beautyField(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == noti_warnMsg {
            //: return
            return
        }
        //: if let status = MountCustom.share.reachability?.connection, status != .unavailable {
        if let status = MountCustom.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.creationChapter(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func darkAcrossCalculate(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = MountCustom.share.appConfigMode.ws
        self.linkAddrArr = MountCustom.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = characteristicTail()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: main_channelName)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = MountCustom.share.loginUid
        let uid = MountCustom.share.loginUid
        //: let sid = MountCustom.share.loginSessionId
        let sid = MountCustom.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (appSignPath.replacingOccurrences(of: "translation", with: "/") + String(constInstallTitle)) + "\(sid)" + (String(kFollowThresholdTagId.prefix(5))) + "\(uid)" + (String(userSituationPath) + String(dataThePromiseId.prefix(5))) + "\(kJoinKey)" + (String(appDelicateValue.prefix(5)) + String(dataBroadcastId)) + "\(data_scientificComposeFormat)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func characteristicTail() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return user_locateTitle ? [(String(user_receiverOriginRomanString.prefix(4)) + "erver.") + "\(dataServiceName)" + (String(dataBlueOverId.suffix(5)) + String(dataLevelCustomValue))] : [(notiEhStr.replacingOccurrences(of: "exception", with: "12") + "9.18" + dataExtraKey.capitalized + ":776" + String(app_flowRelationPath))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension PanCurrentStatus {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func segment(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            MusicThread.layerLanguage(msg: String(bytes: k_itsPutMessage.map{poLarge(timing: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.beautyField(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.creationChapter(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func pad() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.creationChapter(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.computeTransaction()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension PanCurrentStatus {
    /// socket保活心跳包
    //: private func sendPing() {
    private func behindAccept() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: mainLikeMethodThresholdKey, block: { timer in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.banAcross(info: [(String(notiBorderBusinessString)): (notiNothingPath.replacingOccurrences(of: "inner", with: "g"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func noSplit() {
        //: endTalkingPing()
        orangeImpact()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: const_frameId, block: { timer in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.usage || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.usageVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.usageVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.usage || (self.alongLarge()?.isKind(of: ButtonViewController.self) == false && self.alongLarge()?.isKind(of: BindSource.self) == false) && (self.usageVC?.isKind(of: ButtonViewController.self) == false && self.usageVC?.isKind(of: BindSource.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.banAcross(info: [(String(notiBorderBusinessString)): (String(main_adStr) + String(main_levelId))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func orangeImpact() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func computeTransaction() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(showPlainPath) + String(showThinLiveUrl))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < userCountensityChainData {
                //: MountCustom.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                MountCustom.share.pauseWith(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension PanCurrentStatus {
    /// 开启直播心跳包
    //: func startLivePing() {
    func announcementLayerReserve() {
        //: endLivePing()
        tapFail()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: k_compareOriginData, block: { timer in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.banAcross(info: [(String(notiBorderBusinessString)): (String(show_lastTitle.prefix(5)) + k_skipFormat.replacingOccurrences(of: "graphic", with: "rt") + "Beat")])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func tapFail() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension PanCurrentStatus {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func storageVisitSum() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = ControlRebuild()
        //: let keyWindow = StopBrush.getWindow()
        let keyWindow = StopBrush.correct()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.usage
        self.containerView?.isHidden = !self.usage
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func processSound() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension PanCurrentStatus {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func beautyField(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (show_sinceId.replacingOccurrences(of: "path", with: "关") + String(noti_frequencyAppealDiscountString)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: noti_warnMsg, reason: (String(showModelSystemTitle.suffix(6)) + "er log" + String(notiRefreshlyContinuingName)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        orangeImpact()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            processSound()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func report() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = PanCurrentStatus.shared.darkAcrossCalculate(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.creationChapter(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func creationChapter(timeInterval: TimeInterval, type: EquivalentLarge) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            MusicThread.layerLanguage(msg: (String(dataShadowName)) + "\(type.rawValue)" + (show_zoneData.capitalized + String(main_versionMessage)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.feedbackAlongside(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func feedbackAlongside(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                MusicThread.layerLanguage(msg: (String(userInfoExtendId)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                MusicThread.layerLanguage(msg: (showEasyValue.replacingOccurrences(of: "uniform", with: "K") + "ET 正" + constDefineTitle + notiSingleUrl.replacingOccurrences(of: "check", with: "新连")))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.usage {
        if self.usage {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(notiExploreName) + main_exclusiveValue.replacingOccurrences(of: "plus", with: "1")))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(k_manStr)) + "\(PutDirection.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        beautyField(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kIsolateValue
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        MusicThread.layerLanguage(msg: (String(app_finishPath.prefix(9)) + dataLiveStr) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func banAcross(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if MountCustom.share.reachability?.connection == nil || MountCustom.share.reachability?.connection == .unavailable {
        if MountCustom.share.reachability?.connection == nil || MountCustom.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(notiBorderBusinessString)): (String(noti_twoKey.suffix(6)) + "isabl" + noti_independentSubtleFormat.replacingOccurrences(of: "skip", with: "d")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (String(kCombinedTitle)): "\(appReducePath)",
                                       //: "data": [:]]
                                       (mainThemeKey.replacingOccurrences(of: "cell", with: "a")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            broadSophisticated(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(notiBorderBusinessString))] as? String == (notiNothingPath.replacingOccurrences(of: "inner", with: "g")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                MusicThread.layerLanguage(msg: (String(main_carePastMsg) + String(mainProvideUrl)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(notiBorderBusinessString))] as? String == (String(main_adStr) + String(main_levelId)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                MusicThread.layerLanguage(msg: String(bytes: main_behaviorPath.map{failureArrow(keep: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(notiBorderBusinessString))] as? String == (String(show_lastTitle.prefix(5)) + k_skipFormat.replacingOccurrences(of: "graphic", with: "rt") + "Beat") {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                MusicThread.layerLanguage(msg: String(bytes: kReplacementValue.map{spellDay(lose: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(notiBorderBusinessString))] as? String == (notiNothingPath.replacingOccurrences(of: "inner", with: "g")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(notiBorderBusinessString)): (String(constManageWindowPath.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (String(kCombinedTitle)): "\(appFutureScaleMessage)",
                                           //: "data": [:]]
                                           (mainThemeKey.replacingOccurrences(of: "cell", with: "a")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                broadSophisticated(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.creationChapter(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func broadSophisticated(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(notiBorderBusinessString))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(String(kCombinedTitle))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(mainYellowValue.replacingOccurrences(of: "man", with: "s"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(mainThemeKey.replacingOccurrences(of: "cell", with: "a"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (dataCrushString + appPlainPath.capitalized + String(main_sustainableBoyStr.suffix(4)) + "ame:") + "\(cmdName)" + (String(dataCalculateId.prefix(6)) + String(main_addStr.suffix(2))) + "\(errorNo)" + (appMoreFormat.replacingOccurrences(of: "equally", with: ",") + String(kPoseId.prefix(5))) + "\(msg)" + (String(notiGalleryPath.prefix(6)) + appSeaName.replacingOccurrences(of: "implement", with: ":")) + "\(data)")

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            MusicThread.layerLanguage(msg: (String(data_destroyMessage) + String(data_addressName.prefix(7))) + "\(cmdName)" + (String(dataCalculateId.prefix(6)) + String(main_addStr.suffix(2))) + "\(errorNo)" + (appMoreFormat.replacingOccurrences(of: "equally", with: ",") + String(kPoseId.prefix(5))) + "\(msg)" + (String(notiGalleryPath.prefix(6)) + appSeaName.replacingOccurrences(of: "implement", with: ":")) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: MountCustom.share.loginUserMode.freeCallTimes = 0
                MountCustom.share.loginUserMode.freeCallTimes = 0
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.documentSingle(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(constDeadlineStageCountryName.prefix(5)) + "eivedG" + noti_clickData.replacingOccurrences(of: "harassment", with: "f")) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: userPostName, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(mainAngleValue)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.occurFilter(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(app_feedThanMessage.prefix(4)) + "ques" + String(noti_oldSwitchdData.prefix(5))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.eachEager(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(main_transactionCouldPath.prefix(5)) + String(notiWeightStr)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(user_chapterRawName) + app_methodId.replacingOccurrences(of: "train", with: "l")) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(mainLightUrl.suffix(6)) + "ptCall") { // 接收方发送acceptCall回执
            //: self.usage = true
            self.usage = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            noSplit()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.no(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(user_processorKey.prefix(6)) + "Call") { // 接收方发送stopCall回执
            //: self.usage = false
            self.usage = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            orangeImpact()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(constYesterdayCancelKey.suffix(5)) + String(user_replacementTriggerId))] as? Int {
                //: MountCustom.share.loginUserMode.freeCallTimes = freeCallTimes
                MountCustom.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: MountCustom.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            MountCustom.share.loginUserMode.mf_coin = json[(mainThemeKey.replacingOccurrences(of: "cell", with: "a"))][(String(notiUsedShowTitle.prefix(6)))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            backgroundSomeone(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: noti_mountFlowFormat, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(const_everyUrl) + String(app_countryWindowUrl.suffix(5))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.dealRate(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(notiConversionKey) + String(appSpellKey.suffix(5))) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.usage = true
            self.usage = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            noSplit()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.someTop(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (main_wholeKey.replacingOccurrences(of: "robot", with: "st") + "Call") { // 接收方/拨打方 挂断了通话
            //: self.usage = false
            self.usage = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            orangeImpact()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.more(data: data)
            // 更新余额
            //: MountCustom.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            MountCustom.share.loginUserMode.mf_coin = json[(mainThemeKey.replacingOccurrences(of: "cell", with: "a"))][(String(notiUsedShowTitle.prefix(6)))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(constYesterdayCancelKey.suffix(5)) + String(user_replacementTriggerId))] as? Int {
                //: MountCustom.share.loginUserMode.freeCallTimes = freeCallTimes
                MountCustom.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            backgroundSomeone(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: noti_mountFlowFormat, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false

            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (main_photoString.lowercased() + "estCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: notiFractionTotalervalStr, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: MountCustom.share.recieve1v1TalkRequest(info: data)
                MountCustom.share.pauseWith(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(showPlainPath) + String(showThinLiveUrl))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: data_historyNoteTitle.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(mainThemeKey.replacingOccurrences(of: "cell", with: "a"))][(const_publisherString.replacingOccurrences(of: "visit", with: "e"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: main_signPath.reversed(), encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                AssociateMaterial.spaceRegulate(uid: String(json[(mainThemeKey.replacingOccurrences(of: "cell", with: "a"))][(String(show_meBlankString))].intValue),
                                                //: title: data["nickname"] as? String,
                                                title: data[(String(data_rapidPath))] as? String,
                                                //: body: body,
                                                body: body,
                                                //: imageUrl: data["headPic"] as? String)
                                                imageUrl: data[(String(kNothingBeautyName))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                AssociateMaterial.shared.ignoreThroughMultiple()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(showLoadNoticePath.suffix(6)) + String(userRecognizeMessage.prefix(6))) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.usage = true
            self.usage = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            noSplit()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(showLoadNoticePath.suffix(6)) + String(userRecognizeMessage.prefix(6)))] = "1"
            //: MountCustom.share.recieve1v1TalkResumeRequest(info: resumeData)
            MountCustom.share.barEnable(info: resumeData)

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (kSizeValue.lowercased() + userTapBasicTitle.lowercased() + "lCall") { // 拨打方拒绝了通话，接收方收到该指令
            //: self.usage = false
            self.usage = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            orangeImpact()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.isMissReload(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                AssociateMaterial.romanOn(identifier: String(json[(mainThemeKey.replacingOccurrences(of: "cell", with: "a"))][(String(show_meBlankString))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                AssociateMaterial.shared.choiceResolve()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (noti_absUrl.lowercased() + user_portFormat.replacingOccurrences(of: "certain", with: "e") + "Call") { // 接收方拒绝了通话，拨打方收到该指令
            //: self.usage = false
            self.usage = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            orangeImpact()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.count(data: data)

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(app_integrationString.suffix(8)) + String(main_zoneString) + String(showAspectConfirmValue)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.notFade(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(dataLabData) + String(kLabelPath)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(app_detailString.prefix(6)) + "tatus")] as? Int ?? -1
            //: if MountCustom.share.appStatus == AppSkinStatus.normal.rawValue {
            if MountCustom.share.appStatus == SensorRow.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: userMissionStatusRenderTitle, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: userPostName, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: userMissionStatusRenderTitle, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(showThreadRankFormat.suffix(6)) + "ChatMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            ClearButton.bridge().draftSection(msg: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(const_quitTitle) + noti_loadTingId.replacingOccurrences(of: "yellow", with: "ck")) { // 用户被封号
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: show_exerciseString, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension PanCurrentStatus {
    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func backgroundSomeone(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        CombineBoth.shared.doShape()
        //: guard MountCustom.share.loginUserMode.sex == Gender.male.rawValue else {
        guard MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(k_shareValue.suffix(5)) + "harge")] as? Bool == false {
            //: RetainProcess.share.func_pushToDiscountRechargeView()
            RetainProcess.share.tableMore()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(const_remoteKey.lowercased())] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(const_disappearId.suffix(9))) || source == (String(showMarginGraphVidUrl.prefix(7)) + String(user_applicationFormat)) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(noti_shShakeFlowTitle.replacingOccurrences(of: "texture", with: "i"))] as? Int) ?? 0 < 60 {
            //: RetainProcess.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            RetainProcess.share.adjustExample(webViewType: .RechargeHalfPage)
        }
    }
}
