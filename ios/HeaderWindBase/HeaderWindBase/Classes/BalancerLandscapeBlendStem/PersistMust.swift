
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataEnterName:[UInt8] = [0x22,0x25,0x22,0x3f,0x63,0x28,0x24,0x2f,0x2e,0x39,0x71,0x62,0x6b,0x23,0x2a,0x38,0x6b,0x25,0x24,0x3f,0x6b,0x29,0x2e,0x2e,0x25,0x6b,0x22,0x26,0x3b,0x27,0x2e,0x26,0x2e,0x25,0x3f,0x2e,0x2f]

private func connectStackTail(substantial num: UInt8) -> UInt8 {
    return num ^ 75
}

/*: "icon_lounge" :*/
fileprivate let appInmateData:String = "icon_lmatch stack center"
fileprivate let show_manValue:[Character] = ["o","u","n","g","e"]

/*: "icon_home_v" :*/
fileprivate let const_partnerKey:String = "icon_hotiming next feed problem spread"
fileprivate let kArchitectureUrl:[Character] = ["m","e","_","v"]

/*: "My album" :*/
fileprivate let k_filterKey:String = "My apersist automatic drown class press"

/*: "0A071F" :*/
fileprivate let constWhoString:String = "min prepare capable0A071F"

/*: "#C5BEFF" :*/
fileprivate let k_cutKey:[Character] = ["#","C","5","B","E","F","F"]

/*: "🎉congratulations! She may be very interested in you. She's not far from you. \nWhy don't you say hello to her?" :*/
fileprivate let showMidName:[UInt8] = [0x5e,0xd,0xfc,0xf7,0xd1,0xdd,0xdc,0xd5,0xe0,0xcf,0xe2,0xe3,0xda,0xcf,0xe2,0xd7,0xdd,0xdc,0xe1,0x8f,0x8e,0xc1,0xd6,0xd3,0x8e,0xdb,0xcf,0xe7,0x8e,0xd0,0xd3,0x8e,0xe4,0xd3,0xe0,0xe7,0x8e,0xd7,0xdc,0xe2,0xd3,0xe0,0xd3,0xe1,0xe2,0xd3,0xd2,0x8e,0xd7,0xdc,0x8e,0xe7,0xdd,0xe3,0x9c,0x8e,0xc1,0xd6,0xd3,0x95,0xe1,0x8e,0xdc,0xdd,0xe2,0x8e,0xd4,0xcf,0xe0,0x8e,0xd4,0xe0,0xdd,0xdb,0x8e,0xe7,0xdd,0xe3,0x9c,0x8e,0x78,0xc5,0xd6,0xe7,0x8e,0xd2,0xdd,0xdc,0x95,0xe2,0x8e,0xe7,0xdd,0xe3,0x8e,0xe1,0xcf,0xe7,0x8e,0xd6,0xd3,0xda,0xda,0xdd,0x8e,0xe2,0xdd,0x8e,0xd6,0xd3,0xe0,0xad]

fileprivate func sharedAdvice(quote num: UInt8) -> UInt8 {
    let value = Int(num) - 110
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Why don't you say hello to her?" :*/
fileprivate let userImpressionPath:[UInt8] = [0xed,0xfe,0xf,0xb6,0xfa,0x5,0x4,0xbd,0xa,0xb6,0xf,0x5,0xb,0xb6,0x9,0xf7,0xf,0xb6,0xfe,0xfb,0x2,0x2,0x5,0xb6,0xa,0x5,0xb6,0xfe,0xfb,0x8,0xd5]

fileprivate func towardAdministrative(person num: UInt8) -> UInt8 {
    let value = Int(num) - 150
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "🎉congratulations! He may be very interested in you. He's not far from you. \nWhy don't you say hello to him?" :*/
fileprivate let k_easeMsg:[UInt8] = [0xdc,0x8b,0x7a,0x75,0x4f,0x5b,0x5a,0x53,0x5e,0x4d,0x60,0x61,0x58,0x4d,0x60,0x55,0x5b,0x5a,0x5f,0xd,0xc,0x34,0x51,0xc,0x59,0x4d,0x65,0xc,0x4e,0x51,0xc,0x62,0x51,0x5e,0x65,0xc,0x55,0x5a,0x60,0x51,0x5e,0x51,0x5f,0x60,0x51,0x50,0xc,0x55,0x5a,0xc,0x65,0x5b,0x61,0x1a,0xc,0x34,0x51,0x13,0x5f,0xc,0x5a,0x5b,0x60,0xc,0x52,0x4d,0x5e,0xc,0x52,0x5e,0x5b,0x59,0xc,0x65,0x5b,0x61,0x1a,0xc,0xf6,0x43,0x54,0x65,0xc,0x50,0x5b,0x5a,0x13,0x60,0xc,0x65,0x5b,0x61,0xc,0x5f,0x4d,0x65,0xc,0x54,0x51,0x58,0x58,0x5b,0xc,0x60,0x5b,0xc,0x54,0x55,0x59,0x2b]

fileprivate func missAssert(max num: UInt8) -> UInt8 {
    let value = Int(num) - 236
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Why don't you say hello to him?" :*/
fileprivate let main_bottomPopularLessFormat:[UInt8] = [0x91,0xae,0xbf,0xe6,0xa2,0xa9,0xa8,0xe1,0xb2,0xe6,0xbf,0xa9,0xb3,0xe6,0xb5,0xa7,0xbf,0xe6,0xae,0xa3,0xaa,0xaa,0xa9,0xe6,0xb2,0xa9,0xe6,0xae,0xaf,0xab,0xf9]

private func graphicHoney(rounding num: UInt8) -> UInt8 {
    return num ^ 198
}

/*: "#FFD13A" :*/
fileprivate let show_respectiveValue:[Character] = ["#","F","F","D","1","3","A"]

/*: "icon_male_default" :*/
fileprivate let appCurrencyAttributeFormat:[UInt8] = [0x9,0x3,0xf,0xe,0x3f,0xd,0x1,0xc,0x5,0x3f,0x4,0x5,0x6,0x1,0x15,0xc,0x14]

private func specialGo(projection num: UInt8) -> UInt8 {
    return num ^ 96
}

/*: "icon_female_default" :*/
fileprivate let data_grayData:String = "exclusive redicon_fe"
fileprivate let kHarassmentClubValue:String = "male_provide sub interested enjoy"

/*: "My interests：" :*/
fileprivate let appTowardWouldValue:String = "clearly bit clearly define hundredMy int"
fileprivate let show_oldTrackValue:String = "constant choice title relatedts："

/*: "btn_me_edit" :*/
fileprivate let const_pullOperationTitle:[Character] = ["b","t","n","_","m","e"]
fileprivate let user_birthdayName:[Character] = ["_","e","d","i","t"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PersistMust.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/1.
//

//: import UIKit
import UIKit

//: class TalkingChatMatchMsgCell: TalkingChatBaseMsgCell {
class PersistMust: UniformSkip {
    //: var matchData: TalkingChatMatchMsgCellData?
    var matchData: BounceShadow?

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: setupSubviews()
        click()
        //: bindInteraction()
        betweenPan()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataEnterName.map{connectStackTail(substantial: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy Load

    //: private lazy var infoView: UIView = {
    private lazy var infoView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.layer.cornerRadius = 12
        view.layer.cornerRadius = 12
        //: view.layer.masksToBounds = true
        view.layer.masksToBounds = true
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: return view
        return view
        //: }()
    }()

    //: private lazy var vipBgView: UIImageView = {
    private lazy var vipBgView: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.isHidden = true
        img.isHidden = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.layer.cornerRadius = 54/2
        imgV.layer.cornerRadius = 54 / 2
        //: imgV.layer.masksToBounds = true
        imgV.layer.masksToBounds = true
        //: imgV.contentMode = .scaleAspectFill
        imgV.contentMode = .scaleAspectFill
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var infoNameLab: UILabel = {
    private lazy var infoNameLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.labelMomentum()
        //: label.font = UIFont.pingfangMediumFont(fontSize: 18)
        label.font = UIFont.monsterColumn(fontSize: 18)
        //: return label
        return label
        //: }()
    }()

    //: private lazy var loungeImgV: UIImageView = {
    private lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.evolutionary(name: (String(appInmateData.prefix(6)) + String(show_manValue)))
        //: img.isHidden = true
        img.isHidden = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var authImgview: UIImageView = {
    private lazy var authImgview: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image = UIImage.BundleImageNamed(name: "icon_home_v")
        imgV.image = UIImage.evolutionary(name: (String(const_partnerKey.prefix(7)) + String(kArchitectureUrl)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var sexBtn: UIButton = {
    private lazy var sexBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 10)
        btn.titleLabel?.font = .stepTransition(type: .Medium, fontSize: 10)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var interestsLab: UILabel = {
    private lazy var interestsLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: return label
        return label
        //: }()
    }()

    //: private lazy var imgTitleLab: UILabel = {
    private lazy var imgTitleLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = UIFont.pingfangMediumFont(fontSize: 15)
        label.font = UIFont.monsterColumn(fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.labelMomentum()
        //: label.text = "My album".localized
        label.text = (String(k_filterKey.prefix(4)) + "lbum").localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var pictureView: UIView = {
    private lazy var pictureView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: return view
        return view
        //: }()
    }()

    //: private lazy var bgView: UIView = {
    private lazy var bgView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor(hex: "0A071F")?.withAlphaComponent(0.3)
        view.backgroundColor = UIColor(hex: (String(constWhoString.suffix(6))))?.withAlphaComponent(0.3)
        //: view.layer.cornerRadius = 12
        view.layer.cornerRadius = 12
        //: view.layer.masksToBounds = true
        view.layer.masksToBounds = true
        //: view.layer.borderWidth = 1
        view.layer.borderWidth = 1
        //: view.layer.borderColor = UIColor(hex: "#C5BEFF")?.cgColor
        view.layer.borderColor = UIColor(hex: (String(k_cutKey)))?.cgColor
        //: return view
        return view
        //: }()
    }()

    //: private lazy var matchLab: UILabel = {
    private lazy var matchLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: var str = "🎉congratulations! She may be very interested in you. She's not far from you. \nWhy don't you say hello to her?".localized
        var str = String(bytes: showMidName.map{sharedAdvice(quote: $0)}, encoding: .utf8)!.localized
        //: var matchStr = "Why don't you say hello to her?".localized
        var matchStr = String(bytes: userImpressionPath.map{towardAdministrative(person: $0)}, encoding: .utf8)!.localized

        //: if MountCustom.share.loginUserMode.sex == "2" {
        if MountCustom.share.loginUserMode.sex == "2" {
            //: str = "🎉congratulations! He may be very interested in you. He's not far from you. \nWhy don't you say hello to him?".localized
            str = String(bytes: k_easeMsg.map{missAssert(max: $0)}, encoding: .utf8)!.localized
            //: matchStr = "Why don't you say hello to him?".localized
            matchStr = String(bytes: main_bottomPopularLessFormat.map{graphicHoney(rounding: $0)}, encoding: .utf8)!.localized
        }
        //: var attriString = NSMutableAttributedString.init(string: str as String, attributes: [.font: UIFont.pingfangMediumFont(fontSize: 15), .foregroundColor: UIColor.white])
        var attriString = NSMutableAttributedString(string: str as String, attributes: [.font: UIFont.monsterColumn(fontSize: 15), .foregroundColor: UIColor.white])
        //: if let range = str.range(of: matchStr) {
        if let range = str.range(of: matchStr) {
            //: let nsRange = NSRange(range, in: str)
            let nsRange = NSRange(range, in: str)
            //: attriString.addAttributes([NSAttributedString.Key.foregroundColor: UIColor(hex: "#FFD13A")!], range: nsRange)
            attriString.addAttributes([NSAttributedString.Key.foregroundColor: UIColor(hex: (String(show_respectiveValue)))!], range: nsRange)
        }
        //: label.attributedText = attriString
        label.attributedText = attriString
        //: return label
        return label
        //: }()
    }()
}

// MARK: - 刷新

//: extension TalkingChatMatchMsgCell {
extension PersistMust {
    //: override func fill(with data: TCommonCellData) {
    override func creationCharacter(with data: EventColor) {
        //: super.fill(with: data)
        super.creationCharacter(with: data)
        //: self.matchData = data as? TalkingChatMatchMsgCellData
        self.matchData = data as? BounceShadow
        //: self.avatarView.isHidden = true
        self.avatarView.isHidden = true
        //: self.isReadImg.isHidden = true
        self.isReadImg.isHidden = true
        //: self.coinIconImg.isHidden = true
        self.coinIconImg.isHidden = true
        //: self.coinLabel.isHidden = true
        self.coinLabel.isHidden = true
        //: self.retryView.isHidden = true
        self.retryView.isHidden = true
        //: self.iconBorder.isHidden = true
        self.iconBorder.isHidden = true
        //: self.bubbleImgView.isHidden = true
        self.bubbleImgView.isHidden = true
        //: self.imgTitleLab.isHidden = true
        self.imgTitleLab.isHidden = true
        //: self.pictureView.isHidden = true
        self.pictureView.isHidden = true

        // 为空时必须初始化
        //: let userInfoModel = self.matchData?.msgModel.userInfoModel ?? BoundMeasureTrack.init()
        let userInfoModel = self.matchData?.msgModel.userInfoModel ?? BoundMeasureTrack()
        //: guard userInfoModel.uid.isEmpty == false else {
        guard userInfoModel.uid.isEmpty == false else {
            //: self.bubbleImgView.isHidden = true
            self.bubbleImgView.isHidden = true
            //: return
            return
        }

        //: self.vipBgView.isHidden = !userInfoModel.loungePlus
        self.vipBgView.isHidden = !userInfoModel.loungePlus
        //: if self.vipBgView.isHidden == false {
        if self.vipBgView.isHidden == false {
            //: if let vipModel = MountCustom.share.appUserConfigMode.vipSetting.filter({$0.vipSkinId == userInfoModel.vipSkinId}).first {
            if let vipModel = MountCustom.share.appUserConfigMode.vipSetting.filter({ $0.vipSkinId == userInfoModel.vipSkinId }).first {
                //: self.vipBgView.setUrlImage(urlStr: vipModel.vipChatSkin, placeImg: nil)
                self.vipBgView.accessiblePending(urlStr: vipModel.vipChatSkin, placeImg: nil)
            }
        }
        //: self.headImgView.setUrlImage(urlStr: userInfoModel.headPic)
        self.headImgView.accessiblePending(urlStr: userInfoModel.headPic)
        //: self.infoNameLab.textColor = userInfoModel.loungePlus ? .userVipColor():.appTitleColor()
        self.infoNameLab.textColor = userInfoModel.loungePlus ? .fireReplace() : .labelMomentum()
        //: self.infoNameLab.text = userInfoModel.nickname
        self.infoNameLab.text = userInfoModel.nickname
        //: self.loungeImgV.isHidden = !userInfoModel.loungePlus
        self.loungeImgV.isHidden = !userInfoModel.loungePlus
        //: self.authImgview.isHidden = !userInfoModel.tpAuth
        self.authImgview.isHidden = !userInfoModel.tpAuth
        //: let imgStr = userInfoModel.sex == Gender.male.rawValue ? "icon_male_default" : "icon_female_default"
        let imgStr = userInfoModel.sex == BasicIndependent.male.rawValue ? String(bytes: appCurrencyAttributeFormat.map{specialGo(projection: $0)}, encoding: .utf8)! : (String(data_grayData.suffix(7)) + String(kHarassmentClubValue.prefix(5)) + "default")
        //: sexBtn.setBackgroundImage(UIImage.BundleImageNamed(name: imgStr), for: .normal)
        sexBtn.setBackgroundImage(UIImage.evolutionary(name: imgStr), for: .normal)
        //: sexBtn.setTitle("   " + String(userInfoModel.age), for: .normal)
        sexBtn.setTitle("   " + String(userInfoModel.age), for: .normal)

        //: if (userInfoModel.interest.count > 0) {
        if userInfoModel.interest.count > 0 {
            //: self.interestsLab.isHidden = false
            self.interestsLab.isHidden = false
            //: let matchStr = "My interests：".localized
            let matchStr = (String(appTowardWouldValue.suffix(6)) + "eres" + String(show_oldTrackValue.suffix(3))).localized
            //: let arr_1: NSArray = userInfoModel.interest as NSArray
            let arr_1: NSArray = userInfoModel.interest as NSArray
            //: let str_1 = arr_1.componentsJoined(by: "，")
            let str_1 = arr_1.componentsJoined(by: "，")
            //: let str: NSString = "\(matchStr)\(str_1 as String)" as NSString
            let str: NSString = "\(matchStr)\(str_1 as String)" as NSString
            //: let attributes = [.font: UIFont.pingfangMediumFont(fontSize: 15), .foregroundColor: UIColor.appThemeColor()] as [NSAttributedString.Key: Any]
            let attributes = [.font: UIFont.monsterColumn(fontSize: 15), .foregroundColor: UIColor.alongOccur()] as [NSAttributedString.Key: Any]
            //: let attrString = NSMutableAttributedString.init(string: str as String, attributes: attributes)
            let attrString = NSMutableAttributedString(string: str as String, attributes: attributes)
            //: attrString.addAttributes([NSAttributedString.Key.foregroundColor: UIColor.appTitleColor()], range: str.range(of: matchStr))
            attrString.addAttributes([NSAttributedString.Key.foregroundColor: UIColor.labelMomentum()], range: str.range(of: matchStr))
            //: self.interestsLab.attributedText = attrString
            self.interestsLab.attributedText = attrString
            //: } else {
        } else {
            //: self.interestsLab.isHidden = true
            self.interestsLab.isHidden = true
        }
        //: if userInfoModel.picture.count > 0 {
        if userInfoModel.picture.count > 0 {
            //: self.imgTitleLab.isHidden = false
            self.imgTitleLab.isHidden = false
            //: self.pictureView.isHidden = false
            self.pictureView.isHidden = false
            //: self.updateUserImgView()
            self.build()
        }
    }

    /// 布局相册
    //: func updateUserImgView() {
    func build() {
        //: self.pictureView.subviews.forEach { $0.removeFromSuperview() }
        self.pictureView.subviews.forEach { $0.removeFromSuperview() }
        //: guard let matchData = self.matchData else { return }
        guard let matchData = self.matchData else { return }

        //: let maxCount = Int((ScreenWidth-60) / 62)
        let maxCount = Int((mainCellMsg - 60) / 62)
        //: for index in 0 ..< min(maxCount, matchData.msgModel.userInfoModel.picture.count) {
        for index in 0 ..< min(maxCount, matchData.msgModel.userInfoModel.picture.count) {
            //: let imgV = UIImageView()
            let imgV = UIImageView()
            //: imgV.layer.cornerRadius = 5
            imgV.layer.cornerRadius = 5
            //: imgV.layer.masksToBounds = true
            imgV.layer.masksToBounds = true
            //: imgV.contentMode = .scaleAspectFill
            imgV.contentMode = .scaleAspectFill
            //: imgV.setUrlImage(urlStr: matchData.msgModel.userInfoModel.picture[index])
            imgV.accessiblePending(urlStr: matchData.msgModel.userInfoModel.picture[index])
            //: self.pictureView.addSubview(imgV)
            self.pictureView.addSubview(imgV)
            //: imgV.snp.remakeConstraints { make in
            imgV.snp.remakeConstraints { make in
                //: make.leading.equalTo(index*62)
                make.leading.equalTo(index * 62)
                //: make.top.equalToSuperview()
                make.top.equalToSuperview()
                //: make.width.height.equalTo(54)
                make.width.height.equalTo(54)
            }
        }
        //: if matchData.msgModel.userInfoModel.picture.count > maxCount {
        if matchData.msgModel.userInfoModel.picture.count > maxCount {
            //: let imgV = UIImageView()
            let imgV = UIImageView()
            //: imgV.image = UIImage.BundleImageNamed(name: "btn_me_edit")
            imgV.image = UIImage.evolutionary(name: (String(const_pullOperationTitle) + String(user_birthdayName)))
            //: self.pictureView.addSubview(imgV)
            self.pictureView.addSubview(imgV)
            //: imgV.snp.remakeConstraints { make in
            imgV.snp.remakeConstraints { make in
                //: make.leading.equalTo(self.pictureView.snp.trailing).offset(-12)
                make.leading.equalTo(self.pictureView.snp.trailing).offset(-12)
                //: make.centerY.equalToSuperview()
                make.centerY.equalToSuperview()
                //: make.width.equalTo(8)
                make.width.equalTo(8)
                //: make.height.equalTo(12)
                make.height.equalTo(12)
            }
        }
    }

    //: override public func updateConstraints() {
    override public func updateConstraints() {
        //: super.updateConstraints()
        super.updateConstraints()
        //: self.container.snp.remakeConstraints { make in
        self.container.snp.remakeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: let userInfoModel = self.matchData?.msgModel.userInfoModel ?? BoundMeasureTrack.init()
        let userInfoModel = self.matchData?.msgModel.userInfoModel ?? BoundMeasureTrack()
        //: if userInfoModel.uid.isEmptyString {
        if userInfoModel.uid.isEmptyString {
            //: self.infoView.isHidden = true
            self.infoView.isHidden = true
            //: self.infoView.snp.remakeConstraints { make in
            self.infoView.snp.remakeConstraints { make in
                //: make.leading.equalTo(15)
                make.leading.equalTo(15)
                //: make.top.width.height.equalTo(0)
                make.top.width.height.equalTo(0)
            }

            //: } else {
        } else {
            //: self.infoView.isHidden = false
            self.infoView.isHidden = false
            //: self.infoView.snp.remakeConstraints { make in
            self.infoView.snp.remakeConstraints { make in
                //: make.leading.equalTo(15)
                make.leading.equalTo(15)
                //: make.top.equalTo(5)
                make.top.equalTo(5)
                //: make.width.equalTo(ScreenWidth-30)
                make.width.equalTo(mainCellMsg - 30)
                //: make.height.equalTo(100)
                make.height.equalTo(100)
            }

            //: vipBgView.snp.remakeConstraints { make in
            vipBgView.snp.remakeConstraints { make in
                //: make.leading.top.width.equalToSuperview()
                make.leading.top.width.equalToSuperview()
                //: make.height.equalTo(actualWidth(w: 78))
                make.height.equalTo(actualWidth(w: 78))
            }

            //: headImgView.snp.remakeConstraints { make in
            headImgView.snp.remakeConstraints { make in
                //: make.leading.top.equalTo(12)
                make.leading.top.equalTo(12)
                //: make.width.height.equalTo(54)
                make.width.height.equalTo(54)
            }

            //: infoNameLab.snp.remakeConstraints { make in
            infoNameLab.snp.remakeConstraints { make in
                //: make.leading.equalTo(headImgView.snp.trailing).offset(12)
                make.leading.equalTo(headImgView.snp.trailing).offset(12)
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: loungeImgV.snp.remakeConstraints { make in
            loungeImgV.snp.remakeConstraints { make in
                //: make.leading.equalTo(infoNameLab.snp.trailing).offset(4)
                make.leading.equalTo(infoNameLab.snp.trailing).offset(4)
                //: make.centerY.equalTo(infoNameLab)
                make.centerY.equalTo(infoNameLab)
                //: make.width.height.equalTo(20)
                make.width.height.equalTo(20)
                //: make.trailing.lessThanOrEqualToSuperview().offset(-12)
                make.trailing.lessThanOrEqualToSuperview().offset(-12)
            }

            //: authImgview.snp.remakeConstraints { make in
            authImgview.snp.remakeConstraints { make in
                //: make.leading.equalTo(infoNameLab)
                make.leading.equalTo(infoNameLab)
                //: make.top.equalTo(infoNameLab.snp.bottom)
                make.top.equalTo(infoNameLab.snp.bottom)
                //: make.width.equalTo(16)
                make.width.equalTo(16)
                //: make.height.equalTo(16)
                make.height.equalTo(16)
            }
            //: sexBtn.snp.remakeConstraints { make in
            sexBtn.snp.remakeConstraints { make in
                //: if userInfoModel.tpAuth {
                if userInfoModel.tpAuth {
                    //: make.leading.equalTo(authImgview.snp.trailing).offset(4)
                    make.leading.equalTo(authImgview.snp.trailing).offset(4)
                    //: } else {
                } else {
                    //: make.leading.equalTo(self.authImgview)
                    make.leading.equalTo(self.authImgview)
                }
                //: make.top.equalTo(authImgview)
                make.top.equalTo(authImgview)
                //: make.width.equalTo(34)
                make.width.equalTo(34)
                //: make.height.equalTo(16)
                make.height.equalTo(16)
            }

            //: var YHeight: CGFloat = 80
            var YHeight: CGFloat = 80
            //: if userInfoModel.interest.count > 0 {
            if userInfoModel.interest.count > 0 {
                //: let inTheight = self.interestsLab.sizeThatFits(CGSize(width: ScreenWidth-54, height: CGFLOAT_MAX)).height
                let inTheight = self.interestsLab.sizeThatFits(CGSize(width: mainCellMsg - 54, height: CGFLOAT_MAX)).height
                //: self.interestsLab.snp.remakeConstraints { make in
                self.interestsLab.snp.remakeConstraints { make in
                    //: make.leading.equalTo(12)
                    make.leading.equalTo(12)
                    //: make.top.equalTo(YHeight)
                    make.top.equalTo(YHeight)
                    //: make.width.equalTo(ScreenWidth-54)
                    make.width.equalTo(mainCellMsg - 54)
                    //: make.height.equalTo(inTheight)
                    make.height.equalTo(inTheight)
                }
                //: YHeight += (inTheight + 8)
                YHeight += (inTheight + 8)
            }
            //: if userInfoModel.picture.count > 0 {
            if userInfoModel.picture.count > 0 {
                //: self.imgTitleLab.snp.remakeConstraints { make in
                self.imgTitleLab.snp.remakeConstraints { make in
                    //: make.leading.equalTo(12)
                    make.leading.equalTo(12)
                    //: make.top.equalTo(YHeight)
                    make.top.equalTo(YHeight)
                    //: make.width.equalTo(ScreenWidth-54)
                    make.width.equalTo(mainCellMsg - 54)
                    //: make.height.equalTo(18)
                    make.height.equalTo(18)
                }
                //: self.pictureView.snp.remakeConstraints { make in
                self.pictureView.snp.remakeConstraints { make in
                    //: make.leading.width.equalTo(imgTitleLab)
                    make.leading.width.equalTo(imgTitleLab)
                    //: make.top.equalTo(imgTitleLab.snp.bottom).offset(6)
                    make.top.equalTo(imgTitleLab.snp.bottom).offset(6)
                    //: make.height.equalTo(54)
                    make.height.equalTo(54)
                }
                //: YHeight += 92
                YHeight += 92
            }
            //: self.infoView.snp.updateConstraints { make in
            self.infoView.snp.updateConstraints { make in
                //: make.height.equalTo(YHeight)
                make.height.equalTo(YHeight)
            }
        }

        //: self.bgView.snp.remakeConstraints { make in
        self.bgView.snp.remakeConstraints { make in
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.top.equalTo(self.infoView.snp.bottom).offset(10)
            make.top.equalTo(self.infoView.snp.bottom).offset(10)
            //: make.width.equalTo(ScreenWidth-30)
            make.width.equalTo(mainCellMsg - 30)
            //: make.height.equalTo(70)
            make.height.equalTo(70)
        }
        //: let labelSize = self.matchLab.sizeThatFits(CGSize(width: (ScreenWidth-30-24), height: CGFLOAT_MAX))
        let labelSize = self.matchLab.sizeThatFits(CGSize(width: mainCellMsg - 30 - 24, height: CGFLOAT_MAX))
        //: self.matchLab.snp.remakeConstraints { make in
        self.matchLab.snp.remakeConstraints { make in
            //: make.leading.equalTo(12)
            make.leading.equalTo(12)
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(labelSize)
            make.size.equalTo(labelSize)
        }
    }
}

// MARK: - 布局

//: extension TalkingChatMatchMsgCell {
extension PersistMust {
    // 添加视图
    //: private func setupSubviews() {
    private func click() {
        //: self.container.addSubview(infoView)
        self.container.addSubview(infoView)
        //: self.infoView.addSubview(vipBgView)
        self.infoView.addSubview(vipBgView)
        //: self.infoView.addSubview(headImgView)
        self.infoView.addSubview(headImgView)
        //: self.infoView.addSubview(infoNameLab)
        self.infoView.addSubview(infoNameLab)
        //: self.infoView.addSubview(loungeImgV)
        self.infoView.addSubview(loungeImgV)
        //: self.infoView.addSubview(authImgview)
        self.infoView.addSubview(authImgview)
        //: self.infoView.addSubview(sexBtn)
        self.infoView.addSubview(sexBtn)
        //: self.infoView.addSubview(interestsLab)
        self.infoView.addSubview(interestsLab)
        //: self.infoView.addSubview(imgTitleLab)
        self.infoView.addSubview(imgTitleLab)
        //: self.infoView.addSubview(pictureView)
        self.infoView.addSubview(pictureView)
        //: self.container.addSubview(bgView)
        self.container.addSubview(bgView)
        //: self.bgView.addSubview(matchLab)
        self.bgView.addSubview(matchLab)
    }

    //: private func bindInteraction() {
    private func betweenPan() {
        //: for  ges in self.container.gestureRecognizers! {
        for ges in self.container.gestureRecognizers! {
            //: self.container.removeGestureRecognizer(ges)
            self.container.removeGestureRecognizer(ges)
        }

        //: let tapGes = UITapGestureRecognizer(target: self, action: #selector(tapCellMessage(_:)))
        let tapGes = UITapGestureRecognizer(target: self, action: #selector(contact(_:)))
        //: tapGes.numberOfTapsRequired = 1
        tapGes.numberOfTapsRequired = 1
        //: tapGes.numberOfTouchesRequired = 1
        tapGes.numberOfTouchesRequired = 1
        //: self.infoView.addGestureRecognizer(tapGes)
        self.infoView.addGestureRecognizer(tapGes)
        //: self.infoView.isUserInteractionEnabled = true
        self.infoView.isUserInteractionEnabled = true
        //: self.bgView.isUserInteractionEnabled = false
        self.bgView.isUserInteractionEnabled = false
    }

    //: @objc func tapCellMessage(_ tapGes: UITapGestureRecognizer) {
    @objc func contact(_ tapGes: UITapGestureRecognizer) {
        //: if self.delegate != nil {
        if self.delegate != nil {
            //: self.delegate?.onSelectMessage(cell: self)
            self.delegate?.skin(cell: self)
        }
    }
}
