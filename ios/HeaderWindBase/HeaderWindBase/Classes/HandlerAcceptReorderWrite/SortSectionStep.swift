
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataAnalyzeStr:[UInt8] = [0x62,0x65,0x62,0x7f,0x23,0x68,0x64,0x6f,0x6e,0x79,0x31,0x22,0x2b,0x63,0x6a,0x78,0x2b,0x65,0x64,0x7f,0x2b,0x69,0x6e,0x6e,0x65,0x2b,0x62,0x66,0x7b,0x67,0x6e,0x66,0x6e,0x65,0x7f,0x6e,0x6f]

private func customerBrush(context num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "Cancel" :*/
fileprivate let app_elementUrl:String = "mask tableCancel"

/*: "Submit" :*/
fileprivate let app_midStr:String = "i product his genderSubmi"
fileprivate let appAfterStr:String = "T"

/*: "Please tell us your problems                   " :*/
fileprivate let data_deviceGlobalFormat:[UInt8] = [0x90,0xac,0xa5,0xa1,0xb3,0xa5,0x60,0xb4,0xa5,0xac,0xac,0x60,0xb5,0xb3,0x60,0xb9,0xaf,0xb5,0xb2,0x60,0xb0,0xb2,0xaf,0xa2,0xac,0xa5,0xad,0xb3,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60]

fileprivate func whoBoy(small num: UInt8) -> UInt8 {
    let value = Int(num) - 64
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "You need to choose at least one." :*/
fileprivate let constMentionMusicStr:[UInt8] = [0xea,0x0,0x6,0xb1,0xff,0xf6,0xf6,0xf5,0xb1,0x5,0x0,0xb1,0xf4,0xf9,0x0,0x0,0x4,0xf6,0xb1,0xf2,0x5,0xb1,0xfd,0xf6,0xf2,0x4,0x5,0xb1,0x0,0xff,0xf6,0xbf]

fileprivate func moreShared(board num: UInt8) -> UInt8 {
    let value = Int(num) + 111
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Please tell us your problems." :*/
fileprivate let appNatureStr:String = "region factor three constructPlease"
fileprivate let constRemoveMsg:String = "us youaspect main maintain need"
fileprivate let appBubbleGirlMsg:String = "r pcloud future"
fileprivate let main_kitId:String = "flowobl"

/*: "app/userReport" :*/
fileprivate let kFillData:[Character] = ["a","p","p","/","u","s","e"]
fileprivate let noti_radioString:String = "rReportanonymous mask continuing current head"

/*: "reason" :*/
fileprivate let k_feeName:String = "reasoptionn"

/*: "desc" :*/
fileprivate let dataSampleKey:[UInt8] = [0x35,0x36,0x44,0x34]

fileprivate func divideAdditionTranslate(cut num: UInt8) -> UInt8 {
    let value = Int(num) + 47
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/report" :*/
fileprivate let noti_recordEndMessage:String = "mf/moadvertising volume large expression void"
fileprivate let constMechanismValue:[Character] = ["/","r","e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let notiCityValue:[UInt8] = [0xcc,0xce,0xcc,0xc4,0xcf,0xd5,0xe8,0xc5]

private func bulletPeople(form num: UInt8) -> UInt8 {
    return num ^ 161
}

/*: "mf/momentReply/report" :*/
fileprivate let app_maintainValue:String = "social bemf/mo"
fileprivate let user_heapId:String = "feed signal foreReply/"
fileprivate let mainItsUrl:String = "repformatrt"

/*: "app/videoReport" :*/
fileprivate let notiFillDatabaseMessage:String = "app/force rank"
fileprivate let constPhasePromptStr:[Character] = ["o","R","e","p","o","r","t"]

/*: "logId" :*/
fileprivate let showMutualUrl:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "Submitted successfully" :*/
fileprivate let data_informationValue:String = "Submivisit gate terms"
fileprivate let main_poolKey:String = " succeimplementation conversation pan tell"
fileprivate let show_windId:[Character] = ["s","s","f","u","l","l","y"]

/*: "Nudity" :*/
fileprivate let const_receiverPath:String = "destroy processing don grain iNudity"

/*: "Violence" :*/
fileprivate let const_modelData:[Character] = ["V","i","o"]
fileprivate let const_buttTitle:[Character] = ["l","e","n","c","e"]

/*: "Harassment" :*/
fileprivate let mainArtWordStr:[Character] = ["H","a","r","a"]
fileprivate let main_todayIsolateMsg:[Character] = ["s","s","m","e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let app_receivePath:String = "light return cycle mpSuic"
fileprivate let kTempValue:[Character] = ["r"," ","S","e","l","f","-","i","n","j","u","r","y"]

/*: "False Information" :*/
fileprivate let app_giveSimultaneouslyId:String = "Falserandom more gallery daily"
fileprivate let constDensityRobotServerFormat:String = "rmatiowhite"

/*: "Spam" :*/
fileprivate let user_galleryKey:String = "never faceSpam"

/*: "Hate Speech" :*/
fileprivate let notiGraphicMessage:String = "custom aboutHate Sp"
fileprivate let user_donPlainId:String = "EECH"

/*: "Terrorism" :*/
fileprivate let dataFollowTitle:String = "earn gift context inviteTerro"
fileprivate let app_visualSumSmallData:String = "risagile"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let constComponentString:[UInt8] = [0x30,0x2,0xf,0x6,0x43,0xc,0x5,0x43,0x2a,0xf,0xf,0x6,0x4,0x2,0xf,0x43,0xc,0x11,0x43,0x31,0x6,0x4,0x16,0xf,0x2,0x17,0x6,0x7,0x43,0x24,0xc,0xc,0x7,0x10]

/*: "Sending disgusting messages" :*/
fileprivate let app_hiddenCircleFormatUrl:[Character] = ["S","e","n","d","i","n","g"," ","d","i","s","g","u","s","t","i","n","g"," ","m","e","s"]
fileprivate let main_versusBrownKey:String = "srefusees"

/*: "Using photos of nudity or drugs" :*/
fileprivate let notiAbsName:[UInt8] = [0x73,0x67,0x75,0x72,0x64,0x20,0x72,0x6f,0x20,0x79,0x74,0x69,0x64,0x75,0x6e,0x20,0x66,0x6f,0x20,0x73,0x6f,0x74,0x6f,0x68,0x70,0x20,0x67,0x6e,0x69,0x73,0x55]

/*: "Harassing or sending ads" :*/
fileprivate let const_uniformResolveMessage:String = "both agile circle musicHara"
fileprivate let constCropSinceValue:String = "g or sbuild check reply name without"
fileprivate let notiArchitectureFormat:String = "g adsfeedback provision para"

/*: "Defraud or using fake info" :*/
fileprivate let mainSpeakerManagerMsg:String = "commit highlight tellDefrau"
fileprivate let noti_mobileTitle:String = "d ordelivery ready half"
fileprivate let user_answerId:String = " fakepicture hold player integration quality"

/*: "Others" :*/
fileprivate let kIncomeHoldData:String = "fail nor date foundation emptyOthers"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SortSectionStep.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum ActionCustom: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class SortSectionStep: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: ActionCustom?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [ExceptFragment] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = ExceptFragment()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: ActionCustom?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        dismiss()
        //: setupSubViewsConstraint()
        destroy()
        //: setupReasons()
        prepare()
        //: bindInteraction()
        succeed()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataAnalyzeStr.map{customerBrush(context: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(app_elementUrl.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.alongOccur(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.stepTransition(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.alongOccur().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(might), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(app_midStr.suffix(5)) + appAfterStr.lowercased()).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.stepTransition(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.untilFactory(colors: UIColor.opInMay(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(weight), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: mainCellMsg - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.roundItem(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = NextView(placeholder: String(bytes: data_deviceGlobalFormat.map{whoBoy(small: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.capBy(), frame: CGRect(x: 10, y: 10, width: mainCellMsg - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .stepTransition(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.labelMomentum()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .stepTransition(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.roundItem(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(GemReduce.self, forCellReuseIdentifier: GemReduce.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension SortSectionStep {
    //: @objc func func__TapGestureRecognizer() {
    @objc func put() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func tillCon(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = StopBrush.getWindow()
            let window = StopBrush.correct()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func distanceForMay(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func might() {
        //: self.dismissReportView(succeed: false)
        self.distanceForMay(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func weight() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.signWhen(showMsg: String(bytes: constMentionMusicStr.map{moreShared(board: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.signWhen(showMsg: (String(appNatureStr.suffix(6)) + " tell " + String(constRemoveMsg.prefix(6)) + String(appBubbleGirlMsg.prefix(3)) + main_kitId.replacingOccurrences(of: "flow", with: "r") + "ems.").localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(kFillData) + String(noti_radioString.prefix(7)))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (k_feeName.replacingOccurrences(of: "option", with: "o")): selectedModel.type, String(bytes: dataSampleKey.map{divideAdditionTranslate(cut: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(noti_recordEndMessage.prefix(5)) + "ment" + String(constMechanismValue))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: notiCityValue.map{bulletPeople(form: $0)}, encoding: .utf8)!: tempId, (k_feeName.replacingOccurrences(of: "option", with: "o")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(app_maintainValue.suffix(5)) + "ment" + String(user_heapId.suffix(6)) + mainItsUrl.replacingOccurrences(of: "format", with: "o"))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (k_feeName.replacingOccurrences(of: "option", with: "o")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(notiFillDatabaseMessage.prefix(4)) + "vide" + String(constPhasePromptStr))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: showMutualUrl.reversed(), encoding: .utf8)!: tempId, (k_feeName.replacingOccurrences(of: "option", with: "o")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        PrepareReplace.towardShould(reqPath: path, reqParams: param) { succeed, result, errorModel in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.notFade(showMsg: (String(data_informationValue.prefix(5)) + "tted" + String(main_poolKey.prefix(6)) + String(show_windId)).localized)
                //: self.dismissReportView(succeed: true)
                self.distanceForMay(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension SortSectionStep: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in tableView: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: GemReduce.className()) as! GemReduce
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.dimension(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension SortSectionStep {
    /// 添加视图
    //: private func setupSubviews() {
    private func dismiss() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func destroy() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func prepare() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(const_receiverPath.suffix(6))).localized,
                           //: "Violence".localized,
                           (String(const_modelData) + String(const_buttTitle)).localized,
                           //: "Harassment".localized,
                           (String(mainArtWordStr) + String(main_todayIsolateMsg)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(app_receivePath.suffix(4)) + "ide o" + String(kTempValue)).localized,
                           //: "False Information".localized,
                           (String(app_giveSimultaneouslyId.prefix(5)) + " Info" + constDensityRobotServerFormat.replacingOccurrences(of: "white", with: "n")).localized,
                           //: "Spam".localized,
                           (String(user_galleryKey.suffix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(notiGraphicMessage.suffix(7)) + user_donPlainId.lowercased()).localized,
                           //: "Terrorism".localized,
                           (String(dataFollowTitle.suffix(5)) + app_visualSumSmallData.replacingOccurrences(of: "agile", with: "m")).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: constComponentString.map{$0^99}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(app_hiddenCircleFormatUrl) + main_versusBrownKey.replacingOccurrences(of: "refuse", with: "ag")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: notiAbsName.reversed(), encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(const_uniformResolveMessage.suffix(4)) + "ssin" + String(constCropSinceValue.prefix(6)) + "endin" + String(notiArchitectureFormat.prefix(5))).localized,
                           //: "Defraud or using fake info".localized,
                           (String(mainSpeakerManagerMsg.suffix(6)) + String(noti_mobileTitle.prefix(4)) + " using" + String(user_answerId.prefix(5)) + " info").localized,
                           //: "Others".localized]
                           (String(kIncomeHoldData.suffix(6))).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = MountCustom.share.appConfigMode.videoReport["reason"] {
            if let reason = MountCustom.share.appConfigMode.videoReport[(k_feeName.replacingOccurrences(of: "option", with: "o"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = ExceptFragment()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func succeed() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] recognizer in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
