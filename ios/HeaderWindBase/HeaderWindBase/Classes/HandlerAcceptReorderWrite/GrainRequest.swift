
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let data_displayMsg:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let mainSeaString:String = "subject hung transfer number ceilingLoad."
fileprivate let userDensityBuilderPath:String = ".."

/*: "Skip" :*/
fileprivate let noti_monthStr:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let noti_secretPath:[UInt8] = [0x68,0x54,0x54,0x50,0x74,0x4f,0x4f,0x4c,0xe,0x6e,0x41,0x54,0x49,0x56,0x45,0x74,0x4f,0x6a,0x53,0x8,0x7,0x52,0x45,0x43,0x48,0x41,0x52,0x47,0x45,0x7,0x9]

private func purpleCluster(core num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let appTrainMsg:[UInt8] = [0x23,0x15,0x1a,0x10,0x1b,0x23,0xda,0x1b,0x1a,0xfc,0xd,0x13,0x11,0xff,0x14,0x1b,0x23,0xd2,0xd2,0x1b,0x1a,0xfc,0xd,0x13,0x11,0xff,0x14,0x1b,0x23,0xd4,0xd5,0xe7]

fileprivate func maxRemind(reader num: UInt8) -> UInt8 {
    let value = Int(num) + 84
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let constMemberStr:String = "honey isolate artjsEv"
fileprivate let app_rateStr:[Character] = ["n","P","a","g","e","S","h","o","w",")",":"," "]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let main_jumpHealthyName:[UInt8] = [0x3b,0x29,0x28,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageHide):  :*/
fileprivate let notiChildData:String = "jsEveconcrete mix continuing framework"
fileprivate let appContextCurStr:String = "chart filterPag"
fileprivate let constGameCoordinateStr:String = "eHide): constraint examine better"

/*: "title" :*/
fileprivate let mainAppearancePath:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let kPackPath:String = "canGoBaofficial date process"
fileprivate let notiHeadServerPath:String = "cbound"

/*: "PHPSESSID= :*/
fileprivate let app_injurySpeedMessage:String = "dayHdaySE"
fileprivate let app_boardFormat:String = "mutual awake untilSSID="

/*: ";uid= :*/
fileprivate let notiDestinationData:String = "cover priority for region;uid="

/*: "Cookie" :*/
fileprivate let kUnlessTipUrl:String = "word ofCookie"

/*: "nav_back_black_nor" :*/
fileprivate let userIncludePath:String = "nav_btemp don operate disabled"
fileprivate let const_generalComplyDimensionKey:String = "lack_nonorm going visual recognize nobody"
fileprivate let dataSignalId:String = "drawer"

/*: "ico_intimate_list_sm" :*/
fileprivate let notiRemoteKey:[Character] = ["i","c","o","_","i","n","t","i"]
fileprivate let show_changeFormat:String = "mate_limic mention"
fileprivate let dataMaxUrl:String = "charm none mode modify todayst_sm"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let data_imageDistanceFormat:[UInt8] = [0xb8,0xb3,0xbf,0xa9,0xb1,0xb9,0xb2,0xa8,0xf2,0xbb,0xb9,0xa8,0x99,0xb0,0xb9,0xb1,0xb9,0xb2,0xa8,0xaf,0x9e,0xa5,0x88,0xbd,0xbb,0x92,0xbd,0xb1,0xb9,0xf4,0xfb,0xbe,0xb3,0xb8,0xa5,0xfb,0xf5,0x87,0xec,0x81,0xf2,0xaf,0xa8,0xa5,0xb0,0xb9,0xf2,0xbe,0xbd,0xbf,0xb7,0xbb,0xae,0xb3,0xa9,0xb2,0xb8,0xe1,0xfb,0xae,0xbb,0xbe,0xbd,0xf4,0xec,0xf0,0xec,0xf0,0xec,0xf0,0xec,0xf5,0xfb]

private func receiveRoot(reject num: UInt8) -> UInt8 {
    return num ^ 220
}

/*: "Alert" :*/
fileprivate let dataLightPath:String = "Alertam bridge pro about"

/*: "Cancle" :*/
fileprivate let const_siblingPageStr:[Character] = ["C","a","n","c","l"]
fileprivate let dataMUrl:String = "E"

/*: "Done" :*/
fileprivate let main_mergeValue:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let showTensionName:[Character] = ["j","s"," ","c","a","l"]
fileprivate let showStackHelpKey:String = "same"
fileprivate let const_bodyPhoneData:String = "if character gold what want met"
fileprivate let show_translationName:String = "group you fee waistame = "

/*: , message =  :*/
fileprivate let userAbsoluteName:[Character] = [","," ","m","e","s","s","a","g","e"," ","="," "]

/*: "http://" :*/
fileprivate let kStimulateDoingerString:String = "error alongside text stimulate confirmhttp://"

/*: "https://" :*/
fileprivate let constImpactStr:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let dataBroadSubStr:[UInt8] = [0xdc,0xcb,0xd8,0x8a,0xc9,0xc5,0xc5,0xc1,0xc3,0xcf,0xe4,0xcb,0xc7,0xcf,0xd9,0x8a,0x97,0x8a,0xce,0xc5,0xc9,0xdf,0xc7,0xcf,0xc4,0xde,0x84,0xc9,0xc5,0xc5,0xc1,0xc3,0xcf,0x84,0xd9,0xda,0xc6,0xc3,0xde,0x82,0x8d,0x91,0x8a,0x8d,0x83,0x84,0xc7,0xcb,0xda,0x82,0xcc,0xdf,0xc4,0xc9,0xde,0xc3,0xc5,0xc4,0x82,0xc9,0xc5,0xc5,0xc1,0xc3,0xcf,0x83,0x8a,0xd1,0x8a,0xd8,0xcf,0xde,0xdf,0xd8,0xc4,0x8a,0xc9,0xc5,0xc5,0xc1,0xc3,0xcf,0x84,0xd9,0xda,0xc6,0xc3,0xde,0x82,0x8d,0x97,0x8d,0x83,0xf1,0x9a,0xf7,0x8a,0xd7,0x8a,0x83,0x91,0xa0]

private func regularCos(destination num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "uid= :*/
fileprivate let appEvolutionSaveString:String = "uid=lose i sub"

/*: ;domain= :*/
fileprivate let dataTopTitle:[Character] = [";","d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let noti_hisYieldId:String = "lite mask box;path=/"

/*: "document.cookie=' :*/
fileprivate let userExpectedTitle:String = "purchasecumen"
fileprivate let userMarkValue:String = "year center meet maximum everyonet.c"
fileprivate let kEffUrl:[Character] = ["o","o","k","i","e","=","\'"]

/*: ;domin= :*/
fileprivate let userLocalValue:String = ";domin=observer material we roman domain"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let dataNeedMessage:[UInt8] = [0x18,0x17,0x51,0x59,0x12,0x1e,0x1e,0x1a,0x18,0x14,0x3f,0x10,0x1c,0x14,0x2,0x5f,0x18,0x1f,0x15,0x14,0x9,0x3e,0x17,0x59,0x56,0x21,0x39,0x21,0x22,0x34,0x22,0x22,0x38,0x35,0x56,0x58,0x51,0x4c,0x4c,0x51,0x5c,0x40,0x58,0x51,0xa,0x51,0x15,0x1e,0x12,0x4,0x1c,0x14,0x1f,0x5,0x5f,0x12,0x1e,0x1e,0x1a,0x18,0x14,0x4c,0x56]

/*: "h5Domain =  :*/
fileprivate let user_calculationFormat:String = "h5Dompurchase resume sign"
fileprivate let noti_levelLayValue:String = "ain = front bean shot build maintain"

/*: "getAllCookies =  :*/
fileprivate let show_tierOnceUrl:[Character] = ["g","e","t","A","l","l"]
fileprivate let k_myId:String = "Cookiassert observer architecture max behind"

/*: "PHPSESSID" :*/
fileprivate let data_zzId:String = "PsinceSESS"
fileprivate let userCircleMessage:String = "Ichannel"

/*: "uid" :*/
fileprivate let main_launchName:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let dataItsReceiverString:String = "characteristic target cookie exitappCo"
fileprivate let noti_mapAskKey:String = "first=first"

/*: "UID" :*/
fileprivate let show_revealPadPath:String = "transformID"

/*: "navigator.userAgent" :*/
fileprivate let show_indexMidEaseString:String = "licenseavi"
fileprivate let userDomainFitFormat:String = ".userAelse tween"
fileprivate let mainIncludeTitle:String = "GENT"

/*: "xunwu_iphone" :*/
fileprivate let userTitlePath:[Character] = ["x","u","n","w","u"]
fileprivate let dataBassUrl:String = "_iphoneheavy log holder connect particle"

/*: " xunwu_device_id/ :*/
fileprivate let constUntilAlwaysData:String = " xunwuexplain loop"
fileprivate let const_closedData:String = "ice_id/component speed"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let data_specifyTitle:String = "xunrelation"
fileprivate let kObserverValue:String = "plain radioone xu"
fileprivate let data_heartMirrorData:[Character] = ["r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let userRidName:String = " xunpriority size composition sign"
fileprivate let user_platformMsg:String = "actensiong"
fileprivate let notiCommonMsg:String = "our object pop push movemente/"

/*:  xunwu_bundleId/ :*/
fileprivate let k_descriptionHelpRoundingMusicStr:[Character] = [" ","x","u","n","w","u"]
fileprivate let mainKindTitle:[Character] = ["_","b","u","n","d","l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let dataTagMsg:[Character] = [" "," ","w","e","b","k","i","t","/","1"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GrainRequest.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class GrainRequest: ReplaceMaster {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: EquipmentImplementation!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: FractionMoveButt?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: FractionMoveButt?, webConfig: EquipmentImplementation) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: data_displayMsg.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        sensorDownConfirm()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        termsIn()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(mainSeaString.suffix(5)) + userDensityBuilderPath.capitalized)
        //: if config.isHideBackBtn {
        if config.isHideBackBtn {
            //: hideNavi = true
            hideNavi = true
        }
        //: addNotification()
        sumScope()
        //: func__checkConfig()
        pastVersion()
        //: func__setUpWebView()
        tabTask()
        //: func__registerObserver()
        yellowByBelow()
        //: func__beginStartRequest()
        exceptJudgeVersus()
        //: func_checkAppleIAP()
        isolateDown()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: RetainProcess.share.func__pushToWebVC(webViewType: .DiscountPopup)
                RetainProcess.share.versusResManageLocalUniqueColorExit(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: const_naturalStr, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            opPlace(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.alongOccur(), .font: UIFont.stepTransition(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(noti_monthStr)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension GrainRequest {
    /// 添加通知
    //: private func addNotification() {
    private func sumScope() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(bodySkin),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: k_lastPath,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(sensorDownConfirm),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func bodySkin() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: noti_secretPath.map{purpleCluster(core: $0)}, encoding: .utf8)!) { data, error in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func sensorDownConfirm() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: appTrainMsg.map{maxRemind(reader: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(constMemberStr.suffix(4)) + "ent(o" + String(app_rateStr)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func termsIn() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: main_jumpHealthyName.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(notiChildData.prefix(5)) + "nt(on" + String(appContextCurStr.suffix(3)) + String(constGameCoordinateStr.prefix(8))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension GrainRequest {
    //: func func__registerObserver() {
    func yellowByBelow() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(mainAppearancePath)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(kPackPath.prefix(7)) + notiHeadServerPath.replacingOccurrences(of: "bound", with: "k")))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(withMomentum), name: noti_mountFlowFormat, object: nil)
    }

    //: func func__setUpWebView() {
    func tabTask() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        loadSize()
        //: func__addBridgeMethod()
        extraNatural()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.boardUnexpected()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            LevelBoxIndex.someDisplaySub(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(data_visitorPath)
            }
        }
    }

    //: func func__beginStartRequest() {
    func exceptJudgeVersus() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.rawProcess()
            }
            //: } else {
        } else {
            //: func__startRequest()
            rawProcess()
        }
    }

    //: func func__startRequest() {
    func rawProcess() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = MountCustom.share.loginUid
        let uid = MountCustom.share.loginUid
        //: let ssid = MountCustom.share.loginSessionId
        let ssid = MountCustom.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (app_injurySpeedMessage.replacingOccurrences(of: "day", with: "P") + String(app_boardFormat.suffix(5))) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(notiDestinationData.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(kUnlessTipUrl.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        sectionIndependent()
        //: func__initRightButton()
        structureInsideCaptureWhite()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func isolateDown() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            ReductionInclude.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.adjustExcept(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            ReductionInclude.shared.easyLab()
        }
    }

    //: func func__initLeftButton() {
    func enableVolume() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.evolutionary(name: (String(userIncludePath.prefix(5)) + "ack_b" + String(const_generalComplyDimensionKey.prefix(7)) + dataSignalId.replacingOccurrences(of: "drawer", with: "r"))), style: .plain, target: self, action: #selector(tipAway(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func structureInsideCaptureWhite() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(FractionMoveButt.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.evolutionary(name: (String(notiRemoteKey) + String(show_changeFormat.prefix(7)) + String(dataMaxUrl.suffix(5)))), style: .plain, target: self, action: #selector(charm))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func tipAway(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        progressLanguage()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.alongLarge() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: GrainRequest.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! GrainRequest).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! GrainRequest).sensorDownConfirm()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func pastVersion() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func minimizeHolder(sucess: Bool) {
        //: ProgressHUD.dismiss()
        LevelBoxIndex.gestureOff()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func sectionIndependent() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: data_imageDistanceFormat.map{receiveRoot(reject: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func okNet(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: RetainProcess.share.func__pushToWebVC(urlStr: url)
            RetainProcess.share.mpCurrency(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func keepWould(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func withMomentum() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: noti_mountFlowFormat, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func charm() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = CustomBeyondPresent(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
        //: view.show()
        view.bounce()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension GrainRequest: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            LevelBoxIndex.someDisplaySub(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            minimizeHolder(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        enableVolume()
        //: func__clearJSBgColor()
        sectionIndependent()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            minimizeHolder(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            minimizeHolder(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(dataLightPath.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(const_siblingPageStr) + dataMUrl.lowercased()), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(main_mergeValue)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension GrainRequest: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(showTensionName) + showStackHelpKey.replacingOccurrences(of: "same", with: "l") + String(const_bodyPhoneData.suffix(4)) + "hod n" + String(show_translationName.suffix(6))) + "\(message.name)" + (String(userAbsoluteName)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.tapYear(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func extraNatural() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in EnableMethod.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func progressLanguage() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in EnableMethod.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension GrainRequest {
    //: func func__addUserScript() {
    func awayIntervaleraction() {
        //: var htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
        var htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(kStimulateDoingerString.suffix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(kStimulateDoingerString.suffix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(constImpactStr))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(constImpactStr)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = MountCustom.share.loginUserMode.userID
        let tempUid: String = MountCustom.share.loginUserMode.userID
        //: let tempSessionID: String = MountCustom.share.loginSessionId
        let tempSessionID: String = MountCustom.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: dataBroadSubStr.map{regularCos(destination: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(appEvolutionSaveString.prefix(4))) + "\(tempUid)" + (String(dataTopTitle)) + "\(htmlDomain)" + (String(noti_hisYieldId.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((userExpectedTitle.replacingOccurrences(of: "purchase", with: "do") + String(userMarkValue.suffix(3)) + String(kEffUrl)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (app_injurySpeedMessage.replacingOccurrences(of: "day", with: "P") + String(app_boardFormat.suffix(5))) + "\(tempSessionID)" + (String(userLocalValue.prefix(7))) + "\(htmlDomain)" + (String(noti_hisYieldId.suffix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((userExpectedTitle.replacingOccurrences(of: "purchase", with: "do") + String(userMarkValue.suffix(3)) + String(kEffUrl)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: dataNeedMessage.map{$0^113}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(appEvolutionSaveString.prefix(4))) + "\(tempUid)" + (String(dataTopTitle)) + "\(htmlDomain)" + (String(noti_hisYieldId.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((userExpectedTitle.replacingOccurrences(of: "purchase", with: "do") + String(userMarkValue.suffix(3)) + String(kEffUrl)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(appEvolutionSaveString.prefix(4))) + "\(tempSessionID)" + (String(dataTopTitle)) + "\(htmlDomain)" + (String(noti_hisYieldId.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((userExpectedTitle.replacingOccurrences(of: "purchase", with: "do") + String(userMarkValue.suffix(3)) + String(kEffUrl)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func opPlace(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
            let htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(user_calculationFormat.prefix(5)) + String(noti_levelLayValue.prefix(6))) + "\(htmlDomain)")
            //: let userID: String = MountCustom.share.loginUserMode.userID
            let userID: String = MountCustom.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(show_tierOnceUrl) + String(k_myId.prefix(5)) + "es = ") + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (data_zzId.replacingOccurrences(of: "since", with: "HP") + userCircleMessage.replacingOccurrences(of: "channel", with: "D")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(main_launchName)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.exceptJudgeVersus()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.noKind(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func noKind(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
                let htmlDomain: String = MountCustom.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(user_calculationFormat.prefix(5)) + String(noti_levelLayValue.prefix(6))) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: PutDirection.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(dataItsReceiverString.suffix(5)) + "okies" + noti_mapAskKey.replacingOccurrences(of: "first", with: " ")) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (show_revealPadPath.replacingOccurrences(of: "transform", with: "U")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(main_launchName))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (data_zzId.replacingOccurrences(of: "since", with: "HP") + userCircleMessage.replacingOccurrences(of: "channel", with: "D")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (data_zzId.replacingOccurrences(of: "since", with: "HP") + userCircleMessage.replacingOccurrences(of: "channel", with: "D"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.exceptJudgeVersus()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func loadSize() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((show_indexMidEaseString.replacingOccurrences(of: "license", with: "n") + "gator" + String(userDomainFitFormat.prefix(6)) + mainIncludeTitle.lowercased())) { [weak self] result, err in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (show_indexMidEaseString.replacingOccurrences(of: "license", with: "n") + "gator" + String(userDomainFitFormat.prefix(6)) + mainIncludeTitle.lowercased()))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(userTitlePath) + String(dataBassUrl.prefix(7)))) {
                //: let str = getUserAgentString()
                let str = romanNear()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.beforeMethod { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(constUntilAlwaysData.prefix(6)) + "_dev" + String(const_closedData.prefix(7))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func romanNear() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (data_specifyTitle.replacingOccurrences(of: "relation", with: "w") + "u_iph" + String(kObserverValue.suffix(6)) + "nwu_ve" + String(data_heartMirrorData)) + "\(kJoinKey)" + (String(userRidName.prefix(4)) + "wu_p" + user_platformMsg.replacingOccurrences(of: "tension", with: "ka") + String(notiCommonMsg.suffix(2))) + "\(data_scientificComposeFormat)" + (String(k_descriptionHelpRoundingMusicStr) + String(mainKindTitle)) + "\(show_embraceValue)" + (String(dataTagMsg))
    }
}
