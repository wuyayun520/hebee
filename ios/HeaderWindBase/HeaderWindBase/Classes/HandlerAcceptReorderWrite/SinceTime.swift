
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_markerPath:[UInt8] = [0x84,0x83,0x84,0x99,0xc5,0x8e,0x82,0x89,0x88,0x9f,0xd7,0xc4,0xcd,0x85,0x8c,0x9e,0xcd,0x83,0x82,0x99,0xcd,0x8f,0x88,0x88,0x83,0xcd,0x84,0x80,0x9d,0x81,0x88,0x80,0x88,0x83,0x99,0x88,0x89]

/*: "#128CFF" :*/
fileprivate let showExistingFormat:String = "#128CFFrepeat dinner design ting expression"

/*: "Feedback" :*/
fileprivate let noti_beMarkName:String = "Feedpersonal onto shot contain segment"

/*: "#8370FF" :*/
fileprivate let noti_portStr:String = "#8370FFcalled top"

/*: "Quick Greeting" :*/
fileprivate let mainConditionValue:[Character] = ["Q","u","i","c","k"," ","G","r","e","e"]
fileprivate let noti_colorMsg:[Character] = ["t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let notiSheUrl:String = "element succeedBeaut"
fileprivate let constMirrorData:String = "bridgetbridges"

/*: "url" :*/
fileprivate let main_stimulateCustomTitle:String = "assetl"

/*: "unlockGift" :*/
fileprivate let notiInvisibleUrl:String = "area search ya general specifyunlockG"
fileprivate let data_resignMissKey:String = "frequencyft"

/*: "isBurn" :*/
fileprivate let dataPropertyString:[UInt8] = [0x10,0xa,0x3b,0xc,0xb,0x17]

private func donPlus(difficult num: UInt8) -> UInt8 {
    return num ^ 121
}

/*: "giftId" :*/
fileprivate let data_sceneFeedId:String = "creation layoff shadowgiftI"
fileprivate let main_closedMsg:String = "medium"

/*: "rYMsgType" :*/
fileprivate let show_provisionKey:[Character] = ["r","Y","M","s","g","T","y"]
fileprivate let noti_projectRangeString:String = "PE"

/*: "GJ:CallCustom" :*/
fileprivate let user_liveBrownMyMessage:String = "GJ:CallCgate handle library"
fileprivate let noti_destroyKey:[Character] = ["u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let userDisableLackName:String = "alive comment fire someone scanchangeDi"
fileprivate let notiSignalMsg:String = "rectiotransfer"

/*: "extra" :*/
fileprivate let k_redOrientationStr:String = "extrgoing"

/*: "msgInfo" :*/
fileprivate let appCurrentPath:String = "msgInthen butt track"
fileprivate let kInjuryTitle:[Character] = ["f","o"]

/*: "toUid" :*/
fileprivate let dataBeatValue:String = "toUidattribute giving face"

/*: "wantGift" :*/
fileprivate let notiTranslationTailKey:String = "wenhance"
fileprivate let mainOrangeMessage:[Character] = ["n","t","G","i","f","t"]

/*: "jumpKey" :*/
fileprivate let appUpdatelyUrl:[Character] = ["j","u","m","p","K","e","y"]

/*: "logType" :*/
fileprivate let k_turnStr:[Character] = ["l","o","g","T","y","p","e"]

/*: "content" :*/
fileprivate let main_pauseSplitStr:[UInt8] = [0xe9,0xe5,0xe4,0xfe,0xef,0xe4,0xfe]

private func mainData(what num: UInt8) -> UInt8 {
    return num ^ 138
}

/*: "msgType" :*/
fileprivate let noti_numbValue:[UInt8] = [0x65,0x70,0x79,0x54,0x67,0x73,0x6d]

/*: "txt" :*/
fileprivate let mainResponseStr:String = "movementxmovement"

/*: "message" :*/
fileprivate let dataUnlessData:[UInt8] = [0x9c,0x94,0x82,0x82,0x90,0x96,0x94]

private func clusterMeet(ceiling num: UInt8) -> UInt8 {
    return num ^ 241
}

/*: "replayId" :*/
fileprivate let noti_yaId:[UInt8] = [0xf4,0xe7,0xf2,0xee,0xe3,0xfb,0xcb,0xe6]

fileprivate func commentMeetingPerform(there num: UInt8) -> UInt8 {
    let value = Int(num) - 130
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let mainClickString:[Character] = ["t","y","p","e"]

/*: "TxtMsg" :*/
fileprivate let app_projectFemaleFormat:String = "TxtMsgrelation marker air purchase"

/*: "params" :*/
fileprivate let app_hungString:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "MultiQuickMsg" :*/
fileprivate let constMeasureMsg:String = "significant evaluation indexMult"
fileprivate let noti_nativeFormat:[Character] = ["k","M","s","g"]

/*: "QuickMsg" :*/
fileprivate let show_cloudName:String = "QuickMsgresolve maintain volume text recording"

/*: "txtId" :*/
fileprivate let constRatingPath:String = "safe appointed index timingtxtId"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let appCommentTranslationName:[UInt8] = [0xb7,0x8e,0x88,0x82,0x84,0xc1,0x89,0x84,0x8d,0x8d,0x8e,0xc1,0x85,0x8e,0x96,0x8f,0x8d,0x8e,0x80,0x85,0xc1,0x87,0x80,0x88,0x8d,0x84,0x85,0xcf,0xc1,0xb1,0x93,0x8e,0x82,0x84,0x85,0x94,0x93,0x84]

private func alterHeavy(check num: UInt8) -> UInt8 {
    return num ^ 225
}

/*: "length" :*/
fileprivate let appBindForwardPath:String = "explainength"

/*: "audio" :*/
fileprivate let notiDateLargeConstraintValue:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let dataFirstWhoName:String = "auddecrease"
fileprivate let showLeastData:String = "Idcycle next insert"

/*: "audioLength" :*/
fileprivate let dataBorderTitle:[UInt8] = [0xc6,0xda,0xc9,0xce,0xd4,0xb1,0xca,0xd3,0xcc,0xd9,0xcd]

fileprivate func finishMaker(sum num: UInt8) -> UInt8 {
    let value = Int(num) + 155
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let dataJumpAdviceStr:[Character] = ["a","u","d","i","o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let userMixString:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x6e,0x65,0x73]

/*: "AudioMsg" :*/
fileprivate let noti_trackAwakeValue:String = "Audicontinue ease fee"

/*: "Can't send blank message" :*/
fileprivate let dataAppealValue:[Character] = ["C","a","n","\'","t"," ","s","e","n"]
fileprivate let dataSunMessage:String = "d blaclear week"
fileprivate let show_unexpectedString:String = "ssdrowne"

/*: "quoteMsgId" :*/
fileprivate let notiThumbId:String = "shadeote"
fileprivate let dataFromKey:String = "MsgIdexisting implement document correct"

/*: "quoteMsgInfo" :*/
fileprivate let userRecentReservePath:String = "QUOTE"

/*: "contentType" :*/
fileprivate let dataOutputPath:[UInt8] = [0xc5,0xc9,0xc8,0xd2,0xc3,0xc8,0xd2,0xf2,0xdf,0xd6,0xc3]

/*: "ImgMsg" :*/
fileprivate let showBetweenFormat:String = "ImgMscur info"
fileprivate let mainProduceTitle:[Character] = ["g"]

/*: "img" :*/
fileprivate let show_densityEmptyValue:String = "ilayer"

/*: "imageUri" :*/
fileprivate let data_reflectComplyName:[UInt8] = [0xa1,0xa5,0xa9,0xaf,0xad,0x9d,0xba,0xa1]

/*: "customSend" :*/
fileprivate let appContactAppealData:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let notiPackUrl:[Character] = ["p","i","c"]

/*: "VideoMsg" :*/
fileprivate let appCountName:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "video" :*/
fileprivate let app_accountValue:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let constGroupPath:[UInt8] = [0x95,0xa1,0xa8,0x97,0xa4,0x7b,0x9f,0x99]

fileprivate func averageWed(m num: UInt8) -> UInt8 {
    let value = Int(num) - 50
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "videoUrl" :*/
fileprivate let userSizeMessage:[UInt8] = [0x2d,0x20,0x1b,0x1c,0x26,0xc,0x29,0x23]

fileprivate func valueLocal(turn num: UInt8) -> UInt8 {
    let value = Int(num) + 73
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "duration" :*/
fileprivate let notiPastMessage:[UInt8] = [0x14,0x5,0x2,0x11,0x4,0x19,0x1f,0x1e]

private func iRetain(reject num: UInt8) -> UInt8 {
    return num ^ 112
}

/*: "len" :*/
fileprivate let kSunId:[UInt8] = [0xb7,0xb0,0xb9]

fileprivate func rootComplyComposition(strategy num: UInt8) -> UInt8 {
    let value = Int(num) + 181
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Documents/User/voice/" :*/
fileprivate let data_remainId:String = "easy despite onto true afterDocum"
fileprivate let mainGenderMessage:[Character] = ["e","n","t","s","/","U","s","e","r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let notiMountMessage:String = "Documlaunch only mid"
fileprivate let notiDueMovementMsg:String = "entmenu"
fileprivate let userAppropriateValue:[Character] = ["c","o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let showClubPath:String = "healthy page fire suspend buttonmsgI"

/*: "messageType" :*/
fileprivate let show_topMessage:String = "MESS"
fileprivate let userCoreString:String = "wee"

/*: "WantGift" :*/
fileprivate let userSensorItemName:String = "inside"
fileprivate let kAPath:[Character] = ["a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let mainProcessStr:String = "_Privabirthday nor inside whole"
fileprivate let kAverageUrl:String = "psTikey accelerate"

/*: "msgId" :*/
fileprivate let appPushFormat:String = "scan replacement environment priormsgId"

/*: "totalMfCoin" :*/
fileprivate let appRecognizeData:[Character] = ["t","o","t","a","l","M","f","C","o"]
fileprivate let kAdvertisingMessage:[Character] = ["i","n"]

/*: "attachSysMsg" :*/
fileprivate let noti_interruptMessage:String = "acoordinateac"

/*: "intimatePhoto" :*/
fileprivate let userLastAddString:String = "bagtim"
fileprivate let userReductionStr:String = "versiono"

/*: "totalIntimate" :*/
fileprivate let main_yourselfYieldKey:String = "kind remain decisiontotalIn"
fileprivate let kMonthSignalFormat:[Character] = ["t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let appResultId:[UInt8] = [0xd5,0xc9,0xc4]

private func cornerAspect(thin num: UInt8) -> UInt8 {
    return num ^ 160
}

/*: "intimate" :*/
fileprivate let kPartyAgainMsg:[UInt8] = [0x7,0x0,0x1a,0x7,0x3,0xf,0x1a,0xb]

private func blackAssociated(mp num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let main_enoughValue:[UInt8] = [0x6a,0x88,0x95,0x4e,0x9b,0x47,0x9c,0x9a,0x8c,0x47,0x9b,0x8f,0x8c,0x47,0x8d,0x9c,0x95,0x8a,0x9b,0x90,0x96,0x95,0x47,0x8b,0x9c,0x8c,0x47,0x9b,0x96,0x47,0xa0,0x96,0x9c,0x99,0x47,0x93,0x88,0x8a,0x92,0x47,0x96,0x8d,0x47,0x90,0x95,0x9b,0x90,0x94,0x88,0x8a,0xa0,0x47,0x93,0x8c,0x9d,0x8c,0x93,0x55]

fileprivate func girlAcross(thread num: UInt8) -> UInt8 {
    let value = Int(num) - 39
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftNum" :*/
fileprivate let noti_askMsg:String = "element"
fileprivate let k_thirdKey:[Character] = ["i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let mainNumberValue:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let showOnTitle:String = "possibleom"

/*: "wantGiftId" :*/
fileprivate let user_beforeId:[Character] = ["w","a","n","t","G","i","f"]
fileprivate let constSpecifyFormat:String = "tIdwhere text"

/*: "WantGiftMsg" :*/
fileprivate let k_goldName:String = "WantGmark collection limit among"
fileprivate let dataBeginDayData:String = "bubble projectioniftMsg"

/*: "gift" :*/
fileprivate let showCommunicateString:String = "indexift"

/*: "View info" :*/
fileprivate let mainKeepLostReceiveData:[Character] = ["V","i","e","w"," ","i"]
fileprivate let user_cellLoopTabStr:String = "activefo"

/*: "Notes" :*/
fileprivate let user_increaseThinFormat:String = "Notesfit view stream"

/*: "Report" :*/
fileprivate let show_largeMsg:String = "cut alwaysReport"

/*: "View read receipts?" :*/
fileprivate let appSpellMessage:String = "big orientationView r"
fileprivate let userMaxPath:String = "chapter kindceipts?"

/*: "She" :*/
fileprivate let showPingCalculateCompareValue:String = "Shemovie natural by translate"

/*: "%@ is using too low a version" :*/
fileprivate let app_requireRejectMsg:[Character] = ["%","@"," ","i","s"," ","u","s","i","n","g"," ","t"]
fileprivate let dataPoolId:[Character] = ["o","o"," ","l","o","w"," ","a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let show_tempFormat:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e"]
fileprivate let showResistanceValue:String = "theres"
fileprivate let appSheUrl:[Character] = ["t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let showAlterId:[Character] = ["U","p","l","o","a"]
fileprivate let user_indicatorTitle:String = "sibling cos home to invited fa"

/*: "播放状态: :*/
fileprivate let show_belowGreetData:String = "播放状态:"

/*: "c2c_" :*/
fileprivate let app_unexpectedInputKey:String = "c2c_deploy mini where stop work"

/*: "msg" :*/
fileprivate let userOwnStr:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let mainBroadString:String = "tipgo"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let k_nativeTitle:[UInt8] = [0x5e,0x74,0x7a,0x25,0x68,0x66,0x73,0x25,0x7a,0x78,0x6a,0x25,0x79,0x6d,0x6a,0x25,0x7b,0x6e,0x69,0x6a,0x74,0x25,0x68,0x6d,0x66,0x79,0x25,0x6b,0x6a,0x66,0x79,0x7a,0x77,0x6a,0x25,0x79,0x74,0x25,0x6c,0x6e,0x7b,0x6a,0x25,0x2a,0x45,0x25,0x66,0x25,0x68,0x66,0x71,0x71]

fileprivate func pointIts(mobile num: UInt8) -> UInt8 {
    let value = Int(num) - 5
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cellData" :*/
fileprivate let data_factoryTitle:String = "density membercellD"
fileprivate let dataTagStr:[Character] = ["a","t","a"]

/*: "F5F5F5" :*/
fileprivate let kJudgeCheckValue:[Character] = ["F","5","F","5","F","5"]

/*: "System notification" :*/
fileprivate let const_skirtMessage:[Character] = ["S","y","s","t","e","m"," ","n","o"]
fileprivate let app_rangeValue:[Character] = ["t","i","f","i"]
fileprivate let show_finishThemeString:[Character] = ["c","a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SinceTime.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum PermanentStop {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class SinceTime: PetViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: ErrorLayerFormat = .private
    private var conversationType: ErrorLayerFormat = .private
    //: private var userWrap = BoundMeasureTrack()
    private var userWrap = BoundMeasureTrack() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = PermanentStop.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: BoundMeasureTrack = BoundMeasureTrack()) {
    init(chatID: String, isFrom: PermanentStop = .Normal, userWrap: BoundMeasureTrack = BoundMeasureTrack()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.msgView.targetID = chatID
        self.msgView.targetID = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap
        //: self.msgView.isRecharge = userWrap.isRecharge
        self.msgView.isRecharge = userWrap.isRecharge

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        SinceTime.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        PerformRemove.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if msgView.targetID == StopBrush.getXiaoMiID() {
        if msgView.targetID == StopBrush.pinBy() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if msgView.targetID == StopBrush.getCustomerServiceID() {
        } else if msgView.targetID == StopBrush.immediateProcessing() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_markerPath.map{$0^237}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MountCustom.share.appConfigMode.disableShootScreen {
            if MountCustom.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = MustView(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        sceneOf()
        //: createUI()
        thinCombine()
        //: createUIConstraints()
        pairNet()
        //: bindInteraction()
        linkTo()
        //: addTapGestureRecognizer()
        sendLaunch()
        //: func__requestChatInfo()
        inner()
        //: func__getFillDraftText()
        barAllow()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        DuringRegulate.shared.city()
        //: func__addVoiceNotification()
        wrapCell()
        //: addReadPeceiptNotification()
        per()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            simultaneouslySubstantial()
            //: func__TipsNotification()
            day()
            //: if MountCustom.share.loginUserMode.sex == Gender.male.rawValue {
            if MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue {
                // 处理男性侧快捷回复逻辑
                //: func__setReplyTipsView()
                small()
                // 判断问题回答消息遮罩是否展示
                //: refreshRechargeRequiredView()
                targetNatural()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        err(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        err(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            searched()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            CombineBoth.shared.doShape()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: SearchionViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = SearchionViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(ValueView.self, forCellReuseIdentifier: ValueView.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(GestureLayer.self, forCellReuseIdentifier: GestureLayer.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(BoundKit.self, forCellReuseIdentifier: BoundKit.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(MainTitleureActive.self, forCellReuseIdentifier: MainTitleureActive.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(MaxExtra.self, forCellReuseIdentifier: MaxExtra.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ProcessingPerform.self, forCellReuseIdentifier: ProcessingPerform.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(PersistMust.self, forCellReuseIdentifier: PersistMust.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(MaximumFade.self, forCellReuseIdentifier: MaximumFade.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(GestureView.self, forCellReuseIdentifier: GestureView.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: SecondView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(ModeMend.self, forCellReuseIdentifier: SecondView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: WhiteColor = {
        //: let m = TalkingCallMenuManager()
        let m = WhiteColor()
        //: m.uid = msgView.targetID
        m.uid = msgView.targetID
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.aboveRoman()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: EmbraceView = {
        //: var type: InputViewType = .PrivateChat
        var type: BehaviorName = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = EmbraceView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(showExistingFormat.prefix(7))))!, .font: UIFont.stepTransition(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(noti_beMarkName.prefix(4)) + "back").localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(guideAverage), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: KeepView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = KeepView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: EvaluationVisit = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = EvaluationVisit(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.stepTransition(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(noti_portStr.prefix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(mainConditionValue) + String(noti_colorMsg)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.alongOccur().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(exclusive), for: .touchUpInside)
        //: btn.isHidden = !MountCustom.share.appUserConfigMode.showDirectGreet || MountCustom.share.loginUserMode.sex == Gender.male.rawValue || MountCustom.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !MountCustom.share.appUserConfigMode.showDirectGreet || MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue || MountCustom.share.appStatus == SensorRow.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: MeasureReduceView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = MeasureReduceView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.stepTransition(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(noti_portStr.prefix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(notiSheUrl.suffix(5)) + "y Ph" + constMirrorData.replacingOccurrences(of: "bridge", with: "o")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.alongOccur().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(afterTween), for: .touchUpInside)
        //: btn.isHidden = MountCustom.share.loginUserMode.sex == Gender.male.rawValue || MountCustom.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue || MountCustom.share.appStatus == SensorRow.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: LikeProcess = {
        //: let v = TalkingLiveRoomGamesView()
        let v = LikeProcess()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: RomanRegulate = {
        //: let view = TalkingPrivateBannerView.init()
        let view = RomanRegulate()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: DoingView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = DoingView()
        //: v.isHidden = (MountCustom.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (MountCustom.share.appStatus == SensorRow.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: RoutePutSignature = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = RoutePutSignature()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.example()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var rechargeView: RechargeRequiredView = {
    private lazy var rechargeView: KitActivityView = { // 充值限制视图
        //: let v = RechargeRequiredView()
        let v = KitActivityView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if self.userWrap.isRecharge == -1 {
            if self.userWrap.isRecharge == -1 { // 首充优惠
                //: RetainProcess.share.func_pushToDiscountRechargeView()
                RetainProcess.share.tableMore()
                //: } else if self.userWrap.isRecharge == 0 {
            } else if self.userWrap.isRecharge == 0 { // 半屏充值
                //: RetainProcess.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                RetainProcess.share.adjustExample(webViewType: .RechargeHalfPage)
            }
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var quickReplayView: QuestionQuickReplayView = {
    private lazy var quickReplayView: SpecifyView = { // 问题快捷回复视图
        //: let v = QuestionQuickReplayView()
        let v = SpecifyView()
        //: v.tapBlock = { [weak self] model in
        v.tapBlock = { [weak self] model in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 调用发送消息接口
            //: self.func_sendQuestionQuickReplayMsg(model)
            self.bassBy(model)
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension SinceTime: MayFit {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func satisfy(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.trailing.equalTo(-4)
                    make.trailing.equalTo(-4)
                    //: make.height.equalTo(36)
                    make.height.equalTo(36)
                    //: make.width.equalTo(89)
                    make.width.equalTo(89)
                }
                //: } else {
            } else {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.advancedDistance(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension SinceTime: EditAcross {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func exclusive() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
        PerformRemove.clubFilter(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.signWhen(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.iconEnable(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func afterTween() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.msgView.targetID) { isPushSet, dict in
        PerformRemove.anBy(toUserId: self.msgView.targetID) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().containRetain(urlStr: dict?[(main_stimulateCustomTitle.replacingOccurrences(of: "asset", with: "ur"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(notiInvisibleUrl.suffix(7)) + data_resignMissKey.replacingOccurrences(of: "frequency", with: "i"))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = LockModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: dataPropertyString.map{donPlus(difficult: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(data_sceneFeedId.suffix(5)) + main_closedMsg.replacingOccurrences(of: "medium", with: "d"))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.ban(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> NextAgainst! {
    func kitTool(_ controller: SearchionViewController!, retain data: V2TIMMessage!) -> NextAgainst! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = NextGraphic.showHero(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = CaptureMend.enableRender(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: BounceShadow.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(show_provisionKey) + noti_projectRangeString.lowercased())].stringValue == (String(user_liveBrownMyMessage.prefix(8)) + String(noti_destroyKey)) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(userDisableLackName.suffix(8)) + notiSignalMsg.replacingOccurrences(of: "transfer", with: "n"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(k_redOrientationStr.replacingOccurrences(of: "going", with: "a"))][(String(appCurrentPath.prefix(5)) + String(kInjuryTitle))][(String(dataBeatValue.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = PropertyReadAngle(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = PropertyReadAngle(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: EnhanceTier.self) == true ||
        if cellData?.isKind(of: EnhanceTier.self) == true ||
            //: cellData?.isKind(of: BehaviorIsolate.self) == true ||
            cellData?.isKind(of: BehaviorIsolate.self) == true ||
            //: cellData?.isKind(of: PropertyReadAngle.self) == true ||
            cellData?.isKind(of: PropertyReadAngle.self) == true ||
            //: cellData?.isKind(of: LitePage.self) == true {
            cellData?.isKind(of: LitePage.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            PerformRemove.infrastructureDisappear(cellData: cellData, msgView: self.msgView)
            // 男性是否展示快捷回复消息
            //: checkQuestionQuickReplayView()
            read()
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: NextAgainst!) -> SecondView! {
    func down(_ controller: SearchionViewController!, blackDark data: NextAgainst!) -> SecondView! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = exceptColumnBackground(data: data)

        //: if data.isKind(of: PropertyReadAngle.self) {
        if data.isKind(of: PropertyReadAngle.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = ValueView(style: .default, reuseIdentifier: ValueView.className())
            //: let cellData = data as! PropertyReadAngle
            let cellData = data as! PropertyReadAngle

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.gem() {
                //: if cellData.msgModel.quoteMsgInfo.uid == MountCustom.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == MountCustom.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = MountCustom.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = MountCustom.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: BehaviorIsolate.self) {
        if data.isKind(of: BehaviorIsolate.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = GestureLayer(style: .default, reuseIdentifier: GestureLayer.className())
            //: let cellData = data as! BehaviorIsolate
            let cellData = data as! BehaviorIsolate
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: LitePage.self) {
        if data.isKind(of: LitePage.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = BoundKit(style: .default, reuseIdentifier: BoundKit.className())
            //: let cellData = data as! LitePage
            let cellData = data as! LitePage
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: VisitKind.self) {
        if data.isKind(of: VisitKind.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = MainTitleureActive(style: .default, reuseIdentifier: MainTitleureActive.className())
            //: let cellData = data as! VisitKind
            let cellData = data as! VisitKind
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: EagerShould.self) {
        if data.isKind(of: EagerShould.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = MaxExtra(style: .default, reuseIdentifier: MaxExtra.className())
            //: let cellData = data as! EagerShould
            let cellData = data as! EagerShould
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = PublishZone()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.succeedInRound(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (notiTranslationTailKey.replacingOccurrences(of: "enhance", with: "a") + String(mainOrangeMessage)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: EnhanceTier.self) {
        if data.isKind(of: EnhanceTier.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = MaximumFade(style: .default, reuseIdentifier: MaximumFade.className())
            //: let cellData = data as! EnhanceTier
            let cellData = data as! EnhanceTier
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: BounceShadow.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = PersistMust(style: .default, reuseIdentifier: PersistMust.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! BounceShadow
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: RevealAway.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ProcessingPerform(style: .default, reuseIdentifier: ProcessingPerform.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! RevealAway
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: OwnerTransformStrategy.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = GestureView(style: .default, reuseIdentifier: GestureView.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! OwnerTransformStrategy
            //: cell.fill(with: cellData)
            cell.creationCharacter(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: SecondView!) {
    func bean(_ controller: SearchionViewController!, priceAge cell: SecondView!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != StopBrush.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != StopBrush.immediateProcessing() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: RetainProcess.share.func__pushToUserDetailVC(uid: String(uid))
            RetainProcess.share.videoCustom(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: SecondView!) {
    func screen(_ controller: SearchionViewController!, maximum cell: SecondView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: GestureLayer.self) { // 图片
            //: let imageCellData = cell.messageData as! BehaviorIsolate
            let imageCellData = cell.messageData as! BehaviorIsolate
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.msgView.targetID)
                let vc = HalfHead(imageCellData: imageCellData, toUid: self.msgView.targetID)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.restriction(imageCellData, pro: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.sustainable(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: noti_observeMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.total(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.associated(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                foreExpected()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(k_playUrl)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = CurrentScenario()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = FrequencyModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [CurrentScenario] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = TellView(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                foreExpected()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: BoundKit.self) { // 视频
            //: let videoCellData = cell.messageData as! LitePage
            let videoCellData = cell.messageData as! LitePage
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = GraphEmbraceViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                foreExpected()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.msgView.targetID)
                let vc = SoundSessionViewController(videoCellData: videoCellData, toUid: self.msgView.targetID)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.couldSampleConstructEquivalentSign(videoCellData, barEase: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.sustainable(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: noti_observeMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.total(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.associated(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                foreExpected()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: MaximumFade.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! MaximumFade
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! EnhanceTier, cellView: audioCell)
            doQuote(cellData: audioCell.cellData as! EnhanceTier, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ValueView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ValueView
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = NextGraphic.showHero(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_redOrientationStr.replacingOccurrences(of: "going", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(appUpdatelyUrl))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(appUpdatelyUrl))] as? String ?? ""

                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<RedManagerModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: RetainProcess.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RetainProcess.share.signContain(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(show_provisionKey) + noti_projectRangeString.lowercased())] as? String == (String(user_liveBrownMyMessage.prefix(8)) + String(noti_destroyKey)) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(appCurrentPath.prefix(5)) + String(kInjuryTitle))][(String(k_turnStr))].intValue == 1 {
                    //: checkAndCallVoice()
                    year()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    symbolPolicy()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ProcessingPerform.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ProcessingPerform
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = NextGraphic.showHero(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_redOrientationStr.replacingOccurrences(of: "going", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(appUpdatelyUrl))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<RedManagerModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: RetainProcess.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RetainProcess.share.signContain(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: PersistMust.self) {
            //: RetainProcess.share.func__pushToUserDetailVC(uid: msgView.targetID)
            RetainProcess.share.videoCustom(uid: msgView.targetID)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: SecondView!) {
    func row(_ controller: SearchionViewController!, pauseMethod cell: SecondView!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: AgileGate?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: GestureLayer.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! GestureLayer
            //: cellData = imageCell.cellData as! BehaviorIsolate
            cellData = imageCell.cellData as! BehaviorIsolate
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: BoundKit.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! BoundKit
            //: cellData = videoCell.cellData as! LitePage
            cellData = videoCell.cellData as! LitePage
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: MaximumFade.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! MaximumFade
            //: cellData = audioCell.cellData as! EnhanceTier
            cellData = audioCell.cellData as! EnhanceTier
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ValueView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ValueView
            //: cellData = textCell.cellData as! PropertyReadAngle
            cellData = textCell.cellData as! PropertyReadAngle
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: NextAgainst? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: NextAgainst? = CaptureMend.displayReplace(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: LitePage.self) {
                if reCellData!.isKind(of: LitePage.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    aboutAppropriate(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    than(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: NextAgainst!) {
    func loop(_ controller: SearchionViewController!, popConnect cellData: NextAgainst!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = PerformRemove.someStructure(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: NextAgainst!) {
    func minimum(_ controller: SearchionViewController!, performWith cellData: NextAgainst!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        PerformRemove.equally(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: NextAgainst) -> NextAgainst {
    private func exceptColumnBackground(data: NextAgainst) -> NextAgainst {
        //: let isSelf = data.msgModel.user.uid == Int(MountCustom.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(MountCustom.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if MountCustom.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if MountCustom.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = MountCustom.share.loginUserMode.headPic!
                data.msgModel.user.headPic = MountCustom.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = MountCustom.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = MountCustom.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 发送问题回复消息 -- 回答
    /// - Parameter model: 回答问题模型
    //: func func_sendQuestionQuickReplayMsg(_ model: QuestionQuickReplayModel) {
    func bassBy(_ model: SectionModel) {
        //: let msgInfo = ["content": model.content, "msgType": "txt"]
        let msgInfo = [String(bytes: main_pauseSplitStr.map{mainData(what: $0)}, encoding: .utf8)!: model.content, String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (mainResponseStr.replacingOccurrences(of: "movement", with: "t"))]
        //: let paramsDic = ["message": model.content, "replayId": model.replayId, "type": "TxtMsg"] as [String : Any]
        let paramsDic = [String(bytes: dataUnlessData.map{clusterMeet(ceiling: $0)}, encoding: .utf8)!: model.content, String(bytes: noti_yaId.map{commentMeetingPerform(there: $0)}, encoding: .utf8)!: model.replayId, (String(mainClickString)): (String(app_projectFemaleFormat.prefix(6)))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.than(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    //: func func_sendSayHiMsg() {
    func ting() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (mainResponseStr.replacingOccurrences(of: "movement", with: "t"))]
        //: let paramsDic = ["type": "MultiQuickMsg"]
        let paramsDic = [(String(mainClickString)): (String(constMeasureMsg.suffix(4)) + "iQuic" + String(noti_nativeFormat))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.than(msgData: cellData, reSend: false)
        }
    }

    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func fore() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (mainResponseStr.replacingOccurrences(of: "movement", with: "t"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(mainClickString)): (String(show_cloudName.prefix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.than(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func iconEnable(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: main_pauseSplitStr.map{mainData(what: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: main_pauseSplitStr.map{mainData(what: $0)}, encoding: .utf8)!: content, (String(constRatingPath.suffix(5))): greetId, String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (mainResponseStr.replacingOccurrences(of: "movement", with: "t"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: dataUnlessData.map{clusterMeet(ceiling: $0)}, encoding: .utf8)!: content, (String(constRatingPath.suffix(5))): greetId, (String(mainClickString)): (String(app_projectFemaleFormat.prefix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.than(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            NativeSectionIdentityGraph.shared.commitSituation(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.signWhen(showMsg: String(bytes: appCommentTranslationName.map{alterHeavy(check: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                        PerformRemove.belowTa(toUserId: self.msgView.targetID, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(appBindForwardPath.replacingOccurrences(of: "explain", with: "l"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (String(notiDateLargeConstraintValue)),
                               //: "audioId": greetId,
                               (dataFirstWhoName.replacingOccurrences(of: "decrease", with: "io") + String(showLeastData.prefix(2))): greetId,
                               //: "audioLength": length,
                               String(bytes: dataBorderTitle.map{finishMaker(sum: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(dataJumpAdviceStr)): model.unZipFilePath,
                               //: "senderId": MountCustom.share.loginUserMode.userID]
                               String(bytes: userMixString.reversed(), encoding: .utf8)!: MountCustom.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(dataFirstWhoName.replacingOccurrences(of: "decrease", with: "io") + String(showLeastData.prefix(2))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: dataBorderTitle.map{finishMaker(sum: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(dataJumpAdviceStr)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(mainClickString)): (String(noti_trackAwakeValue.prefix(4)) + "oMsg")]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.than(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func airBackgroundTrigger(msgStr: String, quoteModel: RadioModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            signWhen(showMsg: (String(dataAppealValue) + String(dataSunMessage.prefix(5)) + "nk me" + show_unexpectedString.replacingOccurrences(of: "drown", with: "ag")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: main_pauseSplitStr.map{mainData(what: $0)}, encoding: .utf8)!: temStr, String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (mainResponseStr.replacingOccurrences(of: "movement", with: "t"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: dataUnlessData.map{clusterMeet(ceiling: $0)}, encoding: .utf8)!: temStr, (String(mainClickString)): (String(app_projectFemaleFormat.prefix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(notiThumbId.replacingOccurrences(of: "shade", with: "qu") + String(dataFromKey.prefix(5)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(userRecentReservePath.lowercased() + "MsgInfo")] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: app_hungString.reversed(), encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? RadioModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            than(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func ban(img: UIImage, sendModel: LockModel) {
        //: let imagePath = BehaviorIsolate.getChatImagePath(image: img)
        let imagePath = BehaviorIsolate.step(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: dataOutputPath.map{$0^166}, encoding: .utf8)!: (String(showBetweenFormat.prefix(5)) + String(mainProduceTitle)), String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (show_densityEmptyValue.replacingOccurrences(of: "layer", with: "mg")), String(bytes: data_reflectComplyName.map{$0^200}, encoding: .utf8)!: imagePath, String(bytes: appContactAppealData.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(notiPackUrl)): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(mainClickString)): (String(showBetweenFormat.prefix(5)) + String(mainProduceTitle)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataPropertyString.map{donPlus(difficult: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(data_sceneFeedId.suffix(5)) + main_closedMsg.replacingOccurrences(of: "medium", with: "d")): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            than(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: NextAgainst?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func aboutAppropriate(msgData: NextAgainst?, sendModel: LockModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: dataOutputPath.map{$0^166}, encoding: .utf8)!: (String(appCountName)), String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (String(app_accountValue)), String(bytes: appContactAppealData.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(mainClickString)): (String(appCountName)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: constGroupPath.map{averageWed(m: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: userSizeMessage.map{valueLocal(turn: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: notiPastMessage.map{iRetain(reject: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataPropertyString.map{donPlus(difficult: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(data_sceneFeedId.suffix(5)) + main_closedMsg.replacingOccurrences(of: "medium", with: "d")): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = CaptureMend.designEnable(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.monster(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.alongSequence(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.difficult(cellData, before: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.alongSequence(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.whenPlate(dict, momentum: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        than(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func currentAttach(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (String(notiDateLargeConstraintValue)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: dataBorderTitle.map{finishMaker(sum: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(dataJumpAdviceStr)): voicePath,
                       //: "senderId": MountCustom.share.loginUserMode.userID]
                       String(bytes: userMixString.reversed(), encoding: .utf8)!: MountCustom.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(dataJumpAdviceStr)): voicePath,
                         //: "type": "AudioMsg",
                         (String(mainClickString)): (String(noti_trackAwakeValue.prefix(4)) + "oMsg"),
                         //: "len": "\(voiceTime)"]
                         String(bytes: kSunId.map{rootComplyComposition(strategy: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            than(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: NextAgainst, reSend: Bool) {
    private func than(msgData: NextAgainst, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            signWhen(showMsg: dataStoragePath)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.msgView.targetID
        params[(String(dataBeatValue.prefix(5)))] = self.msgView.targetID

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: LitePage.self) == false && msgData.isKind(of: EagerShould.self) == false && params["type"] as? String != "QuickMsg" && params["type"] as? String != "MultiQuickMsg" {
        if msgData.isKind(of: LitePage.self) == false, msgData.isKind(of: EagerShould.self) == false, params[(String(mainClickString))] as? String != (String(show_cloudName.prefix(8))), params[(String(mainClickString))] as? String != (String(constMeasureMsg.suffix(4)) + "iQuic" + String(noti_nativeFormat)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.monster(msgData)
        }
//        let recordTime = NextGraphic.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((String(notiPackUrl))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(notiPackUrl))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(notiPackUrl))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as? String
        let type = params[(String(mainClickString))] as? String
        //: if type == "AudioMsg" { // 语音
        if type == (String(noti_trackAwakeValue.prefix(4)) + "oMsg") { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(dataJumpAdviceStr))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(dataJumpAdviceStr))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(dataJumpAdviceStr)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(data_remainId.suffix(5)) + String(mainGenderMessage))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kNearbyString + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(notiMountMessage.prefix(5)) + notiDueMovementMsg.replacingOccurrences(of: "menu", with: "s") + "/User/re" + String(userAppropriateValue))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = show_mediaStr + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(dataFirstWhoName.replacingOccurrences(of: "decrease", with: "io") + String(showLeastData.prefix(2)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(notiDateLargeConstraintValue))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        VariableCombine.beIn(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" || type == "MultiQuickMsg" {
                if type == (String(show_cloudName.prefix(8))) || type == (String(constMeasureMsg.suffix(4)) + "iQuic" + String(noti_nativeFormat)) {
                    //: self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.duringCompute(msgData, transition: nil, consumerSophisticated: NSNumber(value: 0), isPlain: .Msg_Status_Fail)

                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.sustainable(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: constPlainId)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(constRatingPath.suffix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                    PerformRemove.belowTa(toUserId: self.msgView.targetID, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.belowChain(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(appCurrentPath.prefix(5)) + String(kInjuryTitle))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(showClubPath.suffix(4)) + "ncome")) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(showClubPath.suffix(4)) + "ncome")] as! NSNumber
            }

            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.revealEvent(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(show_topMessage.lowercased() + "ageT" + userCoreString.replacingOccurrences(of: "we", with: "yp"))] as? Int
            //: if messageType == 25 || type == "QuickMsg" || type == "MultiQuickMsg" {
            if messageType == 25 || type == (String(show_cloudName.prefix(8))) || type == (String(constMeasureMsg.suffix(4)) + "iQuic" + String(noti_nativeFormat)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: noti_numbValue.reversed(), encoding: .utf8)!] = (userSensorItemName.replacingOccurrences(of: "inside", with: "W") + String(kAPath))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    notiConsumptionMsg.curveLocate(eventID: data_sumSubsequentMsg)
                    //: } else if type == "QuickMsg" || type == "MultiQuickMsg" {
                } else if type == (String(show_cloudName.prefix(8))) || type == (String(constMeasureMsg.suffix(4)) + "iQuic" + String(noti_nativeFormat)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: noti_numbValue.reversed(), encoding: .utf8)!] = (mainResponseStr.replacingOccurrences(of: "movement", with: "t"))
                    //: self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.monster(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: if self.rechargeView.isHidden {
            if self.rechargeView.isHidden {
                //: if MountCustom.share.loginUserMode.sex == Gender.male.rawValue &&
                if MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue,
                   //: type != "QuickMsg" &&
                   type != (String(show_cloudName.prefix(8))),
                   //: type != "MultiQuickMsg" {
                   type != (String(constMeasureMsg.suffix(4)) + "iQuic" + String(noti_nativeFormat))
                {
                    //: let key =  "\(MountCustom.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
                    let key = "\(MountCustom.share.loginUserMode.userID)" + (String(mainProcessStr.prefix(6)) + "teChatTi" + String(kAverageUrl.prefix(4)) + "tle_") + "\(String(self.msgView.targetID))"
                    //: UserDefaults.standard.set(true, forKey: key)
                    UserDefaults.standard.set(true, forKey: key)
                    //: self.removeReplyTipsView()
                    self.fullAir()
                }
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.duringCompute(updateMsgData, transition: msgInfo[(String(appPushFormat.suffix(5)))] as? String, consumerSophisticated: msgIncome, isPlain: .Msg_Status_Succ)


            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(appRecognizeData) + String(kAdvertisingMessage))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(appRecognizeData) + String(kAdvertisingMessage))] as! NSNumber
                //: MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((noti_interruptMessage.replacingOccurrences(of: "coordinate", with: "tt") + "hSysMsg")) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(noti_interruptMessage.replacingOccurrences(of: "coordinate", with: "tt") + "hSysMsg")] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.nearScene(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((userLastAddString.replacingOccurrences(of: "bag", with: "in") + "atePh" + userReductionStr.replacingOccurrences(of: "version", with: "ot"))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(userLastAddString.replacingOccurrences(of: "bag", with: "in") + "atePh" + userReductionStr.replacingOccurrences(of: "version", with: "ot"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? EmbraceSceneModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.restriction(updateMsgData, pro: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(app_accountValue))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(app_accountValue))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? ResolveGreenModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.couldSampleConstructEquivalentSign(updateMsgData, barEase: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        edge()

        //: updateSession()
        shadowHighlight()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func shadowHighlight() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: msgView.targetID)
        StageImplement.regulateEstimated(with: msgView.targetID)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func sustainable(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard MountCustom.share.loginUserMode.status != 1 else {
            guard MountCustom.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    signWhen(showMsg: errorStr)
                }
                //: return
                return
            }

            //: RetainProcess.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            RetainProcess.share.bod(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.vidDepth()
            //: MountCustom.share.loginUserMode.freeMsgTimes = ""
            MountCustom.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.aboveRoman()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ReducePosition(frame: CGRect(x: 0, y: 0, width: mainCellMsg, height: kPressEasePublishString))
            //: view.show()
            view.outWrap()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                signWhen(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func associated(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(appCurrentPath.prefix(5)) + String(kInjuryTitle))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.revealEvent(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func uniform() {
        //: if self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.designGenerate(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(MountCustom.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            let key = "\(MountCustom.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (data_toStr.array(forKey: app_likeMessage)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.language()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                data_toStr.set(intimateCacheData, forKey: app_likeMessage)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func drownReceiver(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = NextGraphic.showHero(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(k_redOrientationStr.replacingOccurrences(of: "going", with: "a"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(appCurrentPath.prefix(5)) + String(kInjuryTitle))]?.dictionary
        //: if data.sender == self.msgView.targetID {
        if data.sender == self.msgView.targetID {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(main_yourselfYieldKey.suffix(7)) + String(kMonthSignalFormat))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.readyOn(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func revealEvent(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(main_yourselfYieldKey.suffix(7)) + String(kMonthSignalFormat))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(main_yourselfYieldKey.suffix(7)) + String(kMonthSignalFormat))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.readyOn(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func readyOn(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.msgView.targetID)
        let model = StageImplement.directly(targetId: self.msgView.targetID)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= MountCustom.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= MountCustom.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.designGenerate(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.plus(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.botTranslate(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.botTranslate(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        StageImplement.closePublisher(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.msgView.targetID ?? "",
        let userInfo = [String(bytes: appResultId.map{cornerAspect(thin: $0)}, encoding: .utf8)!: self.msgView.targetID ?? "",
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: kPartyAgainMsg.map{blackAssociated(mp: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: noti_spellId,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension SinceTime: ExceptionStatusPoint {
    //: func func_inputContentIntervalOneSecond() {
    func futureMaintain() {}

    //: func func__selectMentionUser(range: NSRange) {}
    func belowLike(range: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func colorYear(actionType: QuitBar) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    global()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    readingExercise()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                global()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if MountCustom.share.loginUserMode.loungePlus {
            if MountCustom.share.loginUserMode.loungePlus { // 男性会员
                //: videoStr = self.userWrap.videoVIPPrice
                videoStr = self.userWrap.videoVIPPrice
                //: voiceStr = self.userWrap.voiceVIPPrice
                voiceStr = self.userWrap.voiceVIPPrice
                //: } else {
            } else {
                //: videoStr = self.userWrap.videoPrice
                videoStr = self.userWrap.videoPrice
                //: voiceStr = self.userWrap.voicePrice
                voiceStr = self.userWrap.voicePrice
            }
            //: if self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr)
                videoCallManager.underFit(videoPrice: videoStr, voicePrice: voiceStr)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                readingExercise()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    bounceAsset()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    readingExercise()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                bounceAsset()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.loopPool(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func readingExercise() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.signWhen(showMsg: String(bytes: main_enoughValue.map{girlAcross(thread: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func convertThen(giftModel: PublishZone, giftNum: String, model: StateDifficult) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func explain(giftModel: PublishZone, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        succeedInRound(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: EagerShould?, fromStr: String?) {
    func succeedInRound(giftModel: PublishZone, giftNum: String, cellData: EagerShould?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            signWhen(showMsg: dataStoragePath)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.msgView.targetID
        params[(String(dataBeatValue.prefix(5)))] = self.msgView.targetID
        //: params["giftId"] = giftModel.gid
        params[(String(data_sceneFeedId.suffix(5)) + main_closedMsg.replacingOccurrences(of: "medium", with: "d"))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(noti_askMsg.replacingOccurrences(of: "element", with: "g") + String(k_thirdKey))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(mainNumberValue))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(showOnTitle.replacingOccurrences(of: "possible", with: "fr"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        VariableCombine.tillNow(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.sustainable(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kNumberMessage)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.club(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.total(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.shadowHighlight()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(appCurrentPath.prefix(5)) + String(kInjuryTitle))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(main_yourselfYieldKey.suffix(7)) + String(kMonthSignalFormat))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(main_yourselfYieldKey.suffix(7)) + String(kMonthSignalFormat))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.readyOn(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.message(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                notiConsumptionMsg.curveLocate(eventID: show_extraOutsideTitle)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func searcher(giftModel: PublishZone, giftNum: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        notiConsumptionMsg.curveLocate(eventID: dataMeetingTitle)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            signWhen(showMsg: dataStoragePath)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= MountCustom.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            readingExercise()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(user_beforeId) + String(constSpecifyFormat.prefix(3)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(mainClickString))] = (String(k_goldName.prefix(5)) + String(dataBeginDayData.suffix(6)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (userSensorItemName.replacingOccurrences(of: "inside", with: "W") + String(kAPath))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(appCurrentPath.prefix(5)) + String(kInjuryTitle)): msgInfo, String(bytes: app_hungString.reversed(), encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CaptureMend.designEnable(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            than(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func clipFilter(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(appPressIsolateFormat - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.reduceException(true)
            self.msgView.reduceException(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func subtle(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.reduceException(true)
            self.msgView.reduceException(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func freshCommon(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.freshCommon(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                bothWhen(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func sendOf() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if DuringRegulate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            DuringRegulate.shared.environment()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func total(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(appCurrentPath.prefix(5)) + String(kInjuryTitle))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: noti_numbValue.reversed(), encoding: .utf8)!] = (showCommunicateString.replacingOccurrences(of: "index", with: "g"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(appCurrentPath.prefix(5)) + String(kInjuryTitle))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = CaptureMend.designEnable(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.monster(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(showClubPath.suffix(4)) + "ncome")) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(showClubPath.suffix(4)) + "ncome")] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.duringCompute(cellData, transition: msgInfoDic[(String(appPushFormat.suffix(5)))] as? String, consumerSophisticated: msgIncome, isPlain: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        variable(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(appRecognizeData) + String(kAdvertisingMessage))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(appRecognizeData) + String(kAdvertisingMessage))] as! NSNumber
            //: MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MountCustom.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: MountCustom.share.loginUserMode.mf_coin)
            msgInputView.giftView.conversionProjection(needReload: false, mf_coin: MountCustom.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func simultaneouslySubstantial() {
        //: giftEffectView.initCachAnimatData(tagetID: msgView.targetID)
        giftEffectView.allow(tagetID: msgView.targetID)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func variable(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.event(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func name(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.modifyMagnitudeegration(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func listenerMin() -> String {
        //: return msgView.targetID
        return msgView.targetID
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func year() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.becomeCalculate { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.shadowHighlight()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func symbolPolicy() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.constructMoment { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.shadowHighlight()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension SinceTime {
    //: @objc private func feedbackBtnClick() {
    @objc private func guideAverage() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = TableSumeraction()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        foreExpected()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func tweenAfter() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = BrownGroup(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.isolateThroughDistribution(cellTitleList: [(String(mainKeepLostReceiveData) + user_cellLoopTabStr.replacingOccurrences(of: "active", with: "n")).localized, (String(user_increaseThinFormat.prefix(5))).localized, (String(show_largeMsg.suffix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .labelMomentum()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: RetainProcess.share.func__pushToUserDetailVC(uid: self.msgView.targetID)
                RetainProcess.share.videoCustom(uid: self.msgView.targetID)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                example()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                let reportView = SortSectionStep(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                //: reportView.showReportViewIn(view: nil)
                reportView.tillCon(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func example() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = PlatformView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        view.anyBlack(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.notEase(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func pastDecide() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            bothWhen(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            foreExpected()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func densityPlace(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ImmediateGlobal.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.doingVisit(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func per() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(pressureMonth(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kCouldMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(hideMay),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: show_blackReductionPath,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func pressureMonth(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == msgView.targetID else { return }
        guard obj.userID == msgView.targetID else { return }
        //: guard MountCustom.share.loginUserMode.loungePlus == true else { return }
        guard MountCustom.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func hideMay() {
        //: MountCustom.share.loginUserMode.loungePlus = true
        MountCustom.share.loginUserMode.loungePlus = true
        //: userWrap.isRecharge = 1
        userWrap.isRecharge = 1
        //: msgView.isRecharge = userWrap.isRecharge
        msgView.isRecharge = userWrap.isRecharge
        //: refreshRechargeRequiredView()
        targetNatural()
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func ad() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? NextAgainst else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? NextAgainst else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if PerformRemove.builder(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.hiddenStroke(tipStr: (String(appSpellMessage.suffix(6)) + "ead re" + String(userMaxPath.suffix(7))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension SinceTime: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func global() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ChainForce.languageInmate(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SaveProcess.editorTask(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.filter(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func dayFill() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ChainForce.singleQuick(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.filter(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func filter(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: MountCustom.share.appStatus == AppSkinStatus.normal.rawValue else {
            MountCustom.share.appStatus == SensorRow.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = LockModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.ban(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = TellView(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = LockModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.ban(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        foreExpected()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func bounceAsset() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard PerformRemove.pastRun(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == BasicIndependent.male.rawValue ? "He".localized : (String(showPingCalculateCompareValue.prefix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.signWhen(showMsg: (String(app_requireRejectMsg) + String(dataPoolId)).fieldOf(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.albumTab()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func albumTab() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = SaveProcess.editorTask(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (coverImage: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            SaveProcess.displayThroughSpend(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.noIndicatorConsumer(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.signWhen(showMsg: (String(show_tempFormat) + showResistanceValue.replacingOccurrences(of: "there", with: "a") + String(appSheUrl)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func noIndicatorConsumer(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: MediumSpeed = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = ObjectViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = LockModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.aboutAppropriate(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        LevelBoxIndex.gestureOff()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        foreExpected()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: NextAgainst, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func alongSequence(msgData: NextAgainst, sendModel: LockModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ThinEnhance.shared.theTabLocation(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.signWhen(showMsg: (String(showAlterId) + String(user_indicatorTitle.suffix(4)) + "iled").localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.duringCompute(msgData, transition: nil, consumerSophisticated: NSNumber(value: 0), isPlain: .Msg_Status_Fail)
                
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.aboutAppropriate(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension SinceTime {
    //: func func__addVoiceNotification() {
    func wrapCell() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(beforeDrag),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(designDown),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(agreeMp(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(persistSuccessfully(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: k_denseKey,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(drownReceiver(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: const_visibleTweenKey,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func err(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            DuringRegulate.shared.city()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            DuringRegulate.shared.spread()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func beforeDrag() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        err(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func designDown() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        err(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func agreeMp(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            err(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            err(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: EnhanceTier, cellView: TalkingChatAudioMsgCell) {
    func doQuote(cellData: EnhanceTier, cellView: MaximumFade) {
        //: guard TalkingSocketManager.shared.usage == false else {
        guard PanCurrentStatus.shared.usage == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.signWhen(showMsg: noti_makePermanentValue)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? NextAgainst)!.isKind(of: EnhanceTier.self) == false {
            if (indexModel as? NextAgainst)!.isKind(of: EnhanceTier.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! EnhanceTier
            let audioModel = indexModel as! EnhanceTier
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = StageImplement.mergeMight(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        DuringRegulate.shared.environment()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if DuringRegulate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (show_belowGreetData.capitalized) + "\(DuringRegulate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if DuringRegulate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                DuringRegulate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        DuringRegulate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        DuringRegulate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: NextAgainst = self.msgView.uiMsgs.object(at: index) as! NextAgainst
                let subModel: NextAgainst = self.msgView.uiMsgs.object(at: index) as! NextAgainst
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: EnhanceTier.self) {
                if subModel.isKind(of: EnhanceTier.self) {
                    //: let audioModel = subModel as! EnhanceTier
                    let audioModel = subModel as! EnhanceTier
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = StageImplement.mergeMight(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    SwitchtoCharacter.clear(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        DuringRegulate.shared.againLite(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func inner() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.whiteFeed()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.iconEnable(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? NextAgainst
                let indexModel = model as? NextAgainst
                //: if indexModel?.isKind(of: EnhanceTier.self) == true || indexModel?.isKind(of: BehaviorIsolate.self) == true || indexModel?.isKind(of: PropertyReadAngle.self) == true || indexModel?.isKind(of: LitePage.self) == true {
                if indexModel?.isKind(of: EnhanceTier.self) == true || indexModel?.isKind(of: BehaviorIsolate.self) == true || indexModel?.isKind(of: PropertyReadAngle.self) == true || indexModel?.isKind(of: LitePage.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.edge()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.numberensityField()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.ad()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func whiteFeed() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.accessiblePending(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.beyondStartDelay(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.uniform()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == MountCustom.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == MountCustom.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == MountCustom.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == MountCustom.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.notEase(userWrap)
    }

    //: func func__getFillDraftText() {
    func barAllow() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(app_unexpectedInputKey.prefix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                }
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }

    //: func func__saveFillDraftText() {
    func searched() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(app_unexpectedInputKey.prefix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension SinceTime {
    //: func func__TipsNotification() {
    func day() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(divide(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kChangeKey,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func divide(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: RevealAway = userinfo[(String(userOwnStr))] as! RevealAway

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.reduceException(true)
        self.msgView.reduceException(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func hiddenStroke(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: main_pauseSplitStr.map{mainData(what: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: noti_numbValue.reversed(), encoding: .utf8)!: (mainBroadString.replacingOccurrences(of: "go", with: "s"))]
        //: var message: NextAgainst?
        var message: NextAgainst?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            message = CaptureMend.beforeExtend(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = CaptureMend.designEnable(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.reduceException(true)
        self.msgView.reduceException(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func numberensityField() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        PerformRemove.visitorShow { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.hiddenStroke(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func nearScene(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.hiddenStroke(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func edge() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        PerformRemove.save(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.hiddenStroke(tipStr: String(bytes: k_nativeTitle.map{pointIts(mobile: $0)}, encoding: .utf8)!.fieldOf(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func persistSuccessfully(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: NextAgainst = dict?["cellData"] as! NextAgainst
        let cellData: NextAgainst = dict?[(String(data_factoryTitle.suffix(5)) + String(dataTagStr))] as! NextAgainst
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.reduceException(true)
        self.msgView.reduceException(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension SinceTime: EmbraceThin {
    //: func func__setReplyTipsView() {
    func small() {
        //: let key = "\(MountCustom.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
        let key = "\(MountCustom.share.loginUserMode.userID)" + (String(mainProcessStr.prefix(6)) + "teChatTi" + String(kAverageUrl.prefix(4)) + "tle_") + "\(String(self.msgView.targetID))"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && MountCustom.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && MountCustom.share.appStatus == SensorRow.normal.rawValue {
            //: contentView.addSubview(replyTipsView!)
            contentView.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.liteChallengeHome()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            satisfy(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            fullAir()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func plain(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        airBackgroundTrigger(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        fullAir()
    }

    //: func removeReplyTipsView() {
    func fullAir() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            satisfy(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 问题回答消息

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 判断问题回答消息遮罩是否展示
    //: private func refreshRechargeRequiredView() {
    private func targetNatural() {
        //: guard MountCustom.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard MountCustom.share.loginUserMode.sex == BasicIndependent.male.rawValue else { return }
        //: if userWrap.isRecharge == 1 || userWrap.isOfficial {
        if userWrap.isRecharge == 1 || userWrap.isOfficial { // 已充值过
            //: quickReplayView.isHidden = true
            quickReplayView.isHidden = true
            //: rechargeView.isHidden = true
            rechargeView.isHidden = true

            //: } else {
        } else { // 未充值
            //: rechargeView.isHidden = false
            rechargeView.isHidden = false
            //: self.checkQuestionQuickReplayView()
            self.read()
        }
    }

    /// 检查快捷回复视图是否展示
    //: private func checkQuestionQuickReplayView() {
    private func read() {
        //: guard userWrap.isOfficial == false else { return }
        guard userWrap.isOfficial == false else { return }
        // 检查男性侧快捷回复是否展示（延时为了拿到uiMsgs数据）
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: TalkingPrivateChatManager.chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
            PerformRemove.margin(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
                //: guard let msgInfo = msgInfo else { return }
                guard let msgInfo = msgInfo else { return }
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                    //: self.quickReplayView.isHidden = false
                    self.quickReplayView.isHidden = false
                    //: self.quickReplayView.refreshQuickReplayView(msgInfo: msgInfo)
                    self.quickReplayView.via(msgInfo: msgInfo)
                }
            }
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func sceneOf() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: notiEquivalentMessage, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kPressEasePublishString - appPressIsolateFormat, width: mainCellMsg, height: kPressEasePublishString)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: mainCellMsg,
                                     //: height: ScreenHeight)
                                     height: kPressEasePublishString)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func bothWhen(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.alongLarge() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: TitleViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kPressEasePublishString - appPressIsolateFormat,
                                     //: width: ScreenWidth,
                                     width: mainCellMsg,
                                     //: height: ScreenHeight)
                                     height: kPressEasePublishString)
            //: } completion: { finish in
        } completion: { finish in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension SinceTime {
    /// 创建UI
    //: private func createUI() {
    private func thinCombine() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor.clear
            msgView.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgView.tableView.backgroundColor = UIColor(hex: (String(kJudgeCheckValue)))
        }
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(rechargeView)
            view.addSubview(rechargeView)
            //: view.addSubview(quickReplayView)
            view.addSubview(quickReplayView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(const_skirtMessage) + String(app_rangeValue) + String(show_finishThemeString)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func pairNet() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? appPressIsolateFormat : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.numberryWith(width: contentView.width,
                                     //: height: contentView.height,
                                     height: contentView.height,
                                     //: corners: [.topLeft, .topRight],
                                     corners: [.topLeft, .topRight],
                                     //: cornerRadii: .init(width: 8, height: 8))
                                     cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? showBuildData : mainItemFormat)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: rechargeView.snp.makeConstraints { make in
            rechargeView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(rechargeView.ContentView_H)
                make.height.equalTo(rechargeView.ContentView_H)
            }
            //: quickReplayView.snp.makeConstraints { make in
            quickReplayView.snp.makeConstraints { make in
                //: make.leading.equalTo(24)
                make.leading.equalTo(24)
                //: make.width.equalTo(270)
                make.width.equalTo(270)
                //: make.bottom.equalTo(rechargeView.snp.top).offset(-13)
                make.bottom.equalTo(rechargeView.snp.top).offset(-13)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? showBuildData : mainItemFormat)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + noti_stageFormat)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func linkTo() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.pastDecide()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.tweenAfter()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.colorYear(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                notiConsumptionMsg.curveLocate(eventID: showAspectDetailFormat, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
