
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_tabName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let showSiblingMsg:[UInt8] = [0x43,0x5f,0x52]

private func greetFilter(extend num: UInt8) -> UInt8 {
    return num ^ 54
}

/*: "headPic" :*/
fileprivate let dataSharedMessage:String = "phi guardheadP"
fileprivate let appIronicNeverId:[Character] = ["i","c"]

/*: "nickname" :*/
fileprivate let mainArrestUrl:String = "nicknaoffer"

/*: "birthday" :*/
fileprivate let noti_warningData:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let user_eventMsg:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let user_screenName:String = "original"
fileprivate let k_portraitData:String = "ocatiopermission"

/*: "cmd" :*/
fileprivate let dataSophisticateSearchLaterTitle:[UInt8] = [0x89,0x93,0x8a]

fileprivate func targetMp(half num: UInt8) -> UInt8 {
    let value = Int(num) - 38
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let dataLoseMessage:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let dataRemainingKey:[UInt8] = [0x1f,0x1a,0xf,0x1a]

/*: "Call interruption" :*/
fileprivate let showDoingdCustomerMessage:String = "automatic trust race reference bridgeCall i"
fileprivate let noti_labelValue:[Character] = ["u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let main_becomeParaName:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let user_analyzeTaName:[UInt8] = [0xad,0xb0,0xa8,0x8a,0xa5]

fileprivate func dragRime(identify num: UInt8) -> UInt8 {
    let value = Int(num) - 65
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let constTotalensityMeasureMsg:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let show_fitData:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let constInputKey:[Character] = ["d","u","r","a","t","i","o"]
fileprivate let noti_literalModifyFormat:String = "gall"

/*: "She has hung up" :*/
fileprivate let notiDuringValue:[Character] = ["S","h","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let show_goldPullTitle:[Character] = ["c","a","n","c"]
fileprivate let const_taTrickValue:[Character] = ["e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let showHardValue:String = "black activeHe "
fileprivate let show_subName:String = "has hraging some table"

/*: "endType" :*/
fileprivate let dataSecretMechanicalFormat:[Character] = ["e","n","d","T","y","p"]
fileprivate let kAppropriateContactId:[Character] = ["e"]

/*: "stopSex" :*/
fileprivate let notiSubjectId:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let noti_wakePath:[UInt8] = [0xbc,0x9b,0x86,0x80,0x93,0x93,0x9c,0x96,0x9c,0x90,0x9b,0x81,0xd5,0x92,0x9a,0x99,0x91,0xd5,0x96,0x9a,0x9c,0x9b,0x86,0xd9,0xd5,0x96,0x94,0x99,0x99,0xd5,0x9d,0x94,0x86,0xd5,0x90,0x9b,0x91,0x90,0x91]

private func blackTotalensity(red num: UInt8) -> UInt8 {
    return num ^ 245
}

/*: "The other hung up the phone" :*/
fileprivate let k_nothingFirstFormat:String = "write scope conversationThe o"
fileprivate let k_constantId:String = "cover distribution levelung u"
fileprivate let app_installMessage:String = "HONE"

/*: "type" :*/
fileprivate let main_insertTitle:String = "pinpe"

/*: "jumpUrl" :*/
fileprivate let mainRiseName:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let const_onlyUrl:String = "Cancelfactory universal"

/*: "Get gold coins" :*/
fileprivate let notiAddressLiteralValue:[Character] = ["G","e","t"," ","g","o","l","d"]
fileprivate let main_coordinatorPaintFormat:[Character] = [" ","c","o","i","n","s"]

/*: "refuseSex" :*/
fileprivate let show_thenConfirmMessage:String = "array tole privacy tworefuseSex"

/*: "onAcceptCall" :*/
fileprivate let noti_futureString:[Character] = ["o","n","A","c"]
fileprivate let appAgainstString:String = "ceptCalllater flex directly destination burden"

/*: "acceptCall" :*/
fileprivate let appHalfStr:String = "sustainableccep"
fileprivate let app_scaleNextUrl:[Character] = ["t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let kSumeractionMsg:[Character] = ["C","a","l","l","s"]
fileprivate let notiReMomentumName:[Character] = [" ","a","r","e"," ","c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let dataAngleLanguageUrl:String = "The extent rime some refrigerator sign"
fileprivate let appDocumentValue:String = "portrait launch compute has en"
fileprivate let data_postData:[Character] = ["d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EmptyContainerScientific.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let show_quitCommonCharacterTitle = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class EmptyContainerScientific: MasterClip {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: RespectiveModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PerSmart? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: AttachView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        QuantityensityAction.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        QuantityensityAction.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        QuantityensityAction.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_tabName.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! FinishController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.subtle()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            systemCur()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.versusScopeVC = nil
            QuantityensityAction.shared.versusScopeVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.sinceFixed()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        normProgress()
        //: setupSubViewsConstraint()
        asBroad()
        //: initNotificationObserver()
        exceptAm()
        //: req_callGetUserInfo()
        mid()
        //: timeoutRules()
        amOf()
        //: resumeTalk()
        writtenForPermission()
        //: setUpBGMPlayer()
        trigger()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if VideoMagnitudeerval.share.appConfigMode.disableShootScreen {
            if VideoMagnitudeerval.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ProviderSineView(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.versusScope = false
        QuantityensityAction.shared.versusScope = false
        //: TalkingSocketManager.shared.isCalling = false
        QuantityensityAction.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.clubRe()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: DirectionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = DirectionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: FlushResume = {
        //: let m = TalkingVoiceChatManager()
        let m = FlushResume()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension EmptyContainerScientific {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func mid() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        AtAnalyzeObject.sameOn(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: showSiblingMsg.map{greetFilter(extend: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(dataSharedMessage.suffix(5)) + String(appIronicNeverId))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(mainArrestUrl.replacingOccurrences(of: "offer", with: "me"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(noti_warningData))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(user_eventMsg))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(user_screenName.replacingOccurrences(of: "original", with: "l") + k_portraitData.replacingOccurrences(of: "permission", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.exceptScript()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension EmptyContainerScientific {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func systemCur() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        ReduceLevelDoingtoPrior.afterRoad(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.conclusion(showMsg: k_boldEquivalentMsg)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(dataLoseMessage)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: showSiblingMsg.map{greetFilter(extend: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.isDocument()
                //: return
                return
            }

            //: self.stopBGM()
            self.income()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PerSmart.complete(type: .Response)
            //: self.startBGM()
            self.subtle()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func clubRe() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.income()
        //: TalkingSocketManager.shared.callerDelegate = nil
        QuantityensityAction.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        QuantityensityAction.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        QuantityensityAction.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func isDocument(animated: Bool = true) {
        //: self.stopBGM()
        self.income()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func commit() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.consumption(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func writtenForPermission() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            commit()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension EmptyContainerScientific {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func exceptAm() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(mapBean(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(body(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: show_firstUndertakeLikeFormat,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        show_quitCommonCharacterTitle.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func mapBean(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            isDocument()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func body(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard QuantityensityAction.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        QuantityensityAction.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        QuantityensityAction.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        QuantityensityAction.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.pastMinimum(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension EmptyContainerScientific: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.conclusion(showMsg: (String(showDoingdCustomerMessage.suffix(6)) + "nterr" + String(noti_labelValue)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(main_becomeParaName)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: user_analyzeTaName.map{dragRime(identify: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(constTotalensityMeasureMsg)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: user_analyzeTaName.map{dragRime(identify: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(dataLoseMessage)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: showSiblingMsg.map{greetFilter(extend: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.bothPinAbove()
                //: TalkingSocketManager.shared.versusScopeVC = nil
                QuantityensityAction.shared.versusScopeVC = nil
            }
            //: popCurrentViewController()
            isDocument()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.three(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension EmptyContainerScientific {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func trigger() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? CeilingOval.Call : CeilingOval.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PerSmart.complete(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func subtle() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.strategy()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func income() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.implementAttention()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension EmptyContainerScientific {
    /// 超时定时器
    //: private func timeoutRules() {
    private func amOf() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? user_adjustMessage : self.chatModel?.intervalGreet()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            forceOp()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.forceOp()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func forceOp() {
        //: guard !TalkingSocketManager.shared.versusScope else { return }
        guard !QuantityensityAction.shared.versusScope else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.conclusion(showMsg: (String(show_fitData)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(constTotalensityMeasureMsg)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: user_analyzeTaName.map{dragRime(identify: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(dataLoseMessage)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: showSiblingMsg.map{greetFilter(extend: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        QuantityensityAction.shared.frameOfFeed(info: socketDict)
        //: popCurrentViewController()
        isDocument()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension EmptyContainerScientific {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func exceptResBottom(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: user_analyzeTaName.map{dragRime(identify: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(constInputKey) + noti_literalModifyFormat.replacingOccurrences(of: "gall", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.intervalensity(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.isolateCos()
            //: createTalkRoom()
            commit()
            //: self.stopBGM()
            self.income()
        }
    }
}

// MARK: - AngleEdge【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension EmptyContainerScientific: AngleEdge {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func refreshDown(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(notiDuringValue)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(show_goldPullTitle) + String(const_taTrickValue))].stringValue == ChannelDown.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showHardValue.suffix(3)) + String(show_subName.prefix(5)) + "ung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.conclusion(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bothPinAbove()
            //: TalkingSocketManager.shared.versusScopeVC = nil
            QuantityensityAction.shared.versusScopeVC = nil
        }

        //: popCurrentViewController()
        isDocument()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func proWith(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: user_analyzeTaName.map{dragRime(identify: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.income()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.isolateCos()
        //: self.createTalkRoom()
        self.commit()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func multiRe(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(dataSecretMechanicalFormat) + String(kAppropriateContactId))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(notiDuringValue)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(notiSubjectId))].stringValue == ChannelDown.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(showHardValue.suffix(3)) + String(show_subName.prefix(5)) + "ung up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue {
            if VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: noti_wakePath.map{blackTotalensity(red: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(k_nothingFirstFormat.suffix(5)) + "ther h" + String(k_constantId.suffix(5)) + "p the p" + app_installMessage.lowercased()).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.conclusion(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bothPinAbove()
            //: TalkingSocketManager.shared.versusScopeVC = nil
            QuantityensityAction.shared.versusScopeVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.micFrame()
        //: self.popCurrentViewController()
        self.isDocument()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kFunctionalCornerPath, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func untilBy(msg: String, data: [String: Any]) {
        //: guard VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(main_insertTitle.replacingOccurrences(of: "pin", with: "ty"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(mainRiseName))] as? String else { return }
            //: let httpsUrl = VideoMagnitudeerval.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = VideoMagnitudeerval.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = StartTranslate()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            ScopeEmbrace.share.painter(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            TapBounce.notMovementSick(title: nil,
                                             //: message: msg,
                                             message: msg,
                                             //: leftBtnTitle: "Cancel".localized,
                                             leftBtnTitle: (String(const_onlyUrl.prefix(6))).localized,
                                             //: rightBtnTitle: "Get gold coins".localized) {
                                             rightBtnTitle: (String(notiAddressLiteralValue) + String(main_coordinatorPaintFormat)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                TapBounce.outConvert()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                ScopeEmbrace.share.rateBreeze(clickEvent: k_cornerId, sufficient: false)
            }
        }
    }
}

// MARK: - FloorConsume【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension EmptyContainerScientific: FloorConsume {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func noneInstance(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(notiDuringValue)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(show_thenConfirmMessage.suffix(9)))].stringValue == ChannelDown.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showHardValue.suffix(3)) + String(show_subName.prefix(5)) + "ung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.conclusion(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bothPinAbove()
            //: TalkingSocketManager.shared.versusScopeVC = nil
            QuantityensityAction.shared.versusScopeVC = nil
        }
        //: popCurrentViewController()
        isDocument()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func beforeTranslate(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: user_analyzeTaName.map{dragRime(identify: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.income()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.isolateCos()
        //: self.createTalkRoom()
        self.commit()
    }
}

// MARK: - ReduceException【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension EmptyContainerScientific: ReduceException {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func pinAudience(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(dataLoseMessage)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.conclusion(showMsg: (String(show_fitData)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.bothPinAbove()
                //: TalkingSocketManager.shared.versusScopeVC = nil
                QuantityensityAction.shared.versusScopeVC = nil
            }
            //: self.popCurrentViewController()
            self.isDocument()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(noti_futureString) + String(appAgainstString.prefix(8))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue else { return }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(sufficient: false)
            ScopeEmbrace.share.rateBreeze(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(noti_futureString) + String(appAgainstString.prefix(8))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.conclusion(showMsg: msg)
            //: self.popCurrentViewController()
            self.isDocument()
        }
    }
}

// MARK: - MovementStyle【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension EmptyContainerScientific: MovementStyle {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func constraint() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        QuantityensityAction.shared.frameOfFeed(info: [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (appHalfStr.replacingOccurrences(of: "sustainable", with: "a") + String(app_scaleNextUrl)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: showSiblingMsg.map{greetFilter(extend: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func listFlagEdit() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.conclusion(showMsg: (String(kSumeractionMsg) + String(notiReMomentumName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            QuantityensityAction.shared.frameOfFeed(info: [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(constTotalensityMeasureMsg)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: user_analyzeTaName.map{dragRime(identify: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.conclusion(showMsg: (String(kSumeractionMsg) + String(notiReMomentumName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            QuantityensityAction.shared.frameOfFeed(info: [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(dataLoseMessage)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: showSiblingMsg.map{greetFilter(extend: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.conclusion(showMsg: (String(dataAngleLanguageUrl.prefix(4)) + "call" + String(appDocumentValue.suffix(7)) + String(data_postData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            QuantityensityAction.shared.frameOfFeed(info: [String(bytes: dataSophisticateSearchLaterTitle.map{targetMp(half: $0)}, encoding: .utf8)!: (String(main_becomeParaName)), String(bytes: dataRemainingKey.map{$0^123}, encoding: .utf8)!: [String(bytes: user_analyzeTaName.map{dragRime(identify: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.micFrame()
        //: self.popCurrentViewController()
        self.isDocument()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func okRank(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.afterItem(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func orientation(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.betweenOutput(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func average() {
        //: TalkingSocketManager.shared.versusScopeVC = self
        QuantityensityAction.shared.versusScopeVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = AttachView.computeMight()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.congressionalFor()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.awakeSubstantial()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.change(talkingTime)
        //: popCurrentViewController()
        isDocument()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func dayOn(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.change(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func bounceMention(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.behindQuote(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension EmptyContainerScientific {
    /// 添加视图
    //: private func setupSubviews() {
    private func normProgress() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func asBroad() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
