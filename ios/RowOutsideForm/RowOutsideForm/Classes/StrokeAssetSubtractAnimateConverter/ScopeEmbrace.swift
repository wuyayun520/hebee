
//: Declare String Begin

/*: "fee" :*/
fileprivate let noti_earlySufficientName:String = "fintegrity"

/*: "VIPFee" :*/
fileprivate let mainScaleUrl:String = "VIPFeeexamination exist"

/*: "chattingNum" :*/
fileprivate let dataAddressDirectionPath:String = "region"
fileprivate let notiPathKey:[Character] = ["h","a","t","t","i","n","g","N","u","m"]

/*: "You're already in her live room" :*/
fileprivate let showLayerHiddenStr:[UInt8] = [0x6f,0x59,0x43,0x11,0x44,0x53,0x16,0x57,0x5a,0x44,0x53,0x57,0x52,0x4f,0x16,0x5f,0x58,0x16,0x5e,0x53,0x44,0x16,0x5a,0x5f,0x40,0x53,0x16,0x44,0x59,0x59,0x5b]

private func cosAbout(beauty num: UInt8) -> UInt8 {
    return num ^ 54
}

/*: "toUid" :*/
fileprivate let notiSponsorGiftTitle:[UInt8] = [0xff,0xfa,0xe0,0xf4,0xef]

fileprivate func angleNearby(detail num: UInt8) -> UInt8 {
    let value = Int(num) - 139
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "momentId" :*/
fileprivate let mainConditionMsg:[UInt8] = [0xf9,0xfb,0xf9,0xf1,0xfa,0xe0,0xdd,0xf0]

private func progressExamineerMigration(play num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "extra" :*/
fileprivate let userToolTitle:[UInt8] = [0x61,0x72,0x74,0x78,0x65]

/*: "uid" :*/
fileprivate let main_constructMsg:[UInt8] = [0xb7,0xab,0xa6]

fileprivate func manageRespective(version num: UInt8) -> UInt8 {
    let value = Int(num) - 66
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let dataMagnitudeerruptPath:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let showRoleKey:[UInt8] = [0x21,0x3b,0x6,0x2d,0x3f,0x1d,0x3b,0x2d,0x3a]

/*: "userStatus" :*/
fileprivate let k_screenId:[UInt8] = [0x83,0x85,0x93,0x84,0xa5,0x82,0x97,0x82,0x83,0x85]

/*: "Account is restricted！" :*/
fileprivate let constResignUrl:[Character] = ["A","c","c","o","u","n","t"," ","i","s"," ","r","e"]
fileprivate let showMissingLayStr:String = "handleri"
fileprivate let showMarriedStreamUrl:String = "cted！choice rapidly occur"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let const_sortCounteractionValue:[UInt8] = [0x81,0x9c,0xa4,0xa7,0xa0,0x9f,0x5b,0xaf,0xaa,0x5b,0xa0,0xa9,0xaf,0xa0,0xad,0x5b,0xaf,0xa3,0xa0,0x5b,0x9e,0xa3,0x9c,0xaf,0x5b,0xad,0xaa,0xaa,0xa8,0x69,0x5b,0x8b,0xa7,0xa0,0x9c,0xae,0xa0,0x5b,0xaf,0xad,0xb4,0x5b,0x9c,0xa2,0x9c,0xa4,0xa9,0x5b,0xa7,0x9c,0xaf,0xa0,0xad]

fileprivate func followBottom(marker num: UInt8) -> UInt8 {
    let value = Int(num) - 59
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "url" :*/
fileprivate let show_indexValue:[Character] = ["u","r","l"]

/*: "userDetail" :*/
fileprivate let constEqualGuidePath:String = "compute black interesteduserDeta"
fileprivate let showAnonymousEraseTowelString:[Character] = ["i","l"]

/*: "&type=6" :*/
fileprivate let k_betterStr:[Character] = ["&","t","y","p","e"]
fileprivate let app_romanMsg:String = "=curve"

/*: "&type=7" :*/
fileprivate let app_textName:[Character] = ["&"]
fileprivate let app_likeUrl:String = "type=7restriction trust"

/*: "amount" :*/
fileprivate let user_slowStr:[UInt8] = [0x74,0x6e,0x75,0x6f,0x6d,0x61]

/*: "https://" :*/
fileprivate let constPartStr:String = "htowel"
fileprivate let const_connectionOintmentMsg:String = "feedback hide arraytps://"

/*: "http://" :*/
fileprivate let noti_mechanismLatMsg:String = "http://decade bind profile"

/*: "transparency" :*/
fileprivate let app_paintUrl:String = "trbeforespare"
fileprivate let mainDisabledPath:String = "generaly"

/*: "version= :*/
fileprivate let data_textureMsg:[Character] = ["v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let kNativeBeMsg:[Character] = ["&","p","a","c","k","a","g"]
fileprivate let appCollectId:String = "eId=owner lab push man"

/*: &bundleId= :*/
fileprivate let constAwlTitle:String = "along step&bundl"
fileprivate let appPackageCrucialFormat:[Character] = ["e","I","d","="]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ScopeEmbrace.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class TalkingAppPushManager: NSObject {
public class ScopeEmbrace: NSObject {
    //: @objc static public let share = TalkingAppPushManager()
    @objc public static let share = ScopeEmbrace()
    //: private override init() {}
    override private init() {}
}

//: extension TalkingAppPushManager {
extension ScopeEmbrace {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func grate(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: main_combineName)
        //: guard TalkingSocketManager.shared.versusScope == false else {
        guard QuantityensityAction.shared.versusScope == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.conclusion(showMsg: notiReducePath)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ReduceLevelDoingtoPrior.sprinkle { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            HandleControlLimitBasicHead.property { succeed, result, errorModel in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = AlwaysViewController()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(noti_earlySufficientName.replacingOccurrences(of: "integrity", with: "ee"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(mainScaleUrl.prefix(6)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(dataAddressDirectionPath.replacingOccurrences(of: "region", with: "c") + String(notiPathKey))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.congressionalFor()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func receiverDoingStop(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.congressionalFor() else { return }
        //: if let profileVC = currentVC as? TalkingUserDetailViewController {
        if let profileVC = currentVC as? MoveGraph {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.chart()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = MoveGraph(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        priorityAssetSpace()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func nite(uid: String, enterType: EnableChangeListenerReduce) {
        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(RestoreChain.substantialAcrossBefore().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.conclusion(showMsg: String(bytes: showLayerHiddenStr.map{cosAbout(beauty: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        CalculateAttach().situationCommand(streamerUid: uid) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = LocationFramework.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.beyondState()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: RoleBarTap.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! RoleBarTap).successTo()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            RestoreChain.substantialAcrossBefore().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = RoleBarTap()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: constBetweenValue, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: user_evaluateStr, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func weirdFore(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = EmptyBelow(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = beyondState()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func violationQuantity(chatID: String, isFrom: VideoInformation = .Normal, completion: ((_ vc: BoundSmallThin) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != FormatDistribution.getXiaoMiID() {
        if chatID != FormatDistribution.raging() { // 系统消息
            //: ProgressHUD.show()
            MediaSquare.saveHead()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: notiSponsorGiftTitle.map{angleNearby(detail: $0)}, encoding: .utf8)!: chatID, String(bytes: mainConditionMsg.map{progressExamineerMigration(play: $0)}, encoding: .utf8)!: "0", String(bytes: userToolTitle.reversed(), encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            AnalyzeMapCommon.dark(param: param) { [weak self] succeed, result, errorModel in
                //: ProgressHUD.dismiss()
                MediaSquare.spell()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = TypicalCommand.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = TypicalCommand.reserveDownColumn(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                FlexDivide.readingEvaluate(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: main_constructMsg.map{manageRespective(version: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: dataMagnitudeerruptPath.reversed(), encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: showRoleKey.map{$0^72}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: k_screenId.map{$0^246}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: appColorRoundMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.conclusion(showMsg: (String(constResignUrl) + showMissingLayStr.replacingOccurrences(of: "handle", with: "st") + String(showMarriedStreamUrl.prefix(5))).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.onceCouncil(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: TypicalCommand())
                let chatVC = self.onceCouncil(chatID: chatID, isFrom: isFrom, userWrap: TypicalCommand())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: TypicalCommand ) -> TalkingPrivateChatController {
    func onceCouncil(chatID: String, isFrom: VideoInformation = .Normal, userWrap: TypicalCommand) -> BoundSmallThin {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = beyondState()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: BoundSmallThin.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! BoundSmallThin
                    //: if vc.msgView.targetID == chatID {
                    if vc.msgView.targetID == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = BoundSmallThin(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func slowEvery(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        MediaSquare.saveHead()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        AnalyzeMapCommon.somePurple(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            MediaSquare.spell()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.conclusion(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if FormatDistribution.isGroupChat(groupId) {
            if FormatDistribution.mechanicalBass(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.originalTowel(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = ThemePostLocate(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.beyondState()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, desc in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.originalTowel(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = ThemePostLocate(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.beyondState()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.conclusion(showMsg: String(bytes: const_sortCounteractionValue.map{followBottom(marker: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func originalTowel(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.beyondState()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: ThemePostLocate.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! ThemePostLocate
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func awayOriginal(uid: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func aboveMeasure() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = beyondState()
        //: let vc = TalkingFeedbackVC.init()
        let vc = CombineCompose()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func transactionDocument(jumpModel: TotaleractionStop) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (String(show_indexValue)) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            painter(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(constEqualGuidePath.suffix(8)) + String(showAnonymousEraseTowelString)) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func refresh() {
        //: TalkingAppPushManager.share.func__pushToWebVC(webViewType: .SubscribePage)
        ScopeEmbrace.share.attentionOf(webViewType: .SubscribePage)
    }
}

// MARK: - 跳转H5

//: extension TalkingAppPushManager {
extension ScopeEmbrace {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func attentionOf(webViewType: EnvironmentLog) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        fromCharm(webViewType: webViewType, webConfig: StartTranslate())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func commissioner(popupCallIndex: Int, appendParams: String = (String(k_betterStr) + app_romanMsg.replacingOccurrences(of: "curve", with: "6"))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = EditCoordinator()
        //: view.show()
        view.occurThroughFor()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.constraintOn(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                preciseOf(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func rateBreeze(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(app_textName) + String(app_likeUrl.prefix(6)))) {
        //: let payWinType = VideoMagnitudeerval.share.appUserConfigMode.payWinType
        let payWinType = VideoMagnitudeerval.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = EditCoordinator()
        //: view.show()
        view.occurThroughFor()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (VideoMagnitudeerval.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (VideoMagnitudeerval.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.constraintOn(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                preciseOf(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func preciseOf(appendParams: String = (String(app_textName) + String(app_likeUrl.prefix(6)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = MediaCharacteristic.outsideTrick(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: urlStr)
        ScopeEmbrace.share.painter(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        k_frameUrl.instanceHard(eventID: userWrapMsg, parameterStr: [String(bytes: user_slowStr.reversed(), encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func constraintOn(webViewType: EnvironmentLog, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = StartTranslate()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        fromCharm(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            data_viaTableUrl = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                k_frameUrl.instanceHard(eventID: clickEvent, parameterStr: [String(bytes: user_slowStr.reversed(), encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func applicationTier() {
        //: guard !VideoMagnitudeerval.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !VideoMagnitudeerval.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = VideoMagnitudeerval.share.appConfigMode.urlH5Domain + VideoMagnitudeerval.share.appUserConfigMode.discountUrl
        let httpsUrl = VideoMagnitudeerval.share.appConfigMode.urlH5Domain + VideoMagnitudeerval.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (constPartStr.replacingOccurrences(of: "towel", with: "t") + String(const_connectionOintmentMsg.suffix(6))), with: (String(noti_mechanismLatMsg.prefix(7))))
        //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpUrl)
        ScopeEmbrace.share.painter(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func fromCharm(webViewType: EnvironmentLog, webConfig: StartTranslate) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = MediaCharacteristic.outsideTrick(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        similarMake(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func painter(urlStr: String?, webConfig: StartTranslate? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? StartTranslate() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        similarMake(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func similarMake(urlStr: String?, webViewType: EnvironmentLog?, webConfig: StartTranslate) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(app_paintUrl.replacingOccurrences(of: "before", with: "an") + mainDisabledPath.replacingOccurrences(of: "general", with: "nc"))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(data_textureMsg)) + "\(main_persistDirectlyStr)" + (String(kNativeBeMsg) + String(appCollectId.prefix(4))) + "\(user_oneData)" + (String(constAwlTitle.suffix(6)) + String(appPackageCrucialFormat)) + "\(const_progressValue)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = BoldSort(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = beyondState() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: BoldSort.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! BoldSort).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.enableicial(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: notiExploreKey, height: notiExploreKey / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension TalkingAppPushManager {
public extension ScopeEmbrace {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func beyondState() -> UIViewController? {
        //: return currentViewController()
        return congressionalFor()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct TotaleractionStop: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
