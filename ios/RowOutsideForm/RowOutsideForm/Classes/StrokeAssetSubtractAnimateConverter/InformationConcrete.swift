
//: Declare String Begin

/*: "msgType" :*/
fileprivate let mainDayFormat:String = "beat conversion very expected frequencymsgType"

/*: "tips" :*/
fileprivate let const_willUsId:String = "thaveps"

/*: "extra" :*/
fileprivate let main_rebuildTitle:String = "extrmid"

/*: "msgInfo" :*/
fileprivate let app_responseKey:[Character] = ["m","s","g","I","n","f"]
fileprivate let notiBlindEveryoneKey:String = "week"

/*: "user" :*/
fileprivate let app_meMsg:String = "questioner"

/*: "uid" :*/
fileprivate let constGallString:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let kSuccessfullyName:[UInt8] = [0x8a,0x85,0x7f,0x87,0x8a,0x7d,0x89,0x81]

fileprivate func lemmaSlow(jar num: UInt8) -> UInt8 {
    let value = Int(num) - 28
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let k_bulletChangePath:[UInt8] = [0x51,0x5c,0x58,0x5d,0x69,0x50,0x5a]

private func doingdBad(structure num: UInt8) -> UInt8 {
    return num ^ 57
}

/*: "age" :*/
fileprivate let kProtectionFormat:[UInt8] = [0x4f,0x49,0x4b]

private func historyInmate(nor num: UInt8) -> UInt8 {
    return num ^ 46
}

/*: "gift" :*/
fileprivate let k_intensityIndependentPath:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let data_rapidlyId:[Character] = ["i","s","G","r","o","u","p"]

/*: "WantGift" :*/
fileprivate let showChainValue:String = "passing colorWa"
fileprivate let noti_resultLiteralString:[Character] = ["n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let dataWarnComputeAssetFormat:String = "tcondition"

/*: "img" :*/
fileprivate let mainStageAverageString:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let data_layParaKey:[Character] = ["v","i","d","e","o"]

/*: "audio" :*/
fileprivate let mainProtectionMsg:String = "aucontinuingo"

/*: "messageType" :*/
fileprivate let const_priceValue:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let notiMajorTitle:[Character] = ["c"]
fileprivate let main_ceilingName:[Character] = ["o","n","t","e","n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let show_denyData:[Character] = ["I","m","g","M","s","g"]

/*: "imageUri" :*/
fileprivate let user_layMsg:String = "dump trainimageUri"

/*: "type" :*/
fileprivate let appGapDeployMessage:String = "tysegment"

/*: "pic" :*/
fileprivate let appCalculateMessage:String = "pone"

/*: "isBurn" :*/
fileprivate let appCombinedStr:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let showHeartKidPath:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let const_frequencyStr:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let dataControlId:String = "document frozenaudioLe"
fileprivate let user_streamExplorationTitle:String = "nstrategyh"

/*: "len" :*/
fileprivate let notiEntryMessage:String = "totalen"

/*: "audioSandbox" :*/
fileprivate let show_rageData:[Character] = ["a","u","d","i","o","S","a","n"]
fileprivate let showInformationData:String = "dbourx"

/*: "senderId" :*/
fileprivate let notiTopTitle:[Character] = ["s","e","n","d","e","r","I"]
fileprivate let showSingleFormat:String = "command"

/*: "AudioMsg" :*/
fileprivate let noti_currencyTowelStr:String = "AudioMsgrage provision secret"

/*: "content" :*/
fileprivate let const_goingShareUrl:[Character] = ["c"]
fileprivate let showVideoStr:String = "oactualteactualt"

/*: "TxtMsg" :*/
fileprivate let kSaveName:String = "TxtMsgshould import thumb"

/*: "message" :*/
fileprivate let notiOddFormat:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let const_recoverString:[Character] = ["q","u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let show_supportUrl:String = "quportte"
fileprivate let appHandleMsg:String = "MsgInfodeny large team combined"

/*: "replayId" :*/
fileprivate let data_interestProperlyValue:String = "repdimension"

/*: "rYMsgType" :*/
fileprivate let user_amStatinTitle:String = "rYMsgTypemarried para"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let mainPendingString:[Character] = ["G","J",":","S","e","s","s",":","A","u"]
fileprivate let k_wedName:String = "ok assistanttoGr"
fileprivate let mainIncreaseMsg:String = "tCustomtopic funeral burn pic"

/*: "changeDirection" :*/
fileprivate let show_lastTitle:String = "chagive"
fileprivate let const_outerData:String = "stopct"
fileprivate let data_showPath:String = "iosupport"

/*: "system" :*/
fileprivate let userCustomUrl:[Character] = ["s","y","s","t","e","m"]

/*: "title" :*/
fileprivate let noti_helpUrl:String = "tpictle"

/*: "icon" :*/
fileprivate let userSecondRankFormat:String = "icvisitn"

/*: "all" :*/
fileprivate let showPleasePath:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let app_accuseName:String = "iimplementn"
fileprivate let main_yaName:String = "effect index tracks_pcr"

/*: "status" :*/
fileprivate let app_requireKey:String = "sregiontu"
fileprivate let userQueryPath:[Character] = ["s"]

/*: "msgIncome" :*/
fileprivate let k_movieString:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "msgId" :*/
fileprivate let dataLiteralPictureId:[Character] = ["m","s","g","I","d"]

/*: "MasterMaximum" :*/
fileprivate let noti_enableMissingTitle:String = "directly toTMsgT"
fileprivate let dataSectionResistanceRemarkFormat:String = "rtweennsl"
fileprivate let const_acceptFrameFormat:String = "arankus"

/*: "TMsgTranslatedContent" :*/
fileprivate let app_installBeyondValue:[Character] = ["T","M","s","g"]
fileprivate let noti_exerciseString:String = "insert yourself exTran"
fileprivate let main_measureRouteName:String = "Contentclub begin thread sun"

/*: "formatType" :*/
fileprivate let data_nearKey:[Character] = ["f","o","r","m","a","t","T","y","p","e"]

/*: "plain" :*/
fileprivate let data_presentationString:String = "plaftern"

/*: "imageData" :*/
fileprivate let dataInsideVoiceString:[Character] = ["i","m","a","g"]
fileprivate let main_signatureMsg:[Character] = ["e","D","a","t","a"]

/*: "VideoMsg" :*/
fileprivate let show_priceCommonUrl:String = "picture zone transform markerVide"

/*: "match" :*/
fileprivate let app_askString:[Character] = ["m","a","t","c","h"]

/*: "userIntro" :*/
fileprivate let constWeeklyUnableMsg:String = "explore object multiple nor rateuserIn"
fileprivate let data_yesterdayPath:String = "trbit"

/*: "targetId" :*/
fileprivate let mainScriptPremiumId:[Character] = ["t","a","r","g","e"]
fileprivate let constNormHemeFormat:String = "tIdbecome standard"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let noti_homeMessage:String = "YYYY"
fileprivate let notiEnoughData:String = "maker official memory sinced HH:m"
fileprivate let dataCommentName:[Character] = ["m",":","s","s"]

/*: "sendTime" :*/
fileprivate let userWindowSkinStr:[Character] = ["s","e","n","d","T","i","m","e"]

/*: "isRead" :*/
fileprivate let constKidMsg:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let kBrushFormat:String = "description mutual phi half characteraudioD"
fileprivate let showSecretUrl:String = "forwarda"

/*: "audioUri" :*/
fileprivate let kYieldString:String = "security state defineaudioUri"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InformationConcrete.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum EffectCollection: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - InformationConcrete

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class InformationConcrete: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func sumeraction() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> TaskAction? {
    public class func enableicialMark(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> TaskAction? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = only(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: SpellAround.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            SpellAround.shared.theDay(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> TaskAction? {
    public class func only(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> TaskAction? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(mainDayFormat.suffix(7)))] = (const_willUsId.replacingOccurrences(of: "have", with: "i"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(main_rebuildTitle.replacingOccurrences(of: "mid", with: "a")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = title(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SecondDepthLike(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ConnectMemberModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? TriggerModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(app_responseKey) + notiBlindEveryoneKey.replacingOccurrences(of: "week", with: "o"))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(app_meMsg.replacingOccurrences(of: "question", with: "us"))] = [
            //: "uid": VideoMagnitudeerval.share.loginUserMode.userID,
            (String(constGallString)): VideoMagnitudeerval.share.loginUserMode.userID,
            //: "nickname": VideoMagnitudeerval.share.loginUserMode.nickname,
            String(bytes: kSuccessfullyName.map{lemmaSlow(jar: $0)}, encoding: .utf8)!: VideoMagnitudeerval.share.loginUserMode.nickname,
            //: "headPic": VideoMagnitudeerval.share.loginUserMode.headPic,
            String(bytes: k_bulletChangePath.map{doingdBad(structure: $0)}, encoding: .utf8)!: VideoMagnitudeerval.share.loginUserMode.headPic,
            //: "age": VideoMagnitudeerval.share.loginUserMode.age,
            String(bytes: kProtectionFormat.map{historyInmate(nor: $0)}, encoding: .utf8)!: VideoMagnitudeerval.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(main_rebuildTitle.replacingOccurrences(of: "mid", with: "a")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = title(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(mainDayFormat.suffix(7)))] as? String == (String(k_intensityIndependentPath)) {
            //: var data: TaskAction
            var data: TaskAction
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(data_rapidlyId))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = SignPage(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = QuantityegrityMax(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = QuantityegrityMax(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ConnectMemberModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(mainDayFormat.suffix(7)))] as? String == (String(showChainValue.suffix(2)) + String(noti_resultLiteralString)) {
            //: let data = PopFixed(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = PopFixed(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ConnectMemberModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(mainDayFormat.suffix(7)))] as? String == (dataWarnComputeAssetFormat.replacingOccurrences(of: "condition", with: "xt")) {
            //: var data: TaskAction
            var data: TaskAction
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(data_rapidlyId))] as? String == "1" { // 群聊
                //: data = SkinUp(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = SkinUp(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ImageAngle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ImageAngle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ConnectMemberModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(mainDayFormat.suffix(7)))] as? String == (String(mainStageAverageString)) {
            //: let data = MenuAdjust(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MenuAdjust(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ConnectMemberModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(mainDayFormat.suffix(7)))] as? String == (String(data_layParaKey)) {
            //: let data = SharedImplementation(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SharedImplementation(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ConnectMemberModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(mainDayFormat.suffix(7)))] as? String == (mainProtectionMsg.replacingOccurrences(of: "continuing", with: "di")) {
            //: let data = BoxOrangeDistribution(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = BoxOrangeDistribution(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ConnectMemberModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(k_intensityIndependentPath))] != nil && (msgInfo[(String(const_priceValue))] as? Int) == 4 {
            //: let data = ImageAngle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ImageAngle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ConnectMemberModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: TaskAction) -> TaskAction? {
    public class func pastEven(withData cellData: TaskAction) -> TaskAction? {
        // 图片
        //: if let reCellData = cellData as? MenuAdjust {
        if let reCellData = cellData as? MenuAdjust {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(FormatDistribution.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(FormatDistribution.priority())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(notiMajorTitle) + String(main_ceilingName))] = (String(show_denyData))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(mainDayFormat.suffix(7)))] = (String(mainStageAverageString))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(user_layMsg.suffix(8)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(app_responseKey) + notiBlindEveryoneKey.replacingOccurrences(of: "week", with: "o"))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(appGapDeployMessage.replacingOccurrences(of: "segment", with: "pe"))] = (String(show_denyData))
            //: params["pic"] = filePath
            params[(appCalculateMessage.replacingOccurrences(of: "one", with: "ic"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(appCombinedStr))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(appCombinedStr))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(showHeartKidPath))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(showHeartKidPath))] = "0"
            }

            //: dic["params"] = params
            dic[(String(const_frequencyStr))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = only(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? SharedImplementation {
        if let reCellData = cellData as? SharedImplementation {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? BoxOrangeDistribution {
        if let reCellData = cellData as? BoxOrangeDistribution {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = FlexDivide.wrapConnect(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(dataControlId.suffix(7)) + user_streamExplorationTitle.replacingOccurrences(of: "strategy", with: "gt"))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(notiEntryMessage.replacingOccurrences(of: "total", with: "l"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(show_rageData) + showInformationData.replacingOccurrences(of: "our", with: "o"))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(show_rageData) + showInformationData.replacingOccurrences(of: "our", with: "o"))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(mainDayFormat.suffix(7)))] = (mainProtectionMsg.replacingOccurrences(of: "continuing", with: "di"))
            //: msgInfo["senderId"] = VideoMagnitudeerval.share.loginUserMode.userID
            msgInfo[(String(notiTopTitle) + showSingleFormat.replacingOccurrences(of: "command", with: "d"))] = VideoMagnitudeerval.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(app_responseKey) + notiBlindEveryoneKey.replacingOccurrences(of: "week", with: "o"))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(appGapDeployMessage.replacingOccurrences(of: "segment", with: "pe"))] = (String(noti_currencyTowelStr.prefix(8)))
            //: dic["params"] = params
            dic[(String(const_frequencyStr))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = only(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ImageAngle {
        if let reCellData = cellData as? ImageAngle {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(const_goingShareUrl) + showVideoStr.replacingOccurrences(of: "actual", with: "n"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(mainDayFormat.suffix(7)))] = (dataWarnComputeAssetFormat.replacingOccurrences(of: "condition", with: "xt"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(app_responseKey) + notiBlindEveryoneKey.replacingOccurrences(of: "week", with: "o"))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(appGapDeployMessage.replacingOccurrences(of: "segment", with: "pe"))] = (String(kSaveName.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(notiOddFormat))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.willEqual() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(const_recoverString))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(show_supportUrl.replacingOccurrences(of: "port", with: "o") + String(appHandleMsg.prefix(7)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: if let replayId = reCellData.msgModel.params["replayId"] { // 问题回答消息重发
            if let replayId = reCellData.msgModel.params[(data_interestProperlyValue.replacingOccurrences(of: "dimension", with: "l") + "ayId")] { // 问题回答消息重发
                //: params["replayId"] = replayId
                params[(data_interestProperlyValue.replacingOccurrences(of: "dimension", with: "l") + "ayId")] = replayId
            }

            //: dic["params"] = params
            dic[(String(const_frequencyStr))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = only(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> TaskAction? {
    public class func foreBound(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> TaskAction? {
        //: var cellData: TaskAction?
        var cellData: TaskAction?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(main_rebuildTitle.replacingOccurrences(of: "mid", with: "a"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(app_responseKey) + notiBlindEveryoneKey.replacingOccurrences(of: "week", with: "o"))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(app_meMsg.replacingOccurrences(of: "question", with: "us"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ConnectMemberModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == VideoMagnitudeerval.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == VideoMagnitudeerval.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == VideoMagnitudeerval.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == VideoMagnitudeerval.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(user_amStatinTitle.prefix(9)))] as? String == (String(mainPendingString) + String(k_wedName.suffix(4)) + "eet:Tx" + String(mainIncreaseMsg.prefix(7))) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(show_lastTitle.replacingOccurrences(of: "give", with: "n") + "geDi" + const_outerData.replacingOccurrences(of: "stop", with: "re") + data_showPath.replacingOccurrences(of: "support", with: "n"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = ignore(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(mainDayFormat.suffix(7)))] as? String == (String(userCustomUrl)) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ConnectMemberModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? TriggerModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SecondDepthLike(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(const_goingShareUrl) + showVideoStr.replacingOccurrences(of: "actual", with: "n"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = MenuAdjust(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MenuAdjust(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = SharedImplementation(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SharedImplementation(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = SignPage(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = QuantityegrityMax(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = QuantityegrityMax(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = ceilingStep(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(main_rebuildTitle.replacingOccurrences(of: "mid", with: "a"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(app_responseKey) + notiBlindEveryoneKey.replacingOccurrences(of: "week", with: "o"))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? DepthArchitectureModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = PopFixed(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = PopFixed(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = BoxOrangeDistribution(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BoxOrangeDistribution(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? BoxOrangeDistribution {
            if let audioCellData = cellData as? BoxOrangeDistribution {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = paraBottom(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = DensityRedRender.distanceThroughBubble(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = DensityRedRender.session(findWithinProgress(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SignSmall(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(noti_helpUrl.replacingOccurrences(of: "pic", with: "i"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(String(const_goingShareUrl) + showVideoStr.replacingOccurrences(of: "actual", with: "n"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(mainStageAverageString))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = TriggerEnvironment(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = SkinUp(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = SkinUp(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ImageAngle(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ImageAngle(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? EfficiencyModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(userSecondRankFormat.replacingOccurrences(of: "visit", with: "o"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: VideoMagnitudeerval.share.loginUserMode.headPic ?? ""
                        VideoMagnitudeerval.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = DepthArchitectureModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(const_goingShareUrl) + showVideoStr.replacingOccurrences(of: "actual", with: "n"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(main_rebuildTitle.replacingOccurrences(of: "mid", with: "a"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = SharedResumeThinModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(showPleasePath))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (app_accuseName.replacingOccurrences(of: "implement", with: "co") + "_chat" + String(main_yaName.suffix(5)))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.magnitudeeractionMusic(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(app_requireKey.replacingOccurrences(of: "region", with: "ta") + String(userQueryPath))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(k_movieString))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(const_priceValue))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(dataLiteralPictureId))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(noti_enableMissingTitle.suffix(5)) + dataSectionResistanceRemarkFormat.replacingOccurrences(of: "tween", with: "a") + "ateSt" + const_acceptFrameFormat.replacingOccurrences(of: "rank", with: "t"))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = MasterMaximum(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(app_installBeyondValue) + String(noti_exerciseString.suffix(4)) + "slated" + String(main_measureRouteName.prefix(7)))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = VerticalBuildReceiveGreenObserverModel.detail()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = VerticalBuildReceiveGreenObserverModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = ProvideModel.consume()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = ProvideModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = FrequencyModel.beforeObject()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func ignore(_ extraDic: [String: Any]) -> EffectCollection {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(main_rebuildTitle.replacingOccurrences(of: "mid", with: "a"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(app_responseKey) + notiBlindEveryoneKey.replacingOccurrences(of: "week", with: "o"))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? ConnectMemberModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(data_nearKey))] as? String == (data_presentationString.replacingOccurrences(of: "after", with: "ai")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(user_layMsg.suffix(8)))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(dataInsideVoiceString) + String(main_signatureMsg))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(show_priceCommonUrl.suffix(4)) + "oMsg") {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = containerFactor(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(String(app_askString))] as? Int) == 1 || (msgInfo?[(String(app_askString))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(mainDayFormat.suffix(7)))] as? String == (const_willUsId.replacingOccurrences(of: "have", with: "i")) || (dic?[(String(const_priceValue))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(data_nearKey))] as? String == "mf") && ((dic?[(String(const_priceValue))] as? Int) == 15 || (dic?[(String(const_priceValue))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(mainDayFormat.suffix(7)))] as? String == (String(constWeeklyUnableMsg.suffix(6)) + data_yesterdayPath.replacingOccurrences(of: "bit", with: "o")) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(mainDayFormat.suffix(7)))] as? String == (String(showChainValue.suffix(2)) + String(noti_resultLiteralString)) || (msgInfo?[(String(const_priceValue))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func findWithinProgress(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.magnitudeeractionMusic(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(main_rebuildTitle.replacingOccurrences(of: "mid", with: "a"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(app_responseKey) + notiBlindEveryoneKey.replacingOccurrences(of: "week", with: "o"))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(app_meMsg.replacingOccurrences(of: "question", with: "us"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(notiTopTitle) + showSingleFormat.replacingOccurrences(of: "command", with: "d"))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == VideoMagnitudeerval.share.loginUserMode.userID {
        if senderId == VideoMagnitudeerval.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(notiTopTitle) + showSingleFormat.replacingOccurrences(of: "command", with: "d"))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(mainScriptPremiumId) + String(constNormHemeFormat.prefix(3)))] = message.userID ?? (user?[(String(mainScriptPremiumId) + String(constNormHemeFormat.prefix(3)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(notiTopTitle) + showSingleFormat.replacingOccurrences(of: "command", with: "d"))] = "\(msgInfo?[(String(constGallString))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(mainScriptPremiumId) + String(constNormHemeFormat.prefix(3)))] = "\(msgInfo?[(String(constGallString))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(dataLiteralPictureId))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(dataLiteralPictureId))] = "\(msgInfo?[(String(dataLiteralPictureId))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.magnitudeeractionMusic(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(dataLiteralPictureId))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(dataLiteralPictureId))] = "\(customDic[(String(dataLiteralPictureId))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(dataLiteralPictureId))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(show_rageData) + showInformationData.replacingOccurrences(of: "our", with: "o"))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(show_rageData) + showInformationData.replacingOccurrences(of: "our", with: "o"))] = "\(msgInfo?[(String(show_rageData) + showInformationData.replacingOccurrences(of: "our", with: "o"))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(dataControlId.suffix(7)) + user_streamExplorationTitle.replacingOccurrences(of: "strategy", with: "gt"))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(dataControlId.suffix(7)) + user_streamExplorationTitle.replacingOccurrences(of: "strategy", with: "gt"))] = "\(msgInfo?[(String(dataControlId.suffix(7)) + user_streamExplorationTitle.replacingOccurrences(of: "strategy", with: "gt"))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (noti_homeMessage.lowercased() + "-MM-d" + String(notiEnoughData.suffix(6)) + String(dataCommentName))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(userWindowSkinStr))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == VideoMagnitudeerval.share.loginUserMode.userID ? "1" : "0"
        info?[(String(constKidMsg))] = senderId == VideoMagnitudeerval.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func paraBottom(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = findWithinProgress(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(dataLiteralPictureId))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func ceilingStep(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func title(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func containerFactor(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(mainDayFormat.suffix(7)))] as? String, msgType == (mainProtectionMsg.replacingOccurrences(of: "continuing", with: "di")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(notiMajorTitle) + String(main_ceilingName))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(noti_currencyTowelStr.prefix(8)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(kBrushFormat.suffix(6)) + showSecretUrl.replacingOccurrences(of: "forward", with: "at"))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(kYieldString.suffix(8)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
