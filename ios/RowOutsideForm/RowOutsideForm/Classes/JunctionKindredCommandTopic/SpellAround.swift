
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let notiAssignmentMasterData:String = "IM退出club team ago"
fileprivate let notiGirlTitle:String = "secret adolescent next tractorode:"

/*: , desc: :*/
fileprivate let appRegionData:String = "past pro pack, desc:"

/*: "IM登录失败，code: :*/
fileprivate let notiAdviceString:[Character] = ["I","M","登","录","失","败","\u{ff0c}","c","o","d","e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let noti_thinkingShareData:String = "IM获取未读tool scale"
fileprivate let show_listenDirectGestureName:String = "adjust sterol失败，cod"
fileprivate let userRawLemmaFormat:String = "light populare:"

/*: "crushBadge" :*/
fileprivate let appDiscountValue:String = "cruoperateh"

/*: "badNumber" :*/
fileprivate let appTrickProcessMsg:[UInt8] = [0xb3,0xb0,0xb5,0x9f,0xa4,0xbc,0xb3,0xb4,0xa3]

private func versionName(fill num: UInt8) -> UInt8 {
    return num ^ 209
}

/*: "plaza" :*/
fileprivate let data_disabledData:String = "pconsumptionaza"

/*: "roomNum" :*/
fileprivate let constFactorMsg:[Character] = ["r","o","o","m","N","u","m"]

/*: "num" :*/
fileprivate let data_requirePriorValue:String = "endm"

/*: "onlyOneRoomId" :*/
fileprivate let show_globalTodayId:[Character] = ["o","n","l","y","O","n","e","R"]
fileprivate let appRevealString:[Character] = ["o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let showLeastStr:String = "opinion any vitalLIVE-"
fileprivate let kRiseMsg:[Character] = ["C","H","A","T"]

/*: "extra" :*/
fileprivate let user_introHimData:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let dataOwnerEndTitle:String = "mwho"
fileprivate let show_dueTunTitle:String = "medium event fun ceiling rimegType"

/*: "newPkgGift" :*/
fileprivate let notiHiddenScriptFormat:[Character] = ["n","e","w","P","k","g","G","i"]
fileprivate let main_againstTitle:[Character] = ["f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let notiSustainableRobotTitle:[Character] = ["M","F",":","C","h","a","t","M","e","n","t","i","o","n","M","s"]
fileprivate let app_drawingKey:String = "child"

/*: "uid" :*/
fileprivate let k_riseGeneralRemoteFormat:[UInt8] = [0x5,0x19,0x14]

private func nobodyStop(mid num: UInt8) -> UInt8 {
    return num ^ 112
}

/*: "onlineStatus" :*/
fileprivate let noti_rainEntertainmentId:[UInt8] = [0x99,0x98,0x9a,0x9f,0x98,0x93,0xa5,0x82,0x97,0x82,0x83,0x85]

private func exactlyTension(location num: UInt8) -> UInt8 {
    return num ^ 246
}

/*: "isNewUser" :*/
fileprivate let user_backgroundName:[UInt8] = [0x63,0x79,0x44,0x6f,0x7d,0x5f,0x79,0x6f,0x78]

private func matchParent(major num: UInt8) -> UInt8 {
    return num ^ 10
}

/*: "c2c_ :*/
fileprivate let app_greetUrl:String = "strip key choose commissioner eachc2c_"

/*: "IM获取会话失败，convId: :*/
fileprivate let noti_videoStr:String = "IM\u{83b7}\u{53d6}会"
fileprivate let k_volumeString:String = "onvIdaround stroke"
fileprivate let data_lessId:String = "thumb"

/*: , code: :*/
fileprivate let showRawTitle:[Character] = [","," ","c","o","d","e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let userUntilPath:String = "translate agile later incredible thatIM插入"
fileprivate let showDesignChartMoreStr:String = "taaloneg"

/*: "changeDirection" :*/
fileprivate let user_dailyFormat:String = "changeDiraccess accuse delicate nothing"
fileprivate let main_spellMinePath:[Character] = ["e","c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let data_purpleIonKey:String = "rYMscomply let bottom pro"
fileprivate let app_unknownPictureValue:String = "gTypepic occur across"

/*: "GJ:CallCustom" :*/
fileprivate let mainBotMsg:[Character] = ["G","J",":","C","a","l","l","C"]
fileprivate let kCanvasId:String = "USTOM"

/*: "IM获取会话列表失败，code: :*/
fileprivate let mainHeavyUrl:String = "comment past meeting shortIM获取会"
fileprivate let dataCornerKey:String = "，codscope match path formal violation"
fileprivate let mainJobKey:String = "deny away place professionale:"

/*: "msgInfo" :*/
fileprivate let showThinPurpleAudienceTitle:[Character] = ["m","s","g","I","n","f","o"]

/*: "needFold" :*/
fileprivate let userActiveUrl:String = "ribboneed"

/*: "group_ :*/
fileprivate let constIncredibleBeforeString:String = "global straightengroup_"

/*: "IM删除会话失败，convID: :*/
fileprivate let main_smartCameraStr:String = "grain possibleIM删除会"
fileprivate let appNativeId:[Character] = ["话","失","败","\u{ff0c}","c","o","n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let data_lightMsg:String = "IM置顶会condition obtain comprehensive"
fileprivate let show_stageCenterUrl:String = "convId:publisher violation due profile"

/*: "SDK 正在连接到服务器" :*/
fileprivate let main_masterHeavyStr:String = "Spool"
fileprivate let notiVisibleBuildUrl:String = " 正在连"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let noti_readyShareId:String = "SDK 已future gray stack yea"
fileprivate let constPackPath:String = "接到服\u{52a1}器"

/*: "IM连接服务器失败，code: :*/
fileprivate let user_tingReminderTitle:String = "go equal shIM连接服务"
fileprivate let noti_councilFormat:String = "live line enjoyode:"

/*: , error: :*/
fileprivate let notiGenuineData:[Character] = [","," ","e","r","r"]
fileprivate let showBeginTitle:[Character] = ["o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let notiWeekPath:[Character] = ["I","M","账","号","被","\u{6324}","\u{4e0b}"]
fileprivate let notiHappyPhaseCouldData:[Character] = ["线","。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let user_limitUrl:[UInt8] = []

private func workPublish(video num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "Offline Notification" :*/
fileprivate let user_vidUrl:[Character] = ["O","f","f","l","i","n","e"," ","N","o","t","i","f","i","c","a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let noti_keepUrl:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let notiContextSquareValue:String = "king simultaneouslyRe Login"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SpellAround.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol AlongNameure: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func globalVisual(data: [EdgeBelow])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func around(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func hidden(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum ProjectTotalegration {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class SpellAround: NSObject {
@objcMembers public class SpellAround: NSObject {
    //: public static let shared = SpellAround()
    public static let shared = SpellAround()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [EdgeBelow] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [EdgeBelow] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [EdgeBelow] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [EdgeBelow] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [EdgeBelow] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: EdgeBelow]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.installQuality()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func scopeError() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(k_constraintTitle, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func installQuality() {
        //: func__removeAllDelegate()
        evolution()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.tap()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func tap() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = EdgeBelow.savePush(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = EdgeBelow.savePush(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func feedback() -> [EdgeBelow] {
        // 审核模式，在消息列表移除官方客服
        //: if VideoMagnitudeerval.share.appStatus == AppSkinStatus.special.rawValue {
        if VideoMagnitudeerval.share.appStatus == PromiseTime.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == FormatDistribution.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == FormatDistribution.yea() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension SpellAround {
extension SpellAround {
    //: func func__removeAllDelegate() {
    func evolution() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func keep(_ delegate: AlongNameure) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func again(_ delegate: AlongNameure) {
        //: self.func__invokeDelegate {
        self.curDaily {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func curDaily(_ invocation: (AlongNameure) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! AlongNameure)
        }
    }
}

// MARK: - IM登录态管理

//: extension SpellAround {
extension SpellAround {
    /// 退出登录IM
    //: func func__LogingOut() {
    func reNetwork() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            TrainGreat.bitRemove(msg: (String(notiAssignmentMasterData.prefix(4)) + "登录失败，c" + String(notiGirlTitle.suffix(4))) + "\(code)" + (String(appRegionData.suffix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.installQuality()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func belowThrough(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = VideoMagnitudeerval.share.appUserConfigMode.uSig
        let uSigStr = VideoMagnitudeerval.share.appUserConfigMode.uSig
        //: let uidStr = VideoMagnitudeerval.share.loginUserMode.userID
        let uidStr = VideoMagnitudeerval.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.inviteElement()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            TrainGreat.bitRemove(msg: (String(notiAdviceString)) + "\(code)" + (String(appRegionData.suffix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func outContainOccur() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || VideoMagnitudeerval.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || VideoMagnitudeerval.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.conclusion(showMsg: appMoveTrainTitle)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension SpellAround {
extension SpellAround {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func inviteElement() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
//            TrainGreat.bitRemove(msg: (String(noti_thinkingShareData.prefix(6)) + "消息总数" + String(show_listenDirectGestureName.suffix(6)) + String(userRawLemmaFormat.suffix(2))) + "\(code)" + (String(appRegionData.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func computeFresh() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        AnalyzeMapCommon.dragWithLike { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(appDiscountValue.replacingOccurrences(of: "operate", with: "s") + "Badge")] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: appUntilName, object: self, userInfo: [String(bytes: appTrickProcessMsg.map{versionName(fill: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func architectureExercise(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        AnalyzeMapCommon.future { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(data_disabledData.replacingOccurrences(of: "consumption", with: "l"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(constFactorMsg))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = EdgeBelow.savePush(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(data_requirePriorValue.replacingOccurrences(of: "end", with: "nu"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(show_globalTodayId) + String(appRevealString))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension SpellAround: V2TIMAdvancedMsgListener {
extension SpellAround: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(showLeastStr.suffix(5)) + String(kRiseMsg)))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            YellowNumberensity.background().primaryWith(msg: msg)
            //: return
            return
        }
        //: if msg.sender == VideoMagnitudeerval.share.loginUserMode.userID { return }
        if msg.sender == VideoMagnitudeerval.share.loginUserMode.userID { return }

        //: let dic = MaintainSection.parseTXMessageData(data: msg.customElem.data)
        let dic = MaintainSection.resumeOfCover(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(user_introHimData))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? ConnectMemberModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: main_addNearUrl, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(dataOwnerEndTitle.replacingOccurrences(of: "who", with: "s") + String(show_dueTunTitle.suffix(5)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(notiHiddenScriptFormat) + String(main_againstTitle)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: k_accuracyMsg, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if MaintainSection.checkVoiceMessage(message: msg) {
            if MaintainSection.towardProviderWork(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = InformationConcrete.findWithinProgress(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                DensityRedRender.session(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.exit(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            GateSpecify.shared.userTransform(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            GateSpecify.shared.usedTo(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(notiSustainableRobotTitle) + app_drawingKey.replacingOccurrences(of: "child", with: "g")) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            SymbolBold.share.easyAdd(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: SustainableSaveerGift.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: SustainableSaveerGift.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != FormatDistribution.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != FormatDistribution.raging() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.curDaily { $0.hidden?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            ReliabilitySend.shared.cycleCamera(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: k_riseGeneralRemoteFormat.map{nobodyStop(mid: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: noti_rainEntertainmentId.map{exactlyTension(location: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: user_backgroundName.map{matchParent(major: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: appColorRoundMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            FlexDivide.plusSkin(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            SustainableSaveerGift.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: notiFilePath, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TaskAction, isPrivete: Bool) {
    @objc public func theDay(targetId: String, msgCellData: TaskAction, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.kit(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(app_greetUrl.suffix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.asFeedback(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.convertDownRemind(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                TrainGreat.bitRemove(msg: (noti_videoStr + "话失败\u{ff0c}c" + String(k_volumeString.prefix(5)) + data_lessId.replacingOccurrences(of: "thumb", with: ":")) + "\(convId)" + (String(showRawTitle)) + "\(code)" + (String(appRegionData.suffix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func kit(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = VideoMagnitudeerval.share.loginUserMode.userID
        let senderStr = VideoMagnitudeerval.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                TrainGreat.bitRemove(msg: (String(userUntilPath.suffix(4)) + "消\u{606f}失败，" + showDesignChartMoreStr.replacingOccurrences(of: "alone", with: "r") + "etId:") + "\(targetId)" + (String(showRawTitle)) + "\(code)" + (String(appRegionData.suffix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func exit(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(user_dailyFormat.prefix(9)) + String(main_spellMinePath))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && TalkingAppPushManager.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(data_purpleIonKey.prefix(4)) + String(app_unknownPictureValue.prefix(5)))].stringValue == (String(mainBotMsg) + kCanvasId.lowercased()) && isSelf && ScopeEmbrace.share.beyondState()!.isKind(of: BoundSmallThin.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: kBothData) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: kBothData)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension SpellAround: V2TIMConversationListener {
extension SpellAround: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.asFeedback(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.convertDownRemind(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.asFeedback(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.convertDownRemind(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: VideoMagnitudeerval.share.unreadMessageNum = Int(totalUnreadCount)
        VideoMagnitudeerval.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.curDaily {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.around?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func convertDownRemind(conVersationList: [EdgeBelow]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if FlexDivide.whenWithSend().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                FlexDivide.whenWithSend().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.curDaily {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.globalVisual?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func exceptReply(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [EdgeBelow] = []

        // V2TIMConversation->EdgeBelow
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = EdgeBelow(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.threadPhase(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [EdgeBelow] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = PainterServiceSkin.contextLimited(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.spreadAccessible(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension SpellAround {
extension SpellAround {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func month(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.exceptReply(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.convertDownRemind(conVersationList: arr as? [EdgeBelow] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
//            TrainGreat.bitRemove(msg: (String(mainHeavyUrl.suffix(5)) + "话列表失败" + String(dataCornerKey.prefix(4)) + String(mainJobKey.suffix(2))) + "\(code)" + (String(appRegionData.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func pushThin(targetId: String) -> EdgeBelow? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func asFeedback(convList: [V2TIMConversation]) -> [EdgeBelow] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [EdgeBelow] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = EdgeBelow(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.threadPhase(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.spreadAccessible(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func spreadAccessible(convModel: EdgeBelow, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.tap()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: ProjectTotalegration = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - VideoMagnitudeerval.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - VideoMagnitudeerval.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = FlexDivide.panel(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = MaintainSection.parseTXMessageData(data: elemData)
            let dict = MaintainSection.resumeOfCover(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(user_introHimData))][(String(showThinPurpleAudienceTitle))][(userActiveUrl.replacingOccurrences(of: "ribbon", with: "n") + "Fold")].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.remindConsumer(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.remindConsumer(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func remindConsumer(convList: [EdgeBelow], dataType: ProjectTotalegration) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func flexibleOnicial(targetId: String, dataType: ProjectTotalegration) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(app_greetUrl.suffix(4))) + "\(targetId)" : (String(constIncredibleBeforeString.suffix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            FlexDivide.phone(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(VideoMagnitudeerval.share.loginUserMode.userID, toUid: targetId)
            DensityRedRender.allow(VideoMagnitudeerval.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.inviteElement()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            TrainGreat.bitRemove(msg: (String(main_smartCameraStr.suffix(5)) + String(appNativeId)) + "\(convID)" + (String(showRawTitle)) + "\(code)" + (String(appRegionData.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func all(convModel: EdgeBelow, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            TrainGreat.bitRemove(msg: (String(data_lightMsg.prefix(5)) + "话失败\u{ff0c}" + String(show_stageCenterUrl.prefix(7))) + "\(convModel.conversationID)" + (String(showRawTitle)) + "\(code)" + (String(appRegionData.suffix(7))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.remindConsumer(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.remindConsumer(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension SpellAround: V2TIMSDKListener {
extension SpellAround: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (main_masterHeavyStr.replacingOccurrences(of: "pool", with: "DK") + notiVisibleBuildUrl.capitalized + "接到服务器"))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(noti_readyShareId.prefix(5)) + "经成功\u{8fde}" + constPackPath))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        TrainGreat.bitRemove(msg: (String(user_tingReminderTitle.suffix(6)) + "器失\u{8d25}，c" + String(noti_councilFormat.suffix(4))) + "\(code)" + (String(notiGenuineData) + String(showBeginTitle)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        TrainGreat.bitRemove(msg: (String(notiWeekPath) + String(notiHappyPhaseCouldData)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        QuantityensityAction.shared.redTag(userLoginOut: true)
        //: SpellAround.shared.isConnection = false
        SpellAround.shared.isConnection = false

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: user_limitUrl.map{workPublish(video: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = ImpactMobileContainer()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if ImmediateSumensityObserver.share.interfaceLang == RouteCritical.ar.rawValue || ImmediateSumensityObserver.share.interfaceLang == RouteCritical.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            ImmediateSumensityObserver.share.interfaceLang == RouteCritical.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.theSearcher(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        TapBounce.notMovementSick()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        TapBounce.notMovementSick(title: (String(user_vidUrl)).localized, message: msg, leftBtnTitle: (String(noti_keepUrl)).localized, rightBtnTitle: (String(notiContextSquareValue.suffix(8))).localized, leftBlock: {
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            NameExercise.belowThinFixed { t in
                //: ProgressHUD.dismiss()
                MediaSquare.spell()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: userWaitScopeTitle, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            NameExercise.substantial { t in
                //: if t {
                if t {
                    //: SpellAround.shared.func__LogingIn { _ in
                    SpellAround.shared.belowThrough { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if RestoreChain.substantialAcrossBefore().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(RestoreChain.substantialAcrossBefore().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    QuantityensityAction.shared.greenProfile()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
