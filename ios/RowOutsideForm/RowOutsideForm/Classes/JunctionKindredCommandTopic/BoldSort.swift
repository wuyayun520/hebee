
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let data_padKey:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let userShareRespectiveMessage:String = "sake zoneLoad..."

/*: "Skip" :*/
fileprivate let appPersistValue:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let userEnhanceName:[UInt8] = [0x97,0xab,0xab,0xaf,0x8b,0xb0,0xb0,0xb3,0xf1,0x91,0xbe,0xab,0xb6,0xa9,0xba,0x8b,0xb0,0x95,0xac,0xf7,0xf8,0xad,0xba,0xbc,0xb7,0xbe,0xad,0xb8,0xba,0xf8,0xf6]

private func custodyLoop(recommendation num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let notiWayMessage:[UInt8] = [0x6,0x18,0x1f,0x15,0x1e,0x6,0x5f,0x1e,0x1f,0x21,0x10,0x16,0x14,0x22,0x19,0x1e,0x6,0x57,0x57,0x1e,0x1f,0x21,0x10,0x16,0x14,0x22,0x19,0x1e,0x6,0x59,0x58,0x4a]

/*: "jsEvent(onPageShow):  :*/
fileprivate let const_phiValue:String = "net residency alive passingjsEven"
fileprivate let userChapterKey:String = "depth ego memberageSh"
fileprivate let k_singleData:[Character] = ["o","w",")",":"," "]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let constTopFormat:[UInt8] = [0x4e,0x50,0x57,0x5d,0x56,0x4e,0x17,0x56,0x57,0x69,0x58,0x5e,0x5c,0x71,0x50,0x5d,0x5c,0x1f,0x1f,0x56,0x57,0x69,0x58,0x5e,0x5c,0x71,0x50,0x5d,0x5c,0x11,0x10,0x2]

/*: "jsEvent(onPageHide):  :*/
fileprivate let showBodyPath:String = "hide inherentjsEve"
fileprivate let appKingInvitationValue:String = "umber follownt(onPag"
fileprivate let appRemindDeadlineMessage:[Character] = ["e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let user_reserveTitle:String = "factorifactorle"

/*: "canGoBack" :*/
fileprivate let app_childMessage:[Character] = ["c","a","n","G","o","B","a","c","k"]

/*: "PHPSESSID= :*/
fileprivate let constEnablelyReplyStr:String = "yetHyet"
fileprivate let noti_panLowString:[Character] = ["S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let main_holdKey:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let dataReplyValue:String = "gender gate successCookie"

/*: "nav_back_black_nor" :*/
fileprivate let appEqualUrl:[Character] = ["n","a","v","_","b","a","c","k","_","b","l"]
fileprivate let app_marginTitle:String = "ack_noraverage anonymous"

/*: "ico_intimate_list_sm" :*/
fileprivate let user_specieData:String = "ico_inpossibly without sink"
fileprivate let const_delayFormat:String = "e_lisdisplay the"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let app_teamTitle:[UInt8] = [0xd,0x6,0xa,0x1c,0x4,0xc,0x7,0x1d,0x47,0xe,0xc,0x1d,0x2c,0x5,0xc,0x4,0xc,0x7,0x1d,0x1a,0x2b,0x10,0x3d,0x8,0xe,0x27,0x8,0x4,0xc,0x41,0x4e,0xb,0x6,0xd,0x10,0x4e,0x40,0x32,0x59,0x34,0x47,0x1a,0x1d,0x10,0x5,0xc,0x47,0xb,0x8,0xa,0x2,0xe,0x1b,0x6,0x1c,0x7,0xd,0x54,0x4e,0x1b,0xe,0xb,0x8,0x41,0x59,0x45,0x59,0x45,0x59,0x45,0x59,0x40,0x4e]

private func talkProgress(exploration num: UInt8) -> UInt8 {
    return num ^ 105
}

/*: "Alert" :*/
fileprivate let user_whyBringParaData:String = "export mid current play equivalentAlert"

/*: "Cancle" :*/
fileprivate let main_mergeFormat:String = "Canclehoney ceiling esthetic convert transition"

/*: "Done" :*/
fileprivate let showAwakeRewardStrengthString:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let noti_dialogId:[Character] = ["j","s"," ","c","a","l","l"," ","m","e","t","h","o","d"]
fileprivate let kSearchSameData:[Character] = [" "]
fileprivate let dataTimingLaterMessage:[Character] = ["n","a","m","e"," ","="," "]

/*: , message =  :*/
fileprivate let constWrapString:String = ", messagetemplate m resume judge"
fileprivate let notiExploreStr:String = " = "

/*: "http://" :*/
fileprivate let show_tailPath:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let const_securityString:String = "employee"
fileprivate let dataPutSickMessage:[Character] = ["t","t","p","s",":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let userProFormat:[UInt8] = [0xd2,0xc5,0xd6,0x84,0xc7,0xcb,0xcb,0xcf,0xcd,0xc1,0xea,0xc5,0xc9,0xc1,0xd7,0x84,0x99,0x84,0xc0,0xcb,0xc7,0xd1,0xc9,0xc1,0xca,0xd0,0x8a,0xc7,0xcb,0xcb,0xcf,0xcd,0xc1,0x8a,0xd7,0xd4,0xc8,0xcd,0xd0,0x8c,0x83,0x9f,0x84,0x83,0x8d,0x8a,0xc9,0xc5,0xd4,0x8c,0xc2,0xd1,0xca,0xc7,0xd0,0xcd,0xcb,0xca,0x8c,0xc7,0xcb,0xcb,0xcf,0xcd,0xc1,0x8d,0x84,0xdf,0x84,0xd6,0xc1,0xd0,0xd1,0xd6,0xca,0x84,0xc7,0xcb,0xcb,0xcf,0xcd,0xc1,0x8a,0xd7,0xd4,0xc8,0xcd,0xd0,0x8c,0x83,0x99,0x83,0x8d,0xff,0x94,0xf9,0x84,0xd9,0x84,0x8d,0x9f,0xae]

private func minimumEnter(failure num: UInt8) -> UInt8 {
    return num ^ 164
}

/*: "uid= :*/
fileprivate let data_seekTranslateStr:String = "uid=this social towel blind appropriate"

/*: ;domain= :*/
fileprivate let noti_visibleTitle:String = ";domaimobile shot safe"
fileprivate let noti_turnAutomaticId:[Character] = ["n","="]

/*: ;path=/" :*/
fileprivate let k_inevitablyAllotMessage:String = ";path=/automatically gray"

/*: "document.cookie=' :*/
fileprivate let userConnectionTableStr:String = "docsponsor"
fileprivate let const_shadowFormat:String = "ookie=\'"

/*: ;domin= :*/
fileprivate let app_publishMsg:String = ";"
fileprivate let app_makerString:String = "tell incredible para finishdomin="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let const_todayFormat:[UInt8] = [0xd5,0xd2,0x8c,0x94,0xcf,0xdb,0xdb,0xd7,0xd5,0xd1,0xba,0xcd,0xd9,0xd1,0xdf,0x9a,0xd5,0xda,0xd0,0xd1,0xe4,0xbb,0xd2,0x94,0x93,0xbc,0xb4,0xbc,0xbf,0xb1,0xbf,0xbf,0xb5,0xb0,0x93,0x95,0x8c,0xa9,0xa9,0x8c,0x99,0x9d,0x95,0x8c,0xe7,0x8c,0xd0,0xdb,0xcf,0xe1,0xd9,0xd1,0xda,0xe0,0x9a,0xcf,0xdb,0xdb,0xd7,0xd5,0xd1,0xa9,0x93]

fileprivate func guideRemindConversation(extra num: UInt8) -> UInt8 {
    let value = Int(num) - 108
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "h5Domain =  :*/
fileprivate let noti_mapData:[Character] = ["h","5","D","o","m","a","i","n"," "]
fileprivate let kPicName:[Character] = ["="," "]

/*: "getAllCookies =  :*/
fileprivate let app_possiblyMessage:String = "getAllsteer equity crush marker accept"
fileprivate let show_sendValue:[Character] = [" ","="," "]

/*: "PHPSESSID" :*/
fileprivate let notiHoldMsg:[Character] = ["P","H","P","S","E","S","S","I"]
fileprivate let main_strategyWholePackageString:String = "straighten"

/*: "uid" :*/
fileprivate let noti_favoriteUrl:String = "UID"

/*: "appCookies =  :*/
fileprivate let appCosStr:String = "appColit music slow global"
fileprivate let noti_spaceStr:String = "okirise"

/*: "UID" :*/
fileprivate let user_sufficientFormat:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let notiExploreDirectionStr:[Character] = ["n","a","v","i","g"]
fileprivate let k_channelFormat:[Character] = ["a","t","o","r",".","u","s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let noti_sectionRocketName:String = "xidentitynwidentity"
fileprivate let main_whyEarlyData:[Character] = ["o","n","e"]

/*: " xunwu_device_id/ :*/
fileprivate let user_inherentWedId:String = " xunwucookie extent"
fileprivate let appAnimaString:String = "by merge calendar systemice_id/"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let mainAttentionTitle:String = "name"
fileprivate let noti_botFreshValue:[Character] = ["u","n","w","u","_","i","p","h","o","n","e"," ","x","u","n","w","u","_","v","e","r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let kTargetKey:[Character] = [" ","x","u","n","w","u","_","p","a","c","k","a","g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let noti_uniqueData:[Character] = [" ","x","u","n","w","u"]
fileprivate let constRefuseFormat:String = "something robot coordinate chain_bundl"

/*:   webkit/1" :*/
fileprivate let user_weeklyKey:String = "  webkitwriting examination delivery reliability character"
fileprivate let user_computeString:String = "/filter"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BoldSort.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class BoldSort: ObjectViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: StartTranslate!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: EnvironmentLog?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: EnvironmentLog?, webConfig: StartTranslate) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: data_padKey.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        thinInside()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        error()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(userShareRespectiveMessage.suffix(7)))
        //: if config.isHideBackBtn {
        if config.isHideBackBtn {
            //: hideNavi = true
            hideNavi = true
        }
        //: addNotification()
        parentBy()
        //: func__checkConfig()
        equipmentException()
        //: func__setUpWebView()
        bigMount()
        //: func__registerObserver()
        doSh()
        //: func__beginStartRequest()
        kitInvite()
        //: func_checkAppleIAP()
        thinking()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: TalkingAppPushManager.share.func__pushToWebVC(webViewType: .DiscountPopup)
                ScopeEmbrace.share.attentionOf(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: const_characterBothFormat, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            roadAge(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.tress(), .font: UIFont.statinEssay(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(appPersistValue)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension BoldSort {
    /// 添加通知
    //: private func addNotification() {
    private func parentBy() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(situation),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: notiRadioMaxValue,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(thinInside),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func situation() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: userEnhanceName.map{custodyLoop(recommendation: $0)}, encoding: .utf8)!) { data, error in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func thinInside() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiWayMessage.map{$0^113}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(const_phiValue.suffix(6)) + "t(onP" + String(userChapterKey.suffix(5)) + String(k_singleData)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func error() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: constTopFormat.map{$0^57}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(showBodyPath.suffix(5)) + String(appKingInvitationValue.suffix(8)) + String(appRemindDeadlineMessage)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension BoldSort {
    //: func func__registerObserver() {
    func doSh() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (user_reserveTitle.replacingOccurrences(of: "factor", with: "t")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(app_childMessage)))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(examination), name: kFunctionalCornerPath, object: nil)
    }

    //: func func__setUpWebView() {
    func bigMount() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        alongFrozenPlus()
        //: func__addBridgeMethod()
        yerBy()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.counterrupt()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            MediaSquare.changeAndMedia(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(show_uniformKey)
            }
        }
    }

    //: func func__beginStartRequest() {
    func kitInvite() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.nor()
            }
            //: } else {
        } else {
            //: func__startRequest()
            nor()
        }
    }

    //: func func__startRequest() {
    func nor() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = VideoMagnitudeerval.share.loginUid
        let uid = VideoMagnitudeerval.share.loginUid
        //: let ssid = VideoMagnitudeerval.share.loginSessionId
        let ssid = VideoMagnitudeerval.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (constEnablelyReplyStr.replacingOccurrences(of: "yet", with: "P") + String(noti_panLowString)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(main_holdKey)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(dataReplyValue.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        ewer()
        //: func__initRightButton()
        balanceInvisible()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func thinking() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            CapDrawSignificant.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.pastGreat(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            CapDrawSignificant.shared.recognizeTake()
        }
    }

    //: func func__initLeftButton() {
    func largely() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.installBind(name: (String(appEqualUrl) + String(app_marginTitle.prefix(7)))), style: .plain, target: self, action: #selector(objectJar(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func balanceInvisible() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(EnvironmentLog.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.installBind(name: (String(user_specieData.prefix(6)) + "timat" + String(const_delayFormat.prefix(5)) + "t_sm")), style: .plain, target: self, action: #selector(triggerMale))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func objectJar(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        tabFor()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.congressionalFor() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: BoldSort.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! BoldSort).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! BoldSort).thinInside()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func equipmentException() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func anBoth(sucess: Bool) {
        //: ProgressHUD.dismiss()
        MediaSquare.spell()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func ewer() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: app_teamTitle.map{talkProgress(exploration: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func doPublisher(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: url)
            ScopeEmbrace.share.painter(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func enableDecideFront(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func examination() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kFunctionalCornerPath, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func triggerMale() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = SineMultiple(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
        //: view.show()
        view.isBurn()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension BoldSort: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            MediaSquare.changeAndMedia(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            anBoth(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        largely()
        //: func__clearJSBgColor()
        ewer()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            anBoth(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            anBoth(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(user_whyBringParaData.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(main_mergeFormat.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(showAwakeRewardStrengthString)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension BoldSort: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(noti_dialogId) + String(kSearchSameData) + String(dataTimingLaterMessage)) + "\(message.name)" + (String(constWrapString.prefix(9)) + notiExploreStr.capitalized) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.breezeAcross(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func yerBy() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in FlexibleMaster.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func tabFor() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in FlexibleMaster.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension BoldSort {
    //: func func__addUserScript() {
    func cot() {
        //: var htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
        var htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(show_tailPath))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(show_tailPath)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((const_securityString.replacingOccurrences(of: "employee", with: "h") + String(dataPutSickMessage))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (const_securityString.replacingOccurrences(of: "employee", with: "h") + String(dataPutSickMessage)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = VideoMagnitudeerval.share.loginUserMode.userID
        let tempUid: String = VideoMagnitudeerval.share.loginUserMode.userID
        //: let tempSessionID: String = VideoMagnitudeerval.share.loginSessionId
        let tempSessionID: String = VideoMagnitudeerval.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: userProFormat.map{minimumEnter(failure: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(data_seekTranslateStr.prefix(4))) + "\(tempUid)" + (String(noti_visibleTitle.prefix(6)) + String(noti_turnAutomaticId)) + "\(htmlDomain)" + (String(k_inevitablyAllotMessage.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((userConnectionTableStr.replacingOccurrences(of: "sponsor", with: "u") + "ment.c" + const_shadowFormat) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (constEnablelyReplyStr.replacingOccurrences(of: "yet", with: "P") + String(noti_panLowString)) + "\(tempSessionID)" + (app_publishMsg.capitalized + String(app_makerString.suffix(6))) + "\(htmlDomain)" + (String(k_inevitablyAllotMessage.prefix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((userConnectionTableStr.replacingOccurrences(of: "sponsor", with: "u") + "ment.c" + const_shadowFormat) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: const_todayFormat.map{guideRemindConversation(extra: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(data_seekTranslateStr.prefix(4))) + "\(tempUid)" + (String(noti_visibleTitle.prefix(6)) + String(noti_turnAutomaticId)) + "\(htmlDomain)" + (String(k_inevitablyAllotMessage.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((userConnectionTableStr.replacingOccurrences(of: "sponsor", with: "u") + "ment.c" + const_shadowFormat) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(data_seekTranslateStr.prefix(4))) + "\(tempSessionID)" + (String(noti_visibleTitle.prefix(6)) + String(noti_turnAutomaticId)) + "\(htmlDomain)" + (String(k_inevitablyAllotMessage.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((userConnectionTableStr.replacingOccurrences(of: "sponsor", with: "u") + "ment.c" + const_shadowFormat) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func roadAge(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
            let htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(noti_mapData) + String(kPicName)) + "\(htmlDomain)")
            //: let userID: String = VideoMagnitudeerval.share.loginUserMode.userID
            let userID: String = VideoMagnitudeerval.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(app_possiblyMessage.prefix(6)) + "Cookies" + String(show_sendValue)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(notiHoldMsg) + main_strategyWholePackageString.replacingOccurrences(of: "straighten", with: "D")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (noti_favoriteUrl.lowercased()), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.kitInvite()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.than(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func than(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
                let htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(noti_mapData) + String(kPicName)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: ImmediateSumensityObserver.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(appCosStr.prefix(5)) + noti_spaceStr.replacingOccurrences(of: "rise", with: "e") + "s = ") + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(user_sufficientFormat)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (noti_favoriteUrl.lowercased())
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(notiHoldMsg) + main_strategyWholePackageString.replacingOccurrences(of: "straighten", with: "D")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(notiHoldMsg) + main_strategyWholePackageString.replacingOccurrences(of: "straighten", with: "D"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.kitInvite()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func alongFrozenPlus() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(notiExploreDirectionStr) + String(k_channelFormat))) { [weak self] result, err in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(notiExploreDirectionStr) + String(k_channelFormat)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((noti_sectionRocketName.replacingOccurrences(of: "identity", with: "u") + "_iph" + String(main_whyEarlyData))) {
                //: let str = getUserAgentString()
                let str = emptyConstant()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.cameraFraction { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(user_inherentWedId.prefix(6)) + "_dev" + String(appAnimaString.suffix(7))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func emptyConstant() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (mainAttentionTitle.replacingOccurrences(of: "name", with: "x") + String(noti_botFreshValue)) + "\(main_persistDirectlyStr)" + (String(kTargetKey)) + "\(user_oneData)" + (String(noti_uniqueData) + String(constRefuseFormat.suffix(6)) + "eId/") + "\(const_progressValue)" + (String(user_weeklyKey.prefix(8)) + user_computeString.replacingOccurrences(of: "filter", with: "1"))
    }
}
