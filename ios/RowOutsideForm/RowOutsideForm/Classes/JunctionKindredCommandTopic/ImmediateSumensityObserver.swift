
//: Declare String Begin

/*: "http://" :*/
fileprivate let notiEmployString:String = "the taphttp://"

/*: "https://" :*/
fileprivate let dataGreatValue:[Character] = ["h","t"]
fileprivate let constKitId:String = "mid thirdtps://"

/*: "http:// :*/
fileprivate let mainComputeData:String = "structure mitt appearance errorhttp://"

/*: "serverSelctIndex =  :*/
fileprivate let const_observerMessage:String = "sdisablevdisable"
fileprivate let dataDisabledTimingKey:String = "against skin license kind clustertInde"
fileprivate let kPresentQuotePath:String = "x = entity kid painter capacity"

/*: "serverUrlStr =  :*/
fileprivate let appSteerPath:[Character] = ["s","e","r","v","e"]
fileprivate let user_popularPromptSignificantlyData:String = "above combinedrUrl"
fileprivate let mainAnimaMessage:String = "delicate remind disabledStr = "

/*: "app. :*/
fileprivate let dataAgileMsg:String = "app.description road continuing array collaboration"

/*: .com" :*/
fileprivate let main_behindTitle:[Character] = [".","c","o","m"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let const_publisherString:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let main_inviteColumnValue:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N","o","t"]
fileprivate let userTackName:String = "turnfy"

/*: "user/appleLogin" :*/
fileprivate let user_teenageKey:String = "uscale"
fileprivate let dataCapacityFormat:[Character] = ["e","r","/","a","p","p","l","e","L","o","g","i","n"]

/*: "IM/getMyBlackList" :*/
fileprivate let mainArcValue:[Character] = ["I","M","/","g","e"]
fileprivate let const_stripTitle:String = "tMyBdecision cut origin direct"

/*: "mf/business/sendMsg" :*/
fileprivate let notiCanvasUrl:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s"]
fileprivate let const_frozenMessage:[Character] = ["e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let userHeroAdvertisingId:String = "service boy consumer bean texturemf/bus"
fileprivate let dataDensityUrl:[Character] = ["i","n"]
fileprivate let data_circleMsg:String = "ess/sref item"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let mainSakeVoicePath:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e"]
fileprivate let mainCenterReplyName:[Character] = ["n","d","M","s","g"]

/*: "GJ21FB" :*/
fileprivate let kCoordinateMinimumString:[Character] = ["G","J","2","1","F"]
fileprivate let k_capableValue:[Character] = ["B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let notiSelectUrl:String = "完\u{6210}了检测,"

/*: "接口请求失败" :*/
fileprivate let k_insideData:String = "\u{63a5}口请\u{6c42}失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let app_movementEquityShouldStr:String = "way成了检测,"
fileprivate let userBeatString:String = "需要corner换"
fileprivate let show_arrowId:[Character] = ["s","e","r","v","e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let userPullValue:String = "abs asset cancel timing portraitHTTPProxy"

/*: "__SCOPED__" :*/
fileprivate let app_translationPassingValue:String = "beside processor__SCOPE"
fileprivate let constNearbyMessage:String = "coordinate admin under portrait hereD__"

/*: "tap" :*/
fileprivate let mainCounselIonId:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let const_flexValue:String = "TUN"

/*: "ipsec" :*/
fileprivate let show_dueTitle:String = "ipsemount"

/*: "ppp" :*/
fileprivate let mainMinPath:String = "PPP"

/*: "func__updateTalkingCookies =  :*/
fileprivate let userObjectCoolPath:String = "facceptc"
fileprivate let noti_missionStr:String = "tier consumer previous conversation playdateT"
fileprivate let noti_heavyId:[Character] = ["n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let user_thereMessage:[Character] = ["h","t","t","p","s"]
fileprivate let dataCreationMessage:String = "://"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let showAppealItemKey:[UInt8] = [0x86,0x95,0x8e,0x83,0x7f,0x7f,0x84,0x85,0x8c,0x85,0x94,0x85,0x73,0x81,0x8d,0x85,0x63,0x8f,0x8f,0x8b,0x89,0x85,0x93,0x77,0x89,0x94,0x88,0x75,0x92,0x8c,0x40,0x5d,0x40]

fileprivate func connectHello(eave num: UInt8) -> UInt8 {
    let value = Int(num) - 32
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let data_presentationReplacePath:String = "UIcompose"

/*: "PHPSESSID" :*/
fileprivate let kGameComedyName:[Character] = ["P","H","P","S","E","S","S","I"]
fileprivate let show_policyFloorName:[Character] = ["D"]

/*: "func__saveAllCookies =  :*/
fileprivate let mainAgainstUrl:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l"]
fileprivate let k_digitalData:[Character] = ["l","C","o","o","k","i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let showRagingData:String = "func_gray theme"
fileprivate let app_givingMessage:String = "explore"
fileprivate let show_receiverSucceedMsg:String = "estimated photo ticklCoo"
fileprivate let show_scientificDrawingData:String = "weight rise her employ feed  error"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ImmediateSumensityObserver.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum RouteCritical: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class ImmediateSumensityObserver: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = ImmediateSumensityObserver()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        areaByDistance()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(notiEmployString.suffix(7)))) || newValue.hasPrefix((String(dataGreatValue) + String(constKitId.suffix(6)))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if userAcceptUrl && !main_reductionPetStr {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(mainComputeData.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(mainComputeData.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    monitor(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func areaByDistance() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = appEfficiencyStorageStr.integer(forKey: show_runValue)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (const_observerMessage.replacingOccurrences(of: "disable", with: "er") + "Selc" + String(dataDisabledTimingKey.suffix(5)) + String(kPresentQuotePath.prefix(4))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = labelOn()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiEmployString.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if userAcceptUrl && !main_reductionPetStr {
            //: serverPrefix = "http://"
            serverPrefix = (String(notiEmployString.suffix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(appSteerPath) + String(user_popularPromptSignificantlyData.suffix(4)) + String(mainAnimaMessage.suffix(6))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func labelOn() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func noChange(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(dataAgileMsg.prefix(4))) + "\(user_basicString)" + (String(main_behindTitle)), (String(dataAgileMsg.prefix(4))) + "\(user_basicString)" + (String(main_behindTitle))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(const_publisherString)),
         //: "recharge/applePayNotify",
         (String(main_inviteColumnValue) + userTackName.replacingOccurrences(of: "turn", with: "i")),
         //: "user/appleLogin"] + slowPathArr1
         (user_teenageKey.replacingOccurrences(of: "scale", with: "s") + String(dataCapacityFormat))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(mainArcValue) + String(const_stripTitle.prefix(4)) + "lackList"),
         //: "mf/business/sendMsg",
         (String(notiCanvasUrl) + String(const_frozenMessage)),
         //: "mf/business/sendGift",
         (String(userHeroAdvertisingId.suffix(6)) + String(dataDensityUrl) + String(data_circleMsg.prefix(5)) + "endGift"),
         //: "mf/chatRoom/sendMsg"]
         (String(mainSakeVoicePath) + String(mainCenterReplyName))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = FormatDistribution.getSystemLangCode()
        let lang = FormatDistribution.columnistOpen()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .noPermission(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(kCoordinateMinimumString) + String(k_capableValue)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension ImmediateSumensityObserver {
    //: func checkServerUrl() {
    func alone() {
        //: if VideoMagnitudeerval.share.networkStatus != .Unavailable {
        if VideoMagnitudeerval.share.networkStatus != .Unavailable {
            //: func_requestHost()
            beyondResolve()
            //: } else {
        } else {
            //: func_starCodeTime()
            enforce()
        }
    }

    //: func func_starCodeTime() {
    func enforce() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] num in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.beyondResolve()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func nine() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func beyondResolve() {
        //: if VideoMagnitudeerval.share.networkStatus == .Unavailable {
        if VideoMagnitudeerval.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        nine()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        MethodAgile.workBy { [weak self] succeed, result, errorModel in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (notiSelectUrl + "请\u{6c42}状态正常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (k_insideData))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.enforce()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (app_movementEquityShouldStr.replacingOccurrences(of: "way", with: "完") + userBeatString.replacingOccurrences(of: "corner", with: "更") + String(show_arrowId)))
                    //: self.func__changeHostUrl()
                    self.provider()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func provider() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        nine()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        appEfficiencyStorageStr.set(serverSelctIndex, forKey: show_runValue)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiEmployString.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if userAcceptUrl, !main_reductionPetStr {
            //: serverPrefix = "http://"
            serverPrefix = (String(notiEmployString.suffix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension ImmediateSumensityObserver {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func hiDirect() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(userPullValue.suffix(9)))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func halfReduce() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(app_translationPassingValue.suffix(7)) + String(constNearbyMessage.suffix(3)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(mainCounselIonId))) || keyStr.contains((const_flexValue.lowercased())) || keyStr.contains((show_dueTitle.replacingOccurrences(of: "mount", with: "c"))) || keyStr.contains((mainMinPath.lowercased())) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension ImmediateSumensityObserver {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func monitor(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (userObjectCoolPath.replacingOccurrences(of: "accept", with: "un") + "__up" + String(noti_missionStr.suffix(5)) + "alki" + String(noti_heavyId)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = doingOnd()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
        var urlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(notiEmployString.suffix(7))), with: (String(dataGreatValue) + String(constKitId.suffix(6))))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(dataGreatValue) + String(constKitId.suffix(6)))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(user_thereMessage) + dataCreationMessage.capitalized) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            overReAllot(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        likeOn()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func overReAllot(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: showAppealItemKey.map{connectHello(eave: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func doingOnd() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiEmployString.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if userAcceptUrl, !main_reductionPetStr {
            //: serverPrefix = "https://"
            serverPrefix = (String(dataGreatValue) + String(constKitId.suffix(6)))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(dataAgileMsg.prefix(4))) + "\(user_basicString)" + (String(main_behindTitle)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func mitt(note: Notification) {
        //: if VideoMagnitudeerval.share.loginUid.isEmptyString {
        if VideoMagnitudeerval.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            reporter()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            expectedOccupy()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func reporter() {
        //: var htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
        var htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(kGameComedyName) + String(show_policyFloorName)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(kGameComedyName) + String(show_policyFloorName))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(kGameComedyName) + String(show_policyFloorName)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(kGameComedyName) + String(show_policyFloorName))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))), h5Cookie.keys.contains((data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(kGameComedyName) + String(show_policyFloorName))), h5Cookie.keys.contains((String(kGameComedyName) + String(show_policyFloorName))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(kGameComedyName) + String(show_policyFloorName))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(kGameComedyName) + String(show_policyFloorName))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func expectedOccupy() {
        //: let htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
        let htmlDomain: String = VideoMagnitudeerval.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == VideoMagnitudeerval.share.loginUserMode.userID {
            if cookie.name == (data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D")), cookie.value == VideoMagnitudeerval.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(kGameComedyName) + String(show_policyFloorName)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(kGameComedyName) + String(show_policyFloorName)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(kGameComedyName) + String(show_policyFloorName))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(data_presentationReplacePath.replacingOccurrences(of: "compose", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(kGameComedyName) + String(show_policyFloorName))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(kGameComedyName) + String(show_policyFloorName))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func likeOn() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(mainAgainstUrl) + String(k_digitalData)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            appEfficiencyStorageStr.set(cookieData, forKey: appConstructFormat)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(showRagingData.prefix(5)) + "_saveA" + app_givingMessage.replacingOccurrences(of: "explore", with: "l") + String(show_receiverSucceedMsg.suffix(4)) + "kies" + String(show_scientificDrawingData.suffix(7))))
        }
    }

    //: func func__setAppCookies() {
    func sprinkleTarget() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = appEfficiencyStorageStr.object(forKey: appConstructFormat)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func serviceOn() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        appEfficiencyStorageStr.removeObject(forKey: appConstructFormat)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
