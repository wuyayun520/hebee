
//: Declare String Begin

/*: ; build: :*/
fileprivate let userSumerestedName:String = "absolute blind; bu"

/*: ; iOS  :*/
fileprivate let constAgreeString:[Character] = [";"," ","i","O","S"," "]

/*: "audio" :*/
fileprivate let dataConsumptionValue:String = "AUDIO"

/*: .wav" :*/
fileprivate let appOuterCloudData:String = ".wavcollaboration sake listener meet dismiss"

/*: "audio/wav" :*/
fileprivate let appMMessage:[Character] = ["a","u","d","i","o"]
fileprivate let app_brushTitle:String = "/wavrelation reliability easy decision"

/*: .jpg" :*/
fileprivate let userFuneralKey:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let main_sireBlockData:String = "imabetween"
fileprivate let showHeartPreciseUrl:String = "ya"

/*: "://app." :*/
fileprivate let user_onPath:String = "entry/"
fileprivate let k_sameMsg:String = "/app.busy environment raging hold remote"

/*: "data" :*/
fileprivate let noti_largelyPath:String = "dillustratetillustrate"

/*: ":null" :*/
fileprivate let k_agreeUnderName:[Character] = [":","n","u","l","l"]

/*: "Request failed, Try again later" :*/
fileprivate let noti_pickUponBoardKey:[UInt8] = [0xae,0x99,0x8d,0x89,0x99,0x8f,0x88,0xdc,0x9a,0x9d,0x95,0x90,0x99,0x98,0xd0,0xdc,0xa8,0x8e,0x85,0xdc,0x9d,0x9b,0x9d,0x95,0x92,0xdc,0x90,0x9d,0x88,0x99,0x8e]

private func withoutWeAlready(input num: UInt8) -> UInt8 {
    return num ^ 252
}

/*: "json error" :*/
fileprivate let show_galleryValue:String = "json mechanism obtain scale"
fileprivate let showCrushItMessage:[Character] = ["e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let main_byId:[Character] = ["p","l","a","t","f","o","r","m","=","i","p","h"]
fileprivate let showAlreadyId:String = "ON"
fileprivate let main_downBarValue:String = "game sibling buttone&vers"

/*: &packageId= :*/
fileprivate let app_publisherFormat:String = "&paenhance until lost"
fileprivate let data_ridString:[Character] = ["c","k","a","g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let dataOldData:[Character] = ["&","b","u","n"]
fileprivate let appPlusData:[Character] = ["d","l","e","I","d","="]

/*: &lang= :*/
fileprivate let mainTwoUrl:[Character] = ["&","l","a","n","g","="]

/*: "/route/slowindex" :*/
fileprivate let appStatusPath:String = "/routeextra craft"
fileprivate let user_networkStr:String = "ndhalfx"

/*: "/route/index" :*/
fileprivate let app_noteTitle:String = "/routtarget array position compose fatal"

/*: "?path= :*/
fileprivate let mainRemindMessage:[Character] = ["?"]
fileprivate let notiFindBackString:String = "single calculation example row monitorpath="

/*: "&GJNonceStr= :*/
fileprivate let main_ofUrl:String = "speed selected execute rate miss&GJNon"
fileprivate let constArrestMediaKey:[Character] = ["c","e","S","t","r","="]

/*: "&params= :*/
fileprivate let const_illegalString:[Character] = ["&","p","a","r","a","m","s","="]

/*: "token" :*/
fileprivate let appAgoPortData:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let main_todayUrl:String = "\u{8bf7}求成功"

/*: "AAAAAAAA" :*/
fileprivate let user_missPadUrl:[Character] = ["A","A","A","A","A","A","A","A"]

/*: "下载完成 -  :*/
fileprivate let const_packValue:[Character] = ["下","载","\u{5b8c}","成"," ","-"," "]

/*: "/Documents/" :*/
fileprivate let user_measureString:String = "/Documenrunning orientation clearly tress"
fileprivate let showSpecAdminUrl:String = "bar peer disabledts/"

/*: ".mp4" :*/
fileprivate let dataStrengthData:String = "contact pose would.mp4"

/*: "移动地址: :*/
fileprivate let notiDayMsg:[Character] = ["移","\u{52a8}","地","\u{5740}",":"]

/*: "后台任务下载回来" :*/
fileprivate let app_poleMsg:String = "后台"
fileprivate let show_amMessage:String = "任务下载回来"

/*: "下载成功 : :*/
fileprivate let userMinTitle:[Character] = ["下","载","成","\u{529f}"," ",":"]

/*: "Net Error" :*/
fileprivate let appDetailString:String = "architecture play area environmentNet E"

/*: "下载失败 : :*/
fileprivate let data_specData:String = "下载judge败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ExitChannel.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let user_difficultMovieMessage = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let kBottomMsg = "\(user_sinkValue)/\(appBehaviorValue) (\(const_progressValue)" + (String(userSumerestedName.suffix(4)) + "ild:") + "\(main_priceMessage)" + (String(constAgreeString)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: ComprehensiveEnhance?) -> Void

//: let GJ = TalkingRequestTool.init()
let const_equivalentValue = ExitChannel()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class ExitChannel: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func panelAttract(model: PopAdjust, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.restore() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.tarProvide(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.offConstraint(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func tarProvide(model: PopAdjust, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.toolAlways(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.awl(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    darkInvisible(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ComprehensiveEnhance(errorCode: AboutYield.NetError.rawValue, errorMsg: appMoveTrainTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        conclusion(showMsg: appMoveTrainTitle)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (dataConsumptionValue.lowercased()) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.annualTransaction())+\(user_oneData)" + (String(appOuterCloudData.prefix(4))), mimeType: (String(appMMessage) + String(app_brushTitle.prefix(4))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(userFuneralKey)), mimeType: (main_sireBlockData.replacingOccurrences(of: "between", with: "g") + "e/jpe" + showHeartPreciseUrl.replacingOccurrences(of: "ya", with: "g")))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    darkInvisible(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ComprehensiveEnhance(errorCode: AboutYield.NetError.rawValue, errorMsg: appMoveTrainTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        conclusion(showMsg: appMoveTrainTitle)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func offConstraint(model: PopAdjust, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.toolAlways(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.awl(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: main_reductionPetStr ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = user_difficultMovieMessage }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    darkInvisible(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ComprehensiveEnhance(errorCode: AboutYield.NetError.rawValue, errorMsg: appMoveTrainTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        conclusion(showMsg: appMoveTrainTitle)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = user_difficultMovieMessage }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    darkInvisible(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ComprehensiveEnhance(errorCode: AboutYield.NetError.rawValue, errorMsg: appMoveTrainTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        conclusion(showMsg: appMoveTrainTitle)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func darkInvisible(model: PopAdjust, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            ImmediateSumensityObserver.share.monitor(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if main_reductionPetStr, model.requestServer.contains((user_onPath.replacingOccurrences(of: "entry", with: ":") + String(k_sameMsg.prefix(5)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.asRespective(key: ImmediateSumensityObserver.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (noti_largelyPath.replacingOccurrences(of: "illustrate", with: "a")) + "\"" + (String(k_agreeUnderName)), with: "" + "\"" + (noti_largelyPath.replacingOccurrences(of: "illustrate", with: "a")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !main_reductionPetStr {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(k_agreeUnderName)), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<ThinDense>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == AboutYield.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, ComprehensiveEnhance(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    conclusion(showMsg: responseModel.msg ?? String(bytes: noti_pickUponBoardKey.map{withoutWeAlready(input: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case AboutYield.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: userWaitScopeTitle, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, ComprehensiveEnhance(errorCode: AboutYield.NetError.rawValue, errorMsg: (String(show_galleryValue.prefix(5)) + String(showCrushItMessage)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func toolAlways(model: PopAdjust) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(main_byId) + showAlreadyId.lowercased() + String(main_downBarValue.suffix(6)) + "ion=") + "\(main_persistDirectlyStr)" + (String(app_publisherFormat.prefix(3)) + String(data_ridString)) + "\(user_oneData)" + (String(dataOldData) + String(appPlusData)) + "\(const_progressValue)" + (String(mainTwoUrl)) + "\(ImmediateSumensityObserver.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if main_reductionPetStr, serverUrl.contains((user_onPath.replacingOccurrences(of: "entry", with: ":") + String(k_sameMsg.prefix(5)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if ImmediateSumensityObserver.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(appStatusPath.prefix(6)) + "/slowi" + user_networkStr.replacingOccurrences(of: "half", with: "e")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(app_noteTitle.prefix(5)) + "e/index")) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(mainRemindMessage) + String(notiFindBackString.suffix(5))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(main_ofUrl.suffix(6)) + String(constArrestMediaKey)) + "\(ImmediateSumensityObserver.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.umber(key: ImmediateSumensityObserver.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(const_illegalString)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func awl(model: PopAdjust) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(kBottomMsg)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(appAgoPortData)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func sure(model: PopAdjust) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.toolAlways(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { data, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (main_todayUrl) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(user_missPadUrl))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (String(const_packValue)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(user_measureString.prefix(8)) + String(showSpecAdminUrl.suffix(3))) + NSDate.annualTransaction() + (String(dataStrengthData.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(notiDayMsg)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (app_poleMsg.capitalized + show_amMessage.capitalized))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = LightSend.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func breeze(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: data_micSumegrationPinMsg.asLink(), with: user_basicString)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(userMinTitle)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ComprehensiveEnhance(errorCode: AboutYield.Normal.rawValue, errorMsg: (String(appDetailString.suffix(5)) + "rror").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ComprehensiveEnhance(errorCode: AboutYield.NetError.rawValue, errorMsg: (String(appDetailString.suffix(5)) + "rror").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (data_specData.replacingOccurrences(of: "judge", with: "失")) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func weirdCombine(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: data_micSumegrationPinMsg.asLink(), with: user_basicString)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(userMinTitle)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ComprehensiveEnhance(errorCode: AboutYield.Normal.rawValue, errorMsg: (String(appDetailString.suffix(5)) + "rror").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ComprehensiveEnhance(errorCode: AboutYield.NetError.rawValue, errorMsg: (String(appDetailString.suffix(5)) + "rror").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (data_specData.replacingOccurrences(of: "judge", with: "失")) + "\(errMsg)")
            }
        }
    }
}
