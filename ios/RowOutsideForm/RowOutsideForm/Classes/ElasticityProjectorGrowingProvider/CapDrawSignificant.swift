
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let show_harassmentData:[UInt8] = [0x95,0xa4,0xa4,0xa0,0x99,0xb3,0x9d,0x95,0xa4,0xb3,0xa4,0x95,0xad,0xb3,0xa7,0xa9,0x97,0x97,0x99,0x99,0x98,0xb3,0xa2,0xa3,0xa8,0x9d,0x9a,0x9d,0x97,0x95,0xa8,0x9d,0xa3,0xa2]

fileprivate func serviceBagSale(hour num: UInt8) -> UInt8 {
    let value = Int(num) - 84
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let const_thereTitle:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x44,0x45,0x45,0x43,0x43,0x55,0x53,0x5f,0x45,0x42,0x49,0x52,0x43,0x53,0x42,0x55,0x53,0x5f,0x50,0x41,0x49,0x5f,0x45,0x4c,0x50,0x50,0x41]

/*: "mf/recharge/createApplePay" :*/
fileprivate let user_serverData:[Character] = ["m","f","/","r","e","c","h","a","r","g","e","/","c","r","e","a","t","e","A","p","p"]
fileprivate let app_umberMsg:String = "camera first soundlePay"

/*: "productId" :*/
fileprivate let notiUsValue:String = "prcommonduc"
fileprivate let user_tradeString:[Character] = ["t","I","d"]

/*: "source" :*/
fileprivate let app_meetingOpenName:String = "thanourc"
fileprivate let mainSoakUrl:String = "failure"

/*: "IAP充值创建订单失败: :*/
fileprivate let data_productionTradeUrl:String = "scope closeIAP充值创"

/*: "orderNum" :*/
fileprivate let k_funeralData:[Character] = ["o","r","d","e","r","N","u","m"]

/*: "mf/recharge/applePayNotify" :*/
fileprivate let constVersionPlaceEssayString:[Character] = ["m","f","/","r","e","c","h"]
fileprivate let kTextName:String = "due"
fileprivate let main_busyStr:[Character] = ["g","e","/","a","p","p","l","e","P","a","y","N","o","t","i","f","y"]

/*: "IAP充值校验失败: :*/
fileprivate let const_signId:[Character] = ["I","A","P","充","值","校","验"]
fileprivate let dataSupportFormat:String = "follow:"

/*: "transactionId" :*/
fileprivate let app_poseValue:String = "olaransa"

/*: "reportMoney" :*/
fileprivate let user_anonymousFormat:String = "recordingport"
fileprivate let userSuspendKey:String = "identify conclusion exhibition be smartMoney"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let appUmberName:String = "mf/Anormal leading"
fileprivate let dataReminderMessage:String = "ub/Awillingness scientific document schedule track"
fileprivate let mainUntilString:[Character] = ["p","p","l","e","C","r","e","a","t","e","O","r","d","e","r"]

/*: "IAP订阅创建订单失败: :*/
fileprivate let appRoundingName:[Character] = ["I","A","P","\u{8ba2}","阅","创","建","订"]
fileprivate let dataPlaceContextProductUrl:[Character] = ["单","失","败",":"]

/*: "orderId" :*/
fileprivate let notiPerformValue:[UInt8] = [0xee,0xf3,0xe5,0xe4,0xf3,0xc8,0xe5]

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let const_scripString:String = "unknown genuinemf/Au"
fileprivate let appEndString:[Character] = ["t","o","S","u","b","/","A","p","p","l","e","P","a","y","S","u","c","c","e","s","s"]

/*: "IAP订阅校验失败: :*/
fileprivate let constToPath:[Character] = ["I","A","P","订","阅","校","验","\u{5931}","败",":"]

/*: "App" :*/
fileprivate let noti_linkPath:String = "index associated gift sessionApp"

/*: "OrderTransactionInfo_ :*/
fileprivate let const_diskNetworkMsg:String = "OrderTvoice interest reply"
fileprivate let k_rateTitle:[Character] = ["r","a","n","s","a","c","t"]
fileprivate let show_intoStackHelloMessage:String = "ionInfo_outside location called reduce post"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let k_qualityUrl:[UInt8] = [0x2e,0x13,0x5,0x4,0x13,0x35,0x13,0x0,0xf,0x12,0x0,0x2,0x15,0x8,0xe,0xf,0x28,0xf,0x7,0xe,0x3e,0x32,0x14,0x3,0x12,0x2,0x13,0x8,0x3,0x4,0x3e]

private func cellInmate(lat num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "verifyData" :*/
fileprivate let showSomeoneUrl:[UInt8] = [0x6d,0x7e,0x69,0x72,0x7d,0x62,0x5f,0x7a,0x6f,0x7a]

private func assertFeedback(box num: UInt8) -> UInt8 {
    return num ^ 27
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let notiCompositionUrl:[Character] = ["I","A","P","查","询","\u{5185}","购","商","品","失","败",","," ","p","r","o","d","u","c","t","I"]
fileprivate let showReKey:String = "target conditiond:"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let showTillVitalData:String = "regulation spread meet stop exampleIAP查"
fileprivate let kConnectionThresholdUrl:String = "tress would败, p"
fileprivate let appDismissData:String = "cts.explain confirm sprinkle"

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let k_happyStr:[Character] = ["I","A","P","查","询","内","\u{8d2d}","商","品","失"]
fileprivate let notiTapTitle:[Character] = ["败"]
fileprivate let notiSignalRejectValue:String = ", Error:re display"

/*: "IAP-purchased: :*/
fileprivate let main_adCutStr:String = "IAP-psea dismiss mount bottom recommendation"
fileprivate let const_largeString:[Character] = ["d",":"]

/*: , 订单号: :*/
fileprivate let showObserverPath:String = ", \u{8ba2}\u{5355}\u{53f7}:"

/*: , 商品Id: :*/
fileprivate let data_methodFormat:String = ","
fileprivate let main_headData:String = " 商品Id:pure execute per infra"

/*: , 苹果transId: :*/
fileprivate let show_modMsg:[Character] = [","," ","苹","果","t","r"]
fileprivate let constEgoFormat:[Character] = ["a","n","s","I","d",":"]

/*: "IAP-failed: :*/
fileprivate let k_managerMessage:String = "IAP-fabound cover behind background"

/*: "IAP-deferred: :*/
fileprivate let app_memberShadePath:[Character] = ["I","A","P","-","d","e","f","e","r","r","e","d",":"]

/*: "IAP未知交易类型: :*/
fileprivate let showInjuryData:[Character] = ["I","A","P","未","知","交","易","\u{7c7b}","型",":"]

/*: " 未知的交易类型" :*/
fileprivate let appWaitPath:String = " 未知\u{7684}\u{4ea4}易类"
fileprivate let constGalleryProcessPreciseString:String = "\u{578b}"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let showDifficultString:String = "IAP\u{83b7}取"
fileprivate let kDenyLabelMessage:String = "\u{6570}据失败, "
fileprivate let mainPiMsg:[Character] = ["t","r","a","n","s","a","c"]
fileprivate let userFactoryUrl:String = "thin replacetionId:"

/*: , orderId: :*/
fileprivate let appMigrationStr:String = "line constant fit tab sun, orde"

/*: , payType: :*/
fileprivate let const_remainingString:[Character] = [","," ","p","a","y","T"]
fileprivate let appAtMsg:[Character] = ["y","p","e",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CapDrawSignificant.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let show_plateUrl = NSNotification.Name(rawValue: String(bytes: show_harassmentData.map{serviceBagSale(hour: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let k_informationDownStr = NSNotification.Name(rawValue: String(bytes: const_thereTitle.reversed(), encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let noti_toolGraphId = 9

/// 支付类型
//: enum ApplePayType {
enum LargeSustainable {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum BehaviorMedia: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (BehaviorMedia, Double, LargeSustainable) -> Void

//: class AppleIAPManager: NSObject {
class CapDrawSignificant: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: LargeSustainable = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = CapDrawSignificant()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(rangeByWake),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func rangeByWake() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension CapDrawSignificant {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func pickUsed(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PopAdjust()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(user_serverData) + String(app_umberMsg.suffix(5)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(notiUsValue.replacingOccurrences(of: "common", with: "o") + String(user_tradeString))] = productId
        //: dict["source"] = source
        dict[(app_meetingOpenName.replacingOccurrences(of: "than", with: "s") + mainSoakUrl.replacingOccurrences(of: "failure", with: "e"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_equivalentValue.panelAttract(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                TrainGreat.bitRemove(msg: (String(data_productionTradeUrl.suffix(6)) + "\u{5efa}订\u{5355}\u{5931}败:") + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(k_funeralData))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func serviceObserve(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PopAdjust()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(constVersionPlaceEssayString) + kTextName.replacingOccurrences(of: "due", with: "ar") + String(main_busyStr))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_equivalentValue.panelAttract(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    TrainGreat.bitRemove(msg: (String(const_signId) + dataSupportFormat.replacingOccurrences(of: "follow", with: "失败")) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.vital(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.revealMessage()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: show_plateUrl, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(user_anonymousFormat.replacingOccurrences(of: "recording", with: "re") + String(userSuspendKey.suffix(5)))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension CapDrawSignificant {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func majorGrain(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PopAdjust()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(appUmberName.prefix(4)) + "utoS" + String(dataReminderMessage.prefix(4)) + String(mainUntilString))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(notiUsValue.replacingOccurrences(of: "common", with: "o") + String(user_tradeString))] = productId
        //: dict["source"] = source
        dict[(app_meetingOpenName.replacingOccurrences(of: "than", with: "s") + mainSoakUrl.replacingOccurrences(of: "failure", with: "e"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_equivalentValue.panelAttract(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                TrainGreat.bitRemove(msg: (String(appRoundingName) + String(dataPlaceContextProductUrl)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: notiPerformValue.map{$0^129}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func isIsolate(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PopAdjust()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(const_scripString.suffix(5)) + String(appEndString))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_equivalentValue.panelAttract(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    TrainGreat.bitRemove(msg: (String(constToPath)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.vital(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.lot()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: k_informationDownStr, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(user_anonymousFormat.replacingOccurrences(of: "recording", with: "re") + String(userSuspendKey.suffix(5)))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension CapDrawSignificant {
    /// 初始化数据
    //: private func iap_initData() {
    private func agentForTranslate() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = esthetic(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = esthetic(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func esthetic(payType: LargeSustainable) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = revealMessage()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = lot()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func revealMessage() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(noti_linkPath.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = appEfficiencyStorageStr.string(forKey: appSaveData) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(const_diskNetworkMsg.prefix(6)) + String(k_rateTitle) + String(show_intoStackHelloMessage.prefix(8))) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func lot() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(noti_linkPath.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = appEfficiencyStorageStr.string(forKey: appSaveData) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: k_qualityUrl.map{cellInmate(lat: $0)}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func fileEgo(_ transactionId: String, _ payType: LargeSustainable) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: showSomeoneUrl.map{assertFeedback(box: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: showSomeoneUrl.map{assertFeedback(box: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension CapDrawSignificant {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func recognizeTake() {
        //: iap_initData()
        agentForTranslate()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            followingEquipment(dict[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            followingEquipment(dict[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func followingEquipment(_ transactionId: String?, _ payType: LargeSustainable) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        vital(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension CapDrawSignificant {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func kind(productId: String, payType: LargeSustainable, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        agentForTranslate()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            pickUsed(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.exampleContext(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            majorGrain(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.exampleContext(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func exampleContext(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            TrainGreat.bitRemove(msg: (String(notiCompositionUrl) + String(showReKey.suffix(2))) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.everyControl()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func everyControl() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension CapDrawSignificant: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            TrainGreat.bitRemove(msg: (String(showTillVitalData.suffix(4)) + "询内购商\u{54c1}\u{5931}" + String(kConnectionThresholdUrl.suffix(4)) + "rodu" + String(appDismissData.prefix(4)) + "count为0."))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_ request: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        TrainGreat.bitRemove(msg: (String(k_happyStr) + String(notiTapTitle) + String(notiSignalRejectValue.prefix(8))) + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_ request: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension CapDrawSignificant: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    TrainGreat.bitRemove(msg: (String(main_adCutStr.prefix(5)) + "urchase" + String(const_largeString)) + "\(currentPayType)" + (showObserverPath) + "\(String(describing: createOrderId))" + (data_methodFormat.capitalized + String(main_headData.prefix(6))) + "\(transaction.payment.productIdentifier)" + (String(show_modMsg) + String(constEgoFormat)) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    vital(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                TrainGreat.bitRemove(msg: (String(k_managerMessage.prefix(6)) + "iled:") + "\(currentPayType)" + (showObserverPath) + "\(String(describing: createOrderId))" + (data_methodFormat.capitalized + String(main_headData.prefix(6))) + "\(transaction.payment.productIdentifier)" + (String(show_modMsg) + String(constEgoFormat)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: let vc = TalkingContactServiceView.init()
                let vc = StrokeByReflect()
                //: vc.show()
                vc.desert()
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                TrainGreat.bitRemove(msg: (String(app_memberShadePath)) + "\(currentPayType)" + (showObserverPath) + "\(String(describing: createOrderId))" + (data_methodFormat.capitalized + String(main_headData.prefix(6))) + "\(transaction.payment.productIdentifier)" + (String(show_modMsg) + String(constEgoFormat)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                TrainGreat.bitRemove(msg: (String(showInjuryData)) + "\(currentPayType)" + (showObserverPath) + "\(String(describing: createOrderId))" + (data_methodFormat.capitalized + String(main_headData.prefix(6))) + "\(transaction.payment.productIdentifier)" + (String(show_modMsg) + String(constEgoFormat)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((appWaitPath + constGalleryProcessPreciseString))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func vital(_ transactionId: String, _ payType: LargeSustainable) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = fileEgo(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            TrainGreat.bitRemove(msg: (showDifficultString + "\u{672c}地\u{6536}\u{636e}" + kDenyLabelMessage + String(mainPiMsg) + String(userFactoryUrl.suffix(7))) + "\(transactionId)" + (String(appMigrationStr.suffix(6)) + "rId:") + "\(String(describing: createOrderId))" + (String(const_remainingString) + String(appAtMsg)) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")] == transactionId || $0[String(bytes: notiPerformValue.map{$0^129}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId"): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: notiPerformValue.map{$0^129}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: showSomeoneUrl.map{assertFeedback(box: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.revealMessage()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")] == transactionId || $0[String(bytes: notiPerformValue.map{$0^129}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId"): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: notiPerformValue.map{$0^129}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: showSomeoneUrl.map{assertFeedback(box: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.lot()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > noti_toolGraphId {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            serviceObserve(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(app_poseValue.replacingOccurrences(of: "ola", with: "t") + "ctionId")] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            isIsolate(transactionId, params: paramsArr.first!)
        }
    }
}
