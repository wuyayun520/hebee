
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiStatePlateMsg:[UInt8] = [0x7e,0x79,0x7e,0x63,0x3f,0x74,0x78,0x73,0x72,0x65,0x2d,0x3e,0x37,0x7f,0x76,0x64,0x37,0x79,0x78,0x63,0x37,0x75,0x72,0x72,0x79,0x37,0x7e,0x7a,0x67,0x7b,0x72,0x7a,0x72,0x79,0x63,0x72,0x73]

private func labWeight(about num: UInt8) -> UInt8 {
    return num ^ 23
}

/*: "#322870" :*/
fileprivate let k_genderPassingFormat:String = "#322into70"

/*: "icon_home_position" :*/
fileprivate let data_opinionFormat:String = "globalcon"
fileprivate let user_advancedData:[Character] = ["p","o","s","i","t","i","o","n"]

/*: "btn_video_minimize" :*/
fileprivate let showTillPath:[Character] = ["b","t","n","_","v","i"]
fileprivate let dataPosePath:[Character] = ["d","e","o"]
fileprivate let kAudienceId:String = "_minimode pro editor range day"

/*: "btn_video_drop_nor" :*/
fileprivate let k_accelerateData:String = "jar link drawer focusbtn_"
fileprivate let const_springData:String = "everyop"

/*: "btn_video_call_nor" :*/
fileprivate let k_agileResolveProvideId:[Character] = ["b","t","n","_","v","i","d","e","o"]
fileprivate let main_minimizePath:String = "_call_normarker cap storage"

/*: "transform.scale" :*/
fileprivate let show_passId:[Character] = ["t","r","a","n","s","f"]
fileprivate let const_stageUniformString:[Character] = ["o","r","m",".","s","c","a","l","e"]

/*: "animation" :*/
fileprivate let main_tunBringString:String = "astyleim"

/*: "btn_voice_Micr_nor" :*/
fileprivate let noti_faceFormat:[Character] = ["b","t","n","_","v","o","i","c"]
fileprivate let app_largelySickFormat:String = "versus ragee_Mic"

/*: "btn_voice_Micr_pre" :*/
fileprivate let user_spreadMessage:String = "might pop attention than contrastbtn_vo"
fileprivate let user_denseKey:[Character] = ["i","c","e","_"]
fileprivate let show_validStr:[Character] = ["M","i","c","r","_","p","r","e"]

/*: "btn_voice_hands_nor" :*/
fileprivate let showPointData:[Character] = ["b","t","n","_","v","o","i","c","e","_","h","a","n","d","s","_","n","o","r"]

/*: "btn_voice_hands_pre" :*/
fileprivate let main_resourceMessage:String = "anima sortbtn_"
fileprivate let const_bottomValue:String = "graph am hung base bloce_ha"
fileprivate let userDespiteName:String = "gatee"

/*: "icon_coin_nor" :*/
fileprivate let main_quoteMessage:[Character] = ["i","c","o","n","_","c","o","i","n","_","n"]
fileprivate let userCountegrationUrl:String = "boardr"

/*: "btn_voice_gift_nor" :*/
fileprivate let noti_cancelVersionString:[Character] = ["b","t","n","_","v","o","i","c","e"]
fileprivate let app_delicateRadioUrl:String = "for direction straighten data_gift_nor"

/*: "Waiting to answer the phone..." :*/
fileprivate let main_managerMessage:String = "closed refrigerator charm awayWait"
fileprivate let show_decisionUrl:String = " answevariable mine unknown"
fileprivate let user_builderEveryPath:String = "warning after unique certain therer th"
fileprivate let app_distinguishCivilizationData:String = "fixed light allowne..."

/*: "00:00" :*/
fileprivate let const_thinMessage:String = "00:00"

/*: "Waiting for %@ to answer the phone..." :*/
fileprivate let user_drawVisitorCosName:[UInt8] = [0xda,0xec,0xe4,0xf9,0xe4,0xe3,0xea,0xad,0xeb,0xe2,0xff,0xad,0xa8,0xcd,0xad,0xf9,0xe2,0xad,0xec,0xe3,0xfe,0xfa,0xe8,0xff,0xad,0xf9,0xe5,0xe8,0xad,0xfd,0xe5,0xe2,0xe3,0xe8,0xa3,0xa3,0xa3]

private func delicateSave(yellow num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "CIGaussianBlur" :*/
fileprivate let mainAppearanceKey:[Character] = ["C","I","G","a","u","s"]
fileprivate let dataSingleId:[Character] = ["s","i","a","n","B","l","u","r"]

/*: "toUid" :*/
fileprivate let const_reminderValue:String = "reach flexible givetoUid"

/*: "giftId" :*/
fileprivate let showPickFormat:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let app_fillLineBeyondName:String = "raw like maintain re infragiftNum"

/*: "from" :*/
fileprivate let dataAnswerTitle:String = "frothin"

/*: "audioCall" :*/
fileprivate let main_columnPath:String = "audioCallat future"

/*: "pkgItemsetId" :*/
fileprivate let userEqualTitle:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "msgInfo" :*/
fileprivate let const_alreadyStr:String = "someone"
fileprivate let notiMissionExhibitionName:String = "reach you keepsgInfo"

/*: "msgType" :*/
fileprivate let dataReflectBasicUrl:String = "attract hi spellmsgType"

/*: "gift" :*/
fileprivate let kUsedSearchMessage:[Character] = ["g","i","f","t"]

/*: "totalMfCoin" :*/
fileprivate let app_renderNobodyStr:String = "totarecognize"
fileprivate let appDatabaseValue:[Character] = ["M","f","C","o","i","n"]

/*: "cellData" :*/
fileprivate let const_visibleMsg:[UInt8] = [0x4e,0x48,0x41,0x41,0x69,0x4c,0x59,0x4c]

private func pastCounterest(monster num: UInt8) -> UInt8 {
    return num ^ 45
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DirectionView.swift
//  AbroadTalking
//
//  Created by young on 2022/12/15.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VoiceInteractionViewDelegate: NSObjectProtocol {
protocol MovementStyle: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func listFlagEdit()

    /// 接听音频通话
    //: func interactionView_shouldAcceptCall()
    func constraint()

    /// 是否静音
    /// - Parameter isMute: 是否静音（默认否）
    //: func interactionView_shouldChangeMute(isMute: Bool)
    func okRank(isMute: Bool)

    /// 是否免提
    /// - Parameter isHandsfree: 是否免提（默认否）
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool)
    func orientation(isHandsfree: Bool)

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func average()

    /// 更新小窗口通话时间
    /// - Parameter time: 通话时间
    //: func interactionView_shouldUpdateTime(time: Double)
    func dayOn(time: Double)
}

//: class Talking1v1VoiceInteractionView: UIView {
class DirectionView: UIView {
    //: weak var delegate: Talking1v1VoiceInteractionViewDelegate?
    weak var delegate: MovementStyle?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = RespectiveModel() // 引用类型，整个音频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: RespectiveModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        iconClear()
        //: setupSubViewsConstraint()
        theOld()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        isolateCos()
        //: initCachEffectData()
        sinceFixed()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiStatePlateMsg.map{labWeight(about: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
//        self.player.stopPlay()
    }

    // MARK: - Lazy Load

    // 渐变背景视图
    //: lazy var bgView: UIImageView = {
    lazy var bgView: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        // 渐变色
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        // 宽高+1是为了适配在某些设备上出现渐变填充不满的情况
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: notiExploreKey + 1, height: app_keepId + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (k_genderPassingFormat.replacingOccurrences(of: "into", with: "8")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: v.layer.addSublayer(layer)
        v.layer.addSublayer(layer)

        //: return v
        return v
        //: }()
    }()

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(changeInteractionShowState), for: .touchUpInside)
        btn.addTarget(self, action: #selector(grayRequire), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 100)/2
        v.layer.cornerRadius = actualWidth(w: 100) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 20)
        lab.font = UIFont.theSearcher(fontSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: WithoutButton = {
        //: let btn = TalkingButton()
        let btn = WithoutButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.installBind(name: (data_opinionFormat.replacingOccurrences(of: "global", with: "i") + "_home_" + String(user_advancedData))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.sumervalSort(fontSize: 12)
        //: btn.titleLabel?.lineBreakMode = .byTruncatingTail
        btn.titleLabel?.lineBreakMode = .byTruncatingTail
        //: return btn
        return btn
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = UIColor(white: 1, alpha: 1)
        lab.textColor = UIColor(white: 1, alpha: 1)
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.sumervalSort(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.installBind(name: (String(showTillPath) + String(dataPosePath) + String(kAudienceId.prefix(5)) + "mize")), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(among), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.installBind(name: (String(k_accelerateData.suffix(4)) + "video_" + const_springData.replacingOccurrences(of: "every", with: "dr") + "_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(appearanceProjection), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.installBind(name: (String(k_agileResolveProvideId) + String(main_minimizePath.prefix(9)))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(filter), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(show_passId) + String(const_stageUniformString))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (main_tunBringString.replacingOccurrences(of: "style", with: "n") + "ation"))

        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭麦克风按钮
    //: private lazy var micBtn: UIButton = {
    private lazy var micBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_Micr_nor"), for: .normal)
        btn.setImage(UIImage.installBind(name: (String(noti_faceFormat) + String(app_largelySickFormat.suffix(5)) + "r_nor")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_Micr_pre"), for: .selected)
        btn.setImage(UIImage.installBind(name: (String(user_spreadMessage.suffix(6)) + String(user_denseKey) + String(show_validStr))), for: .selected)
        //: btn.addTarget(self, action: #selector(microphoneButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(spark), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭免提按钮
    //: lazy var handsfreeBtn: UIButton = {
    lazy var handsfreeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_hands_nor"), for: .normal)
        btn.setImage(UIImage.installBind(name: (String(showPointData))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_hands_pre"), for: .selected)
        btn.setImage(UIImage.installBind(name: (String(main_resourceMessage.suffix(4)) + "voic" + String(const_bottomValue.suffix(4)) + "nds_" + userDespiteName.replacingOccurrences(of: "gate", with: "pr"))), for: .selected)
        //: btn.addTarget(self, action: #selector(handsfreeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(rawZone), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_coin_nor"), for: .normal)
        btn.setImage(UIImage.installBind(name: (String(main_quoteMessage) + userCountegrationUrl.replacingOccurrences(of: "board", with: "o"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_coin_nor"), for: .selected)
        btn.setImage(UIImage.installBind(name: (String(main_quoteMessage) + userCountegrationUrl.replacingOccurrences(of: "board", with: "o"))), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(panelBan), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_gift_nor"), for: .normal)
        btn.setImage(UIImage.installBind(name: (String(noti_cancelVersionString) + String(app_delicateRadioUrl.suffix(9)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_voice_gift_nor"), for: .selected)
        btn.setImage(UIImage.installBind(name: (String(noti_cancelVersionString) + String(app_delicateRadioUrl.suffix(9)))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(collection), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.theSearcher(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: EquipmentService = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = EquipmentService(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: DivideView = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = DivideView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: PriorMultiple = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = PriorMultiple()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VoiceInteractionView {
extension DirectionView {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func isolateCos() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "Waiting to answer the phone...".localized
            desLab.text = (String(main_managerMessage.suffix(4)) + "ing to" + String(show_decisionUrl.prefix(6)) + String(user_builderEveryPath.suffix(4)) + "e pho" + String(app_distinguishCivilizationData.suffix(5))).localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Recieved:
        case .Recieved:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = false
            acceptBtn.isHidden = false
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: micBtn.isHidden = false
            micBtn.isHidden = false
            //: handsfreeBtn.isHidden = false
            handsfreeBtn.isHidden = false
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: locationBtn.isHidden = true
            locationBtn.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                blindWithinFindControl()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (const_thinMessage.capitalized)
            }
            //: startTalkCount()
            until()
            //: touchHiddenTimer()
            comprehensiveContext()
        }

        //: updateLayout()
        attach()
        //: afterGetInfo()
        exceptScript()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func exceptScript() {
        //: if chatModel.videoStage == .Waiting {
        if chatModel.videoStage == .Waiting {
            //: desLab.text = "Waiting for %@ to answer the phone...".localizedArguments(chatModel.pairNickName)
            desLab.text = String(bytes: user_drawVisitorCosName.map{delicateSave(yellow: $0)}, encoding: .utf8)!.noneRage(chatModel.pairNickName)
        }
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.spread(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        // 设置背景头像
        //: if !chatModel.pairHeadPic.isEmptyString {
        if !chatModel.pairHeadPic.isEmptyString {
            //: bgView.setUrlImage(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
            bgView.spread(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if image != nil {
                if image != nil {
                    //: self.bgView.image = self.blurImage(image!)
                    self.bgView.image = self.noticeOf(image!)
                }
            }
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func among() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.average()
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func appearanceProjection() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.listFlagEdit()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func filter() {
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.constraint()
    }

    /// 麦克风按钮事件
    //: @objc private func microphoneButtonClick() {
    @objc private func spark() {
        //: micBtn.isSelected = !micBtn.isSelected
        micBtn.isSelected = !micBtn.isSelected
        //: delegate?.interactionView_shouldChangeMute(isMute: micBtn.isSelected)
        delegate?.okRank(isMute: micBtn.isSelected)
    }

    /// 免提按钮事件
    //: @objc private func handsfreeButtonClick() {
    @objc private func rawZone() {
        //: delegate?.interactionView_shouldChangeSpeaker(isHandsfree: !handsfreeBtn.isSelected)
        delegate?.orientation(isHandsfree: !handsfreeBtn.isSelected)
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func collection() {
        //: func__sendGift()
        checkAcross()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func panelBan() {
        //: TalkingAppPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        ScopeEmbrace.share.constraintOn(webViewType: .RechargeHalfPage)
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func grayRequire() {
        //: let isHidden = !hangupBtn.isHidden
        let isHidden = !hangupBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: micBtn.isHidden = isHidden
        micBtn.isHidden = isHidden
        //: handsfreeBtn.isHidden = isHidden
        handsfreeBtn.isHidden = isHidden
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            permission()
            //: } else {
        } else {
            //: touchHiddenTimer()
            comprehensiveContext()
        }
    }

    //: func blurImage(_ image: UIImage) -> UIImage {
    func noticeOf(_ image: UIImage) -> UIImage {
        //: let context = CIContext(options: nil)
        let context = CIContext(options: nil)
        //: let inputImage = CIImage(image: image)
        let inputImage = CIImage(image: image)
        // 使用高斯模糊滤镜
        //: let filter = CIFilter(name: "CIGaussianBlur")!
        let filter = CIFilter(name: (String(mainAppearanceKey) + String(dataSingleId)))!
        //: filter.setValue(inputImage, forKey: kCIInputImageKey)
        filter.setValue(inputImage, forKey: kCIInputImageKey)
        // 设置模糊半径值（越大越模糊）
        //: filter.setValue(5, forKey: kCIInputRadiusKey)
        filter.setValue(5, forKey: kCIInputRadiusKey)
        //: let outputCIImage = filter.outputImage!
        let outputCIImage = filter.outputImage!
        //: let rect = CGRect(origin: CGPoint.zero, size: image.size)
        let rect = CGRect(origin: CGPoint.zero, size: image.size)
        //: let cgImage = context.createCGImage(outputCIImage, from: rect)
        let cgImage = context.createCGImage(outputCIImage, from: rect)
        // 显示生成的模糊图片
        //: let newImage = UIImage(cgImage: cgImage!)
        let newImage = UIImage(cgImage: cgImage!)
        //: return newImage
        return newImage
    }
}

// MARK: - 送礼

//: extension Talking1v1VoiceInteractionView {
extension DirectionView {
    //: func func__sendGift() {
    func checkAcross() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        FlexibleTiming.share.appear {
            //: self.func__showGiftChoiceView()
            self.trackDistance()
        }
    }

    //: func func__showGiftChoiceView() {
    func trackDistance() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: VideoMagnitudeerval.share.loginUserMode.mf_coin)
        giftView.textLength(needReload: true, mf_coin: VideoMagnitudeerval.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.towardLock()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: CharacterModel, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftModel: giftModel, giftNum: num)
            self.constraintAcross(giftModel: giftModel, giftNum: num)
        }
    }

    //: func func_giftViewDismiss() {
    func micFrame() {
        //: giftView.dismissView()
        giftView.symbolWith()
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func constraintAcross(giftModel: CharacterModel, giftNum: String) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            conclusion(showMsg: appMoveTrainTitle)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == VideoMagnitudeerval.share.loginUserMode.userID {
        if chatModel.iUid == VideoMagnitudeerval.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(const_reminderValue.suffix(5)))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(const_reminderValue.suffix(5)))] = chatModel.iUid
        }
        //: params["giftId"] = giftModel.gid
        params[(String(showPickFormat))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(app_fillLineBeyondName.suffix(7)))] = giftNum
        //: params["from"] = "audioCall"
        params[(dataAnswerTitle.replacingOccurrences(of: "thin", with: "m"))] = (String(main_columnPath.prefix(9)))
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(userEqualTitle))] = giftModel.pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        AnalyzeMapCommon.quoteWithoutExceptTwo(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.resultSpeed(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.awayOf(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.infrastructure(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func infrastructure(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(const_alreadyStr.replacingOccurrences(of: "someone", with: "m") + String(notiMissionExhibitionName.suffix(6)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(dataReflectBasicUrl.suffix(7)))] = (String(kUsedSearchMessage))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(const_alreadyStr.replacingOccurrences(of: "someone", with: "m") + String(notiMissionExhibitionName.suffix(6)))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        offly(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((app_renderNobodyStr.replacingOccurrences(of: "recognize", with: "l") + String(appDatabaseValue))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(app_renderNobodyStr.replacingOccurrences(of: "recognize", with: "l") + String(appDatabaseValue))] as! NSNumber
            //: VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: VideoMagnitudeerval.share.loginUserMode.mf_coin)
        giftView.textLength(needReload: false, mf_coin: VideoMagnitudeerval.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == VideoMagnitudeerval.share.loginUserMode.userID {
        if chatModel.iUid == VideoMagnitudeerval.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo as [String: Any], toUid: toUid, isSender: true, isTip: false)
        let cellData = InformationConcrete.enableicialMark(withExtral: extralInfo as [String: Any], toUid: toUid, isSender: true, isTip: false)
        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: noti_oldStr, object: self, userInfo: [String(bytes: const_visibleMsg.map{pastCounterest(monster: $0)}, encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func resultSpeed(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard VideoMagnitudeerval.share.loginUserMode.status != 1 else {
            guard VideoMagnitudeerval.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    conclusion(showMsg: errorStr)
                }
                //: return
                return
            }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            ScopeEmbrace.share.rateBreeze(clickEvent: appTimingWarnAfterTitle, sufficient: false)
            //: giftView.dismissView()
            giftView.symbolWith()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = MissionPriorBind(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
            //: view.show()
            view.tail()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                conclusion(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func sinceFixed() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == VideoMagnitudeerval.share.loginUserMode.userID {
        if chatModel.iUid == VideoMagnitudeerval.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.heroComponentTake(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func offly(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.become(dict: giftDic) ?? RunPerform()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in FlexibleTiming.share.waitServer() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [LikeCompose]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.generateBy(model: giftModel)
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.natural(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func behindQuote(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in FlexibleTiming.share.waitServer() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [LikeCompose]
        }
        //: for i in 0 ..< giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.generateBy(model: giftModel as! RunPerform)
        }
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.requireDelicate(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VoiceInteractionView {
extension DirectionView {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func until() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.blindWithinFindControl()
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func blindWithinFindControl() {
        //: let duration = getCurrentTalkingTime()
        let duration = awakeSubstantial()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        // 更新小窗口通话时间
        //: delegate?.interactionView_shouldUpdateTime(time: duration)
        delegate?.dayOn(time: duration)
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func intervalensity(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        blindWithinFindControl()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func awakeSubstantial() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VoiceInteractionView {
extension DirectionView {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func comprehensiveContext() {
        //: invalidateIdleTimer()
        permission()
        //: perform(#selector(hideInteractionView), with: nil, afterDelay: 10)
        perform(#selector(backgroundSuccessfully), with: nil, afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func permission() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(backgroundSuccessfully), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func backgroundSuccessfully() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.grayRequire()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VoiceInteractionView {
extension DirectionView {
    /// 添加视图
    //: private func setupSubviews() {
    private func iconClear() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairPicV)
        addSubview(pairPicV)
        //: addSubview(pairNameLab)
        addSubview(pairNameLab)
        //: addSubview(locationBtn)
        addSubview(locationBtn)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(micBtn)
        addSubview(micBtn)
        //: addSubview(handsfreeBtn)
        addSubview(handsfreeBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(timeLab)
        addSubview(timeLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func theOld() {
        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.top.equalTo(pairPicV.snp.bottom).offset(14)
            make.top.equalTo(pairPicV.snp.bottom).offset(14)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(actualWidth(w: 133))
            make.width.equalTo(actualWidth(w: 133))
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(90)
            make.top.equalTo(pairNameLab.snp.bottom).offset(90)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(show_uniformKey + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }
        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-constPublisherValue - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: let letftPlace = (ScreenWidth-actualWidth(w: 45)*4-actualWidth(w: 25)*3)/2
        let letftPlace = (notiExploreKey - actualWidth(w: 45) * 4 - actualWidth(w: 25) * 3) / 2
        //: micBtn.snp.makeConstraints { make in
        micBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(letftPlace)
            make.leading.equalTo(letftPlace)
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-16)
            make.bottom.equalTo(-constPublisherValue - 16)
            //: make.size.equalTo(actualWidth(w: 45))
            make.size.equalTo(actualWidth(w: 45))
        }
        //: handsfreeBtn.snp.makeConstraints { make in
        handsfreeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalTo(locationBtn.snp.bottom).offset(10)
            make.top.equalTo(locationBtn.snp.bottom).offset(10)
            //: make.leading.equalTo(54)
            make.leading.equalTo(54)
            //: make.trailing.equalTo(-54)
            make.trailing.equalTo(-54)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(show_uniformKey + 16)
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func attach() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2-42-70)
                make.leading.equalTo(notiExploreKey / 2 - 42 - 70)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-constPublisherValue - 161)
                //: make.size.equalTo(70)
                make.size.equalTo(70)
            }
            //: acceptBtn.snp.remakeConstraints { make in
            acceptBtn.snp.remakeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2+42)
                make.leading.equalTo(notiExploreKey / 2 + 42)
                //: make.bottom.equalTo(hangupBtn)
                make.bottom.equalTo(hangupBtn)
                //: make.size.equalTo(hangupBtn)
                make.size.equalTo(hangupBtn)
            }
            //: } else {
        } else {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-constPublisherValue - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
        }
    }
}
