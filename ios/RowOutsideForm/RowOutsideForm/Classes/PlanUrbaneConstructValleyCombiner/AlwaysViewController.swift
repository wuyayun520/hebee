
//: Declare String Begin

/*: "cmd" :*/
fileprivate let user_kidPath:[UInt8] = [0xb4,0xba,0xb3]

private func strokeComedy(policy num: UInt8) -> UInt8 {
    return num ^ 215
}

/*: "cancelCall" :*/
fileprivate let mainLicenseValue:String = "cancelCalspecifically cell go other"
fileprivate let app_withPath:String = "spell"

/*: "data" :*/
fileprivate let mainEvolutionTitle:[UInt8] = [0x6b,0x6e,0x7b,0x6e]

/*: "logId" :*/
fileprivate let main_meCombinedMessage:String = "input situation conclusion split tologId"

/*: "#322870" :*/
fileprivate let dataBeforeProvideMessage:String = "#322official70"

/*: "Tap to start video chatting" :*/
fileprivate let main_womanName:String = "Tap totress install explicit existing shot"
fileprivate let dataTressDrawChoiceValue:String = "during harassment sufficient map video "
fileprivate let data_futureString:[Character] = ["c","h","a"]
fileprivate let k_availableTempTitle:String = "signalsignaling"

/*: "icon_close_nor" :*/
fileprivate let dataCycleValue:String = "ICO"
fileprivate let notiClickPath:[Character] = ["n","_","c","l","o","s","e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let k_sayStrategyTitle:String = "ICON"
fileprivate let user_recentMessage:String = "ws_numbcurrently sea"
fileprivate let k_logWhoStr:String = "esound"

/*: "%@ chatting" :*/
fileprivate let dataLayerCalledMessage:[Character] = ["%","@"," ","c","h"]
fileprivate let dataMilkNormStr:[Character] = ["a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let app_sensorProtestKey:[Character] = ["i","c","o","n","_","l","o","u","n","g"]
fileprivate let main_depthRetainSeedId:[Character] = ["e"]

/*: "icon_rank_coin" :*/
fileprivate let user_destinationMsg:[Character] = ["i","c","o","n","_","r","a","n","k","_","c","o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let app_accuracyKey:String = "vip_satisfy ad"

/*: "btn_nearby_settings_nor" :*/
fileprivate let noti_appearPath:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t","i","n","g","s","_","n"]
fileprivate let notiManagerId:[Character] = ["o","r"]

/*: "%@ Coins/min" :*/
fileprivate let data_fillFormat:String = "%@ Costatus class"
fileprivate let notiLoopFormat:String = "ins/minenvironment cess"

/*: "img_match_connecting" :*/
fileprivate let constRebuildAliveValue:String = "img_matcfull fast"
fileprivate let userPagePowerfulName:[Character] = ["h","_","c","o","n","n","e","c","t","i","n","g"]

/*: "#9F80FF" :*/
fileprivate let constProgressName:String = "#9F80FFsubsequent ready rapidly"

/*: "get json error" :*/
fileprivate let dataBroadcastId:[Character] = ["g","e","t"," ","j","s","o","n"]
fileprivate let show_memoryUrl:String = "fast"
fileprivate let appUniformBreezeMsg:[Character] = ["e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let showMountBecomeValue:String = "matckid"
fileprivate let userScanPreciseId:String = "movement yesterday split pile spaceId"

/*: "errno" :*/
fileprivate let noti_closeStr:[UInt8] = [0xb6,0xa1,0xa1,0xbd,0xbc]

private func heroPlayer(collect num: UInt8) -> UInt8 {
    return num ^ 211
}

/*: "VIPFee" :*/
fileprivate let appCurrencyMsg:[Character] = ["V","I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let show_capacityPath:String = "rain collection advanced period successMatch"
fileprivate let constSingeTractorData:[Character] = ["n","o","w"]

/*: "userTouch" :*/
fileprivate let noti_literalStr:String = "my earn other rimeuserTo"
fileprivate let data_extraString:String = "ucres"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let dataLeaveName:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i","d","e","o","I"]
fileprivate let constEarnConcreteUrl:String = "ncounselo"

/*: "fromType" :*/
fileprivate let app_diskString:[UInt8] = [0x73,0x7f,0x7c,0x7a,0x61,0x86,0x7d,0x72]

fileprivate func oldenGreet(after num: UInt8) -> UInt8 {
    let value = Int(num) - 13
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "You've been barred from receiving calls" :*/
fileprivate let userBagWholeDistinguishName:[UInt8] = [0xc2,0xf4,0xee,0xbc,0xed,0xfe,0xbb,0xf9,0xfe,0xfe,0xf5,0xbb,0xf9,0xfa,0xe9,0xe9,0xfe,0xff,0xbb,0xfd,0xe9,0xf4,0xf6,0xbb,0xe9,0xfe,0xf8,0xfe,0xf2,0xed,0xf2,0xf5,0xfc,0xbb,0xf8,0xfa,0xf7,0xf7,0xe8]

private func reOdd(reminder num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "source" :*/
fileprivate let notiDynamicsPlaceMsg:[UInt8] = [0x2c,0x30,0x2a,0x2d,0x3c,0x3a]

private func migrationDepthIsolate(same num: UInt8) -> UInt8 {
    return num ^ 95
}

/*: "type" :*/
fileprivate let userBurnUrl:[UInt8] = [0xe8,0xed,0xe4,0xd9]

fileprivate func featureDecision(dump num: UInt8) -> UInt8 {
    let value = Int(num) - 116
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let noti_airFindId:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let noti_padUrl:String = "frremote"

/*: "requestCall" :*/
fileprivate let const_retPath:String = "requestCaaccount digital choose flexible"
fileprivate let dataResolutionPath:String = "harassment"

/*: "onVideoMatch" :*/
fileprivate let appPullNextKey:String = "onViappearance least"
fileprivate let k_normalStopPath:String = "heavy next draw protection losedeoM"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let dataLabelId:[UInt8] = [0x99,0x7f,0xb3,0xb2,0xad,0xab,0x9f,0xb2,0xa7,0xa1,0x5e,0xb4,0xa7,0xa2,0xa3,0xad,0x9b,0x5e,0x90,0xa3,0xa1,0xa3,0xa7,0xb4,0xa3,0x5e,0xb4,0xa7,0xa2,0xa3,0xad,0x5e,0xa1,0x9f,0xaa,0xaa,0x5e,0xa7,0xac,0xa1,0xad,0xab,0xa7,0xac,0xa5]

fileprivate func tollCess(button num: UInt8) -> UInt8 {
    let value = Int(num) - 62
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isRandomVCPush" :*/
fileprivate let show_panelHaveId:[Character] = ["i","s","R","a","n","d","o","m","V","C"]
fileprivate let show_implementKey:[Character] = ["P","u","s","h"]

/*: "refuseCall" :*/
fileprivate let show_guideId:String = "rhung"
fileprivate let noti_busyName:String = "camera connect comedy exactlyfuseCall"

/*: "onCancelCall" :*/
fileprivate let main_laterPairPath:[Character] = ["o","n","C","a","n","c","e","l","C"]
fileprivate let mainHalfValue:[Character] = ["a","l","l"]

/*: "retryAfter" :*/
fileprivate let main_leastUrl:String = "the awayretryAf"
fileprivate let userGapPlayTitle:[Character] = ["t","e","r"]

/*: "onRequestCall" :*/
fileprivate let const_failureData:[Character] = ["o","n","R","e","q","u","e","s","t"]
fileprivate let show_controlMessage:String = "scope after region we specificallyCall"

/*: "failType" :*/
fileprivate let notiLanguageValue:[Character] = ["f","a"]
fileprivate let dataPassFormat:String = "ilTypeauto sun"

/*: "callWaitingTime" :*/
fileprivate let noti_effectMessage:String = "callWahere when age comply request"
fileprivate let app_mStripUrl:String = "equipment"

/*: "toUid" :*/
fileprivate let dataAroundValue:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let userPlayerValue:String = "transform start close banFree 1"
fileprivate let k_matchMessage:String = "border bring accept min"

/*: "%@/min" :*/
fileprivate let data_integrationName:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AlwaysViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class AlwaysViewController: MasterClip {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.anyIncomePad()
            }
        }
        //: self.resetCamera()
        self.delayResign()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        chainAlbum()
        //: setupSubViewsConstraint()
        restoreAppropriate()
        //: bindInteraction()
        subsequentSh()
        //: videoManager.startPreview(isMatch: true)
        videoManager.descriptionSecond(isMatch: true)
        //: setPlayer(isRandon: false)
        modelHave(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            executeSatisfy()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            QuantityensityAction.shared.frameOfFeed(info: [String(bytes: user_kidPath.map{strokeComedy(policy: $0)}, encoding: .utf8)!: (String(mainLicenseValue.prefix(9)) + app_withPath.replacingOccurrences(of: "spell", with: "l")), String(bytes: mainEvolutionTitle.map{$0^15}, encoding: .utf8)!: [(String(main_meCombinedMessage.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        today()
        //: if TalkingSocketManager.shared.versusScope == false {
        if QuantityensityAction.shared.versusScope == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            QuantityensityAction.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            QuantityensityAction.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            QuantityensityAction.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            QuantityensityAction.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: AtAnalyzeObject = {
        //: let m = TalkingVideoChatManager()
        let m = AtAnalyzeObject()
        //: let info = TalkingVideoChatModel()
        let info = RespectiveModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .serviceMajor()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(executeSatisfy), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: notiExploreKey + 1, height: app_keepId + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (dataBeforeProvideMessage.replacingOccurrences(of: "official", with: "8")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(main_womanName.prefix(6)) + " start" + String(dataTressDrawChoiceValue.suffix(7)) + String(data_futureString) + k_availableTempTitle.replacingOccurrences(of: "signal", with: "t")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.installBind(name: (dataCycleValue.lowercased() + String(notiClickPath))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(thenRating), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: WithoutButton = {
        //: let btn = TalkingButton()
        let btn = WithoutButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.installBind(name: (k_sayStrategyTitle.lowercased() + "_vie" + String(user_recentMessage.prefix(7)) + k_logWhoStr.replacingOccurrences(of: "sound", with: "r"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.installBind(name: (k_sayStrategyTitle.lowercased() + "_vie" + String(user_recentMessage.prefix(7)) + k_logWhoStr.replacingOccurrences(of: "sound", with: "r"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.statinEssay(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(dataLayerCalledMessage) + String(dataMilkNormStr)).noneRage(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.installBind(name: (String(app_sensorProtestKey) + String(main_depthRetainSeedId)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.installBind(name: (String(user_destinationMsg)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.statinEssay(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.installBind(name: (String(app_accuracyKey.prefix(4)) + "btn_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: WithoutButton = {
        //: let btn = TalkingButton()
        let btn = WithoutButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(isPremiumQuestion), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.installBind(name: (String(noti_appearPath) + String(notiManagerId))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.installBind(name: (String(user_destinationMsg)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .beautyCompose()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(data_fillFormat.prefix(5)) + String(notiLoopFormat.prefix(7))).noneRage(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.statinEssay(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: EventView = //: return EventView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()

    //: private lazy var connectingImg: UIImageView = {
    private lazy var connectingImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "img_match_connecting")
        imgV.image = UIImage.installBind(name: (String(constRebuildAliveValue.prefix(8)) + String(userPagePowerfulName)))
        //: let view = UIActivityIndicatorView.init()
        let view = UIActivityIndicatorView()
        //: view.color = UIColor(hex: "#9F80FF")
        view.color = UIColor(hex: (String(constProgressName.prefix(7))))
        //: imgV.addSubview(view)
        imgV.addSubview(view)
        //: view.snp.makeConstraints { make in
        view.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(20)
            make.size.equalTo(20)
        }
        //: view.startAnimating()
        view.startAnimating()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: return imgV
        return imgV
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension AlwaysViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func modelHave(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = LimitIndicator.default.multiple(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(dataBroadcastId) + show_memoryUrl.replacingOccurrences(of: "fast", with: " ") + String(appUniformBreezeMsg)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func currencyOff(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(showMountBecomeValue.replacingOccurrences(of: "kid", with: "h") + String(userScanPreciseId.suffix(2))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: user_kidPath.map{strokeComedy(policy: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: noti_closeStr.map{heroPlayer(collect: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        HandleControlLimitBasicHead.handlePrepare(params: params) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.resistanceInTier()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(showMountBecomeValue.replacingOccurrences(of: "kid", with: "h") + String(userScanPreciseId.suffix(2)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func quote() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        HandleControlLimitBasicHead.property { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(appCurrencyMsg))].intValue
            //: self.refreshVideoCallPrice()
            self.anyIncomePad()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension AlwaysViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func isPremiumQuestion() {
        //: TalkingAppPushManager.share.func__pushToSubscribeAlert()
        ScopeEmbrace.share.preciseOf()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func thenRating() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        pageKind()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: user_selectThresholdFillString)

        //: if VideoMagnitudeerval.share.appConfigMode.displayMatchV4PopUp, VideoMagnitudeerval.share.loginUserMode.freeCallTimes > 0,
        if VideoMagnitudeerval.share.appConfigMode.displayMatchV4PopUp, VideoMagnitudeerval.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.beyondSign()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        congressionalFor()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func executeSatisfy() {
        //: guard TalkingSocketManager.shared.versusScope == false else {
        guard QuantityensityAction.shared.versusScope == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.conclusion(showMsg: notiReducePath)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        architecture()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(show_capacityPath.suffix(5)) + "ing " + String(constSingeTractorData)).localized
        //: setPlayer(isRandon: true)
        modelHave(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if VideoMagnitudeerval.share.loginUserMode.randomVideoType == 2 && VideoMagnitudeerval.share.loginUserMode.robotVideoNum > 0 {
        if VideoMagnitudeerval.share.loginUserMode.randomVideoType == 2, VideoMagnitudeerval.share.loginUserMode.robotVideoNum > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: k_allId)
            //: req_getVideoInfo()
            alongGray()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: data_clickMsg)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            QuantityensityAction.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            QuantityensityAction.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            QuantityensityAction.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            QuantityensityAction.shared.errorDelegate = self
            //: restart_randomVideo()
            noSophisticated()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func noSophisticated(cmd: String = (String(noti_literalStr.suffix(6)) + data_extraString.replacingOccurrences(of: "res", with: "h")), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        today()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        currencyOff(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func resistanceInTier() {
        // 停止定时器
        //: destroyWaitingTimer()
        today()
        //: showCloseBtn()
        pageKind()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(main_womanName.prefix(6)) + " start" + String(dataTressDrawChoiceValue.suffix(7)) + String(data_futureString) + k_availableTempTitle.replacingOccurrences(of: "signal", with: "t")).localized
        //: setPlayer(isRandon: false)
        modelHave(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.versusScope == false {
        if QuantityensityAction.shared.versusScope == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            QuantityensityAction.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            QuantityensityAction.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            QuantityensityAction.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            QuantityensityAction.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        anyIncomePad()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func today() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func alongGray() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PopAdjust()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(dataLeaveName) + constEarnConcreteUrl.replacingOccurrences(of: "counsel", with: "f"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: app_diskString.map{oldenGreet(after: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        const_equivalentValue.panelAttract(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.resistanceInTier()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: VideoMagnitudeerval.share.loginUserMode.randomVideoType = 1
                    VideoMagnitudeerval.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.executeSatisfy()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.conclusion(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: self.connectingImg.isHidden = false
            self.connectingImg.isHidden = false
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = SharedModel.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = StartMend()
                //: player.setMute(bEnable: true)
                player.graphAcross(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.callVideoUrl, view: UIView.init())
                player.giftOpener(url: model.callVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                InstanceCell.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                ReduceLevelDoingtoPrior.sprinkle { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.fast()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func fast() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: main_clearHandleMsg)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard InstanceCell.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.conclusion(showMsg: String(bytes: userBagWholeDistinguishName.map{reOdd(reminder: $0)}, encoding: .utf8)!.localized)
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            InstanceCell.shared.noteConnectionSound(type: 1) { succeed, result, errorModel in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.resistanceInTier()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = MissionClean()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.congressionalFor()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.resistanceInTier()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func architecture() {
        //: if VideoMagnitudeerval.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if VideoMagnitudeerval.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(VideoMagnitudeerval.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(VideoMagnitudeerval.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func pageKind() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func delayResign() {
        //: guard TalkingSocketManager.shared.versusScope == false else {
        guard QuantityensityAction.shared.versusScope == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.descriptionSecond()
        }
    }

    /// 匹配通话结束时
    //: @objc private func closeTool() {
    @objc private func contactData() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
            //: self.start_randomVideo()
            self.executeSatisfy()
        }
    }
}

// MARK: - GroupPhone

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension AlwaysViewController: GroupPhone {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func securePrompt(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = SelectedFillListen.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(showMountBecomeValue.replacingOccurrences(of: "kid", with: "h") + String(userScanPreciseId.suffix(2))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: notiDynamicsPlaceMsg.map{migrationDepthIsolate(same: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: userBurnUrl.map{featureDecision(dump: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: noti_airFindId.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = OnTool.randow
        //: if VideoMagnitudeerval.share.loginUserMode.freeCallTimes > 0 && VideoMagnitudeerval.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if VideoMagnitudeerval.share.loginUserMode.freeCallTimes > 0 && VideoMagnitudeerval.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (noti_padUrl.replacingOccurrences(of: "remote", with: "om") + "FreeCall"))
            //: TalkingSocketManager.shared.isFreeCall = true
            QuantityensityAction.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: user_kidPath.map{strokeComedy(policy: $0)}, encoding: .utf8)!: (String(const_retPath.prefix(9)) + dataResolutionPath.replacingOccurrences(of: "harassment", with: "ll")), String(bytes: mainEvolutionTitle.map{$0^15}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        QuantityensityAction.shared.frameOfFeed(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            QuantityensityAction.shared.frameOfFeed(info: [String(bytes: user_kidPath.map{strokeComedy(policy: $0)}, encoding: .utf8)!: (String(mainLicenseValue.prefix(9)) + app_withPath.replacingOccurrences(of: "spell", with: "l")), String(bytes: mainEvolutionTitle.map{$0^15}, encoding: .utf8)!: [(String(main_meCombinedMessage.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.noSophisticated(cmd: (String(appPullNextKey.prefix(4)) + String(k_normalStopPath.suffix(4)) + "atch"))
            //: }, repeats: false)
        }, repeats: false)
        //: connectingImg.isHidden = false
        connectingImg.isHidden = false
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func ease(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(main_meCombinedMessage.suffix(5)))].intValue
    }
}

// MARK: - AngleEdge【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension AlwaysViewController: AngleEdge {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func refreshDown(data: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func multiRe(data: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func untilBy(msg: String, data: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func proWith(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            LightSend.theOrange(body: String(bytes: dataLabelId.map{tollCess(button: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = SelectedFillListen.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            maleEnable(toUid: String(uid))
        }
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: stop_randomVideo()
        resistanceInTier()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.ownerBox()
        //: var newData = data
        var newData = data
        //: newData["isRandomVCPush"] = true
        newData[(String(show_panelHaveId) + String(show_implementKey))] = true
        //: VideoMagnitudeerval.share.start1v1TalkCall(info: newData, situation: .Calling)
        VideoMagnitudeerval.share.beforeToInstall(info: newData, situation: .Calling)
    }
}

// MARK: - FloorConsume【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension AlwaysViewController: FloorConsume {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func noneInstance(data: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        noSophisticated(cmd: (show_guideId.replacingOccurrences(of: "hung", with: "e") + String(noti_busyName.suffix(8))))
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func beforeTranslate(data: [String: Any]) {}
}

// MARK: - ReduceException

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension AlwaysViewController: ReduceException {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func pinAudience(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(main_laterPairPath) + String(mainHalfValue)) { // 取消通话异常
            //: stop_randomVideo()
            resistanceInTier()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(appPullNextKey.prefix(4)) + String(k_normalStopPath.suffix(4)) + "atch"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            resistanceInTier()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(showMountBecomeValue.replacingOccurrences(of: "kid", with: "h") + String(userScanPreciseId.suffix(2)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.conclusion(showMsg: msg)
            //: uploadFailTime(type: 2)
            betweenGoldPurple(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(appPullNextKey.prefix(4)) + String(k_normalStopPath.suffix(4)) + "atch"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(main_leastUrl.suffix(7)) + String(userGapPlayTitle))].intValue), block: { [weak self] timer in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(showMountBecomeValue.replacingOccurrences(of: "kid", with: "h") + String(userScanPreciseId.suffix(2)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.noSophisticated(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(const_failureData) + String(show_controlMessage.suffix(4))), errorNo == OddPress.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            resistanceInTier()
            //: guard VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue else { return }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            ScopeEmbrace.share.rateBreeze(clickEvent: app_commonDestroyId, sufficient: false)
            //: uploadFailTime(type: 1)
            betweenGoldPurple(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        noSophisticated(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension AlwaysViewController {
    //: func uploadFailTime(type: Int) {
    func betweenGoldPurple(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(notiLanguageValue) + String(dataPassFormat.prefix(6)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(noti_effectMessage.prefix(6)) + "itingTim" + app_mStripUrl.replacingOccurrences(of: "equipment", with: "e"))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        k_frameUrl.instanceHard(eventID: showGateVolumeStr, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func maleEnable(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(noti_effectMessage.prefix(6)) + "itingTim" + app_mStripUrl.replacingOccurrences(of: "equipment", with: "e"))] = time
        //: parm["toUid"] = toUid
        parm[(String(dataAroundValue))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        k_frameUrl.instanceHard(eventID: mainAgentDuringAroundFormat, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension AlwaysViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func anyIncomePad() {
        //: guard VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !VideoMagnitudeerval.share.loginUserMode.loungePlus
        vipIcon.isHidden = !VideoMagnitudeerval.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = OnTool.randow
        //: if VideoMagnitudeerval.share.loginUserMode.freeCallTimes > 0 && VideoMagnitudeerval.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if VideoMagnitudeerval.share.loginUserMode.freeCallTimes > 0, VideoMagnitudeerval.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if VideoMagnitudeerval.share.appUserConfigMode.callBtnStyle == 1 {
            if VideoMagnitudeerval.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(userPlayerValue.suffix(6)) + String(k_matchMessage.suffix(4))).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = VideoMagnitudeerval.share.loginUserMode.loungePlus ? vipFee:fee
            let price = VideoMagnitudeerval.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(data_integrationName)).noneRage(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(data_integrationName)).noneRage(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.theSearcher(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = VideoMagnitudeerval.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = VideoMagnitudeerval.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = VideoMagnitudeerval.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = VideoMagnitudeerval.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !VideoMagnitudeerval.share.loginUserMode.loungePlus
            priceStackView.isHidden = !VideoMagnitudeerval.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func chainAlbum() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
        //: view.addSubview(connectingImg)
        view.addSubview(connectingImg)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func restoreAppropriate() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(show_uniformKey + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }

        //: connectingImg.snp.makeConstraints { make in
        connectingImg.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
            make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func subsequentSh() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(anyIncomePad),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: show_putEmbraceId,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(quote),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: user_soundTapPath,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeTool), name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(contactData), name: data_pushValue, object: nil)
    }
}
