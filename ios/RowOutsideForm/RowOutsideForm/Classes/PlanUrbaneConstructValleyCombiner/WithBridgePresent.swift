
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let show_giftDistinctionId:String = "link chain exhibitionbg_mess"
fileprivate let noti_thirdMessage:[Character] = ["a","g","e","_","t","o","p"]

/*: "777777" :*/
fileprivate let app_twoTowelId:String = "777777"

/*: "Messages" :*/
fileprivate let appConversionProtestValue:[Character] = ["M"]
fileprivate let notiCleanValue:String = "essemptyges"

/*: "Who like me" :*/
fileprivate let data_groupFormat:String = "Who liketext spell"
fileprivate let app_fitPath:String = " mesplit consume smart transaction hero"

/*: "Call" :*/
fileprivate let k_gravityFormat:[Character] = ["C","a","l","l"]

/*: "#FF2348" :*/
fileprivate let kNumberegrityString:String = "#FF2348bind recommendation time rem fee"

/*: "Current network unavailable" :*/
fileprivate let noti_invitationId:[Character] = ["C","u","r","r","e","n","t"," ","n","e","t"]
fileprivate let constDenseSwitchtoName:String = "panork"
fileprivate let dataInfoData:[Character] = ["i","l","a","b","l","e"]

/*: "icon_yidu_pre" :*/
fileprivate let userExceptionStr:String = "cameraon"
fileprivate let notiDetailedUniformString:[Character] = ["r","e"]

/*: "transform.rotation" :*/
fileprivate let k_colorSecurityPath:[Character] = ["t","r","a","n","s","f","o","r","m",".","r","o","t","a","t","i","o","n"]

/*: "transform.scale" :*/
fileprivate let mainProcedureWelcomeString:String = "trasix"
fileprivate let show_kingValueName:String = "time occupy unless tell.scale"

/*: "zoom&shake" :*/
fileprivate let main_collectPausePath:[Character] = ["z","o","o","m","&","s"]
fileprivate let constPartnerTitle:String = "hakconstruct"

/*: "yyyy-MM-dd" :*/
fileprivate let mainGestureEarnString:String = "kit"
fileprivate let constQualityValue:String = "yyy-permission player loop easy"

/*: "Allow %@ to send you notifications?" :*/
fileprivate let dataRouteNumbName:[UInt8] = [0x3f,0x73,0x6e,0x6f,0x69,0x74,0x61,0x63,0x69,0x66,0x69,0x74,0x6f,0x6e,0x20,0x75,0x6f,0x79,0x20,0x64,0x6e,0x65,0x73,0x20,0x6f,0x74,0x20,0x40,0x25,0x20,0x77,0x6f,0x6c,0x6c,0x41]

/*: "Cancel" :*/
fileprivate let showTempMsg:[Character] = ["C"]
fileprivate let showExerciseMessage:String = "entitycel"

/*: "Settings" :*/
fileprivate let user_appearKey:[Character] = ["S","e","t","t","i"]
fileprivate let show_hiddenPastTitle:String = "ngsibling"

/*: "badNumber" :*/
fileprivate let constButtonTitle:String = "badNumbepresentation called"
fileprivate let app_adjustRecommendName:[Character] = ["r"]

/*: "isConnection" :*/
fileprivate let dataConnectTitle:[Character] = ["i","s","C","o","n","n","e","c"]
fileprivate let appCameraBetweenValue:[Character] = ["t","i","o","n"]

/*: "networkStatus" :*/
fileprivate let userManagerMsg:[Character] = ["n","e","t","w","o","r","k","S"]
fileprivate let dataNoticeFormat:[Character] = ["t","a","t","u","s"]

/*: "unreadMessageNum" :*/
fileprivate let const_agreeFormat:[Character] = ["u","n","r","e","a","d","M","e","s"]
fileprivate let const_broadcastUrl:String = "swriting"
fileprivate let const_neverStr:String = "geNumwarning even hard theme"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let noti_toleFormat:[UInt8] = [0x3f,0x64,0x61,0x65,0x72,0x20,0x73,0x61,0x20,0x73,0x65,0x67,0x61,0x73,0x73,0x65,0x6d,0x20,0x6c,0x6c,0x61,0x20,0x6b,0x72,0x61,0x6d,0x20,0x6f,0x74,0x20,0x74,0x6e,0x61,0x77,0x20,0x75,0x6f,0x79,0x20,0x6f,0x44]

/*: "消息列表一键已读失败：code: :*/
fileprivate let appMarginKey:[Character] = ["消","息","列","表","一","键","已","读"]
fileprivate let mainBubbleId:[Character] = ["失","败","：","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let mainProvideValue:String = "assignment star host, desc:"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WithBridgePresent.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class WithBridgePresent: MasterClip {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        canIn()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        underInstead()
        //: bindInteraction()
        wrap()
        //: func__turnOnSystemNotification()
        analyzeToSwitche()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: 100 + mainLightMessage))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.installBind(name: (String(show_giftDistinctionId.suffix(7)) + String(noti_thirdMessage)))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: MayView = {
        //: let V = TalkingNoticeTipView()
        let V = MayView()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: PointViewController = {
        //: let vc = QYSlideNavigationViewController()
        let vc = PointViewController()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .statinEssay(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .statinEssay(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (app_twoTowelId.capitalized))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.serviceMajor()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [MasterClip] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, DetailSpace()]
        //: if VideoMagnitudeerval.share.loginUserMode.callTabSwitch == 1 {
        if VideoMagnitudeerval.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(ProjectMagnitudeeraction(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: EmptyBelow = {
        //: let vc = TalkingChatListViewController()
        let vc = EmptyBelow()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(appConversionProtestValue) + notiCleanValue.replacingOccurrences(of: "empty", with: "a")).localized, (String(data_groupFormat.prefix(8)) + String(app_fitPath.prefix(3))).localized]
        //: if VideoMagnitudeerval.share.loginUserMode.callTabSwitch == 1 {
        if VideoMagnitudeerval.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(k_gravityFormat)).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: mainLightMessage, width: notiExploreKey, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.sumervalSort(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(kNumberegrityString.prefix(7))))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(noti_invitationId) + constDenseSwitchtoName.replacingOccurrences(of: "pan", with: "w") + " unava" + String(dataInfoData)).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: ErrorFlex = {
        //: let numbLabel = BadgeLab()
        let numbLabel = ErrorFlex()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: ErrorFlex = {
        //: let numbLabel = BadgeLab()
        let numbLabel = ErrorFlex()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.installBind(name: (userExceptionStr.replacingOccurrences(of: "camera", with: "ic") + "_yidu_p" + String(notiDetailedUniformString))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.installBind(name: (userExceptionStr.replacingOccurrences(of: "camera", with: "ic") + "_yidu_p" + String(notiDetailedUniformString))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - ThresholdActive

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension WithBridgePresent: ThresholdActive {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func recent(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: EmptyBelow.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            canIn()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: DetailSpace.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: kOpenId)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension WithBridgePresent {
    //: func setIsTopAll() {
    func notCalendarListener() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? EmptyBelow
            //: vc?.resetToTopItemView()
            vc?.tween()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.roundOperate(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func languageLocal() {
        //: if VideoMagnitudeerval.share.networkStatus != .Unavailable && SpellAround.shared.isConnection {
        if VideoMagnitudeerval.share.networkStatus != .Unavailable, SpellAround.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func brownIn(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func canIn() {
        //: let unreadMsgCount = VideoMagnitudeerval.share.unreadMessageNum
        let unreadMsgCount = VideoMagnitudeerval.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: data_significantStr) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.matchSpotCluster()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func matchSpotCluster() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (String(k_colorSecurityPath)))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (mainProcedureWelcomeString.replacingOccurrences(of: "six", with: "ns") + "form" + String(show_kingValueName.suffix(6))))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(main_collectPausePath) + constPartnerTitle.replacingOccurrences(of: "construct", with: "e")))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func analyzeToSwitche() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        ReduceLevelDoingtoPrior.country { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.bitEdit(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.netScene(date: Date(), dateFormat: (mainGestureEarnString.replacingOccurrences(of: "kit", with: "y") + String(constQualityValue.prefix(4)) + "MM-dd"))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = appEfficiencyStorageStr.string(forKey: showTaskKey), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.bitEdit(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.bitEdit(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = appEfficiencyStorageStr.bool(forKey: dataLabelTitle)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        appEfficiencyStorageStr.set(true, forKey: dataLabelTitle)
                        //: TalkingAlertShow.alert(title: nil,
                        TapBounce.notMovementSick(title: nil,
                                                         //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                         message: String(bytes: dataRouteNumbName.reversed(), encoding: .utf8)!.noneRage(user_sinkValue),
                                                         //: leftBtnTitle: "Cancel".localized,
                                                         leftBtnTitle: (String(showTempMsg) + showExerciseMessage.replacingOccurrences(of: "entity", with: "an")).localized,
                                                         //: rightBtnTitle: "Settings".localized) {
                                                         rightBtnTitle: (String(user_appearKey) + show_hiddenPastTitle.replacingOccurrences(of: "sibling", with: "s")).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            TapBounce.outConvert()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func bitEdit(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(show_uniformKey)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = app_keepId - show_uniformKey - userOutputKey
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(show_uniformKey + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = app_keepId - self.noticeView.bottom - userOutputKey
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension WithBridgePresent {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func ownerHero(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(constButtonTitle.prefix(8)) + String(app_adjustRecommendName))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension WithBridgePresent {
    /// UI
    //: private func createUI() {
    private func underInstead() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(show_uniformKey)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(data_groupFormat.prefix(8)) + String(app_fitPath.prefix(3))).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(appConversionProtestValue) + notiCleanValue.replacingOccurrences(of: "empty", with: "a")).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func wrap() {
        //: SpellAround.shared.rx
        SpellAround.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(dataConnectTitle) + String(appCameraBetweenValue)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.languageLocal()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: VideoMagnitudeerval.share.rx.observeWeakly(Int.self, "networkStatus")
        VideoMagnitudeerval.share.rx.observeWeakly(Int.self, (String(userManagerMsg) + String(dataNoticeFormat)))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.languageLocal()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: VideoMagnitudeerval.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        VideoMagnitudeerval.share.rx.observeWeakly(Int.self, (String(const_agreeFormat) + const_broadcastUrl.replacingOccurrences(of: "writing", with: "a") + String(const_neverStr.prefix(5))))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.brownIn(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = ImpactMobileContainer()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                TapBounce.himLeave(message: String(bytes: noti_toleFormat.reversed(), encoding: .utf8)!.localized, leftBtnTitle: (String(showTempMsg) + showExerciseMessage.replacingOccurrences(of: "entity", with: "an")).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    TapBounce.outConvert()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: data_significantStr)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        TrainGreat.bitRemove(msg: (String(appMarginKey) + String(mainBubbleId)) + "\(code)" + (String(mainProvideValue.suffix(7))) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(ownerHero(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: appUntilName,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.netScene(date: Date(), dateFormat: (mainGestureEarnString.replacingOccurrences(of: "kit", with: "y") + String(constQualityValue.prefix(4)) + "MM-dd"))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            appEfficiencyStorageStr.set(today, forKey: showTaskKey)
            //: self.func__hideNotificationTipView(hide: true)
            self.bitEdit(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] notification in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.analyzeToSwitche()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
