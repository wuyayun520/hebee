
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let user_toleStr:String = "socket"
fileprivate let constCareName:[Character] = ["_","A","D","D"]
fileprivate let appWeeklyCenterUrl:String = "attract migration common focusR_KEY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let k_visualId:[UInt8] = [0x64,0x78,0x74,0x7c,0x72,0x63,0x68,0x61,0x7e,0x73,0x72,0x78,0x74,0x7f,0x76,0x63,0x68,0x65,0x72,0x66,0x62,0x72,0x64,0x63,0x74,0x76,0x7b,0x7b,0x68,0x79,0x78,0x63,0x7e,0x71,0x7e,0x74,0x76,0x63,0x7e,0x78,0x79]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let appItemMessageUrl:[UInt8] = [0x7,0x10,0x16,0x1c,0x3,0x10,0xa,0x13,0x19,0x1a,0x14,0x1,0x6,0x16,0x7,0x10,0x10,0x1b,0xa,0x1b,0x1a,0x1,0x1c,0x13,0x1c,0x16,0x14,0x1,0x1c,0x1a,0x1b]

private func possiblySatisfy(instead num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: "SOCKET 连接成功." :*/
fileprivate let data_portraitStr:String = "StackCK"
fileprivate let constWindowKey:String = "接\u{6210}\u{529f}."

/*: "SOCKET 连接失败, error: :*/
fileprivate let app_honestlyName:String = "SsoakCKE"
fileprivate let appSickSeaData:String = "败, erconsume bounce"

/*: "SOCKET 失败 :*/
fileprivate let show_detailFeeMsg:String = "SOCKannual"
fileprivate let kResidencyValue:String = "T 失\u{8d25}"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let appRatingString:String = "次, \u{5207}换过S"
fileprivate let constFollowingMsg:[Character] = ["o","c","k","e","t","\u{57df}","名","重","新","尝"]
fileprivate let k_harassmentProjectionData:String = "试，\u{5730}址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let appArrestStr:String = "midOCKE"
fileprivate let main_pointRegulateKey:String = "code:calendar play beat associated"

/*: , reason: :*/
fileprivate let main_designName:[Character] = [","," ","r","e","a","s","o","n",":"]

/*: /?sid= :*/
fileprivate let k_wakeStr:[Character] = ["/","?","s","i","d","="]

/*: &uid= :*/
fileprivate let constExceptRowKey:String = "&uid=grate premium margin sine mm"

/*: &platform=iphone&version= :*/
fileprivate let dataOccurSunRefuseKey:[Character] = ["&","p"]
fileprivate let userBoldValue:[Character] = ["l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let noti_maxData:String = "stat my later pending analyze&pac"

/*: "1v1server. :*/
fileprivate let main_localModMsg:[Character] = ["1","v","1","s","e"]
fileprivate let constOgreUrl:[Character] = ["r","v","e","r","."]

/*: .com:7766" :*/
fileprivate let mainTransformBulletName:String = "brown at accuse through.c"
fileprivate let main_ribbonProvideId:String = "craft mount married denyom:7766"

/*: "43.129.185.194:7767" :*/
fileprivate let appForceLibraryPath:[Character] = ["4","3",".","1","2","9",".","1","8","5",".","1","9","4",":","7"]
fileprivate let constDelicateMakeMessage:String = "min6min"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let app_ogreUrl:[UInt8] = [0x97,0x8b,0x87,0x8f,0x81,0x90,0xe4,0x23,0x79,0x55,0x23,0x7f,0x58,0xb6,0xa1,0xa5,0xa7,0xac,0xa5,0xa6,0xad,0xa8,0xad,0xb0,0xbd,0x87,0xac,0xa5,0xaa,0xa3,0xa1,0xa0,0x23,0x5f,0x55,0x21,0x54,0x68,0x2b,0x78,0x48,0x22,0x52,0x69,0x23,0x79,0x55,0x21,0x41,0x77,0x2d,0x53,0x69,0xea]

/*: "cmd" :*/
fileprivate let main_tickData:String = "cgold"

/*: "ping" :*/
fileprivate let user_youVersionData:String = "distributionng"

/*: "callHeartbeat" :*/
fileprivate let data_sharedPriorityMsg:String = "callHeaedge rime just model marker"
fileprivate let k_oddValue:String = "rannouncementbeaannouncement"

/*: "recievedDate" :*/
fileprivate let kTractorFormat:String = "boxcie"

/*: "liveHeartBeat" :*/
fileprivate let main_selectedMsg:String = "liveHeconstant stroke"
fileprivate let main_blankId:[Character] = ["a","r"]
fileprivate let const_voiceUrl:[Character] = ["t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let constCollaborationUrl:String = "🔗"
fileprivate let data_buttString:[Character] = [" ","关","闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let showArcStr:[Character] = ["T","h","e"," ","u","s","e","r"," ","l","o","g","s"," ","o"]
fileprivate let userNumbKey:[Character] = ["u","t"]

/*: "SOCKET  :*/
fileprivate let userSendListenUrl:String = "identify"
fileprivate let appTowardAbsString:String = "OCKET council action"

/*: 重连. 重试次数： :*/
fileprivate let k_heavyMessage:[Character] = ["重","连","."," ","重","试","\u{6b21}","数","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let mainDesertMsg:[Character] = ["S","O","C","K","E","T"," ","连","接","正","常"," ","不","需","要","\u{91cd}","新","连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let main_chopKeepMsg:[Character] = ["S","O","C","K","E"]
fileprivate let user_enforcePath:[Character] = ["T"," ","\u{6b63}","在","连","\u{63a5}"," ","不","需","要","重","新","连","接","."]

/*: "&resume=1" :*/
fileprivate let kAfterMessage:String = "retain name begin&resu"

/*: "&lang= :*/
fileprivate let const_awlId:[Character] = ["&","l","a","n","g"]
fileprivate let showAdolescentPath:String = "="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let show_counselAssociateValue:[Character] = ["S","O","C","K","E","T"," ","开","始","连","接",","]
fileprivate let kLabSignalPath:String = " 地hung："

/*: "onNetDisabled" :*/
fileprivate let appPartnerFormat:String = "arrest opportunity news quit returnonNetDi"
fileprivate let data_transactionId:String = "mayabled"

/*: "errno" :*/
fileprivate let dataThresholdTitle:String = "egraingrainno"

/*: "data" :*/
fileprivate let mainEnoughBlocName:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let mainBroadcastKey:String = "SOCownerE"
fileprivate let const_ownPath:String = "board bring draft sparkcmd: "
fileprivate let k_measureString:String = "basic inevitablyping."

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let notiAlwaysName:[UInt8] = [0xb9,0xa5,0xa9,0xa1,0xaf,0xbe,0xca,0x9e,0x98,0x9e,0x89,0xe,0x55,0x77,0xc,0x5e,0x51,0x89,0x87,0x8e,0xd0,0xca,0x89,0x8b,0x86,0x86,0xa2,0x8f,0x8b,0x98,0x9e,0x88,0x8f,0x8b,0x9e,0xc4]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let k_logValue:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "onP" :*/
fileprivate let app_supportHemeMessage:String = "reader of form actual mmonP"

/*: "msg" :*/
fileprivate let constDeadlineUrl:String = "mcropg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let main_lowTitle:[Character] = ["🔗"," ","处","理","\u{6d88}","息"]
fileprivate let k_introMsg:String = "："
fileprivate let mainPlayKey:String = "critical exceptcmdName:"

/*: , errNo: :*/
fileprivate let data_drawingTitle:[Character] = [","," ","e","r","r","N","o"]
fileprivate let userConsumerString:String = "staff"

/*: , msg: :*/
fileprivate let constLeaveId:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let kHoldFormat:[Character] = [","," ","d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let constApplicationString:String = "painterCK"
fileprivate let k_domainStr:String = "收"
fileprivate let mainAdditionFormat:[Character] = ["\u{5f02}","常","消","\u{606f}","：","c","m","d","N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let kAgentPath:String = "heme section operationonRece"

/*: "onVideoMatch" :*/
fileprivate let data_onceStr:String = "onVideoprior mm bold explore"
fileprivate let appCoverPassingId:[Character] = ["M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let constPauseMsg:[Character] = ["o","n","R","e","q","u","e","s","t"]
fileprivate let main_mittSubmitName:String = "center persistCall"

/*: "onCancelCall" :*/
fileprivate let mainToFrontPresentMessage:[Character] = ["o","n","C","a","n","c","e","l","C","a"]
fileprivate let user_implementationString:String = "match"

/*: "onRefuseCall" :*/
fileprivate let user_integrationStr:[Character] = ["o","n"]
fileprivate let showTodayItsValue:String = "Refusprice powerful and prepare enter"

/*: "onAcceptCall" :*/
fileprivate let kAmongPath:[Character] = ["o","n","A","c","c"]
fileprivate let const_birthdayUrl:String = "eptCalldirection mark infrastructure require task"

/*: "onStopCall" :*/
fileprivate let k_requireBubbleValue:String = "previousn"
fileprivate let dataCharacterNamePath:[Character] = ["S","t","o","p","C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let mainProjectValue:String = "substantial tell could gallfreeCal"
fileprivate let constRequireMsg:String = "significantly"
fileprivate let data_stripMessage:String = "Timesequally union"

/*: "mfCoin" :*/
fileprivate let appZzAddressName:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let notiConstantStr:String = "onMiextension mission app"
fileprivate let appExploreStr:String = "awake reward flexible rawssCoin"

/*: "acceptCall" :*/
fileprivate let showSubTitle:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "stopCall" :*/
fileprivate let user_followingStr:String = "ease eave find construct nonestopCa"
fileprivate let constBarRequestStr:String = "farfar"

/*: "requestCall" :*/
fileprivate let show_refMessage:[Character] = ["r","e","q","u","e","s","t","C"]
fileprivate let main_equivalentId:[Character] = ["a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let dataConsumerId:[UInt8] = [0x18,0xa,0x2d,0x35,0x2a,0x37,0x26,0x63,0x3a,0x2c,0x36,0x63,0x37,0x2c,0x63,0x22,0x63,0x35,0x2a,0x27,0x26,0x2c,0x63,0x20,0x22,0x2f,0x2f,0x6d,0x6d,0x6d,0x1e]

private func taskTractor(region num: UInt8) -> UInt8 {
    return num ^ 67
}

/*: "type" :*/
fileprivate let const_gameFrequencyTrackFormat:String = "TYPE"

/*: "[Invite you to a voice call...]" :*/
fileprivate let data_mendConvertBehindTitle:[UInt8] = [0xdb,0xc9,0xee,0xf6,0xe9,0xf4,0xe5,0xa0,0xf9,0xef,0xf5,0xa0,0xf4,0xef,0xa0,0xe1,0xa0,0xf6,0xef,0xe9,0xe3,0xe5,0xa0,0xe3,0xe1,0xec,0xec,0xae,0xae,0xae,0xdd]

/*: "uid" :*/
fileprivate let constSlideName:String = "UID"

/*: "nickname" :*/
fileprivate let constCollaborationValue:String = "NICKNAME"

/*: "headPic" :*/
fileprivate let mainShareString:String = "headPimap mitt active eastern"
fileprivate let app_careMessage:String = "sufficient"

/*: "onCallResume" :*/
fileprivate let main_canvasCommitTitle:[Character] = ["o","n","C","a","l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let constLiteralTitle:String = "caversioncel"
fileprivate let const_stageId:String = "Callthrough monitor"

/*: "refuseCall" :*/
fileprivate let k_businessStr:String = "associatedefus"
fileprivate let mainMissingKey:[Character] = ["e","C","a","l","l"]

/*: "onSendProgramGift" :*/
fileprivate let appRoundData:String = "onSendhero til"
fileprivate let mainRemarkString:[Character] = ["G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let mainSceneRespectiveName:[Character] = ["o","n","G"]
fileprivate let mainReplyMsg:String = "ameFspeer regulate insert focus visit"

/*: "broadStatus" :*/
fileprivate let constNorMergeKey:String = "broadSattention tension"
fileprivate let k_statusPath:String = "hellotus"

/*: "onCallChatMsg" :*/
fileprivate let app_forceFormat:String = "spring tack audience then sophisticatedonCal"

/*: "onUserBlock" :*/
fileprivate let user_readingAmStr:[Character] = ["o","n","U","s","e","r"]
fileprivate let noti_associatedTitle:String = "Blockpeer married floor"

/*: "isRecharge" :*/
fileprivate let kCleanBrownTopicFormat:[Character] = ["i","s","R","e","c","h"]
fileprivate let notiReplacementKey:String = "argpet"

/*: "source" :*/
fileprivate let notiTotalStr:[Character] = ["s","o","u","r","c","e"]

/*: "invitePop" :*/
fileprivate let notiWakeId:String = "invicapacity"
fileprivate let user_middleKey:String = "ePoppath integration"

/*: "videoPop" :*/
fileprivate let showSiblingUrl:String = "videoPopspecie repeat"

/*: "coin" :*/
fileprivate let main_calculationWakeString:[Character] = ["c","o","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  QuantityensityAction.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let showTopicFormat = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let appProviderString = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let userExtendTransformString = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let noti_layerTitle = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let dataSkinFormat = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let noti_gateTitle = (user_toleStr.uppercased() + String(constCareName) + String(appWeeklyCenterUrl.suffix(5)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let dataSkirtFailTitle = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let userHideInsteadString = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let const_keepValue = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let notiFormSortScaleName = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let show_yieldSpineFormat = NSNotification.Name(rawValue: String(bytes: k_visualId.map{$0^55}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let const_buildStr = NSNotification.Name(rawValue: String(bytes: appItemMessageUrl.map{possiblySatisfy(instead: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol ReduceException: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func pinAudience(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol GroupPhone: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func securePrompt(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func ease(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol AngleEdge: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func proWith(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func multiRe(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func refreshDown(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func untilBy(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol FloorConsume: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func noneInstance(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func beforeTranslate(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class QuantityensityAction: NSObject {
    //: enum SocketRetryType: String {
    enum RoundField: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var versusScopeVC: AnyObject?
    var versusScopeVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: SustainableDepth.shared.direction == .rightToLeft ? 0 : notiExploreKey,
        //: y: StatusBarHeight+190
        y: show_uniformKey + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: SustainableDepth.shared.direction == .rightToLeft ? 0 : notiExploreKey,
        //: y: StatusBarHeight+190
        y: show_uniformKey + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: ReduceException? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: GroupPhone? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: AngleEdge? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: FloorConsume? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var versusScope = false
    @objc dynamic var versusScope = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = QuantityensityAction()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(balance(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(underCharm),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension QuantityensityAction: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        TrainGreat.bitRemove(msg: (data_portraitStr.replacingOccurrences(of: "tack", with: "O") + "ET 连" + constWindowKey))
        //: sendPing()
        belowFragment()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: show_firstUndertakeLikeFormat,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: noti_gateTitle)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        substantialTransfer()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        TrainGreat.bitRemove(msg: (app_honestlyName.replacingOccurrences(of: "soak", with: "O") + "T 连接失" + String(appSickSeaData.prefix(5)) + "ror:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = VideoMagnitudeerval.share.reachability?.connection, status != .unavailable else {
        guard let status = VideoMagnitudeerval.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == noti_layerTitle {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = gateMend(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            TrainGreat.bitRemove(msg: (show_detailFeeMsg.replacingOccurrences(of: "annual", with: "E") + kResidencyValue) + "\(noti_layerTitle)" + (appRatingString + String(constFollowingMsg) + k_harassmentProjectionData) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.lock(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.bothLength(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        TrainGreat.bitRemove(msg: (appArrestStr.replacingOccurrences(of: "mid", with: "S") + "T \u{5df2}关\u{95ed}, " + String(main_pointRegulateKey.prefix(5))) + "\(code)" + (String(main_designName)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.redTag(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == notiFormSortScaleName {
            //: return
            return
        }
        //: if let status = VideoMagnitudeerval.share.reachability?.connection, status != .unavailable {
        if let status = VideoMagnitudeerval.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.lock(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func gateMend(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = VideoMagnitudeerval.share.appConfigMode.ws
        self.linkAddrArr = VideoMagnitudeerval.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = discountFor()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: noti_gateTitle)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = VideoMagnitudeerval.share.loginUid
        let uid = VideoMagnitudeerval.share.loginUid
        //: let sid = VideoMagnitudeerval.share.loginSessionId
        let sid = VideoMagnitudeerval.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(k_wakeStr)) + "\(sid)" + (String(constExceptRowKey.prefix(5))) + "\(uid)" + (String(dataOccurSunRefuseKey) + String(userBoldValue)) + "\(main_persistDirectlyStr)" + (String(noti_maxData.suffix(4)) + "kageId=") + "\(user_oneData)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func discountFor() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return userAcceptUrl ? [(String(main_localModMsg) + String(constOgreUrl)) + "\(user_basicString)" + (String(mainTransformBulletName.suffix(2)) + String(main_ribbonProvideId.suffix(7)))] : [(String(appForceLibraryPath) + constDelicateMakeMessage.replacingOccurrences(of: "min", with: "7"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension QuantityensityAction {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func balance(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            TrainGreat.bitRemove(msg: String(bytes: app_ogreUrl.map{$0^196}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.redTag(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.lock(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func underCharm() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.lock(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.aboveGapProduct()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension QuantityensityAction {
    /// socket保活心跳包
    //: private func sendPing() {
    private func belowFragment() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: showTopicFormat, block: { timer in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.frameOfFeed(info: [(main_tickData.replacingOccurrences(of: "gold", with: "md")): (user_youVersionData.replacingOccurrences(of: "distribution", with: "pi"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func asFailureScale() {
        //: endTalkingPing()
        beyondEach()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: appProviderString, block: { timer in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.versusScope || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.versusScopeVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.versusScopeVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.versusScope || (self.congressionalFor()?.isKind(of: ShadeRounding.self) == false && self.congressionalFor()?.isKind(of: EmptyContainerScientific.self) == false) && (self.versusScopeVC?.isKind(of: ShadeRounding.self) == false && self.versusScopeVC?.isKind(of: EmptyContainerScientific.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.frameOfFeed(info: [(main_tickData.replacingOccurrences(of: "gold", with: "md")): (String(data_sharedPriorityMsg.prefix(7)) + k_oddValue.replacingOccurrences(of: "announcement", with: "t"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func beyondEach() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func aboveGapProduct() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(kTractorFormat.replacingOccurrences(of: "box", with: "re") + "vedDate")] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < app_closedString {
                //: VideoMagnitudeerval.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                VideoMagnitudeerval.share.relation(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension QuantityensityAction {
    /// 开启直播心跳包
    //: func startLivePing() {
    func sampleIn() {
        //: endLivePing()
        theComponent()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: userExtendTransformString, block: { timer in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.frameOfFeed(info: [(main_tickData.replacingOccurrences(of: "gold", with: "md")): (String(main_selectedMsg.prefix(6)) + String(main_blankId) + String(const_voiceUrl))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func theComponent() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension QuantityensityAction {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func substantialTransfer() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = InfoView()
        //: let keyWindow = FormatDistribution.getWindow()
        let keyWindow = FormatDistribution.group()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.versusScope
        self.containerView?.isHidden = !self.versusScope
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func duringConversion() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension QuantityensityAction {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func redTag(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (constCollaborationUrl.capitalized + String(data_buttString)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: notiFormSortScaleName, reason: (String(showArcStr) + String(userNumbKey)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        beyondEach()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            duringConversion()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func greenProfile() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = QuantityensityAction.shared.gateMend(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.lock(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func lock(timeInterval: TimeInterval, type: RoundField) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            TrainGreat.bitRemove(msg: (userSendListenUrl.replacingOccurrences(of: "identify", with: "S") + String(appTowardAbsString.prefix(6))) + "\(type.rawValue)" + (String(k_heavyMessage)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.doSession(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func doSession(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                TrainGreat.bitRemove(msg: (String(mainDesertMsg)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                TrainGreat.bitRemove(msg: (String(main_chopKeepMsg) + String(user_enforcePath)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.versusScope {
        if self.versusScope {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(kAfterMessage.suffix(5)) + "me=1"))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(const_awlId) + showAdolescentPath.capitalized) + "\(ImmediateSumensityObserver.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        redTag(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = dataSkinFormat
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        TrainGreat.bitRemove(msg: (String(show_counselAssociateValue) + kLabSignalPath.replacingOccurrences(of: "hung", with: "址")) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func frameOfFeed(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if VideoMagnitudeerval.share.reachability?.connection == nil || VideoMagnitudeerval.share.reachability?.connection == .unavailable {
        if VideoMagnitudeerval.share.reachability?.connection == nil || VideoMagnitudeerval.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(main_tickData.replacingOccurrences(of: "gold", with: "md")): (String(appPartnerFormat.suffix(7)) + data_transactionId.replacingOccurrences(of: "may", with: "s")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (dataThresholdTitle.replacingOccurrences(of: "grain", with: "r")): "\(const_keepValue)",
                                       //: "data": [:]]
                                       (String(mainEnoughBlocName)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            bothLength(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(main_tickData.replacingOccurrences(of: "gold", with: "md"))] as? String == (user_youVersionData.replacingOccurrences(of: "distribution", with: "pi")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                TrainGreat.bitRemove(msg: (mainBroadcastKey.replacingOccurrences(of: "owner", with: "K") + "T 连接保活" + String(const_ownPath.suffix(5)) + String(k_measureString.suffix(5))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(main_tickData.replacingOccurrences(of: "gold", with: "md"))] as? String == (String(data_sharedPriorityMsg.prefix(7)) + k_oddValue.replacingOccurrences(of: "announcement", with: "t")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                TrainGreat.bitRemove(msg: String(bytes: notiAlwaysName.map{$0^234}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(main_tickData.replacingOccurrences(of: "gold", with: "md"))] as? String == (String(main_selectedMsg.prefix(6)) + String(main_blankId) + String(const_voiceUrl)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                TrainGreat.bitRemove(msg: String(bytes: k_logValue.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(main_tickData.replacingOccurrences(of: "gold", with: "md"))] as? String == (user_youVersionData.replacingOccurrences(of: "distribution", with: "pi")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(main_tickData.replacingOccurrences(of: "gold", with: "md")): (String(app_supportHemeMessage.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (dataThresholdTitle.replacingOccurrences(of: "grain", with: "r")): "\(dataSkirtFailTitle)",
                                           //: "data": [:]]
                                           (String(mainEnoughBlocName)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                bothLength(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.lock(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func bothLength(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(main_tickData.replacingOccurrences(of: "gold", with: "md"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(dataThresholdTitle.replacingOccurrences(of: "grain", with: "r"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(constDeadlineUrl.replacingOccurrences(of: "crop", with: "s"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(mainEnoughBlocName))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(main_lowTitle) + k_introMsg.capitalized + String(mainPlayKey.suffix(8))) + "\(cmdName)" + (String(data_drawingTitle) + userConsumerString.replacingOccurrences(of: "staff", with: ":")) + "\(errorNo)" + (String(constLeaveId)) + "\(msg)" + (String(kHoldFormat)) + "\(data)")

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            TrainGreat.bitRemove(msg: (constApplicationString.replacingOccurrences(of: "painter", with: "SO") + "ET \u{63a5}" + k_domainStr.capitalized + String(mainAdditionFormat)) + "\(cmdName)" + (String(data_drawingTitle) + userConsumerString.replacingOccurrences(of: "staff", with: ":")) + "\(errorNo)" + (String(constLeaveId)) + "\(msg)" + (String(kHoldFormat)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: VideoMagnitudeerval.share.loginUserMode.freeCallTimes = 0
                VideoMagnitudeerval.share.loginUserMode.freeCallTimes = 0
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.pinAudience(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(kAgentPath.suffix(6)) + "ivedGift") { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: main_scientificProcessTitle, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(data_onceStr.prefix(7)) + String(appCoverPassingId)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.securePrompt(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(constPauseMsg) + String(main_mittSubmitName.suffix(4))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.ease(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(mainToFrontPresentMessage) + user_implementationString.replacingOccurrences(of: "match", with: "ll")) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(user_integrationStr) + String(showTodayItsValue.prefix(5)) + "eCall") { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(kAmongPath) + String(const_birthdayUrl.prefix(7))) { // 接收方发送acceptCall回执
            //: self.versusScope = true
            self.versusScope = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            asFailureScale()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.beforeTranslate(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (k_requireBubbleValue.replacingOccurrences(of: "previous", with: "o") + String(dataCharacterNamePath)) { // 接收方发送stopCall回执
            //: self.versusScope = false
            self.versusScope = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            beyondEach()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(mainProjectValue.suffix(7)) + constRequireMsg.replacingOccurrences(of: "significantly", with: "l") + String(data_stripMessage.prefix(5)))] as? Int {
                //: VideoMagnitudeerval.share.loginUserMode.freeCallTimes = freeCallTimes
                VideoMagnitudeerval.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: VideoMagnitudeerval.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            VideoMagnitudeerval.share.loginUserMode.mf_coin = json[(String(mainEnoughBlocName))][(String(appZzAddressName))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            againstOutput(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kFunctionalCornerPath, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(notiConstantStr.prefix(4)) + String(appExploreStr.suffix(6))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.untilBy(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(showSubTitle)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.versusScope = true
            self.versusScope = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            asFailureScale()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.proWith(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(user_followingStr.suffix(6)) + constBarRequestStr.replacingOccurrences(of: "far", with: "l")) { // 接收方/拨打方 挂断了通话
            //: self.versusScope = false
            self.versusScope = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            beyondEach()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.multiRe(data: data)
            // 更新余额
            //: VideoMagnitudeerval.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            VideoMagnitudeerval.share.loginUserMode.mf_coin = json[(String(mainEnoughBlocName))][(String(appZzAddressName))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(mainProjectValue.suffix(7)) + constRequireMsg.replacingOccurrences(of: "significantly", with: "l") + String(data_stripMessage.prefix(5)))] as? Int {
                //: VideoMagnitudeerval.share.loginUserMode.freeCallTimes = freeCallTimes
                VideoMagnitudeerval.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            againstOutput(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kFunctionalCornerPath, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false

            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(show_refMessage) + String(main_equivalentId)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: show_yieldSpineFormat, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: VideoMagnitudeerval.share.recieve1v1TalkRequest(info: data)
                VideoMagnitudeerval.share.relation(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(kTractorFormat.replacingOccurrences(of: "box", with: "re") + "vedDate")] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: dataConsumerId.map{taskTractor(region: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(mainEnoughBlocName))][(const_gameFrequencyTrackFormat.lowercased())].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: data_mendConvertBehindTitle.map{$0^128}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                LightSend.theOrange(uid: String(json[(String(mainEnoughBlocName))][(constSlideName.lowercased())].intValue),
                                            //: title: data["nickname"] as? String,
                                            title: data[(constCollaborationValue.lowercased())] as? String,
                                            //: body: body,
                                            body: body,
                                            //: imageUrl: data["headPic"] as? String)
                                            imageUrl: data[(String(mainShareString.prefix(6)) + app_careMessage.replacingOccurrences(of: "sufficient", with: "c"))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                LightSend.shared.ironic()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(main_canvasCommitTitle)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.versusScope = true
            self.versusScope = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            asFailureScale()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(main_canvasCommitTitle))] = "1"
            //: VideoMagnitudeerval.share.recieve1v1TalkResumeRequest(info: resumeData)
            VideoMagnitudeerval.share.noneJudge(info: resumeData)

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (constLiteralTitle.replacingOccurrences(of: "version", with: "n") + String(const_stageId.prefix(4))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.versusScope = false
            self.versusScope = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            beyondEach()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.refreshDown(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                LightSend.focus(identifier: String(json[(String(mainEnoughBlocName))][(constSlideName.lowercased())].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                LightSend.shared.sterol()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (k_businessStr.replacingOccurrences(of: "associated", with: "r") + String(mainMissingKey)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.versusScope = false
            self.versusScope = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            beyondEach()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.noneInstance(data: data)

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(appRoundData.prefix(6)) + "Program" + String(mainRemarkString)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.tillInvestigator(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(mainSceneRespectiveName) + String(mainReplyMsg.prefix(5))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(constNorMergeKey.prefix(6)) + k_statusPath.replacingOccurrences(of: "hello", with: "ta"))] as? Int ?? -1
            //: if VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue {
            if VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: const_buildStr, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: main_scientificProcessTitle, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: const_buildStr, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(app_forceFormat.suffix(5)) + "lChatMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            FunctionalCeiling.feeMinimize().language(msg: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(user_readingAmStr) + String(noti_associatedTitle.prefix(5))) { // 用户被封号
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: userWaitScopeTitle, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension QuantityensityAction {
    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func againstOutput(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        StopFramework.shared.scientific()
        //: guard VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue else {
        guard VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(kCleanBrownTopicFormat) + notiReplacementKey.replacingOccurrences(of: "pet", with: "e"))] as? Bool == false {
            //: TalkingAppPushManager.share.func_pushToDiscountRechargeView()
            ScopeEmbrace.share.applicationTier()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(String(notiTotalStr))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (notiWakeId.replacingOccurrences(of: "capacity", with: "t") + String(user_middleKey.prefix(4))) || source == (String(showSiblingUrl.prefix(8))) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(String(main_calculationWakeString))] as? Int) ?? 0 < 60 {
            //: TalkingAppPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            ScopeEmbrace.share.constraintOn(webViewType: .RechargeHalfPage)
        }
    }
}
