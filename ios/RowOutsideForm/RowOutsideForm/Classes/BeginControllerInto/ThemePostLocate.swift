
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_columnMsg:[UInt8] = [0x5d,0x5a,0x5d,0x40,0x1c,0x57,0x5b,0x50,0x51,0x46,0xe,0x1d,0x14,0x5c,0x55,0x47,0x14,0x5a,0x5b,0x40,0x14,0x56,0x51,0x51,0x5a,0x14,0x5d,0x59,0x44,0x58,0x51,0x59,0x51,0x5a,0x40,0x51,0x50]

private func snapTil(advertising num: UInt8) -> UInt8 {
    return num ^ 52
}

/*: "roomId" :*/
fileprivate let main_failureData:[Character] = ["r","o","o","m","I","d"]

/*: "type" :*/
fileprivate let notiCountData:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "extra" :*/
fileprivate let k_packageDailyPath:String = "ebrownra"

/*: "jumpKey" :*/
fileprivate let main_absoluteLoopId:[UInt8] = [0xaf,0xb0,0xa8,0xb5,0x8e,0xa0,0xbc]

private func mediumTitle(writing num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "Send Gift" :*/
fileprivate let constGrainName:String = "topic"
fileprivate let main_collectionHeartValue:[Character] = ["e","n","d"," ","G","i","f","t"]

/*: "View info" :*/
fileprivate let app_overResultMessage:[Character] = ["V","i","e","w"," ","i"]
fileprivate let noti_strengthStr:[Character] = ["n","f","o"]

/*: "Report" :*/
fileprivate let mainAutomaticallyNumberMessage:String = "cookie transform additional resistanceReport"

/*: "uid" :*/
fileprivate let data_mountMsg:[UInt8] = [0x89,0x95,0x98]

private func fieldFollowing(spot num: UInt8) -> UInt8 {
    return num ^ 252
}

/*: "name" :*/
fileprivate let app_momentId:[UInt8] = [0x65,0x6d,0x61,0x6e]

/*: "Can't send blank message" :*/
fileprivate let app_paraRimePeanutStr:[Character] = ["C","a"]
fileprivate let app_thresholdId:[Character] = ["n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let data_bringFlushString:String = "useumber"

/*: "content" :*/
fileprivate let showEfficiencyMsg:[UInt8] = [0xac,0xa0,0xa1,0xbb,0xaa,0xa1,0xbb]

private func scientificToo(kit num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "msgType" :*/
fileprivate let main_spellPath:String = "msgTypeburn kind reflect"

/*: "txt" :*/
fileprivate let show_carrierToleranceUrl:String = "TXT"

/*: "isGroup" :*/
fileprivate let main_flexMsg:[Character] = ["i","s","G","r","o","u","p"]

/*: "jumps" :*/
fileprivate let appIntervalPath:String = "juguideps"

/*: "message" :*/
fileprivate let kWakeBusinessFormat:[UInt8] = [0x5d,0x55,0x43,0x43,0x51,0x57,0x55]

/*: "TxtMsg" :*/
fileprivate let const_mySakeData:[Character] = ["T","x","t","M","s","g"]

/*: "toUid" :*/
fileprivate let const_migrationVideoGrainPath:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let main_addGoingCharShowData:String = "msgInfhis brown adolescent red"
fileprivate let appFlexibleBeyondString:String = "sensor"

/*: "params" :*/
fileprivate let noti_funeralId:[UInt8] = [0x79,0x68,0x7b,0x68,0x64,0x7a]

/*: "contentType" :*/
fileprivate let userRocketMessage:[UInt8] = [0x45,0x49,0x48,0x52,0x43,0x48,0x52,0x72,0x5f,0x56,0x43]

private func extentManager(deploy num: UInt8) -> UInt8 {
    return num ^ 38
}

/*: "ImgMsg" :*/
fileprivate let dataBridgeWorkStr:String = "ImgMsgsocial properly sure"

/*: "img" :*/
fileprivate let app_strangerString:String = "productiong"

/*: "imageUri" :*/
fileprivate let notiSymbolStr:[UInt8] = [0x75,0x71,0x7d,0x7b,0x79,0x49,0x6e,0x75]

private func pleaseMedal(architecture num: UInt8) -> UInt8 {
    return num ^ 28
}

/*: "pic" :*/
fileprivate let userSmartAngleViolationValue:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let data_ridScreenStr:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let notiHalfRunningFormat:String = "maxta"

/*: "giftId" :*/
fileprivate let notiFirstContainValue:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let app_opportunityScopeKey:String = "giftNumpush transition"

/*: "pkgItemsetId" :*/
fileprivate let data_commentId:String = "short tick support ever hispkgIte"
fileprivate let k_childTitle:String = "focus drawmsetId"

/*: "msg" :*/
fileprivate let app_blueUrl:String = "mssymbol"

/*: "tips" :*/
fileprivate let appObserverKey:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let notiYeaId:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let userOptionLingFormat:String = "enable"
fileprivate let main_serverMessage:[Character] = ["i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let const_addressPolicySupportMessage:[UInt8] = [0x28,0x25,0x21,0x24,0x10,0x29,0x23]

private func detectQueryArray(edit num: UInt8) -> UInt8 {
    return num ^ 64
}

/*: "All Numbers" :*/
fileprivate let kCommandRemPath:String = "grain obtain interactionAll Num"
fileprivate let show_adjustmentData:String = "BERS"

/*: "sendNum" :*/
fileprivate let userBecomeUrl:String = "creation slide flowsendN"
fileprivate let main_ovalEnableicialUrl:[Character] = ["u","m"]

/*: "toUser" :*/
fileprivate let userJobAdditionTitle:String = "estate heart reveal porttoUser"

/*: "msgIncome" :*/
fileprivate let show_cycleString:String = "empty train truemsgIncome"

/*: "isConnection" :*/
fileprivate let user_connectString:String = "enforce in visitor dimension taskisConn"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ThemePostLocate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class ThemePostLocate: MasterClip {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = CollectionModel() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = EdgeBelow() // 会话cellData
    //: private var conversationType: UserChatType = .group
    private var conversationType: UserChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = CollectionModel.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_columnMsg.map{snapTil(advertising: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if VideoMagnitudeerval.share.appConfigMode.disableShootScreen {
            if VideoMagnitudeerval.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ProviderSineView(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        betweenAnalyzeReceiver()
        //: createUIConstraints()
        access()
        //: bindInteraction()
        wait()
        //: addTapGestureRecognizer()
        panelSufficientReasonResistance()
        //: func__chatRoom_insertTipMsg()
        levelMutual()
        //: chatSettings()
        image()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            sizeSumerest()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.agree(join: false)
        //: self.destroyTipsTimer()
        self.amongCloseFunctional()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: NameViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = NameViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(HalfInfrastructure.self, forCellReuseIdentifier: HalfInfrastructure.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(WhiteExamine.self, forCellReuseIdentifier: WhiteExamine.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(LocalFlexibleTechnique.self, forCellReuseIdentifier: LocalFlexibleTechnique.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(AgileView.self, forCellReuseIdentifier: AgileView.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: OffUsed.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(AgainstThread.self, forCellReuseIdentifier: OffUsed.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: EqualLogView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = EqualLogView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: DivideView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = DivideView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: PriorMultiple = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = PriorMultiple()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: MaxView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = MaxView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: FactoryBlueSystemView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = FactoryBlueSystemView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension ThemePostLocate {
    /// 群聊设置
    //: private func chatSettings() {
    private func image() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        SymbolBold.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        agree(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func agree(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(main_failureData)): self.conversationData.groupID, String(bytes: notiCountData.reversed(), encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        AnalyzeMapCommon.theInput(params: params) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - RoundingInstruction

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension ThemePostLocate: RoundingInstruction {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TaskAction! {
    func over(_ controller: NameViewController!, commonPersist data: V2TIMMessage!) -> TaskAction! {
        //: let dic = MaintainSection.parseTXMessageData(data: data.customElem.data)
        let dic = MaintainSection.resumeOfCover(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = InformationConcrete.foreBound(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TaskAction!) -> OffUsed! {
    func margin(_ controller: NameViewController!, service data: TaskAction!) -> OffUsed! {
        //: if data.isKind(of: SkinUp.self) {
        if data.isKind(of: SkinUp.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = HalfInfrastructure(style: .default, reuseIdentifier: HalfInfrastructure.className())
            //: let cellData = data as! SkinUp
            let cellData = data as! SkinUp
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.my(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.threshold(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: MenuAdjust.self) {
        if data.isKind(of: MenuAdjust.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = WhiteExamine(style: .default, reuseIdentifier: WhiteExamine.className())
            //: let cellData = data as! MenuAdjust
            let cellData = data as! MenuAdjust
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: SignPage.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = LocalFlexibleTechnique(style: .default, reuseIdentifier: LocalFlexibleTechnique.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! SignPage
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SecondDepthLike.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = AgileView(style: .default, reuseIdentifier: AgileView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SecondDepthLike
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: OffUsed!) {
    func might(_ controller: NameViewController!, cluster cell: OffUsed!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(VideoMagnitudeerval.share.loginUid) {
            if userModel.uid == Int(VideoMagnitudeerval.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                my(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                untilChannel(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: OffUsed!) {
    func phase(_ controller: NameViewController!, distribution cell: OffUsed!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: WhiteExamine.self) { // 图片
            //: let imageCellData = cell.messageData as! MenuAdjust
            let imageCellData = cell.messageData as! MenuAdjust
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(dataStateMessage)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = NumberensityMark()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = ExerciseListen()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [NumberensityMark] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = MagnitudeervalManagerFlex(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.congressionalFor()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: HalfInfrastructure.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! HalfInfrastructure
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = MaintainSection.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = MaintainSection.resumeOfCover(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_packageDailyPath.replacingOccurrences(of: "brown", with: "xt"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: main_absoluteLoopId.map{mediumTitle(writing: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<TotaleractionStop>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ScopeEmbrace.share.transactionDocument(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: AgileView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! AgileView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = MaintainSection.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = MaintainSection.resumeOfCover(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_packageDailyPath.replacingOccurrences(of: "brown", with: "xt"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: main_absoluteLoopId.map{mediumTitle(writing: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<TotaleractionStop>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ScopeEmbrace.share.transactionDocument(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: OffUsed!) {
    func nextTemporary(_ controller: NameViewController!, everyCommon cell: OffUsed!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        custom(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension ThemePostLocate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func amongCloseFunctional() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func untilChannel(user: EfficiencyModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = WithSquare(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.street(cellTitleList: [(constGrainName.replacingOccurrences(of: "topic", with: "S") + String(main_collectionHeartValue)).localized, "@", (String(app_overResultMessage) + String(noti_strengthStr)).localized, (String(mainAutomaticallyNumberMessage.suffix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.calendar(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.project(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.my(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.visit(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func calendar(user: EfficiencyModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.control()
        //: let model = TalkingChatRoomMemberModel()
        let model = IndicatorMakeModel()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.buttEnableMeasure(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func project(user: EfficiencyModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: data_mountMsg.map{fieldFollowing(spot: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: app_momentId.reversed(), encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.historyResult(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func my(uid: Int) {
        //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        ScopeEmbrace.share.receiverDoingStop(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func visit(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = CycleView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.thirdFile(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension ThemePostLocate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func down(msgStr: String, quoteModel: SourceShadowExceptionModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            conclusion(showMsg: (String(app_paraRimePeanutStr) + String(app_thresholdId)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: main_absoluteLoopId.map{mediumTitle(writing: $0)}, encoding: .utf8)!: (data_bringFlushString.replacingOccurrences(of: "umber", with: "r")), String(bytes: data_mountMsg.map{fieldFollowing(spot: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: showEfficiencyMsg.map{scientificToo(kit: $0)}, encoding: .utf8)!: temStr, (String(main_spellPath.prefix(7))): (show_carrierToleranceUrl.lowercased()), (String(main_flexMsg)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(appIntervalPath.replacingOccurrences(of: "guide", with: "m"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: kWakeBusinessFormat.map{$0^48}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: notiCountData.reversed(), encoding: .utf8)!: (String(const_mySakeData)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(main_failureData)): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(const_migrationVideoGrainPath))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_addGoingCharShowData.prefix(6)) + appFlexibleBeyondString.replacingOccurrences(of: "sensor", with: "o")): msgInfo, String(bytes: noti_funeralId.map{$0^9}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            custom(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func characteristicDistance(img: UIImage) {
        //: let imagePath = MenuAdjust.getChatImagePath(image: img)
        let imagePath = MenuAdjust.fire(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: userRocketMessage.map{extentManager(deploy: $0)}, encoding: .utf8)!: (String(dataBridgeWorkStr.prefix(6))), (String(main_spellPath.prefix(7))): (app_strangerString.replacingOccurrences(of: "production", with: "im")), String(bytes: notiSymbolStr.map{pleaseMedal(architecture: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(userSmartAngleViolationValue)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: notiCountData.reversed(), encoding: .utf8)!: (String(dataBridgeWorkStr.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(main_failureData)): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_addGoingCharShowData.prefix(6)) + appFlexibleBeyondString.replacingOccurrences(of: "sensor", with: "o")): msgInfo, String(bytes: noti_funeralId.map{$0^9}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            custom(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TaskAction, reSend: Bool) {
    private func custom(msgData: TaskAction, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            conclusion(showMsg: appMoveTrainTitle)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.indicator(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(userSmartAngleViolationValue))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(userSmartAngleViolationValue))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(userSmartAngleViolationValue))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        AnalyzeMapCommon.efficiencyPerform(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.pop(msgData, characterFile: nil, consumptionOff: NSNumber(value: 0), until: .Msg_Status_Fail)

                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.readyQueryer(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(main_addGoingCharShowData.prefix(6)) + appFlexibleBeyondString.replacingOccurrences(of: "sensor", with: "o"))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.pop(msgData, characterFile: msgInfo[(String(data_ridScreenStr))] as? String, consumptionOff: NSNumber(value: 0), until: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((notiHalfRunningFormat.replacingOccurrences(of: "max", with: "to") + "lMfCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(notiHalfRunningFormat.replacingOccurrences(of: "max", with: "to") + "lMfCoin")] as! NSNumber
                //: VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func readyQueryer(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard VideoMagnitudeerval.share.loginUserMode.status != 1 else {
            guard VideoMagnitudeerval.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    conclusion(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            ScopeEmbrace.share.rateBreeze(clickEvent: appTimingWarnAfterTitle, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.symbolWith()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            shadow(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = MissionPriorBind(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
            //: view.show()
            view.tail()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                conclusion(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension ThemePostLocate: MenuGlobal {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func time(giftModel: CharacterModel, giftNum: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func query() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func distributionIn(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = PaintUntil()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.historyResult(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func schedule(actionType: ArchitecturePoolImage) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            past()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func lite(giftModel: CharacterModel, giftNum: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func strengthByExamineion(giftModel: CharacterModel, giftNum: String, model: IndicatorMakeModel) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            conclusion(showMsg: appMoveTrainTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(const_migrationVideoGrainPath))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(notiFirstContainValue))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(app_opportunityScopeKey.prefix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(main_failureData))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(data_commentId.suffix(6)) + String(k_childTitle.suffix(6)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        AnalyzeMapCommon.whoOf(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.readyQueryer(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.awayOf(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.transformTypical(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func empty(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.route(true)
            self.msgView.route(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func common(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.route(true)
            self.msgView.route(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func fit(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.fit(tap)
    }

    //: func func__starRecordBtnDown() {}
    func might() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func equipmentNear(voicePath: String, voiceTime: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ThemePostLocate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func past() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ReduceLevelDoingtoPrior.shadowKid(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = HandleDistinction.roadHonestly(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: TalkingAppPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                ScopeEmbrace.share.beyondState()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.characteristicDistance(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func current() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ReduceLevelDoingtoPrior.dream(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.characteristicDistance(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension ThemePostLocate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func quantityerrupt(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(main_failureData))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension ThemePostLocate {
    //: func func__TipsNotification() {
    func resistanceControl() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(rawOrientation(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: constQuitValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func rawOrientation(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SecondDepthLike = userinfo[(app_blueUrl.replacingOccurrences(of: "symbol", with: "g"))] as! SecondDepthLike

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.route(true)
        self.msgView.route(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func shadow(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: showEfficiencyMsg.map{scientificToo(kit: $0)}, encoding: .utf8)!: tipStr, (String(main_spellPath.prefix(7))): (String(appObserverKey))]
        //: var message: TaskAction?
        var message: TaskAction?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = InformationConcrete.only(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.route(true)
        self.msgView.route(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func levelMutual() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] time in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard VideoMagnitudeerval.share.appUserConfigMode.rTips.count > 0 else { return }
            guard VideoMagnitudeerval.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: VideoMagnitudeerval.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.shadow(tipStr: VideoMagnitudeerval.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension ThemePostLocate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func transformTypical(extral: [String: Any?], model: IndicatorMakeModel, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(main_addGoingCharShowData.prefix(6)) + appFlexibleBeyondString.replacingOccurrences(of: "sensor", with: "o"))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(main_flexMsg))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(main_spellPath.prefix(7)))] = (String(notiYeaId))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(main_addGoingCharShowData.prefix(6)) + appFlexibleBeyondString.replacingOccurrences(of: "sensor", with: "o"))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: data_mountMsg.map{fieldFollowing(spot: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (userOptionLingFormat.replacingOccurrences(of: "enable", with: "n") + String(main_serverMessage)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: const_addressPolicySupportMessage.map{detectQueryArray(edit: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(kCommandRemPath.suffix(7)) + show_adjustmentData.lowercased()).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(notiYeaId))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(notiYeaId))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(userOptionLingFormat.replacingOccurrences(of: "enable", with: "n") + String(main_serverMessage))] = "\(giftdic[(String(userBecomeUrl.suffix(5)) + String(main_ovalEnableicialUrl))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(userJobAdditionTitle.suffix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = InformationConcrete.only(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.indicator(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(show_cycleString.suffix(9)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(show_cycleString.suffix(9)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.pop(cellData, characterFile: msgInfoDic[(String(data_ridScreenStr))] as? String, consumptionOff: msgIncome, until: .Msg_Status_Succ)

        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        odd(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((notiHalfRunningFormat.replacingOccurrences(of: "max", with: "to") + "lMfCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(notiHalfRunningFormat.replacingOccurrences(of: "max", with: "to") + "lMfCoin")] as! NSNumber
            //: VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: VideoMagnitudeerval.share.loginUserMode.mf_coin)
            msgInputView.giftView.textLength(needReload: false, mf_coin: VideoMagnitudeerval.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.roundGraphic()
    }

    //: func initCachEffectData() {
    func sizeSumerest() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.heroComponentTake(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func odd(giftDic: [String: Any], model: IndicatorMakeModel) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.gold(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in FlexibleTiming.share.waitServer() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [LikeCompose]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.generateBy(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.natural(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func successfully(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in FlexibleTiming.share.waitServer() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [LikeCompose]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.generateBy(model: giftModel as! RunPerform)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.requireDelicate(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension ThemePostLocate {
    /// 创建UI
    //: private func createUI() {
    private func betweenAnalyzeReceiver() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func access() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(mainLightMessage)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(mainLightMessage)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - constPublisherValue)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func wait() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = PaintUntil()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! TaskAction).isKind(of: SkinUp.self) {
                    if (cellData as! TaskAction).isKind(of: SkinUp.self) {
                        //: let groupCellData = cellData as! SkinUp
                        let groupCellData = cellData as! SkinUp
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(data_ridScreenStr))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: SpellAround.shared.rx
        SpellAround.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(user_connectString.suffix(6)) + "ection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
