
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainBlackPendingName:[UInt8] = [0xa8,0xaf,0xa8,0xb5,0xe9,0xa2,0xae,0xa5,0xa4,0xb3,0xfb,0xe8,0xe1,0xa9,0xa0,0xb2,0xe1,0xaf,0xae,0xb5,0xe1,0xa3,0xa4,0xa4,0xaf,0xe1,0xa8,0xac,0xb1,0xad,0xa4,0xac,0xa4,0xaf,0xb5,0xa4,0xa5]

private func mustRevenue(portrait num: UInt8) -> UInt8 {
    return num ^ 193
}

/*: "#128CFF" :*/
fileprivate let showJustStr:[Character] = ["#","1","2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let data_fixedHiId:String = "sub club rem foreFeedb"
fileprivate let appLemmaProcedureTitle:[Character] = ["a","c","k"]

/*: "#8370FF" :*/
fileprivate let main_mutualIronicValue:String = "crisis pole shared#8370FF"

/*: "Quick Greeting" :*/
fileprivate let notiPotentMessage:String = "Quicexport over disappear stack run"
fileprivate let kBarUrl:String = "timakerg"

/*: "Beauty Photos" :*/
fileprivate let mainTwoId:String = "Beashow hero ribbon center later"
fileprivate let noti_deployMechanicalMsg:String = "successfully sea dialog finish taguty P"

/*: "url" :*/
fileprivate let user_boaName:String = "ublue"

/*: "unlockGift" :*/
fileprivate let userCapableVolumeMsg:[Character] = ["u","n","l","o","c","k"]
fileprivate let show_connectCompareProblemString:[Character] = ["G","i","f","t"]

/*: "isBurn" :*/
fileprivate let dataEstateBlocTitle:[UInt8] = [0x81,0x8b,0x5a,0x8d,0x8a,0x86]

fileprivate func doingdOutside(work num: UInt8) -> UInt8 {
    let value = Int(num) + 232
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let appMpMessage:[Character] = ["g"]
fileprivate let appEquallyLowerValue:String = "advice significantly owner average leadingiftId"

/*: "rYMsgType" :*/
fileprivate let data_nearPath:[Character] = ["r","Y","M","s","g","T"]
fileprivate let notiCommunicateMsg:String = "ypstatin"

/*: "GJ:CallCustom" :*/
fileprivate let user_refuseGestureSystemValue:[Character] = ["G","J",":","C","a","l","l"]
fileprivate let mainFreshData:String = "moment feed successful maskCustom"

/*: "changeDirection" :*/
fileprivate let constAllowFormat:String = "waithan"
fileprivate let noti_expressionStr:String = "maker"
fileprivate let constMechanismValue:String = "cplainon"

/*: "extra" :*/
fileprivate let show_manualStr:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let notiCityString:[Character] = ["m"]
fileprivate let notiBadBindFormat:[Character] = ["s","g","I","n","f","o"]

/*: "toUid" :*/
fileprivate let const_tensionMsg:String = "toUidflex let illustrate"

/*: "wantGift" :*/
fileprivate let k_saleName:String = "waevery"

/*: "jumpKey" :*/
fileprivate let app_gemNineStr:String = "bit convert decrease veryjumpK"
fileprivate let appEvaluationPath:String = "EY"

/*: "logType" :*/
fileprivate let show_myData:String = "logTypemake counsel"

/*: "content" :*/
fileprivate let show_continuingKey:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let showHiddenData:[UInt8] = [0x65,0x70,0x79,0x54,0x67,0x73,0x6d]

/*: "txt" :*/
fileprivate let dataInstallName:[Character] = ["t","x","t"]

/*: "message" :*/
fileprivate let show_girlStr:[UInt8] = [0x40,0x38,0x46,0x46,0x34,0x3a,0x38]

fileprivate func holderRemind(relate num: UInt8) -> UInt8 {
    let value = Int(num) + 45
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "replayId" :*/
fileprivate let kWillFormat:[UInt8] = [0xbe,0xa9,0xbc,0xa0,0xad,0xb5,0x85,0xa8]

/*: "type" :*/
fileprivate let dataAlwaysKey:[Character] = ["t","y","p","e"]

/*: "TxtMsg" :*/
fileprivate let const_directRemMsg:String = "should lost maxTxtMs"
fileprivate let main_supportKey:[Character] = ["g"]

/*: "params" :*/
fileprivate let k_closeName:[UInt8] = [0x1,0x10,0x3,0x10,0x1c,0x2]

private func consumptionUniform(remain num: UInt8) -> UInt8 {
    return num ^ 113
}

/*: "MultiQuickMsg" :*/
fileprivate let noti_hundredString:String = "version calculate cycleMult"
fileprivate let kBrownId:[Character] = ["s","g"]

/*: "QuickMsg" :*/
fileprivate let kAbsKey:String = "adolescent fraction agile hourQuic"

/*: "txtId" :*/
fileprivate let showSingleManageLaughterGivingData:[Character] = ["t","x","t","I","d"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let data_interestedFormat:[UInt8] = [0xc1,0xda,0xd4,0xce,0xd0,0x8b,0xd3,0xd0,0xd7,0xd7,0xda,0x8b,0xcf,0xda,0xe2,0xd9,0xd7,0xda,0xcc,0xcf,0x8b,0xd1,0xcc,0xd4,0xd7,0xd0,0xcf,0x99,0x8b,0xbb,0xdd,0xda,0xce,0xd0,0xcf,0xe0,0xdd,0xd0]

fileprivate func scripConsumer(largely num: UInt8) -> UInt8 {
    let value = Int(num) - 107
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let notiAdviceSamePremiumTitle:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let constAdviceName:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let show_installKey:String = "audioIshe appearance nite"
fileprivate let data_rateAgoMsg:[Character] = ["d"]

/*: "audioLength" :*/
fileprivate let appDimensionValue:[UInt8] = [0xc8,0xdc,0xcd,0xc0,0xc6,0xe5,0xcc,0xc7,0xce,0xdd,0xc1]

private func distributionWho(year num: UInt8) -> UInt8 {
    return num ^ 169
}

/*: "audioSandbox" :*/
fileprivate let data_instructionValue:[Character] = ["a","u","d","i","o","S","a","n","d","b"]
fileprivate let show_greetCharmKey:String = "olarge"

/*: "senderId" :*/
fileprivate let kFailHelloUrl:[UInt8] = [0xbf,0xb1,0xba,0xb0,0xb1,0xbe,0x95,0xb0]

fileprivate func agoTrust(identity num: UInt8) -> UInt8 {
    let value = Int(num) - 76
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "AudioMsg" :*/
fileprivate let notiTrackTotaleractionMessage:String = "AudioMsgversion problem"

/*: "Can't send blank message" :*/
fileprivate let main_procedureExceptTilString:[Character] = ["C"]
fileprivate let k_isolateTitle:String = "an\'t "
fileprivate let noti_foundData:String = "screen on check memory jump blank"
fileprivate let appOuterMsg:String = "clearlyage"

/*: "quoteMsgId" :*/
fileprivate let main_randomValue:String = "gall breakquoteMsg"
fileprivate let user_modelRegulateDenseValue:String = "transaction comment none expectedId"

/*: "quoteMsgInfo" :*/
fileprivate let noti_conversionInsertMsg:String = "quclosede"
fileprivate let app_chooseData:String = "except"

/*: "contentType" :*/
fileprivate let data_pushDetectId:[UInt8] = [0xb1,0xbd,0xbc,0xc2,0xb3,0xbc,0xc2,0xa2,0xc7,0xbe,0xb3]

fileprivate func shotResource(impact num: UInt8) -> UInt8 {
    let value = Int(num) + 178
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let user_whoTierKey:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let userJobMarginScriptFormat:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let data_similarId:[UInt8] = [0xa1,0xa5,0xa9,0xaf,0xad,0x9d,0xba,0xa1]

private func lowInclude(port num: UInt8) -> UInt8 {
    return num ^ 200
}

/*: "customSend" :*/
fileprivate let constEssayPaintString:[UInt8] = [0xf7,0xe1,0xe7,0xe0,0xfb,0xf9,0xc7,0xf1,0xfa,0xf0]

private func identifyActual(light num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "pic" :*/
fileprivate let show_replyInputMsg:String = "ppurposec"

/*: "VideoMsg" :*/
fileprivate let noti_regionPingKey:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "video" :*/
fileprivate let data_moreName:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let userStorageDomainStr:[UInt8] = [0x67,0x6d,0x49,0x72,0x65,0x76,0x6f,0x63]

/*: "videoUrl" :*/
fileprivate let k_liteId:[UInt8] = [0x6c,0x72,0x55,0x6f,0x65,0x64,0x69,0x76]

/*: "duration" :*/
fileprivate let kSocialPath:[UInt8] = [0x68,0x79,0x76,0x65,0x78,0x6d,0x73,0x72]

fileprivate func conditionBroad(of num: UInt8) -> UInt8 {
    let value = Int(num) - 4
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "len" :*/
fileprivate let dataFactoryPath:[UInt8] = [0x63,0x5c,0x65]

fileprivate func focusSine(select num: UInt8) -> UInt8 {
    let value = Int(num) + 9
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Documents/User/voice/" :*/
fileprivate let showHomeAskMarginName:String = "Docustop clock"
fileprivate let main_blankBesideFamilyKey:[Character] = ["r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let k_personData:String = "Docugreat environment birthday"
fileprivate let userAudienceString:String = "great ceiling/User"
fileprivate let constBindName:String = "healthy draftord/"

/*: "msgIncome" :*/
fileprivate let app_passFragmentMessage:[Character] = ["m"]
fileprivate let showCounselFormat:String = "sgIncomeyourself upon size tab onto"

/*: "messageType" :*/
fileprivate let main_oddQuantityerestMessage:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "WantGift" :*/
fileprivate let k_keepName:String = "sensor"
fileprivate let notiChannelMsg:[Character] = ["a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let noti_flexUrl:[Character] = ["_","P","r","i","v","a","t"]
fileprivate let appConstantStr:[Character] = ["e","C","h","a"]
fileprivate let showTargetMsg:[Character] = ["t","T","i","p","s","T","i","t","l","e","_"]

/*: "msgId" :*/
fileprivate let noti_followingMessage:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let showAssistFormat:String = "totmutual"
fileprivate let showObserverEnhanceStr:[Character] = ["o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let appGoPath:[Character] = ["a","t","t","a","c","h"]
fileprivate let app_shadeStr:String = "position mend processingSysMsg"

/*: "intimatePhoto" :*/
fileprivate let dataFindPath:String = "intihima"
fileprivate let user_waitPath:String = "draw direct called abouttePhoto"

/*: "totalIntimate" :*/
fileprivate let showRefMaintainTitle:String = "totafield"
fileprivate let userRecommendationId:[Character] = ["I","n","t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let mainConditionPath:[UInt8] = [0x14,0x8,0x5]

private func expressionMan(resume num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "intimate" :*/
fileprivate let app_medalName:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let user_laterMessage:[UInt8] = [0x63,0x41,0x4e,0x7,0x54,0x0,0x55,0x53,0x45,0x0,0x54,0x48,0x45,0x0,0x46,0x55,0x4e,0x43,0x54,0x49,0x4f,0x4e,0x0,0x44,0x55,0x45,0x0,0x54,0x4f,0x0,0x59,0x4f,0x55,0x52,0x0,0x4c,0x41,0x43,0x4b,0x0,0x4f,0x46,0x0,0x49,0x4e,0x54,0x49,0x4d,0x41,0x43,0x59,0x0,0x4c,0x45,0x56,0x45,0x4c,0xe]

private func mirrorDecadeEarn(button num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "giftNum" :*/
fileprivate let user_reflectId:String = "first replygiftNum"

/*: "pkgItemsetId" :*/
fileprivate let notiVersionGenderTitle:String = "pkgIgenuine wake ironic sink symbol"
fileprivate let mainGenetOldFormat:String = "require mirror progressetId"

/*: "from" :*/
fileprivate let kLocateRainMsg:String = "FROM"

/*: "wantGiftId" :*/
fileprivate let data_accessibleData:[Character] = ["w","a","n","t","G"]
fileprivate let userAloneString:[Character] = ["i","f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let user_appId:[Character] = ["W","a"]
fileprivate let constEstheticYerData:String = "me place share crisis opinionntGif"

/*: "gift" :*/
fileprivate let showTackTimingFormat:String = "gieasy"

/*: "View info" :*/
fileprivate let k_estimatedMessage:String = "two"
fileprivate let dataSlideValue:String = "weekly behavior add putiew "

/*: "Notes" :*/
fileprivate let appNotePerformName:String = "her broadcast tip resultNotes"

/*: "Report" :*/
fileprivate let const_redName:String = "Reportfuneral your sufficient off say"

/*: "View read receipts?" :*/
fileprivate let showFlowMessage:String = "be photo short evenView"
fileprivate let mainGemRetainName:String = "einviteei"

/*: "She" :*/
fileprivate let data_wedName:String = "response again conversion opShe"

/*: "%@ is using too low a version" :*/
fileprivate let mainStraightenTitle:String = "temporary lost gall comply format%@ i"
fileprivate let main_emptyValue:String = "ng tgravity lose"
fileprivate let app_peopleFormat:[Character] = ["o","w"," ","a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let noti_wakeMessage:String = "way lose charShoot "
fileprivate let main_occasionValue:String = "VID"
fileprivate let constTodayId:String = "eo acatch period race window canvas"
fileprivate let userResponseStr:String = "special publish para go burnst 5 s"

/*: "Upload failed" :*/
fileprivate let mainMaterialStatusValue:[Character] = ["U","p","l","o","a","d"]
fileprivate let show_equivalentFormat:String = "height integration text hold failed"

/*: "播放状态: :*/
fileprivate let appEverId:String = "accelerate放状态:"

/*: "c2c_" :*/
fileprivate let appGoingConsumeKey:[Character] = ["c","2","c","_"]

/*: "msg" :*/
fileprivate let data_generateId:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let noti_fatalUponId:String = "TIPS"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let kCombineFormat:[UInt8] = [0xee,0x4,0xa,0xb5,0xf8,0xf6,0x3,0xb5,0xa,0x8,0xfa,0xb5,0x9,0xfd,0xfa,0xb5,0xb,0xfe,0xf9,0xfa,0x4,0xb5,0xf8,0xfd,0xf6,0x9,0xb5,0xfb,0xfa,0xf6,0x9,0xa,0x7,0xfa,0xb5,0x9,0x4,0xb5,0xfc,0xfe,0xb,0xfa,0xb5,0xba,0xd5,0xb5,0xf6,0xb5,0xf8,0xf6,0x1,0x1]

fileprivate func ehEnjoy(player num: UInt8) -> UInt8 {
    let value = Int(num) - 149
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cellData" :*/
fileprivate let user_successfullyTitle:[Character] = ["c","e","l","l","D","a","t","a"]

/*: "F5F5F5" :*/
fileprivate let notiDistanceName:[Character] = ["F","5","F","5","F","5"]

/*: "System notification" :*/
fileprivate let const_administrativeValue:[Character] = ["S","y","s","t","e","m"," ","n","o","t","i","f","i","c","a","t","i","o"]
fileprivate let noti_hisVoiceStr:[Character] = ["n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BoundSmallThin.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum VideoInformation {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class BoundSmallThin: MasterClip {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: UserChatType = .private
    private var conversationType: UserChatType = .private
    //: private var userWrap = TypicalCommand()
    private var userWrap = TypicalCommand() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = VideoInformation.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: TypicalCommand = TypicalCommand()) {
    init(chatID: String, isFrom: VideoInformation = .Normal, userWrap: TypicalCommand = TypicalCommand()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.msgView.targetID = chatID
        self.msgView.targetID = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap
        //: self.msgView.isRecharge = userWrap.isRecharge
        self.msgView.isRecharge = userWrap.isRecharge

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        BoundSmallThin.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        SustainableSaveerGift.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if msgView.targetID == FormatDistribution.getXiaoMiID() {
        if msgView.targetID == FormatDistribution.raging() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if msgView.targetID == FormatDistribution.getCustomerServiceID() {
        } else if msgView.targetID == FormatDistribution.yea() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainBlackPendingName.map{mustRevenue(portrait: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if VideoMagnitudeerval.share.appConfigMode.disableShootScreen {
            if VideoMagnitudeerval.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ProviderSineView(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        permissionRequire()
        //: createUI()
        report()
        //: createUIConstraints()
        duringVariable()
        //: bindInteraction()
        noSituation()
        //: addTapGestureRecognizer()
        panelSufficientReasonResistance()
        //: func__requestChatInfo()
        option()
        //: func__getFillDraftText()
        underMake()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        DepthFade.shared.yieldHead()
        //: func__addVoiceNotification()
        jump()
        //: addReadPeceiptNotification()
        object()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            system()
            //: func__TipsNotification()
            whenSpecify()
            //: if VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue {
            if VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue {
                // 处理男性侧快捷回复逻辑
                //: func__setReplyTipsView()
                lab()
                // 判断问题回答消息遮罩是否展示
                //: refreshRechargeRequiredView()
                afterMake()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        character(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        character(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            behindSmall()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            StopFramework.shared.scientific()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: NameViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = NameViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(ImpactSpell.self, forCellReuseIdentifier: ImpactSpell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(WhiteExamine.self, forCellReuseIdentifier: WhiteExamine.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(TopButton.self, forCellReuseIdentifier: TopButton.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(WorkEntity.self, forCellReuseIdentifier: WorkEntity.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(DiscoverTail.self, forCellReuseIdentifier: DiscoverTail.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(AgileView.self, forCellReuseIdentifier: AgileView.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(ExclusiveDestroy.self, forCellReuseIdentifier: ExclusiveDestroy.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(PlayBridge.self, forCellReuseIdentifier: PlayBridge.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(ChapterConnect.self, forCellReuseIdentifier: ChapterConnect.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: OffUsed.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(AgainstThread.self, forCellReuseIdentifier: OffUsed.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: CapDirect = {
        //: let m = TalkingCallMenuManager()
        let m = CapDirect()
        //: m.uid = msgView.targetID
        m.uid = msgView.targetID
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.process()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: EqualLogView = {
        //: var type: InputViewType = .PrivateChat
        var type: ScaleLike = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = EqualLogView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(showJustStr)))!, .font: UIFont.statinEssay(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(data_fixedHiId.suffix(5)) + String(appLemmaProcedureTitle)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(viaBorder), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: DivideView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = DivideView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: MaxView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = MaxView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.statinEssay(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(main_mutualIronicValue.suffix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(notiPotentMessage.prefix(4)) + "k Gree" + kBarUrl.replacingOccurrences(of: "maker", with: "n")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.tress().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(roundAccess), for: .touchUpInside)
        //: btn.isHidden = !VideoMagnitudeerval.share.appUserConfigMode.showDirectGreet || VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue || VideoMagnitudeerval.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !VideoMagnitudeerval.share.appUserConfigMode.showDirectGreet || VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue || VideoMagnitudeerval.share.appStatus == PromiseTime.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: DelayView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = DelayView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.statinEssay(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(main_mutualIronicValue.suffix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(mainTwoId.prefix(3)) + String(noti_deployMechanicalMsg.suffix(5)) + "hotos").localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.tress().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(transactionInEnableSpell), for: .touchUpInside)
        //: btn.isHidden = VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue || VideoMagnitudeerval.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue || VideoMagnitudeerval.share.appStatus == PromiseTime.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: FadeChartProvision = {
        //: let v = TalkingLiveRoomGamesView()
        let v = FadeChartProvision()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: TransformCreateHide = {
        //: let view = TalkingPrivateBannerView.init()
        let view = TransformCreateHide()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: PathView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = PathView()
        //: v.isHidden = (VideoMagnitudeerval.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (VideoMagnitudeerval.share.appStatus == PromiseTime.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: ProvideAssociateView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = ProvideAssociateView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.equally()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var rechargeView: RechargeRequiredView = {
    private lazy var rechargeView: BothView = { // 充值限制视图
        //: let v = RechargeRequiredView()
        let v = BothView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if self.userWrap.isRecharge == -1 {
            if self.userWrap.isRecharge == -1 { // 首充优惠
                //: TalkingAppPushManager.share.func_pushToDiscountRechargeView()
                ScopeEmbrace.share.applicationTier()
                //: } else if self.userWrap.isRecharge == 0 {
            } else if self.userWrap.isRecharge == 0 { // 半屏充值
                //: TalkingAppPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                ScopeEmbrace.share.constraintOn(webViewType: .RechargeHalfPage)
            }
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var quickReplayView: QuestionQuickReplayView = {
    private lazy var quickReplayView: AmongSession = { // 问题快捷回复视图
        //: let v = QuestionQuickReplayView()
        let v = AmongSession()
        //: v.tapBlock = { [weak self] model in
        v.tapBlock = { [weak self] model in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 调用发送消息接口
            //: self.func_sendQuestionQuickReplayMsg(model)
            self.activitySuccessfully(model)
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension BoundSmallThin: TextureExceptChapter {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func respective(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.trailing.equalTo(-4)
                    make.trailing.equalTo(-4)
                    //: make.height.equalTo(36)
                    make.height.equalTo(36)
                    //: make.width.equalTo(89)
                    make.width.equalTo(89)
                }
                //: } else {
            } else {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.belowAll(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension BoundSmallThin: RoundingInstruction {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func roundAccess() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
        SustainableSaveerGift.singleThin(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.conclusion(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.plus(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func transactionInEnableSpell() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.msgView.targetID) { isPushSet, dict in
        SustainableSaveerGift.host(toUserId: self.msgView.targetID) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().spread(urlStr: dict?[(user_boaName.replacingOccurrences(of: "blue", with: "rl"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(userCapableVolumeMsg) + String(show_connectCompareProblemString))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = LibraryModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: dataEstateBlocTitle.map{doingdOutside(work: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(appMpMessage) + String(appEquallyLowerValue.suffix(5)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.outputOfMobile(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TaskAction! {
    func over(_ controller: NameViewController!, commonPersist data: V2TIMMessage!) -> TaskAction! {
        //: let dic = MaintainSection.parseTXMessageData(data: data.customElem.data)
        let dic = MaintainSection.resumeOfCover(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = InformationConcrete.foreBound(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: SignSmall.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(data_nearPath) + notiCommunicateMsg.replacingOccurrences(of: "statin", with: "e"))].stringValue == (String(user_refuseGestureSystemValue) + String(mainFreshData.suffix(6))) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(constAllowFormat.replacingOccurrences(of: "wait", with: "c") + "geDi" + noti_expressionStr.replacingOccurrences(of: "maker", with: "re") + constMechanismValue.replacingOccurrences(of: "plain", with: "ti"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(show_manualStr))][(String(notiCityString) + String(notiBadBindFormat))][(String(const_tensionMsg.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ImageAngle(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ImageAngle(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: BoxOrangeDistribution.self) == true ||
        if cellData?.isKind(of: BoxOrangeDistribution.self) == true ||
            //: cellData?.isKind(of: MenuAdjust.self) == true ||
            cellData?.isKind(of: MenuAdjust.self) == true ||
            //: cellData?.isKind(of: ImageAngle.self) == true ||
            cellData?.isKind(of: ImageAngle.self) == true ||
            //: cellData?.isKind(of: SharedImplementation.self) == true {
            cellData?.isKind(of: SharedImplementation.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            SustainableSaveerGift.layerPaint(cellData: cellData, msgView: self.msgView)
            // 男性是否展示快捷回复消息
            //: checkQuestionQuickReplayView()
            exceptClose()
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TaskAction!) -> OffUsed! {
    func margin(_ controller: NameViewController!, service data: TaskAction!) -> OffUsed! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = closeModify(data: data)

        //: if data.isKind(of: ImageAngle.self) {
        if data.isKind(of: ImageAngle.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = ImpactSpell(style: .default, reuseIdentifier: ImpactSpell.className())
            //: let cellData = data as! ImageAngle
            let cellData = data as! ImageAngle

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.willEqual() {
                //: if cellData.msgModel.quoteMsgInfo.uid == VideoMagnitudeerval.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == VideoMagnitudeerval.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = VideoMagnitudeerval.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = VideoMagnitudeerval.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: MenuAdjust.self) {
        if data.isKind(of: MenuAdjust.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = WhiteExamine(style: .default, reuseIdentifier: WhiteExamine.className())
            //: let cellData = data as! MenuAdjust
            let cellData = data as! MenuAdjust
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SharedImplementation.self) {
        if data.isKind(of: SharedImplementation.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = TopButton(style: .default, reuseIdentifier: TopButton.className())
            //: let cellData = data as! SharedImplementation
            let cellData = data as! SharedImplementation
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: QuantityegrityMax.self) {
        if data.isKind(of: QuantityegrityMax.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = WorkEntity(style: .default, reuseIdentifier: WorkEntity.className())
            //: let cellData = data as! QuantityegrityMax
            let cellData = data as! QuantityegrityMax
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: PopFixed.self) {
        if data.isKind(of: PopFixed.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = DiscoverTail(style: .default, reuseIdentifier: DiscoverTail.className())
            //: let cellData = data as! PopFixed
            let cellData = data as! PopFixed
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = CharacterModel()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.progressRecognize(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (k_saleName.replacingOccurrences(of: "every", with: "nt") + "Gift"))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: BoxOrangeDistribution.self) {
        if data.isKind(of: BoxOrangeDistribution.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = PlayBridge(style: .default, reuseIdentifier: PlayBridge.className())
            //: let cellData = data as! BoxOrangeDistribution
            let cellData = data as! BoxOrangeDistribution
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: SignSmall.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = ExclusiveDestroy(style: .default, reuseIdentifier: ExclusiveDestroy.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! SignSmall
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SecondDepthLike.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = AgileView(style: .default, reuseIdentifier: AgileView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SecondDepthLike
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: TriggerEnvironment.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = ChapterConnect(style: .default, reuseIdentifier: ChapterConnect.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! TriggerEnvironment
            //: cell.fill(with: cellData)
            cell.associated(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: OffUsed!) {
    func might(_ controller: NameViewController!, cluster cell: OffUsed!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != FormatDistribution.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != FormatDistribution.yea() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            ScopeEmbrace.share.receiverDoingStop(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: OffUsed!) {
    func phase(_ controller: NameViewController!, distribution cell: OffUsed!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: WhiteExamine.self) { // 图片
            //: let imageCellData = cell.messageData as! MenuAdjust
            let imageCellData = cell.messageData as! MenuAdjust
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.msgView.targetID)
                let vc = TranslatePut(imageCellData: imageCellData, toUid: self.msgView.targetID)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.characteristic(imageCellData, angleAttribute: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.textProduct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataBrushTitle)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.advanced(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.memory(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                priorityAssetSpace()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(dataStateMessage)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = NumberensityMark()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = ExerciseListen()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [NumberensityMark] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = MagnitudeervalManagerFlex(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                priorityAssetSpace()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: TopButton.self) { // 视频
            //: let videoCellData = cell.messageData as! SharedImplementation
            let videoCellData = cell.messageData as! SharedImplementation
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = ConditionViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                priorityAssetSpace()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.msgView.targetID)
                let vc = WhiteEquipmentContain(videoCellData: videoCellData, toUid: self.msgView.targetID)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.effectElement(videoCellData, embrace: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.textProduct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataBrushTitle)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.advanced(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.memory(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                priorityAssetSpace()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: PlayBridge.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! PlayBridge
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! BoxOrangeDistribution, cellView: audioCell)
            sophisticatedTotalry(cellData: audioCell.cellData as! BoxOrangeDistribution, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ImpactSpell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ImpactSpell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = MaintainSection.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = MaintainSection.resumeOfCover(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(show_manualStr))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(app_gemNineStr.suffix(5)) + appEvaluationPath.lowercased())) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(app_gemNineStr.suffix(5)) + appEvaluationPath.lowercased())] as? String ?? ""

                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<TotaleractionStop>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ScopeEmbrace.share.transactionDocument(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(data_nearPath) + notiCommunicateMsg.replacingOccurrences(of: "statin", with: "e"))] as? String == (String(user_refuseGestureSystemValue) + String(mainFreshData.suffix(6))) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(notiCityString) + String(notiBadBindFormat))][(String(show_myData.prefix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    location()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    buttCenter()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: AgileView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! AgileView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = MaintainSection.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = MaintainSection.resumeOfCover(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(show_manualStr))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(app_gemNineStr.suffix(5)) + appEvaluationPath.lowercased())) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<TotaleractionStop>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ScopeEmbrace.share.transactionDocument(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: ExclusiveDestroy.self) {
            //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: msgView.targetID)
            ScopeEmbrace.share.receiverDoingStop(uid: msgView.targetID)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: OffUsed!) {
    func nextTemporary(_ controller: NameViewController!, everyCommon cell: OffUsed!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: AddProcessorCombine?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: WhiteExamine.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! WhiteExamine
            //: cellData = imageCell.cellData as! MenuAdjust
            cellData = imageCell.cellData as! MenuAdjust
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: TopButton.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! TopButton
            //: cellData = videoCell.cellData as! SharedImplementation
            cellData = videoCell.cellData as! SharedImplementation
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: PlayBridge.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! PlayBridge
            //: cellData = audioCell.cellData as! BoxOrangeDistribution
            cellData = audioCell.cellData as! BoxOrangeDistribution
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ImpactSpell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ImpactSpell
            //: cellData = textCell.cellData as! ImageAngle
            cellData = textCell.cellData as! ImageAngle
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TaskAction? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: TaskAction? = InformationConcrete.pastEven(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: SharedImplementation.self) {
                if reCellData!.isKind(of: SharedImplementation.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    path(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    history(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TaskAction!) {
    func betweenPet(_ controller: NameViewController!, attribute cellData: TaskAction!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = SustainableSaveerGift.memberWithinAsset(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TaskAction!) {
    func roundParticle(_ controller: NameViewController!, enter cellData: TaskAction!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        SustainableSaveerGift.failure(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TaskAction) -> TaskAction {
    private func closeModify(data: TaskAction) -> TaskAction {
        //: let isSelf = data.msgModel.user.uid == Int(VideoMagnitudeerval.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(VideoMagnitudeerval.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if VideoMagnitudeerval.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if VideoMagnitudeerval.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = VideoMagnitudeerval.share.loginUserMode.headPic!
                data.msgModel.user.headPic = VideoMagnitudeerval.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = VideoMagnitudeerval.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = VideoMagnitudeerval.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 发送问题回复消息 -- 回答
    /// - Parameter model: 回答问题模型
    //: func func_sendQuestionQuickReplayMsg(_ model: QuestionQuickReplayModel) {
    func activitySuccessfully(_ model: ThinModel) {
        //: let msgInfo = ["content": model.content, "msgType": "txt"]
        let msgInfo = [String(bytes: show_continuingKey.reversed(), encoding: .utf8)!: model.content, String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(dataInstallName))]
        //: let paramsDic = ["message": model.content, "replayId": model.replayId, "type": "TxtMsg"] as [String : Any]
        let paramsDic = [String(bytes: show_girlStr.map{holderRemind(relate: $0)}, encoding: .utf8)!: model.content, String(bytes: kWillFormat.map{$0^204}, encoding: .utf8)!: model.replayId, (String(dataAlwaysKey)): (String(const_directRemMsg.suffix(5)) + String(main_supportKey))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.history(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    //: func func_sendSayHiMsg() {
    func except() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(dataInstallName))]
        //: let paramsDic = ["type": "MultiQuickMsg"]
        let paramsDic = [(String(dataAlwaysKey)): (String(noti_hundredString.suffix(4)) + "iQuickM" + String(kBrownId))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.history(msgData: cellData, reSend: false)
        }
    }

    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func verticalNo() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(dataInstallName))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(dataAlwaysKey)): (String(kAbsKey.suffix(4)) + "kMsg")]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.history(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func plus(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: show_continuingKey.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: show_continuingKey.reversed(), encoding: .utf8)!: content, (String(showSingleManageLaughterGivingData)): greetId, String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(dataInstallName))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: show_girlStr.map{holderRemind(relate: $0)}, encoding: .utf8)!: content, (String(showSingleManageLaughterGivingData)): greetId, (String(dataAlwaysKey)): (String(const_directRemMsg.suffix(5)) + String(main_supportKey))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.history(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            TapMark.shared.localObtain(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.conclusion(showMsg: String(bytes: data_interestedFormat.map{scripConsumer(largely: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                        SustainableSaveerGift.space(toUserId: self.msgView.targetID, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(notiAdviceSamePremiumTitle))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(constAdviceName)),
                               //: "audioId": greetId,
                               (String(show_installKey.prefix(6)) + String(data_rateAgoMsg)): greetId,
                               //: "audioLength": length,
                               String(bytes: appDimensionValue.map{distributionWho(year: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(data_instructionValue) + show_greetCharmKey.replacingOccurrences(of: "large", with: "x")): model.unZipFilePath,
                               //: "senderId": VideoMagnitudeerval.share.loginUserMode.userID]
                               String(bytes: kFailHelloUrl.map{agoTrust(identity: $0)}, encoding: .utf8)!: VideoMagnitudeerval.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(show_installKey.prefix(6)) + String(data_rateAgoMsg)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: appDimensionValue.map{distributionWho(year: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(data_instructionValue) + show_greetCharmKey.replacingOccurrences(of: "large", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(dataAlwaysKey)): (String(notiTrackTotaleractionMessage.prefix(8)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.history(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func down(msgStr: String, quoteModel: SourceShadowExceptionModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            conclusion(showMsg: (String(main_procedureExceptTilString) + k_isolateTitle + "send" + String(noti_foundData.suffix(6)) + " mes" + appOuterMsg.replacingOccurrences(of: "clearly", with: "s")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: show_continuingKey.reversed(), encoding: .utf8)!: temStr, String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(dataInstallName))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: show_girlStr.map{holderRemind(relate: $0)}, encoding: .utf8)!: temStr, (String(dataAlwaysKey)): (String(const_directRemMsg.suffix(5)) + String(main_supportKey))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(main_randomValue.suffix(8)) + String(user_modelRegulateDenseValue.suffix(2)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(noti_conversionInsertMsg.replacingOccurrences(of: "closed", with: "ot") + "MsgInf" + app_chooseData.replacingOccurrences(of: "except", with: "o"))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? SourceShadowExceptionModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            history(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func outputOfMobile(img: UIImage, sendModel: LibraryModel) {
        //: let imagePath = MenuAdjust.getChatImagePath(image: img)
        let imagePath = MenuAdjust.fire(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: data_pushDetectId.map{shotResource(impact: $0)}, encoding: .utf8)!: (String(user_whoTierKey)), String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(userJobMarginScriptFormat)), String(bytes: data_similarId.map{lowInclude(port: $0)}, encoding: .utf8)!: imagePath, String(bytes: constEssayPaintString.map{identifyActual(light: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(show_replyInputMsg.replacingOccurrences(of: "purpose", with: "i")): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(dataAlwaysKey)): (String(user_whoTierKey)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataEstateBlocTitle.map{doingdOutside(work: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(appMpMessage) + String(appEquallyLowerValue.suffix(5))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            history(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TaskAction?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func path(msgData: TaskAction?, sendModel: LibraryModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: data_pushDetectId.map{shotResource(impact: $0)}, encoding: .utf8)!: (String(noti_regionPingKey)), String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(data_moreName)), String(bytes: constEssayPaintString.map{identifyActual(light: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(dataAlwaysKey)): (String(noti_regionPingKey)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: userStorageDomainStr.reversed(), encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: k_liteId.reversed(), encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: kSocialPath.map{conditionBroad(of: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataEstateBlocTitle.map{doingdOutside(work: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(appMpMessage) + String(appEquallyLowerValue.suffix(5))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = InformationConcrete.only(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.indicator(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.err(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.write(cellData, postInstead: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.err(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.prepare(dict, asCompute: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        history(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func equipmentNear(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (String(constAdviceName)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: appDimensionValue.map{distributionWho(year: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(data_instructionValue) + show_greetCharmKey.replacingOccurrences(of: "large", with: "x")): voicePath,
                       //: "senderId": VideoMagnitudeerval.share.loginUserMode.userID]
                       String(bytes: kFailHelloUrl.map{agoTrust(identity: $0)}, encoding: .utf8)!: VideoMagnitudeerval.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(data_instructionValue) + show_greetCharmKey.replacingOccurrences(of: "large", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (String(dataAlwaysKey)): (String(notiTrackTotaleractionMessage.prefix(8))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: dataFactoryPath.map{focusSine(select: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            history(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TaskAction, reSend: Bool) {
    private func history(msgData: TaskAction, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            conclusion(showMsg: appMoveTrainTitle)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.msgView.targetID
        params[(String(const_tensionMsg.prefix(5)))] = self.msgView.targetID

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: SharedImplementation.self) == false && msgData.isKind(of: PopFixed.self) == false && params["type"] as? String != "QuickMsg" && params["type"] as? String != "MultiQuickMsg" {
        if msgData.isKind(of: SharedImplementation.self) == false, msgData.isKind(of: PopFixed.self) == false, params[(String(dataAlwaysKey))] as? String != (String(kAbsKey.suffix(4)) + "kMsg"), params[(String(dataAlwaysKey))] as? String != (String(noti_hundredString.suffix(4)) + "iQuickM" + String(kBrownId)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.indicator(msgData)
        }
//        let recordTime = MaintainSection.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((show_replyInputMsg.replacingOccurrences(of: "purpose", with: "i"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(show_replyInputMsg.replacingOccurrences(of: "purpose", with: "i"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(show_replyInputMsg.replacingOccurrences(of: "purpose", with: "i"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as? String
        let type = params[(String(dataAlwaysKey))] as? String
        //: if type == "AudioMsg" { // 语音
        if type == (String(notiTrackTotaleractionMessage.prefix(8))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(data_instructionValue) + show_greetCharmKey.replacingOccurrences(of: "large", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(data_instructionValue) + show_greetCharmKey.replacingOccurrences(of: "large", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(data_instructionValue) + show_greetCharmKey.replacingOccurrences(of: "large", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(showHomeAskMarginName.prefix(4)) + "ments/Use" + String(main_blankBesideFamilyKey))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = noti_saltKey + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(k_personData.prefix(4)) + "ments" + String(userAudienceString.suffix(5)) + "/rec" + String(constBindName.suffix(4)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = noti_purchaseUrl + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(show_installKey.prefix(6)) + String(data_rateAgoMsg))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(constAdviceName))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        AnalyzeMapCommon.premium(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" || type == "MultiQuickMsg" {
                if type == (String(kAbsKey.suffix(4)) + "kMsg") || type == (String(noti_hundredString.suffix(4)) + "iQuickM" + String(kBrownId)) {
                    //: self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.pop(msgData, characterFile: nil, consumptionOff: NSNumber(value: 0), until: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.textProduct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: userBecomeSingleData)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(showSingleManageLaughterGivingData))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                    SustainableSaveerGift.space(toUserId: self.msgView.targetID, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.specify(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(notiCityString) + String(notiBadBindFormat))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(app_passFragmentMessage) + String(showCounselFormat.prefix(8)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(app_passFragmentMessage) + String(showCounselFormat.prefix(8)))] as! NSNumber
            }

            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.age(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(main_oddQuantityerestMessage))] as? Int
            //: if messageType == 25 || type == "QuickMsg" || type == "MultiQuickMsg" {
            if messageType == 25 || type == (String(kAbsKey.suffix(4)) + "kMsg") || type == (String(noti_hundredString.suffix(4)) + "iQuickM" + String(kBrownId)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: showHiddenData.reversed(), encoding: .utf8)!] = (k_keepName.replacingOccurrences(of: "sensor", with: "W") + String(notiChannelMsg))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: constSectionValue)
                    //: } else if type == "QuickMsg" || type == "MultiQuickMsg" {
                } else if type == (String(kAbsKey.suffix(4)) + "kMsg") || type == (String(noti_hundredString.suffix(4)) + "iQuickM" + String(kBrownId)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: showHiddenData.reversed(), encoding: .utf8)!] = (String(dataInstallName))
                    //: self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.indicator(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: if self.rechargeView.isHidden {
            if self.rechargeView.isHidden {
                //: if VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue &&
                if VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue,
                   //: type != "QuickMsg" &&
                   type != (String(kAbsKey.suffix(4)) + "kMsg"),
                   //: type != "MultiQuickMsg" {
                   type != (String(noti_hundredString.suffix(4)) + "iQuickM" + String(kBrownId))
                {
                    //: let key =  "\(VideoMagnitudeerval.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
                    let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)" + (String(noti_flexUrl) + String(appConstantStr) + String(showTargetMsg)) + "\(String(self.msgView.targetID))"
                    //: UserDefaults.standard.set(true, forKey: key)
                    UserDefaults.standard.set(true, forKey: key)
                    //: self.removeReplyTipsView()
                    self.abovePair()
                }
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.pop(updateMsgData, characterFile: msgInfo[(String(noti_followingMessage))] as? String, consumptionOff: msgIncome, until: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((showAssistFormat.replacingOccurrences(of: "mutual", with: "a") + "lMfC" + String(showObserverEnhanceStr))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(showAssistFormat.replacingOccurrences(of: "mutual", with: "a") + "lMfC" + String(showObserverEnhanceStr))] as! NSNumber
                //: VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(appGoPath) + String(app_shadeStr.suffix(6)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(appGoPath) + String(app_shadeStr.suffix(6)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.sortSample(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((dataFindPath.replacingOccurrences(of: "him", with: "m") + String(user_waitPath.suffix(7)))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(dataFindPath.replacingOccurrences(of: "him", with: "m") + String(user_waitPath.suffix(7)))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? VerticalBuildReceiveGreenObserverModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.characteristic(updateMsgData, angleAttribute: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(data_moreName))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(data_moreName))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? ProvideModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.effectElement(updateMsgData, embrace: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        readyFace()

        //: updateSession()
        coordinator()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func coordinator() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: msgView.targetID)
        FlexDivide.spring(with: msgView.targetID)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func textProduct(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard VideoMagnitudeerval.share.loginUserMode.status != 1 else {
            guard VideoMagnitudeerval.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    conclusion(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            ScopeEmbrace.share.rateBreeze(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.symbolWith()
            //: VideoMagnitudeerval.share.loginUserMode.freeMsgTimes = ""
            VideoMagnitudeerval.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.process()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = MissionPriorBind(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
            //: view.show()
            view.tail()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                conclusion(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func memory(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(notiCityString) + String(notiBadBindFormat))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.age(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func significant() {
        //: if self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.signCheckion(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (appEfficiencyStorageStr.array(forKey: user_whiteWithData)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.uniformSignature()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                appEfficiencyStorageStr.set(intimateCacheData, forKey: user_whiteWithData)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func circle(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = MaintainSection.parseTXMessageData(data: data.customElem.data)
        let dic = MaintainSection.resumeOfCover(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(show_manualStr))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(notiCityString) + String(notiBadBindFormat))]?.dictionary
        //: if data.sender == self.msgView.targetID {
        if data.sender == self.msgView.targetID {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(showRefMaintainTitle.replacingOccurrences(of: "field", with: "l") + String(userRecommendationId))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.underRoman(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func age(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((showRefMaintainTitle.replacingOccurrences(of: "field", with: "l") + String(userRecommendationId))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(showRefMaintainTitle.replacingOccurrences(of: "field", with: "l") + String(userRecommendationId))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.underRoman(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func underRoman(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.msgView.targetID)
        let model = FlexDivide.panel(targetId: self.msgView.targetID)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.signCheckion(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.highlight(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.builder(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.builder(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        FlexDivide.averageBasic(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.msgView.targetID ?? "",
        let userInfo = [String(bytes: mainConditionPath.map{expressionMan(resume: $0)}, encoding: .utf8)!: self.msgView.targetID ?? "",
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: app_medalName.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: noti_keepPathMessage,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension BoundSmallThin: MenuGlobal {
    //: func func_inputContentIntervalOneSecond() {
    func query() {}

    //: func func__selectMentionUser(range: NSRange) {}
    func distributionIn(range: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func schedule(actionType: ArchitecturePoolImage) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    above()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    inmate()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                above()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if VideoMagnitudeerval.share.loginUserMode.loungePlus {
            if VideoMagnitudeerval.share.loginUserMode.loungePlus { // 男性会员
                //: videoStr = self.userWrap.videoVIPPrice
                videoStr = self.userWrap.videoVIPPrice
                //: voiceStr = self.userWrap.voiceVIPPrice
                voiceStr = self.userWrap.voiceVIPPrice
                //: } else {
            } else {
                //: videoStr = self.userWrap.videoPrice
                videoStr = self.userWrap.videoPrice
                //: voiceStr = self.userWrap.voicePrice
                voiceStr = self.userWrap.voicePrice
            }
            //: if self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr)
                videoCallManager.aboveBy(videoPrice: videoStr, voicePrice: voiceStr)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                inmate()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    rowToFuture()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    inmate()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                rowToFuture()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.successEnableicial(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func inmate() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.conclusion(showMsg: String(bytes: user_laterMessage.map{mirrorDecadeEarn(button: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func strengthByExamineion(giftModel: CharacterModel, giftNum: String, model: IndicatorMakeModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func lite(giftModel: CharacterModel, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        progressRecognize(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: PopFixed?, fromStr: String?) {
    func progressRecognize(giftModel: CharacterModel, giftNum: String, cellData: PopFixed?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            conclusion(showMsg: appMoveTrainTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.msgView.targetID
        params[(String(const_tensionMsg.prefix(5)))] = self.msgView.targetID
        //: params["giftId"] = giftModel.gid
        params[(String(appMpMessage) + String(appEquallyLowerValue.suffix(5)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(user_reflectId.suffix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(notiVersionGenderTitle.prefix(4)) + "tems" + String(mainGenetOldFormat.suffix(4)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(kLocateRainMsg.lowercased())] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        AnalyzeMapCommon.quoteWithoutExceptTwo(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.textProduct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: appTimingWarnAfterTitle)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.awayOf(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.advanced(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.coordinator()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(notiCityString) + String(notiBadBindFormat))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((showRefMaintainTitle.replacingOccurrences(of: "field", with: "l") + String(userRecommendationId))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(showRefMaintainTitle.replacingOccurrences(of: "field", with: "l") + String(userRecommendationId))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.underRoman(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.implement(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: userZoneHardKey)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func time(giftModel: CharacterModel, giftNum: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: app_commonLineValue)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            conclusion(showMsg: appMoveTrainTitle)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= VideoMagnitudeerval.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            inmate()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(data_accessibleData) + String(userAloneString))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(dataAlwaysKey))] = (String(user_appId) + String(constEstheticYerData.suffix(5)) + "tMsg")

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (k_keepName.replacingOccurrences(of: "sensor", with: "W") + String(notiChannelMsg))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(notiCityString) + String(notiBadBindFormat)): msgInfo, String(bytes: k_closeName.map{consumptionUniform(remain: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = InformationConcrete.only(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            history(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func empty(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(constPutMsg - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.route(true)
            self.msgView.route(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func common(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.route(true)
            self.msgView.route(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func fit(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.fit(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                orange(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func might() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if DepthFade.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            DepthFade.shared.homeRegulate()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func advanced(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(notiCityString) + String(notiBadBindFormat))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: showHiddenData.reversed(), encoding: .utf8)!] = (showTackTimingFormat.replacingOccurrences(of: "easy", with: "ft"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(notiCityString) + String(notiBadBindFormat))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = InformationConcrete.only(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.indicator(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(app_passFragmentMessage) + String(showCounselFormat.prefix(8)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(app_passFragmentMessage) + String(showCounselFormat.prefix(8)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.pop(cellData, characterFile: msgInfoDic[(String(noti_followingMessage))] as? String, consumptionOff: msgIncome, until: .Msg_Status_Succ)

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        cover(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((showAssistFormat.replacingOccurrences(of: "mutual", with: "a") + "lMfC" + String(showObserverEnhanceStr))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(showAssistFormat.replacingOccurrences(of: "mutual", with: "a") + "lMfC" + String(showObserverEnhanceStr))] as! NSNumber
            //: VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            VideoMagnitudeerval.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: VideoMagnitudeerval.share.loginUserMode.mf_coin)
            msgInputView.giftView.textLength(needReload: false, mf_coin: VideoMagnitudeerval.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func system() {
        //: giftEffectView.initCachAnimatData(tagetID: msgView.targetID)
        giftEffectView.heroComponentTake(tagetID: msgView.targetID)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func cover(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.natural(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func cityFor(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.requireDelicate(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func partner() -> String {
        //: return msgView.targetID
        return msgView.targetID
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func location() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.shareApplication { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.coordinator()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func buttCenter() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.memberVoice { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.coordinator()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    //: @objc private func feedbackBtnClick() {
    @objc private func viaBorder() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = CombineCompose()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        priorityAssetSpace()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func mention() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = WithSquare(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.street(cellTitleList: [(k_estimatedMessage.replacingOccurrences(of: "two", with: "V") + String(dataSlideValue.suffix(4)) + "info").localized, (String(appNotePerformName.suffix(5))).localized, (String(const_redName.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .serviceMajor()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: self.msgView.targetID)
                ScopeEmbrace.share.receiverDoingStop(uid: self.msgView.targetID)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                equally()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                let reportView = CycleView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                //: reportView.showReportViewIn(view: nil)
                reportView.thirdFile(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func equally() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = CreateDark()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        view.executeToImpact(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.willClosed(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func yearAcross() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            orange(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            priorityAssetSpace()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func limited(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ContentReduce.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.editor(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func object() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(radio(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: notiFilePath,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(globalOn),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: show_elementFormat,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func radio(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == msgView.targetID else { return }
        guard obj.userID == msgView.targetID else { return }
        //: guard VideoMagnitudeerval.share.loginUserMode.loungePlus == true else { return }
        guard VideoMagnitudeerval.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func globalOn() {
        //: VideoMagnitudeerval.share.loginUserMode.loungePlus = true
        VideoMagnitudeerval.share.loginUserMode.loungePlus = true
        //: userWrap.isRecharge = 1
        userWrap.isRecharge = 1
        //: msgView.isRecharge = userWrap.isRecharge
        msgView.isRecharge = userWrap.isRecharge
        //: refreshRechargeRequiredView()
        afterMake()
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func launch() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TaskAction else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TaskAction else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if SustainableSaveerGift.remote(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.play(tipStr: (String(showFlowMessage.suffix(4)) + " read r" + mainGemRetainName.replacingOccurrences(of: "invite", with: "c") + "pts?").localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension BoundSmallThin: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func above() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ReduceLevelDoingtoPrior.shadowKid(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = HandleDistinction.roadHonestly(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.temporary(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func modeCoordinator() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ReduceLevelDoingtoPrior.dream(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.temporary(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func temporary(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else {
            VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = LibraryModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.outputOfMobile(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = MagnitudeervalManagerFlex(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = LibraryModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.outputOfMobile(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        priorityAssetSpace()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func rowToFuture() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard SustainableSaveerGift.overMain(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == ChannelDown.male.rawValue ? "He".localized : (String(data_wedName.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.conclusion(showMsg: (String(mainStraightenTitle.suffix(4)) + "s usi" + String(main_emptyValue.prefix(4)) + "oo l" + String(app_peopleFormat)).noneRage(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.fragmentWith()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func fragmentWith() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = HandleDistinction.roadHonestly(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (coverImage: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            HandleDistinction.statin(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.brush(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.conclusion(showMsg: (String(noti_wakeMessage.suffix(6)) + main_occasionValue.lowercased() + String(constTodayId.prefix(4)) + "t lea" + String(userResponseStr.suffix(6)) + "econds").localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func brush(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: UniqueMark = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = AverageTo(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = LibraryModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.path(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        MediaSquare.spell()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        priorityAssetSpace()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TaskAction, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func err(msgData: TaskAction, sendModel: LibraryModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ListHide.shared.pair(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.conclusion(showMsg: (String(mainMaterialStatusValue) + String(show_equivalentFormat.suffix(7))).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.pop(msgData, characterFile: nil, consumptionOff: NSNumber(value: 0), until: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.path(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    //: func func__addVoiceNotification() {
    func jump() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(launchAcross),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(buttNet),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(pastDelivery(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(replacementKitSendBroadcast(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: noti_oldStr,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(circle(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: main_addNearUrl,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func character(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            DepthFade.shared.yieldHead()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            DepthFade.shared.want()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func launchAcross() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        character(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func buttNet() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        character(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func pastDelivery(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            character(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            character(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: BoxOrangeDistribution, cellView: TalkingChatAudioMsgCell) {
    func sophisticatedTotalry(cellData: BoxOrangeDistribution, cellView: PlayBridge) {
        //: guard TalkingSocketManager.shared.versusScope == false else {
        guard QuantityensityAction.shared.versusScope == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.conclusion(showMsg: notiReducePath)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TaskAction)!.isKind(of: BoxOrangeDistribution.self) == false {
            if (indexModel as? TaskAction)!.isKind(of: BoxOrangeDistribution.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! BoxOrangeDistribution
            let audioModel = indexModel as! BoxOrangeDistribution
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = FlexDivide.wrapConnect(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        DepthFade.shared.homeRegulate()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if DepthFade.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (appEverId.replacingOccurrences(of: "accelerate", with: "播")) + "\(DepthFade.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if DepthFade.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                DepthFade.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        DepthFade.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        DepthFade.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TaskAction = self.msgView.uiMsgs.object(at: index) as! TaskAction
                let subModel: TaskAction = self.msgView.uiMsgs.object(at: index) as! TaskAction
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: BoxOrangeDistribution.self) {
                if subModel.isKind(of: BoxOrangeDistribution.self) {
                    //: let audioModel = subModel as! BoxOrangeDistribution
                    let audioModel = subModel as! BoxOrangeDistribution
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = FlexDivide.wrapConnect(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    DensityRedRender.todayStep(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        DepthFade.shared.profile(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func option() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.file()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.plus(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TaskAction
                let indexModel = model as? TaskAction
                //: if indexModel?.isKind(of: BoxOrangeDistribution.self) == true || indexModel?.isKind(of: MenuAdjust.self) == true || indexModel?.isKind(of: ImageAngle.self) == true || indexModel?.isKind(of: SharedImplementation.self) == true {
                if indexModel?.isKind(of: BoxOrangeDistribution.self) == true || indexModel?.isKind(of: MenuAdjust.self) == true || indexModel?.isKind(of: ImageAngle.self) == true || indexModel?.isKind(of: SharedImplementation.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.readyFace()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.beauty()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.launch()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func file() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.symbol(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.photo(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.significant()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == VideoMagnitudeerval.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == VideoMagnitudeerval.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == VideoMagnitudeerval.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == VideoMagnitudeerval.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.willClosed(userWrap)
    }

    //: func func__getFillDraftText() {
    func underMake() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(appGoingConsumeKey)) + msgView.targetID
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                }
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }

    //: func func__saveFillDraftText() {
    func behindSmall() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(appGoingConsumeKey)) + msgView.targetID
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    //: func func__TipsNotification() {
    func whenSpecify() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(mountOn(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: constQuitValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func mountOn(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SecondDepthLike = userinfo[(String(data_generateId))] as! SecondDepthLike

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.route(true)
        self.msgView.route(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func play(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: show_continuingKey.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: showHiddenData.reversed(), encoding: .utf8)!: (noti_fatalUponId.lowercased())]
        //: var message: TaskAction?
        var message: TaskAction?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            message = InformationConcrete.enableicialMark(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = InformationConcrete.only(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.route(true)
        self.msgView.route(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func beauty() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        SustainableSaveerGift.mBy { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.play(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func sortSample(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.play(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func readyFace() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        SustainableSaveerGift.length(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.play(tipStr: String(bytes: kCombineFormat.map{ehEnjoy(player: $0)}, encoding: .utf8)!.noneRage(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func replacementKitSendBroadcast(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TaskAction = dict?["cellData"] as! TaskAction
        let cellData: TaskAction = dict?[(String(user_successfullyTitle))] as! TaskAction
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.route(true)
        self.msgView.route(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension BoundSmallThin: StorageReliability {
    //: func func__setReplyTipsView() {
    func lab() {
        //: let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
        let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)" + (String(noti_flexUrl) + String(appConstantStr) + String(showTargetMsg)) + "\(String(self.msgView.targetID))"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue {
            //: contentView.addSubview(replyTipsView!)
            contentView.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.premiumSecure()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            respective(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            abovePair()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func manager(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        down(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        abovePair()
    }

    //: func removeReplyTipsView() {
    func abovePair() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            respective(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 问题回答消息

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 判断问题回答消息遮罩是否展示
    //: private func refreshRechargeRequiredView() {
    private func afterMake() {
        //: guard VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue else { return }
        //: if userWrap.isRecharge == 1 || userWrap.isOfficial {
        if userWrap.isRecharge == 1 || userWrap.isOfficial { // 已充值过
            //: quickReplayView.isHidden = true
            quickReplayView.isHidden = true
            //: rechargeView.isHidden = true
            rechargeView.isHidden = true

            //: } else {
        } else { // 未充值
            //: rechargeView.isHidden = false
            rechargeView.isHidden = false
            //: self.checkQuestionQuickReplayView()
            self.exceptClose()
        }
    }

    /// 检查快捷回复视图是否展示
    //: private func checkQuestionQuickReplayView() {
    private func exceptClose() {
        //: guard userWrap.isOfficial == false else { return }
        guard userWrap.isOfficial == false else { return }
        // 检查男性侧快捷回复是否展示（延时为了拿到uiMsgs数据）
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: TalkingPrivateChatManager.chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
            SustainableSaveerGift.switchd(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
                //: guard let msgInfo = msgInfo else { return }
                guard let msgInfo = msgInfo else { return }
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                    //: self.quickReplayView.isHidden = false
                    self.quickReplayView.isHidden = false
                    //: self.quickReplayView.refreshQuickReplayView(msgInfo: msgInfo)
                    self.quickReplayView.paintData(msgInfo: msgInfo)
                }
            }
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func permissionRequire() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: k_denseCompositionName, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: app_keepId - constPutMsg, width: notiExploreKey, height: app_keepId)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: notiExploreKey,
                                     //: height: ScreenHeight)
                                     height: app_keepId)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func orange(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.congressionalFor() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: EmptyBelow.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: app_keepId - constPutMsg,
                                     //: width: ScreenWidth,
                                     width: notiExploreKey,
                                     //: height: ScreenHeight)
                                     height: app_keepId)
            //: } completion: { finish in
        } completion: { finish in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension BoundSmallThin {
    /// 创建UI
    //: private func createUI() {
    private func report() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor.clear
            msgView.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgView.tableView.backgroundColor = UIColor(hex: (String(notiDistanceName)))
        }
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(rechargeView)
            view.addSubview(rechargeView)
            //: view.addSubview(quickReplayView)
            view.addSubview(quickReplayView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(const_administrativeValue) + String(noti_hisVoiceStr)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func duringVariable() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? constPutMsg : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.boaEffect(width: contentView.width,
                                  //: height: contentView.height,
                                  height: contentView.height,
                                  //: corners: [.topLeft, .topRight],
                                  corners: [.topLeft, .topRight],
                                  //: cornerRadii: .init(width: 8, height: 8))
                                  cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? dataTranslateKey : mainLightMessage)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: rechargeView.snp.makeConstraints { make in
            rechargeView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(rechargeView.ContentView_H)
                make.height.equalTo(rechargeView.ContentView_H)
            }
            //: quickReplayView.snp.makeConstraints { make in
            quickReplayView.snp.makeConstraints { make in
                //: make.leading.equalTo(24)
                make.leading.equalTo(24)
                //: make.width.equalTo(270)
                make.width.equalTo(270)
                //: make.bottom.equalTo(rechargeView.snp.top).offset(-13)
                make.bottom.equalTo(rechargeView.snp.top).offset(-13)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? dataTranslateKey : mainLightMessage)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + constPublisherValue)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func noSituation() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.yearAcross()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.mention()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.schedule(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                k_frameUrl.trainEssayDrownAccuseResMaxMessage(eventID: notiEffectSourceString, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
