
//: Declare String Begin

/*: "CacheIntimatePhotoUnlockMsgIdArrayKey" :*/
fileprivate let main_toolId:[UInt8] = [0x51,0x73,0x71,0x7a,0x77,0x5b,0x7c,0x66,0x7b,0x7f,0x73,0x66,0x77,0x42,0x7a,0x7d,0x66,0x7d,0x47,0x7c,0x7e,0x7d,0x71,0x79,0x5f,0x61,0x75,0x5b,0x76,0x53,0x60,0x60,0x73,0x6b,0x59,0x77,0x6b]

private func roundingExisting(compute num: UInt8) -> UInt8 {
    return num ^ 18
}

/*: "CacheQuestionReplayMsgIdIdArrayKey" :*/
fileprivate let userUnableTitle:[UInt8] = [0xec,0xce,0xcc,0xc7,0xca,0xfe,0xda,0xca,0xdc,0xdb,0xc6,0xc0,0xc1,0xfd,0xca,0xdf,0xc3,0xce,0xd6,0xe2,0xdc,0xc8,0xe6,0xcb,0xe6,0xcb,0xee,0xdd,0xdd,0xce,0xd6,0xe4,0xca,0xd6]

private func analysisPure(beat num: UInt8) -> UInt8 {
    return num ^ 175
}

/*: "CachePrivateChatVideoTipsIsShow_ :*/
fileprivate let const_mysteryViaTitle:[UInt8] = [0xd7,0xf5,0xf7,0xfc,0xf9,0xe4,0x6,0xfd,0xa,0xf5,0x8,0xf9,0xd7,0xfc,0xf5,0x8,0xea,0xfd,0xf8,0xf9,0x3,0xe8,0xfd,0x4,0x7,0xdd,0x7,0xe7,0xfc,0x3,0xb,0xf3]

fileprivate func errWake(through num: UInt8) -> UInt8 {
    let value = Int(num) - 148
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Quick greeting can only be sent to him once" :*/
fileprivate let k_olaData:[UInt8] = [0xe6,0xc2,0xde,0xd4,0xdc,0x97,0xd0,0xc5,0xd2,0xd2,0xc3,0xde,0xd9,0xd0,0x97,0xd4,0xd6,0xd9,0x97,0xd8,0xd9,0xdb,0xce,0x97,0xd5,0xd2,0x97,0xc4,0xd2,0xd9,0xc3,0x97,0xc3,0xd8,0x97,0xdf,0xde,0xda,0x97,0xd8,0xd9,0xd4,0xd2]

private func numberCheck(very num: UInt8) -> UInt8 {
    return num ^ 183
}

/*: "txt" :*/
fileprivate let notiProvisionUrl:[Character] = ["t","x","t"]

/*: "audio" :*/
fileprivate let constAgentValue:String = "audposition"

/*: "Please add greeting text" :*/
fileprivate let app_tingString:String = "plat copyPleas"
fileprivate let kSpineString:String = "rreplacementreplacementti"

/*: "You have not set up a photo greeting, please set it up before sending!" :*/
fileprivate let notiSignificantFormat:[UInt8] = [0xc5,0xf3,0xe9,0xbc,0xf4,0xfd,0xea,0xf9,0xbc,0xf2,0xf3,0xe8,0xbc,0xef,0xf9,0xe8,0xbc,0xe9,0xec,0xbc,0xfd,0xbc,0xec,0xf4,0xf3,0xe8,0xf3,0xbc,0xfb,0xee,0xf9,0xf9,0xe8,0xf5,0xf2,0xfb,0xb0,0xbc,0xec,0xf0,0xf9,0xfd,0xef,0xf9,0xbc,0xef,0xf9,0xe8,0xbc,0xf5,0xe8,0xbc,0xe9,0xec,0xbc,0xfe,0xf9,0xfa,0xf3,0xee,0xf9,0xbc,0xef,0xf9,0xf2,0xf8,0xf5,0xf2,0xfb,0xbd]

/*: "Cancel" :*/
fileprivate let data_extentGameData:String = "Cancelmovement ceiling"

/*: "Go to set" :*/
fileprivate let showAdditionConValue:[Character] = ["G","o"," ","t","o"," ","s","e","t"]

/*: "#startTime#" :*/
fileprivate let app_violationPath:String = "place question#star"

/*: "#endTime#" :*/
fileprivate let kHostString:String = "inmate pass beside right#endTime"
fileprivate let show_significantKey:[Character] = ["#"]

/*: "replayId" :*/
fileprivate let mainByFormat:[Character] = ["r","e","p","l","a","y","I","d"]

/*: "text" :*/
fileprivate let constRowAppropriateMsg:[Character] = ["t","e","x","t"]

/*: "img" :*/
fileprivate let appHighlightMsg:String = "imtypical"

/*: "video" :*/
fileprivate let notiOutsideNetMsg:String = "VIDEO"

/*: "gift" :*/
fileprivate let k_locationId:String = "turnft"

/*: "PrivateChat 点击引用消息 :*/
fileprivate let notiRocketValue:[Character] = ["P","r","i","v","a","t","e","C","h","a","t"," ","点","击","\u{5f15}","用"]
fileprivate let userStopData:[Character] = ["消","息"]

/*: "Sent " :*/
fileprivate let kHomeStr:String = "Sent shall draw normally scope"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SustainableSaveerGift.swift
//  AbroadTalking
//
//  Created by young on 2023/1/5.
//

//: import UIKit
import UIKit

/// 一键打招呼Block
//: typealias QuickGreetingBlock = (_ errorCode: Int?, _ errorMsg: String?, _ textDict: [String: Any]?, _ audioDict: [String: Any]?) -> Void
typealias QuickGreetingBlock = (_ errorCode: Int?, _ errorMsg: String?, _ textDict: [String: Any]?, _ audioDict: [String: Any]?) -> Void

/// 图片打招呼Block
//: typealias BeautyPhotosBlock = (_ isPushSet: Bool, _ PhotoDict: [String: Any]?) -> Void
typealias BeautyPhotosBlock = (_ isPushSet: Bool, _ PhotoDict: [String: Any]?) -> Void

//: public class TalkingPrivateChatManager: NSObject {
public class SustainableSaveerGift: NSObject {
    /// 记录私信对端已读回执时间 [key：userID，value：对端最后已读回执时间]
    //: var readReceiptDict = [String: Int]()
    var readReceiptDict = [String: Int]()

    //: @objc static let share = TalkingPrivateChatManager()
    @objc static let share = SustainableSaveerGift()
    //: private override init() {}
    override private init() {}
    // 私密照片已解锁礼物消息数组的key
    //: @objc let UnlockMsgIdArrayKey = "CacheIntimatePhotoUnlockMsgIdArrayKey"
    @objc let UnlockMsgIdArrayKey = String(bytes: main_toolId.map{roundingExisting(compute: $0)}, encoding: .utf8)!
    // 问题回答消息已回复消息数组的key
    //: let QuestionReplayMsgIdIdArrayKey = "CacheQuestionReplayMsgIdIdArrayKey"
    let QuestionReplayMsgIdIdArrayKey = String(bytes: userUnableTitle.map{analysisPure(beat: $0)}, encoding: .utf8)!
}

// MARK: - 插入音视频通话提示消息

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    /// 判断是否需要插入音视频通话提示消息
    /// - Parameters:
    ///   - toUid: 对方Id
    ///   - msgView: TUI视图
    ///   - completionHandler: 回调
    //: static func chat_privateChatVideo_insertTipMsg(toUid: String, msgView: TUIMessageController, needInsertHandler: (() -> Void)?) {
    static func length(toUid: String, msgView: NameViewController, needInsertHandler: (() -> Void)?) {
        // 非审核模式
        //: guard VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue else { return }
        // 男性
        //: guard VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue else { return }

        // 是否展示过提示文案
        //: let TalkingPrivateChatVideoTipsIsShow = "CachePrivateChatVideoTipsIsShow_\(String(VideoMagnitudeerval.share.loginUserMode.userID))_\(toUid)"
        let TalkingPrivateChatVideoTipsIsShow = String(bytes: const_mysteryViaTitle.map{errWake(through: $0)}, encoding: .utf8)! + "\(String(VideoMagnitudeerval.share.loginUserMode.userID))_\(toUid)"
        //: let isShow = Defaults.bool(forKey: TalkingPrivateChatVideoTipsIsShow)
        let isShow = appEfficiencyStorageStr.bool(forKey: TalkingPrivateChatVideoTipsIsShow)
        //: guard isShow == false else { return }
        guard isShow == false else { return }

        // 消息达到5条
        //: guard msgView.uiMsgs.count >= 5 else { return }
        guard msgView.uiMsgs.count >= 5 else { return }

        //: var hasVoiceVideo = false
        var hasVoiceVideo = false // 是否有音视频通话消息
        //: var outgoingMsgCount = 0
        var outgoingMsgCount = 0 // 消息发送方已发送消息数量
        //: for indexModel in msgView.uiMsgs {
        for indexModel in msgView.uiMsgs {
            //: let indexModel = indexModel as? TaskAction
            let indexModel = indexModel as? TaskAction
            //: if indexModel != nil {
            if indexModel != nil {
                //: if indexModel!.isKind(of: ImageAngle.self) {
                if indexModel!.isKind(of: ImageAngle.self) { // 是否包含音视频文本
                    //: let textMsgModel = indexModel as! ImageAngle
                    let textMsgModel = indexModel as! ImageAngle
                    //: if textMsgModel.isVoiceVideo == true {
                    if textMsgModel.isVoiceVideo == true {
                        //: hasVoiceVideo = true
                        hasVoiceVideo = true
                        //: break
                        break
                    }
                }

                // 语音消息 || 文本消息 || 图片消息 || 视频消息
                //: if indexModel!.isKind(of: BoxOrangeDistribution.self) ||
                if indexModel!.isKind(of: BoxOrangeDistribution.self) ||
                    //: indexModel!.isKind(of: ImageAngle.self) ||
                    indexModel!.isKind(of: ImageAngle.self) ||
                    //: indexModel!.isKind(of: MenuAdjust.self) ||
                    indexModel!.isKind(of: MenuAdjust.self) ||
                    //: indexModel!.isKind(of: SharedImplementation.self) {
                    indexModel!.isKind(of: SharedImplementation.self)
                {
                    //: if indexModel?.direction == .MsgDirectionOutgoing {
                    if indexModel?.direction == .MsgDirectionOutgoing { // 消息发送方
                        //: outgoingMsgCount += 1
                        outgoingMsgCount += 1
                    }
                }
            }
        }

        //: guard hasVoiceVideo == false else {
        guard hasVoiceVideo == false else {
            //: Defaults.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            appEfficiencyStorageStr.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            //: return
            return
        }
        //: guard outgoingMsgCount >= 5 else { return }
        guard outgoingMsgCount >= 5 else { return }

        // 回调外部插入音视频提示消息
        //: if needInsertHandler != nil {
        if needInsertHandler != nil {
            //: needInsertHandler!()
            needInsertHandler!()
            //: Defaults.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            appEfficiencyStorageStr.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
        }
    }
}

// MARK: - 一键打招呼

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    /// 判断是否可以一键打招呼，回调结果
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - handler: errorCode：1 = 已发送过；2=未添加打招呼数据;
    //: static func chat_sendQuickGreeting(toUserId: String, completionHandler: @escaping QuickGreetingBlock) {
    static func singleThin(toUserId: String, completionHandler: @escaping QuickGreetingBlock) {
        //: let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)_\(TalkingSendAutoGreetIsToKey)_\(toUserId)"
        let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)_\(main_orangeTitle)_\(toUserId)"
        //: let ishave = UserDefaults.standard.bool(forKey: key)
        let ishave = UserDefaults.standard.bool(forKey: key)
        //: guard !ishave else {
        guard !ishave else {
            //: completionHandler(1, "Quick greeting can only be sent to him once".localized, nil, nil)
            completionHandler(1, String(bytes: k_olaData.map{numberCheck(very: $0)}, encoding: .utf8)!.localized, nil, nil)
            //: return
            return
        }

        //: ProgressHUD.show()
        MediaSquare.saveHead()
        //: TalkingChatRequestTool.req_getAutoGreetContent { succeed, result, errorModel in
        AnalyzeMapCommon.densityThat { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            MediaSquare.spell()
            //: guard succeed == true else {
            guard succeed == true else {
                //: completionHandler(2, errorModel?.errorMsg ?? "", nil, nil)
                completionHandler(2, errorModel?.errorMsg ?? "", nil, nil)
                //: return
                return
            }
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: completionHandler(2, "", nil, nil)
                completionHandler(2, "", nil, nil)
                //: return
                return
            }
            //: let textDict = dict["txt"] as? [String: Any]
            let textDict = dict[(String(notiProvisionUrl))] as? [String: Any]
            //: let audioDict = dict["audio"] as? [String: Any]
            let audioDict = dict[(constAgentValue.replacingOccurrences(of: "position", with: "io"))] as? [String: Any]
            //: if textDict?.count == 0 && audioDict?.count == 0 {
            if textDict?.count == 0, audioDict?.count == 0 {
                //: completionHandler(2, "Please add greeting text".localized, nil, nil)
                completionHandler(2, (String(app_tingString.suffix(5)) + "e add g" + kSpineString.replacingOccurrences(of: "replacement", with: "e") + "ng text").localized, nil, nil)
                //: return
                return
            }

            // 一键打招呼本地存储
            //: chat_changeSaveGreetData(toUserId: toUserId, isHave: true)
            space(toUserId: toUserId, isHave: true)
            //: completionHandler(nil, nil, textDict, audioDict)
            completionHandler(nil, nil, textDict, audioDict)
        }
    }

    /// 一键打招呼状态本地存储
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - isHave: 是否已发送
    //: static func chat_changeSaveGreetData(toUserId: String, isHave: Bool) {
    static func space(toUserId: String, isHave: Bool) {
        // 一键打招呼本地cunc
        //: let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)_\(TalkingSendAutoGreetIsToKey)_\(toUserId)"
        let key = "\(VideoMagnitudeerval.share.loginUserMode.userID)_\(main_orangeTitle)_\(toUserId)"
        //: UserDefaults.standard.set(isHave, forKey: key)
        UserDefaults.standard.set(isHave, forKey: key)
    }

    /// 图片打招呼
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - handler: errorCode：1 = 已发送过；2=未添加打招呼数据；
    //: static func chat_sendBeautyPhotos(toUserId: String, completionHandler: @escaping BeautyPhotosBlock) {
    static func host(toUserId: String, completionHandler: @escaping BeautyPhotosBlock) {
        //: ProgressHUD.show()
        MediaSquare.saveHead()
        //: TalkingChatRequestTool.req_getIntimatePhoto(toUid: toUserId) { succeed, result, errorModel in
        AnalyzeMapCommon.observerBy(toUid: toUserId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            MediaSquare.spell()
            /// 未添加打招呼图片
            //: if errorModel?.errorCode == 111 {
            if errorModel?.errorCode == 111 {
                //: let config = ShowAlertConfig()
                let config = ImpactMobileContainer()
                //: config.alignment = .center
                config.alignment = .center
                //: if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
                if ImmediateSumensityObserver.share.interfaceLang == RouteCritical.pt.rawValue {
                    //: config.width = 330
                    config.width = 330
                }
                //: TalkingAlertShow.customAlert(message: "You have not set up a photo greeting, please set it up before sending!".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Go to set".localized, leftBlock: {
                TapBounce.himLeave(message: String(bytes: notiSignificantFormat.map{$0^156}, encoding: .utf8)!.localized, leftBtnTitle: (String(data_extentGameData.prefix(6))).localized, rightBtnTitle: (String(showAdditionConValue)).localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    TapBounce.outConvert()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: TalkingAlertShow.hideAlert()
                    TapBounce.outConvert()
                    //: completionHandler(true, nil)
                    completionHandler(true, nil)
                    //: }, config: config)
                }, config: config)
                //: return
                return
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? [String: Any] else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: completionHandler(false, dict)
                completionHandler(false, dict)
            }
        }
    }
}

// MARK: - 插入与官方客服聊天提示消息

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    ///  判断是否需要插入与官方客服聊天提示消息
    /// - Parameter needInsertHandler: 回调
    //: static func chat_customerServiceCenter_insertTipMsg(needInsertHandler: ((_ tipsStr: String?) -> Void)?) {
    static func mBy(needInsertHandler: ((_ tipsStr: String?) -> Void)?) {
        //: guard VideoMagnitudeerval.share.appConfigMode.CSConfig.systemTips.count > 0 else {
        guard VideoMagnitudeerval.share.appConfigMode.CSConfig.systemTips.count > 0 else { // 接口没下发，无需提示
            //: if needInsertHandler != nil {
            if needInsertHandler != nil {
                //: needInsertHandler!(nil)
                needInsertHandler!(nil)
            }
            //: return
            return
        }

        // 根据设备所在时区转换时间
        //: let startTime = NSDate.getCurrentZoneDateString(shDateStr: VideoMagnitudeerval.share.appConfigMode.CSConfig.startTime)
        let startTime = NSDate.switchicial(shDateStr: VideoMagnitudeerval.share.appConfigMode.CSConfig.startTime)
        //: let endTime = NSDate.getCurrentZoneDateString(shDateStr: VideoMagnitudeerval.share.appConfigMode.CSConfig.endTime)
        let endTime = NSDate.switchicial(shDateStr: VideoMagnitudeerval.share.appConfigMode.CSConfig.endTime)
        //: let isIncluded = NSDate.compareCurrentTimeIsIncluded(startTime: startTime, endTime: endTime)
        let isIncluded = NSDate.whenTension(startTime: startTime, endTime: endTime)

        //: guard isIncluded == false else {
        guard isIncluded == false else {
            //: if needInsertHandler != nil {
            if needInsertHandler != nil {
                //: needInsertHandler!(nil)
                needInsertHandler!(nil)
            }
            //: return
            return
        }

        // 非工作时间段内，添加提示
        //: var tips = VideoMagnitudeerval.share.appConfigMode.CSConfig.systemTips
        var tips = VideoMagnitudeerval.share.appConfigMode.CSConfig.systemTips
        //: tips = tips.replacingOccurrences(of: "#startTime#", with: "\(startTime)")
        tips = tips.replacingOccurrences(of: (String(app_violationPath.suffix(5)) + "tTime#"), with: "\(startTime)")
        //: tips = tips.replacingOccurrences(of: "#endTime#", with: "\(endTime)")
        tips = tips.replacingOccurrences(of: (String(kHostString.suffix(8)) + String(show_significantKey)), with: "\(endTime)")
        //: if needInsertHandler != nil {
        if needInsertHandler != nil {
            //: needInsertHandler!(tips)
            needInsertHandler!(tips)
        }
    }
}

// MARK: - 刷新私密消息（照片、视频）展示状态

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    /// 接收到解锁私密消息（照片、视频）的礼物，刷新本地消息状态
    /// - Parameters:
    ///   - cellData: 数据
    ///   - msgView: TUI视图
    //: static func chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: TaskAction, msgView: TUIMessageController) {
    static func layerPaint(cellData: TaskAction, msgView: NameViewController) {
        //: guard msgView.uiMsgs.count > 0 else {
        guard msgView.uiMsgs.count > 0 else {
            // --------- 【外部页面】收到了解锁礼物消息 ---------
            // 首次进入到当前页面，刷新消息状态
            //: var array = Defaults.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String]
            var array = appEfficiencyStorageStr.object(forKey: SustainableSaveerGift.share.UnlockMsgIdArrayKey) as? [String]

            // 本地有私密消息解锁礼物缓存，且是图片消息 或 视频消息
            //: guard array?.count ?? 0 > 0 &&
            guard array?.count ?? 0 > 0,
                  //: (cellData.isKind(of: MenuAdjust.self) ||
                  cellData.isKind(of: MenuAdjust.self) ||
                  //: cellData.isKind(of: SharedImplementation.self)) else { return }
                  cellData.isKind(of: SharedImplementation.self) else { return }

            // 找到本人发送的私密消息，更新状态为已解锁
            //: if array!.contains(cellData.msgModel.msgInfo.msgId) {
            if array!.contains(cellData.msgModel.msgInfo.msgId) {
                //: inner_changeIntimateMsg(cellData, msgView)
                undertake(cellData, msgView)

                // 更新本地缓存
                //: array!.removeAll(where: { $0 == cellData.msgModel.msgInfo.msgId })
                array!.removeAll(where: { $0 == cellData.msgModel.msgInfo.msgId })
                //: Defaults.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
                appEfficiencyStorageStr.set(array, forKey: SustainableSaveerGift.share.UnlockMsgIdArrayKey)
            }

            //: return
            return
        }

        //  --------- 在【当前页面】收到解锁礼物消息 ---------
        // 只有收到礼物解锁消息才刷新
        //: guard cellData.isKind(of: QuantityegrityMax.self) && cellData.msgModel.gift.unlockMsgId.count > 0 else { return }
        guard cellData.isKind(of: QuantityegrityMax.self), cellData.msgModel.gift.unlockMsgId.count > 0 else { return }

        // 找到本人发送的私密消息，更新状态为已解锁
        //: let unlockMsgId = cellData.msgModel.gift.unlockMsgId
        let unlockMsgId = cellData.msgModel.gift.unlockMsgId
        //: for msgCellData in msgView.uiMsgs {
        for msgCellData in msgView.uiMsgs {
            //: let data = msgCellData as! TaskAction
            let data = msgCellData as! TaskAction
            //: if data.msgModel.msgInfo.msgId == unlockMsgId {
            if data.msgModel.msgInfo.msgId == unlockMsgId {
                //: inner_changeIntimateMsg(data, msgView)
                undertake(data, msgView)
                //: break
                break
            }
        }
    }

    /// 刷新本地消息状态
    /// - Parameters:
    ///   - cellData: 数据
    ///   - msgView: TUI视图
    //: static private func inner_changeIntimateMsg(_ cellData: TaskAction, _ msgView: TUIMessageController) {
    private static func undertake(_ cellData: TaskAction, _ msgView: NameViewController) {
        //: if cellData.isKind(of: MenuAdjust.self) {
        if cellData.isKind(of: MenuAdjust.self) { // 私密照片
            //: let model = cellData.msgModel.msgInfo.intimatePhoto
            let model = cellData.msgModel.msgInfo.intimatePhoto
            //: if model.lockStatus != 0 {
            if model.lockStatus != 0 {
                //: model.lockStatus = 0
                model.lockStatus = 0
                //: msgView.changeIntimatePhotoMsg(cellData, photoModel: model)
                msgView.characteristic(cellData, angleAttribute: model)
            }

            //: } else if cellData.isKind(of: SharedImplementation.self) {
        } else if cellData.isKind(of: SharedImplementation.self) { // 私密视频
            //: let model = cellData.msgModel.msgInfo.video
            let model = cellData.msgModel.msgInfo.video
            //: if model.lockStatus != 0 {
            if model.lockStatus != 0 {
                //: model.lockStatus = 0
                model.lockStatus = 0
                //: msgView.changeIntimateVideoMsg(cellData, videoModel: model)
                msgView.effectElement(cellData, embrace: model)
            }
        }
    }
}

// MARK: - 问题消息快捷回复

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    /// 接收到问题回答消息，判断是否需要展示消息快捷回复弹窗
    /// - Parameters:
    ///   - isRecharge: 是否充值
    ///   - msgView: TUI视图
    ///   - handler: 回调
    //: static func chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: Bool,
    static func switchd(isRecharge: Bool,
                        //: msgView: TUIMessageController,
                        msgView: NameViewController,
                        //: handler: ((AbTalkingChatMsgInfoModel?) -> Void)?) {
                        handler: ((DepthArchitectureModel?) -> Void)?)
    {
        //: guard VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue else { return }
        //: guard isRecharge == false else { return }
        guard isRecharge == false else { return }
        // 获取已处理消息ID数组
        //: let cacheMsgIds = Defaults.object(forKey: TalkingPrivateChatManager.share.QuestionReplayMsgIdIdArrayKey) as? [String] ?? []
        let cacheMsgIds = appEfficiencyStorageStr.object(forKey: SustainableSaveerGift.share.QuestionReplayMsgIdIdArrayKey) as? [String] ?? []
        //: for case let msgModel as TaskAction in msgView.uiMsgs.reversed() {
        for case let msgModel as TaskAction in msgView.uiMsgs.reversed() {
            //: let msgInfo = msgModel.msgModel.msgInfo
            let msgInfo = msgModel.msgModel.msgInfo
            //: if msgInfo.isEnd && msgInfo.replay.isEmpty {
            if msgInfo.isEnd, msgInfo.replay.isEmpty { // 问答已结束
                //: handler?(nil)
                handler?(nil)
                //: return
                return
            }
            //: if msgInfo.isEnd && !msgInfo.replay.isEmpty && !cacheMsgIds.contains(msgInfo.msgId) {
            if msgInfo.isEnd, !msgInfo.replay.isEmpty, !cacheMsgIds.contains(msgInfo.msgId) { // 找到符合条件问答消息
                //: handler?(msgInfo)
                handler?(msgInfo)
                //: return
                return
            }
        }
        //: handler?(nil)
        handler?(nil)
    }
}

// MARK: - 未回复Reply to get points~逻辑

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    /// 是否是回复获取积分消息
    /// - Parameters:
    ///   - direction: 消息方向
    ///   - msgType: 消息类型
    ///   - msgTime: 消息时间
    /// - Returns: 结果
    //: static func isReplyToGetPointsMsg(_ direction: TMsgDirection, _ msgType: Int, msgTime: Date?) -> Bool {
    static func translation(_ direction: RenderLibrary, _ msgType: Int, msgTime: Date?) -> Bool {
        // 非审核模式, messageType == 5, 时间<24小时
        //: guard VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else { return false }
        guard VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue else { return false }
        //: guard direction == .MsgDirectionIncoming else { return false }
        guard direction == .MsgDirectionIncoming else { return false }
        //: guard msgType == 5 else { return false }
        guard msgType == 5 else { return false }
        //: guard let msgTime = msgTime else { return false }
        guard let msgTime = msgTime else { return false }
        //: guard Date().timeIntervalSince(msgTime) < 24*60*60 else { return false }
        guard Date().timeIntervalSince(msgTime) < 24 * 60 * 60 else { return false }
        //: return true
        return true
    }
}

// MARK: - VIP展示消息已读逻辑

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    /// 是否需要插入已读回执引导充值VIP提醒消息
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func needInsertVIPReadReceiptMsg(cellData: TaskAction) -> Bool {
    static func remote(cellData: TaskAction) -> Bool {
        //: if VideoMagnitudeerval.share.loginUserMode.loungePlus == false,
        if VideoMagnitudeerval.share.loginUserMode.loungePlus == false,
           //: cellData.innerMessage.groupID == nil,
           cellData.innerMessage.groupID == nil,
           //: cellData.status == .Msg_Status_Succ,
           cellData.status == .Msg_Status_Succ,
           //: cellData.direction == .MsgDirectionOutgoing,
           cellData.direction == .MsgDirectionOutgoing,
           //: cellData.msgModel.msgInfo.callCmd.count <= 0 {
           cellData.msgModel.msgInfo.callCmd.count <= 0
        {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 是否展示消息 已读/未读 标识
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func isShowReadMsg(cellData: TaskAction) -> Bool {
    static func globalAcross(cellData: TaskAction) -> Bool {
        // vip && 非群聊 && 发送成功 && 本人发送 && 非音视频通话文本消息，展示消息是否已读标识
        //: if VideoMagnitudeerval.share.loginUserMode.loungePlus == true,
        if VideoMagnitudeerval.share.loginUserMode.loungePlus == true,
           //: cellData.innerMessage.groupID == nil,
           cellData.innerMessage.groupID == nil,
           //: cellData.status == .Msg_Status_Succ,
           cellData.status == .Msg_Status_Succ,
           //: cellData.direction == .MsgDirectionOutgoing,
           cellData.direction == .MsgDirectionOutgoing,
           //: cellData.msgModel.msgInfo.callCmd.count <= 0 {
           cellData.msgModel.msgInfo.callCmd.count <= 0
        {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 消息对方是否已读
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func msgIsRead(cellData: TaskAction) -> Bool {
    static func since(cellData: TaskAction) -> Bool {
        //: if cellData.msgModel.params["replayId"] != nil { // 问题回答消息默认已读
        if cellData.msgModel.params[(String(mainByFormat))] != nil { // 问题回答消息默认已读
            //: return true
            return true
        }
        //: guard let selfMsgTime = cellData.innerMessage.timestamp else { return false }
        guard let selfMsgTime = cellData.innerMessage.timestamp else { return false }
        //: if cellData.innerMessage.isPeerRead == true {
        if cellData.innerMessage.isPeerRead == true {
            //: return true
            return true
        }
        //: let readTimeInterval = TalkingPrivateChatManager.share.readReceiptDict[cellData.innerMessage.userID] ?? 0
        let readTimeInterval = SustainableSaveerGift.share.readReceiptDict[cellData.innerMessage.userID] ?? 0
        //: let readTime = Date(timeIntervalSince1970: TimeInterval(readTimeInterval))
        let readTime = Date(timeIntervalSince1970: TimeInterval(readTimeInterval))
        //: if selfMsgTime <= readTime {
        if selfMsgTime <= readTime {
            //: return true
            return true
        }
        //: return false
        return false
    }
}

// MARK: - Reply消息逻辑

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    /// 回复消息
    /// - Parameters:
    ///   - cellData: 数据
    ///   - targetId: 对方uid
    /// - Returns: 引用消息模型
    //: static func handleReplyMsg(cellData: TaskAction, targetId: String) -> AbTalkingChatMsgQuoteModel {
    static func memberWithinAsset(cellData: TaskAction, targetId: String) -> SourceShadowExceptionModel {
        //: let quoteModel = AbTalkingChatMsgQuoteModel()
        let quoteModel = SourceShadowExceptionModel()
        //: quoteModel.msgId = cellData.msgModel.msgInfo.msgId
        quoteModel.msgId = cellData.msgModel.msgInfo.msgId
        //: if cellData.direction == .MsgDirectionIncoming {
        if cellData.direction == .MsgDirectionIncoming {
            //: quoteModel.uid = targetId
            quoteModel.uid = targetId
            //: } else {
        } else {
            //: quoteModel.uid = VideoMagnitudeerval.share.loginUserMode.userID
            quoteModel.uid = VideoMagnitudeerval.share.loginUserMode.userID
        }
        //: let renderData = AbTalkingChatMsgQuoteDataModel()
        let renderData = EquipmentModel()
        //: if cellData.isKind(of: ImageAngle.self) {
        if cellData.isKind(of: ImageAngle.self) {
            //: quoteModel.renderType = "text"
            quoteModel.renderType = (String(constRowAppropriateMsg))
            //: renderData.content = cellData.msgModel.msgInfo.content
            renderData.content = cellData.msgModel.msgInfo.content

            //: } else if cellData.isKind(of: MenuAdjust.self) {
        } else if cellData.isKind(of: MenuAdjust.self) {
            //: quoteModel.renderType = "img"
            quoteModel.renderType = (appHighlightMsg.replacingOccurrences(of: "typical", with: "g"))
            //: renderData.previewUrl = cellData.msgModel.msgInfo.imageUri
            renderData.previewUrl = cellData.msgModel.msgInfo.imageUri

            //: } else if cellData.isKind(of: SharedImplementation.self) {
        } else if cellData.isKind(of: SharedImplementation.self) {
            //: quoteModel.renderType = "video"
            quoteModel.renderType = (notiOutsideNetMsg.lowercased())
            //: renderData.videoUrl = cellData.msgModel.msgInfo.video.videoUrl
            renderData.videoUrl = cellData.msgModel.msgInfo.video.videoUrl
            //: renderData.coverUrl = cellData.msgModel.msgInfo.video.coverImg
            renderData.coverUrl = cellData.msgModel.msgInfo.video.coverImg

            //: } else if cellData.isKind(of: BoxOrangeDistribution.self) {
        } else if cellData.isKind(of: BoxOrangeDistribution.self) {
            //: let audioCelldata = cellData as! BoxOrangeDistribution
            let audioCelldata = cellData as! BoxOrangeDistribution
            //: quoteModel.renderType = "audio"
            quoteModel.renderType = (constAgentValue.replacingOccurrences(of: "position", with: "io"))
            //: let voiceCache = DBUserInfoManager.cache_getAudioDB(message: audioCelldata.innerMessage)
            let voiceCache = FlexDivide.wrapConnect(message: audioCelldata.innerMessage)
            //: renderData.duration = Int(voiceCache.db_audioLength) ?? 0
            renderData.duration = Int(voiceCache.db_audioLength) ?? 0

            //: } else if cellData.isKind(of: QuantityegrityMax.self) {
        } else if cellData.isKind(of: QuantityegrityMax.self) {
            //: quoteModel.renderType = "gift"
            quoteModel.renderType = (k_locationId.replacingOccurrences(of: "turn", with: "gi"))
            //: let giftCellData = cellData as! QuantityegrityMax
            let giftCellData = cellData as! QuantityegrityMax
            //: renderData.giftName = giftCellData.msgModel.gift.name
            renderData.giftName = giftCellData.msgModel.gift.name
            //: renderData.giftNameI18n = giftCellData.msgModel.gift.nameI18n
            renderData.giftNameI18n = giftCellData.msgModel.gift.nameI18n
            //: renderData.giftIcon = giftCellData.msgModel.gift.imgPreview
            renderData.giftIcon = giftCellData.msgModel.gift.imgPreview
            //: renderData.giftPrice = giftCellData.msgModel.gift.price
            renderData.giftPrice = giftCellData.msgModel.gift.price
            //: renderData.giftNum = Int(giftCellData.msgModel.gift.num) ?? 0
            renderData.giftNum = Int(giftCellData.msgModel.gift.num) ?? 0
        }

        //: quoteModel.renderData = renderData
        quoteModel.renderData = renderData
        //: return quoteModel
        return quoteModel
    }

    /// 点击“引用”消息
    /// - Parameters:
    ///   - cellData: 数据
    ///   - targetId: 对方uid
    //: static func handleClickReplyMsg(cellData: TaskAction, targetId: String) {
    static func failure(cellData: TaskAction, targetId: String) {
        //: let renderData = cellData.msgModel.quoteMsgInfo
        let renderData = cellData.msgModel.quoteMsgInfo
        //: UploadLogTool.writeLog(msg: "PrivateChat 点击引用消息\(renderData.renderType).")
        TrainGreat.bitRemove(msg: (String(notiRocketValue) + String(userStopData)) + "\(renderData.renderType).")
        //: if renderData.renderType == "text" {
        if renderData.renderType == (String(constRowAppropriateMsg)) {
            //: let popView = QuoteDetailPopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let popView = SuccessMaterial(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
            //: popView.msgDetailText = renderData.renderData.content
            popView.msgDetailText = renderData.renderData.content
            //: popView.show()
            popView.cosRemote()

            //: } else if renderData.renderType == "img" {
        } else if renderData.renderType == (appHighlightMsg.replacingOccurrences(of: "typical", with: "g")) {
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(dataStateMessage)
            //: filePath = (filePath as NSString).appendingPathComponent((renderData.renderData.previewUrl as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((renderData.renderData.previewUrl as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = NumberensityMark()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: model.imgUrl =  renderData.renderData.previewUrl
                model.imgUrl = renderData.renderData.previewUrl
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = ExerciseListen()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [NumberensityMark] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = MagnitudeervalManagerFlex(momentModel: currenModel, index: 0, type: .normal)
            //: TalkingPrivateChatManager.share.getNavigationController()?.pushViewController(vc, animated: true)
            SustainableSaveerGift.share.priorityAssetSpace()?.pushViewController(vc, animated: true)

            //: } else if renderData.renderType == "video" {
        } else if renderData.renderType == (notiOutsideNetMsg.lowercased()) {
            //: let videoPath = renderData.renderData.videoUrl
            let videoPath = renderData.renderData.videoUrl
            //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
            let vc = ConditionViewController(videoPath: videoPath)
            //: TalkingPrivateChatManager.share.getNavigationController()?.pushViewController(vc, animated: true)
            SustainableSaveerGift.share.priorityAssetSpace()?.pushViewController(vc, animated: true)

            //: } else if renderData.renderType == "audio" {
        } else if renderData.renderType == (constAgentValue.replacingOccurrences(of: "position", with: "io")) {
            //: let cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: renderData.msgId)
            let cacheWrap = DensityRedRender.distanceThroughBubble(with: renderData.msgId)
            //: let playModel = TalkingVoiceMsgPlayModel()
            let playModel = MediaMaster()
            //: if cacheWrap != nil {
            if cacheWrap != nil {
                //: playModel.db_voiceCacheWrap = cacheWrap!
                playModel.db_voiceCacheWrap = cacheWrap!

                //: } else {
            } else {
                //: let model = WCDBVoiceMsgTable()
                let model = DensityRedRender()
                //: model.msgId = renderData.msgId
                model.msgId = renderData.msgId
                //: model.db_voiceUri = renderData.renderData.url
                model.db_voiceUri = renderData.renderData.url
                //: model.db_senduid = renderData.uid
                model.db_senduid = renderData.uid
                //: if renderData.uid == VideoMagnitudeerval.share.loginUserMode.userID {
                if renderData.uid == VideoMagnitudeerval.share.loginUserMode.userID {
                    //: model.db_touid = targetId
                    model.db_touid = targetId
                    //: } else {
                } else {
                    //: model.db_touid = VideoMagnitudeerval.share.loginUserMode.userID
                    model.db_touid = VideoMagnitudeerval.share.loginUserMode.userID
                }
                //: model.db_audioLength = "\(renderData.renderData.duration)"
                model.db_audioLength = "\(renderData.renderData.duration)"
                //: WCDBVoiceMsgTable.db_insertVoiceMsg(model)
                DensityRedRender.embrace(model)
                //: playModel.db_voiceCacheWrap = model
                playModel.db_voiceCacheWrap = model
            }

            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            DepthFade.shared.homeRegulate()
            //: TalkingVoiceMsgPlayManager.shared.starPlayReplyMsg(playModel: playModel)
            DepthFade.shared.confirm(playModel: playModel)

            //: } else if renderData.renderType == "gift" {
        } else if renderData.renderType == (k_locationId.replacingOccurrences(of: "turn", with: "gi")) {
            //: let popView = QuoteDetailPopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let popView = SuccessMaterial(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
            //: popView.msgDetailText = "Sent ".localized + renderData.renderData.giftNameLocal() + " x\(renderData.renderData.giftNum)"
            popView.msgDetailText = (String(kHomeStr.prefix(5))).localized + renderData.renderData.beforePublisherPhase() + " x\(renderData.renderData.giftNum)"
            //: popView.show()
            popView.cosRemote()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatManager {
extension SustainableSaveerGift {
    /// 检测用户短视频版本是否可用【> = 1.1.6 可用】
    /// - Returns: 是否可用
    //: static func checkShortVideo(userVersion: String) -> Bool {
    static func overMain(userVersion: String) -> Bool {
        //: guard let version = Int(userVersion.replacingOccurrences(of: ".", with: "")) else {
        guard let version = Int(userVersion.replacingOccurrences(of: ".", with: "")) else {
            //: return false
            return false
        }
        //: if version*100 < 116*100 {
        if version * 100 < 116 * 100 {
            //: return false
            return false
        }

        //: return true
        return true
    }
}
