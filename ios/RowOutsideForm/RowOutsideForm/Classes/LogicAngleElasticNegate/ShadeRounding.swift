
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userUnlessUrl:[UInt8] = [0x71,0x76,0x71,0x6c,0x30,0x7b,0x77,0x7c,0x7d,0x6a,0x22,0x31,0x38,0x70,0x79,0x6b,0x38,0x76,0x77,0x6c,0x38,0x7a,0x7d,0x7d,0x76,0x38,0x71,0x75,0x68,0x74,0x7d,0x75,0x7d,0x76,0x6c,0x7d,0x7c]

private func agoWeird(publisher num: UInt8) -> UInt8 {
    return num ^ 24
}

/*: "uid" :*/
fileprivate let dataDecideValue:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let appPublishTitle:String = "hversion"
fileprivate let showFamilyGivingSucceedMessage:[Character] = ["a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let kHarassmentName:String = "creationick"

/*: "birthday" :*/
fileprivate let main_previousString:String = "birthdrawer"
fileprivate let k_boardKey:[Character] = ["a","y"]

/*: "videoUrl" :*/
fileprivate let app_ceilingPullKey:String = "seed short spendvideoUr"
fileprivate let show_primaryName:String = "obtain"

/*: "cover" :*/
fileprivate let constMemberMessage:String = "caliveer"

/*: "location" :*/
fileprivate let noti_sprinkleId:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let show_radioMessage:[UInt8] = [0x3e,0x30,0x39]

private func charmContinuing(menu num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "refuseCall" :*/
fileprivate let main_alongMessage:String = "refmutual"
fileprivate let data_plateMsg:String = "heartheart"

/*: "data" :*/
fileprivate let notiAccelerateConTitle:[UInt8] = [0xf7,0xf2,0xe7,0xf2]

private func clearComplaint(deploy num: UInt8) -> UInt8 {
    return num ^ 147
}

/*: "Call interruption" :*/
fileprivate let dataShouldFormat:String = "partner reduction calculate betweenCall "
fileprivate let k_agileStr:String = "ptiofamily"

/*: "stopCall" :*/
fileprivate let userFlexibleArrowUrl:String = "stopCallinvestigator disk without tip purpose"

/*: "logId" :*/
fileprivate let userPermanentString:[UInt8] = [0xc5,0xc8,0xc0,0xa2,0xbd]

fileprivate func modNumb(during num: UInt8) -> UInt8 {
    let value = Int(num) + 167
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let data_recordingExceptKey:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let k_techniqueValue:String = "Nonor disable template"
fileprivate let userDifficultData:String = "period inside network price addition answer"

/*: "duration" :*/
fileprivate let k_shareData:String = "duratisibling"

/*: "She has hung up" :*/
fileprivate let constTeenagePath:String = "yes administrative meeting overShe "
fileprivate let userConsumptionGemString:String = "pic original simultaneously acrosshas "

/*: "cancelSex" :*/
fileprivate let show_alongMessage:String = "cancelSeimpression status compare"
fileprivate let main_automaticString:[Character] = ["x"]

/*: "He has hung up" :*/
fileprivate let show_strokeTitle:[Character] = ["H","e"," ","h","a","s"," "]
fileprivate let constMapMentionKey:String = "hung upelement distinction context record"

/*: "endType" :*/
fileprivate let app_termsClearlyString:String = "E"
fileprivate let notiProName:[Character] = ["n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let kPerResistanceFormat:String = "unknown voidstopSex"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let main_belowPath:[UInt8] = [0x60,0x47,0x5a,0x5c,0x4f,0x4f,0x40,0x4a,0x40,0x4c,0x47,0x5d,0x9,0x4e,0x46,0x45,0x4d,0x9,0x4a,0x46,0x40,0x47,0x5a,0x5,0x9,0x4a,0x48,0x45,0x45,0x9,0x41,0x48,0x5a,0x9,0x4c,0x47,0x4d,0x4c,0x4d]

private func listIgnore(level num: UInt8) -> UInt8 {
    return num ^ 41
}

/*: "The other hung up the phone" :*/
fileprivate let noti_councilBeatName:String = "The ocap maker remaining project reading"
fileprivate let data_editData:String = "ung ulevel wed powerful tractor will"
fileprivate let userCommunicateSocialTitle:String = "quick detail var visit speakere phone"

/*: "type" :*/
fileprivate let const_oneString:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let dataClipTitle:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let k_specialData:String = "rem"
fileprivate let user_partnerTitle:String = "calculatecel"

/*: "Get gold coins" :*/
fileprivate let main_pureFormat:String = "Get gocategory city"
fileprivate let showSearchValue:String = "disable"
fileprivate let k_minimizeUrl:String = " coinsclosed slow train may"

/*: "refuseSex" :*/
fileprivate let noti_reminderName:String = "rowefu"
fileprivate let appPromptString:[Character] = ["s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let userExaminationValue:String = "onAccwake in memory"
fileprivate let show_popInmateTitle:String = "package"
fileprivate let showBetweenKey:String = "marker labptCall"

/*: "acceptCall" :*/
fileprivate let app_textQualityMessage:String = "ACCEP"
fileprivate let noti_placeMessage:String = "every awl within columntCall"

/*: "Calls are cancelled" :*/
fileprivate let mainCrucialKey:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n"]
fileprivate let showWithoutReportId:String = "ceplayed"

/*: "Continue matching with other users？" :*/
fileprivate let const_validValue:[UInt8] = [0xde,0xf2,0xf3,0xe9,0xf4,0xf3,0xe8,0xf8,0xbd,0xf0,0xfc,0xe9,0xfe,0xf5,0xf4,0xf3,0xfa,0xbd,0xea,0xf4,0xe9,0xf5,0xbd,0xf2,0xe9,0xf5,0xf8,0xef,0xbd,0xe8,0xee,0xf8,0xef,0xee,0x72,0x21,0x2]

private func weightWhileAgain(social num: UInt8) -> UInt8 {
    return num ^ 157
}

/*: "Yes" :*/
fileprivate let k_pressureMessage:String = "err across ionYes"

/*: "The call has ended" :*/
fileprivate let const_cameraStr:String = "in weight left subThe ca"
fileprivate let main_entertainmentMomentValue:String = "s encreation bass topic low"
fileprivate let const_spotValue:[Character] = ["d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ShadeRounding.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let userProcessFormat = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class ShadeRounding: MasterClip {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: RespectiveModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PerSmart? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: SThrough? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: SecondHideSpine? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        QuantityensityAction.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        QuantityensityAction.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        QuantityensityAction.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userUnlessUrl.map{agoWeird(publisher: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! FinishController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.totalensityMultiple()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            towardZone()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            taskOrCan(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.versusScopeVC = nil
            QuantityensityAction.shared.versusScopeVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.flexible()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        scriptMaster()
        //: setupSubViewsConstraint()
        shTranslate()
        //: initNotificationObserver()
        random()
        //: req_callGetUserInfo()
        theStripEvaluate()
        //: timeoutRules()
        socialWithoutChallenge()
        //: setUpBGMPlayer()
        everyOperate()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.announcement()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if VideoMagnitudeerval.share.appConfigMode.disableShootScreen {
            if VideoMagnitudeerval.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ProviderSineView(frame: CGRect(x: 0, y: 0, width: notiExploreKey, height: app_keepId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.versusScope = false
        QuantityensityAction.shared.versusScope = false
        //: TalkingSocketManager.shared.isCalling = false
        QuantityensityAction.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.underMeasure()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: OpenAfter = {
        //: let v = TalkingBGVideoView()
        let v = OpenAfter()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.failureTransaction(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: LevelProfile = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = LevelProfile(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: LabelHandle = {
        //: let v = TalkingVideoWindowView()
        let v = LabelHandle()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = notiExploreKey - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if SustainableDepth.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: show_uniformKey + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.weight()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: AtAnalyzeObject = {
        //: let m = TalkingVideoChatManager()
        let m = AtAnalyzeObject()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension ShadeRounding {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func theStripEvaluate() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        AtAnalyzeObject.sameOn(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: dataDecideValue.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(appPublishTitle.replacingOccurrences(of: "version", with: "e") + String(showFamilyGivingSucceedMessage))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(kHarassmentName.replacingOccurrences(of: "creation", with: "n") + "name")].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(main_previousString.replacingOccurrences(of: "drawer", with: "d") + String(k_boardKey))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(app_ceilingPullKey.suffix(7)) + show_primaryName.replacingOccurrences(of: "obtain", with: "l"))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(constMemberMessage.replacingOccurrences(of: "alive", with: "ov"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(noti_sprinkleId))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.productStart()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.conTranslate()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func conTranslate() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = WhiteControl.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = WhiteControl.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.available(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.somePut(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension ShadeRounding {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func towardZone() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ReduceLevelDoingtoPrior.sprinkle { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.conclusion(showMsg: k_boldEquivalentMsg)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (main_alongMessage.replacingOccurrences(of: "mutual", with: "u") + "seCa" + data_plateMsg.replacingOccurrences(of: "heart", with: "l")), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: dataDecideValue.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.flush()
                //: return
                return
            }

            //: self.startPreview()
            self.announcement()
            //: self.stopBGM()
            self.smartJudge()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PerSmart.complete(type: .Response)
            //: self.startBGM()
            self.totalensityMultiple()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func announcement() {
        //: self.videoManager.startPreview()
        self.videoManager.descriptionSecond()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func underMeasure() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.smartJudge()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        FunctionalCeiling.judge()
        //: TalkingSocketManager.shared.callerDelegate = nil
        QuantityensityAction.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        QuantityensityAction.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        QuantityensityAction.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func flush(animated: Bool = true) {
        //: self.stopBGM()
        self.smartJudge()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func weight() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.direction()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.conTranslate()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func criticalSquare() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.reFlag()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.conTranslate()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.sendOf()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func sendOf() {
        //: guard VideoMagnitudeerval.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard VideoMagnitudeerval.share.loginUserMode.sex == ChannelDown.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = SecondHideSpine()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.noImplementation()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension ShadeRounding {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func random() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(gapThreshold(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(succeedBot(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: mainMatchId,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(lostAcross(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: show_firstUndertakeLikeFormat,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        userProcessFormat.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func gapThreshold(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            flush()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func lostAcross(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard QuantityensityAction.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        QuantityensityAction.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        QuantityensityAction.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        QuantityensityAction.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.heapSignificant()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func succeedBot(notification: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.conTranslate()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension ShadeRounding: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.conclusion(showMsg: (String(dataShouldFormat.suffix(5)) + "interru" + k_agileStr.replacingOccurrences(of: "family", with: "n")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (String(userFlexibleArrowUrl.prefix(8))), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (String(data_recordingExceptKey)), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (main_alongMessage.replacingOccurrences(of: "mutual", with: "u") + "seCa" + data_plateMsg.replacingOccurrences(of: "heart", with: "l")), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: dataDecideValue.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.bothPinAbove()
                //: TalkingSocketManager.shared.versusScopeVC = nil
                QuantityensityAction.shared.versusScopeVC = nil
            }
            //: popCurrentViewController()
            flush()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension ShadeRounding {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func everyOperate() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? CeilingOval.Call : CeilingOval.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PerSmart.complete(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func totalensityMultiple() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.strategy()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func smartJudge() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.implementAttention()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension ShadeRounding {
    /// 超时定时器
    //: private func timeoutRules() {
    private func socialWithoutChallenge() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? user_adjustMessage : self.chatModel?.intervalGreet()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            content()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.content()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func content() {
        //: guard !TalkingSocketManager.shared.versusScope else { return }
        guard !QuantityensityAction.shared.versusScope else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.conclusion(showMsg: (String(k_techniqueValue.prefix(2)) + String(userDifficultData.suffix(7))).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (String(data_recordingExceptKey)), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (main_alongMessage.replacingOccurrences(of: "mutual", with: "u") + "seCa" + data_plateMsg.replacingOccurrences(of: "heart", with: "l")), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: dataDecideValue.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        QuantityensityAction.shared.frameOfFeed(info: socketDict)
        //: popCurrentViewController()
        flush()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension ShadeRounding {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func doinger(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(k_shareData.replacingOccurrences(of: "sibling", with: "on"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.submit(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.rapidTake()
            //: createTalkRoom()
            criticalSquare()
            //: self.stopBGM()
            self.smartJudge()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func taskOrCan(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.direction()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.failureTransaction(show: false)
        //: self.stopBGM()
        self.smartJudge()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.rapidTake()
        //: self.createTalkRoom()
        self.criticalSquare()
    }
}

// MARK: - AngleEdge【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension ShadeRounding: AngleEdge {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func refreshDown(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(constTeenagePath.suffix(4)) + String(userConsumptionGemString.suffix(4)) + "hung up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(show_alongMessage.prefix(8)) + String(main_automaticString))].stringValue == ChannelDown.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(show_strokeTitle) + String(constMapMentionKey.prefix(7))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.conclusion(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bothPinAbove()
            //: TalkingSocketManager.shared.versusScopeVC = nil
            QuantityensityAction.shared.versusScopeVC = nil
        }

        //: popCurrentViewController()
        flush()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func proWith(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        taskOrCan(logId: json[String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func multiRe(data: [String: Any]) {
        //: TalkingAlertShow.hideAlert()
        TapBounce.outConvert()
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(app_termsClearlyString.lowercased() + String(notiProName))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(constTeenagePath.suffix(4)) + String(userConsumptionGemString.suffix(4)) + "hung up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kPerResistanceFormat.suffix(7)))].stringValue == ChannelDown.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(show_strokeTitle) + String(constMapMentionKey.prefix(7))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue {
            if VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: main_belowPath.map{listIgnore(level: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(noti_councilBeatName.prefix(5)) + "ther h" + String(data_editData.prefix(5)) + "p th" + String(userCommunicateSocialTitle.suffix(7))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.conclusion(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bothPinAbove()
            //: TalkingSocketManager.shared.versusScopeVC = nil
            QuantityensityAction.shared.versusScopeVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.afterInfo()
        //: popCurrentViewController()
        flush()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func untilBy(msg: String, data: [String: Any]) {
        //: guard VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(const_oneString))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(dataClipTitle))] as? String else { return }
            //: let httpsUrl = VideoMagnitudeerval.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = VideoMagnitudeerval.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = StartTranslate()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            ScopeEmbrace.share.painter(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            TapBounce.notMovementSick(title: nil,
                                             //: message: msg,
                                             message: msg,
                                             //: leftBtnTitle: "Cancel".localized,
                                             leftBtnTitle: (k_specialData.replacingOccurrences(of: "rem", with: "C") + user_partnerTitle.replacingOccurrences(of: "calculate", with: "an")).localized,
                                             //: rightBtnTitle: "Get gold coins".localized) {
                                             rightBtnTitle: (String(main_pureFormat.prefix(6)) + showSearchValue.replacingOccurrences(of: "disable", with: "ld") + String(k_minimizeUrl.prefix(6))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                TapBounce.outConvert()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                ScopeEmbrace.share.rateBreeze(clickEvent: k_cornerId, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func reductionMode(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.bigTitle(giftArr: giftArr)
    }
}

// MARK: - FloorConsume【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ShadeRounding: FloorConsume {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func noneInstance(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(constTeenagePath.suffix(4)) + String(userConsumptionGemString.suffix(4)) + "hung up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(noti_reminderName.replacingOccurrences(of: "row", with: "r") + String(appPromptString))].stringValue == ChannelDown.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(show_strokeTitle) + String(constMapMentionKey.prefix(7))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.conclusion(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bothPinAbove()
            //: TalkingSocketManager.shared.versusScopeVC = nil
            QuantityensityAction.shared.versusScopeVC = nil
        }

        //: popCurrentViewController()
        flush()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func beforeTranslate(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        taskOrCan(logId: json[String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - ReduceException【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension ShadeRounding: ReduceException {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func pinAudience(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (main_alongMessage.replacingOccurrences(of: "mutual", with: "u") + "seCa" + data_plateMsg.replacingOccurrences(of: "heart", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.conclusion(showMsg: (String(k_techniqueValue.prefix(2)) + String(userDifficultData.suffix(7))).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.bothPinAbove()
                //: TalkingSocketManager.shared.versusScopeVC = nil
                QuantityensityAction.shared.versusScopeVC = nil
            }
            //: popCurrentViewController()
            flush()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(userExaminationValue.prefix(5)) + show_popInmateTitle.replacingOccurrences(of: "package", with: "e") + String(showBetweenKey.suffix(6))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard VideoMagnitudeerval.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard VideoMagnitudeerval.share.appStatus == PromiseTime.normal.rawValue else { return }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(sufficient: false)
            ScopeEmbrace.share.rateBreeze(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(userExaminationValue.prefix(5)) + show_popInmateTitle.replacingOccurrences(of: "package", with: "e") + String(showBetweenKey.suffix(6))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.conclusion(showMsg: msg)
            //: popCurrentViewController()
            flush()
        }
    }
}

// MARK: - ListenRounding【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension ShadeRounding: ListenRounding {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func drown() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (app_textQualityMessage.lowercased() + String(noti_placeMessage.suffix(5))), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: dataDecideValue.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func segmentSumo() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.conclusion(showMsg: (String(mainCrucialKey) + showWithoutReportId.replacingOccurrences(of: "play", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (String(data_recordingExceptKey)), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.conclusion(showMsg: (String(mainCrucialKey) + showWithoutReportId.replacingOccurrences(of: "play", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (main_alongMessage.replacingOccurrences(of: "mutual", with: "u") + "seCa" + data_plateMsg.replacingOccurrences(of: "heart", with: "l")), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: dataDecideValue.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: if chatModel?.isRandomVCPush ?? false {
            if chatModel?.isRandomVCPush ?? false {
                //: TalkingAlertShow.alert(title: nil, message: "Continue matching with other users？".localized, leftBtnTitle: "No".localized, rightBtnTitle: "Yes".localized, leftBlock: {
                TapBounce.notMovementSick(title: nil, message: String(bytes: const_validValue.map{weightWhileAgain(social: $0)}, encoding: .utf8)!.localized, leftBtnTitle: "No".localized, rightBtnTitle: (String(k_pressureMessage.suffix(3))).localized, leftBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: false)
                    self.controlBottom(isNewRandow: false)
                    //: }, rightBlock: {
                }, rightBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: true)
                    self.controlBottom(isNewRandow: true)
                    //: })
                })
                //: return
                return
                    //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
                self.conclusion(showMsg: (String(const_cameraStr.suffix(6)) + "ll ha" + String(main_entertainmentMomentValue.prefix(4)) + String(const_spotValue)).localized)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (String(userFlexibleArrowUrl.prefix(8))), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            }
        }

        //: popCurrentViewController()
        flush()
    }

    /// 随机匹配通话挂断时
    //: func randowCallIsEndTool(isNewRandow: Bool) {
    func controlBottom(isNewRandow: Bool) {
        //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
        self.conclusion(showMsg: (String(const_cameraStr.suffix(6)) + "ll ha" + String(main_entertainmentMomentValue.prefix(4)) + String(const_spotValue)).localized)
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
        QuantityensityAction.shared.frameOfFeed(info: [String(bytes: show_radioMessage.map{charmContinuing(menu: $0)}, encoding: .utf8)!: (String(userFlexibleArrowUrl.prefix(8))), String(bytes: notiAccelerateConTitle.map{clearComplaint(deploy: $0)}, encoding: .utf8)!: [String(bytes: userPermanentString.map{modNumb(during: $0)}, encoding: .utf8)!: self.chatModel?.logId]])

        //: if !isNewRandow{
        if !isNewRandow {
            //: if let targetVC = navigationController?.viewControllers.first(where: { $0 is RandomMatchVC }) {
            if let targetVC = navigationController?.viewControllers.first(where: { $0 is SquareViewController }) {
                //: navigationController?.viewControllers = [targetVC]
                navigationController?.viewControllers = [targetVC]
                //: navigationController?.popToViewController(targetVC, animated: false)
                navigationController?.popToViewController(targetVC, animated: false)
            }
            //: }else {
        } else {
            //: popCurrentViewController()
            flush()
            //: NotificationCenter.default.post(name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: data_pushValue, object: nil)
        }
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func variablePosition(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.enableMission(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func queryed(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.failureTransaction(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.quoteMargin(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.conTranslate()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func greet() {
        //: TalkingSocketManager.shared.versusScopeVC = self
        QuantityensityAction.shared.versusScopeVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = SThrough.nearFactory()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.result()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.show()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.congressionalFor()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        flush()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func comment() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension ShadeRounding {
    /// 添加视图
    //: private func setupSubviews() {
    private func scriptMaster() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.opCur(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func shTranslate() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
